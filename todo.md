# Paridad GPUI frente a Flutter

## Objetivo

Lograr paridad visual, funcional e interactiva del cliente desktop GPUI frente a Flutter, usando los proyectos GPUI de referencia en `reference_projects/comet` y `reference_projects/waku`, y validando cada fase con Computer Use. Mantener una sola instancia Flutter y una sola instancia GPUI durante las comparaciones.

## Estado actual

- Fase activa: los 400 commits anteriores siguen mezclados; `main` local se actualizó por fast-forward a `a1265778` y `commits.md` incorpora 16 commits nuevos, #401-#416, que se procesarán uno por uno con adaptación GPUI y validación Flutter/GPUI.
- Decisión de alcance #401 (2026-08-29): tray, Dock/taskbar badge y hide-on-close se mezclan en Flutter pero quedan explícitamente fuera de GPUI; no se implementarán integración nativa ni settings equivalentes en el cliente GPUI.
- Bloque #401-#406 completado: la implementación y correcciones de desktop presence quedaron mezcladas solo para Flutter; GPUI no incorpora tray, badge ni hide-on-close.
- Commit #410 completado: GPUI libera terminal frames, composer inputs/subscriptions, drivers, toolbar state y cachés de editor al cerrar o perder tabs; un path compartido se conserva hasta cerrar su último tab. Agent Canvas y PR ignoran respuestas tardías de workspaces retirados. Pasó `closed_editor_paths_are_retired_only_after_the_last_tab`.
- Bloque #410-#413 completado: el cleanup Flutter, el release del buffer xterm y el merge del PR quedaron mezclados; GPUI añadió cleanup equivalente sobre sus propios owners Rust y no mantiene un segundo buffer dentro del runtime client.
- Replay #401-#416 completado: los 16 commits hasta `a1265778` están mezclados uno por uno. Durante el gate, `main` avanzó a `09d33fe1`; se añadieron #417-#419 y continúan en proceso. Tray/badge permanece excluido de GPUI por decisión explícita.
- Gate visual provisional #416 (2026-08-29): una Flutter y una GPUI a 1302x768 compartieron el runtime dev. Cerrar `readme.md` y `Terminal 1` desde GPUI se reflejó en Flutter; cerrar `readme.md` desde Flutter se reflejó en GPUI; reabrir el editor reconstruyó el contenido y crear una terminal nueva produjo un prompt limpio. Evidencia en `/tmp/alera-parity/main-416/`. Flutter reportó `MissingPluginException` para el nuevo channel de desktop presence; es un hallazgo del feature Flutter y no un gap GPUI, donde tray/badge está excluido.
- Commit #417 completado: el badge dibujado sobre el tray Linux y su setting se mezclaron solo para Flutter. GPUI continúa sin tray, badge ni Codex Chat por las exclusiones explícitas.
- Gate final del replay (2026-08-29): Flutter pasó 3303 tests con 18 skips esperados y `flutter analyze` sin hallazgos; GPUI pasó 166 tests; el workspace Rust pasó su suite completa, Clippy de todos los targets con `-D warnings`, formatters Rust/GPUI/Dart sobre 1685 archivos y `git diff --check`. Los tests autenticados confirman que el host actual enruta `workspaceFiles`, `workspaceGit`, `workspaceSearch` y `workspacePreview` para GPUI.
- Reconciliación final contra `main` (2026-08-29): se recuperaron cuatro cambios Flutter que una resolución histórica había omitido: master-detail redimensionable y su prueba, etiquetas/flujo de instalación de todas las skills y persistencia del root de Source Control. Los goldens afectados se regeneraron porque el commit de optimización de imágenes había alterado 82 y 77 píxeles frente al render actual estable. También se cerró una carrera real del watchdog de transcripciones al entrar en background, validada cinco veces y dentro de la suite completa.
- Gate visual del replay (2026-08-29): Computer Use obtuvo screenshots y árbol AX frescos de exactamente una Flutter y una GPUI a 1302x768 en `/tmp/alera-parity/commit-371/` y `/tmp/alera-parity/commit-393/`. Su pipe nativo cerró en cada click, doble click, click derecho y drag, incluso por bundle/ruta exactos; por ello las interacciones nuevas no se declaran visualmente recorridas. El bloqueo es externo y reproducible, mientras las rutas y estados correspondientes sí quedaron cubiertos por pruebas. Codex Chat continúa fuera de alcance en GPUI.
- Apps de validación: cerradas después del gate #347; durante la comparación hubo exactamente una Flutter y una GPUI en la pantalla nativa, sin duplicados ni modal fatal.
- Última evidencia: Flutter y GPUI debug se reconstruyeron con #347 y Computer Use leyó screenshots/AX frescos a 1302x768. El pipe nativo volvió a cerrarse en la primera acción incluso después de reiniciar el kernel, por lo que la interacción de preview no se declara validada. El contrato GPUI sí quedó implementado y cubierto por tres pruebas dirigidas: single-click preview por split, reemplazo del slot activo, promoción dirty/edit/doble click, título cursivo y Keep Open; PDF conserva su exclusión explícita del POC.
- Hito #350 Keep Alive (2026-08-29): Flutter y GPUI debug se reconstruyeron con el mismo backend nativo `keepawake`. Los recortes 420x90 de la derecha del status bar coinciden en orden `Resources -> Keep Alive -> Update`, borde, Coffee Lucide 3.1.15, gap, texto mono de 10 px y color off; evidencia en `/tmp/alera-parity/hito-350/flutter-status.jpeg` y `gpui-status.jpeg`, enviada como álbum de Telegram. GPUI comparte `general.keepAliveEnabled`, replica el row de Application, tooltips/errores y libera el lock al salir. Computer Use siguió cerrando su pipe en cualquier acción, por lo que el click/toggle no se declara validado por UI; sí pasó el test dirigido de carga/persistencia y el backend nativo viene cubierto por el commit fuente.
- Terminal Toolbar #356 (2026-08-29): GPUI sustituyó los botones absolutos separados por el cluster Flutter de 30 px con Grip Vertical Lucide, Pulse/Composer/Refresh/Agent Canvas, handle al lado exterior según esquina, drag limitado al viewport, snap Top/Bottom + Left/Right, menú contextual, preferencia compartida `terminal.toolbarCorner`, dropdown/search de Settings y desplazamiento de Search cuando el cluster está arriba. Escape, pérdida de activación y cierre de terminal limpian drag/menu. El test de geometría pasa y ambas apps compilaron; Computer Use volvió a cerrar el pipe al hacer click en `Terminal 1`, por lo que no se registra una captura o gesto live falsamente validado.
- AI Assist #371 (2026-08-29): Flutter/runtime y GPUI usan AI Assist en copy, tipos internos, Settings, búsqueda, modelos, Reading Diff, Text Actions, commit/PR y workspace identity; GPUI añadió `Speech Messages` y conserva solamente `aiTextGeneration`/`aiText.*` como compatibilidad histórica. `cargo check` dirigido pasa para GPUI y sidecar; los tests de búsqueda y copy de errores pasan. Computer Use verificó exactamente una Flutter y una GPUI a 1302x768 y guardó `/tmp/alera-parity/commit-371/`, pero su pipe nativo volvió a cerrarse en toda acción, incluso por bundle/ruta exactos, así que el pane no se marca como interacción visual validada.
- Runtime quit quiescence #388 (2026-08-29): el `RuntimeBridge` compartido por GPUI usa un estado atómico de quit y una señal cerrable para rechazar tráfico nuevo, completar inmediatamente requests pendientes, impedir que reconnect lance un host y cerrar la conexión viva. El observador `on_app_quit` cubre menú, shortcut y cierre de ventana y también libera Keep Alive. Las dos pruebas dirigidas del bridge pasan; no hay cambio visual que comparar.
- Terminal attachment recovery #389 (2026-08-29): resync de salida y resize comparten ahora la misma secuencia de attach/restore en GPUI. Un resync se difiere durante attach/restore, resize espera sin perder la última geometría, los errores `not attached`/connection closed disparan reattach y el resync diferido solo se reanuda tras attach exitoso. La regresión dirigida compila y pasa; no cambia la UI estática.
- Source Control context actions #393 (2026-08-29): GPUI añadió el menú right-click de archivos/carpetas en vista árbol y archivos en lista con Open File, Reveal in Explorer, Unstage, Stage y Discard, estados mixtos, busy/disabled, teclado, Escape/click externo, roles AX y restauración de foco. Reveal convierte el scope Git enfocado, expande ancestros, selecciona y desplaza Explorer. `gitDiffViewMode` ahora restaura/persiste y usa tree como default Flutter. Cuatro tests dirigidos pasan. Computer Use guardó `/tmp/alera-parity/commit-393/` con una build de cada cliente a 1302x768, pero su pipe de acciones volvió a cerrarse y el host persistente no conocía `workspaceGit.snapshot`; no se declara falsamente el menú como recorrido visual. La preferencia temporal volvió a `search`, sidebar visible y tree.
- Estado del inventario operativo (37 dominios): 30 con `paridad validada`, 0 `parcial`, 0 `sin validar`, 0 `gap` y 7 `fuera de alcance`. El inventario cubre las 22 features de Flutter, las 222 superficies de presentación, los 30 contratos funcionales de `feature-matrix.md` y las 139 filas de `ui-parity-matrix.md`, además de shell, sidebar, workbench, terminal/editor/explorer, Git/PR, AI/agents, settings, mobile, orchestration, diagnostics, feedback/iconos, accesibilidad, procesos y release. El informe solicitado se envió por Telegram el 2026-08-28; no se reanudan actualizaciones automáticas.
- Preferencia operativa vigente: no gastar tiempo en `cargo fmt`, Clippy, tests de workspace ni checks de diff durante la iteración visual/funcional; solo se ejecutan compilaciones necesarias para levantar GPUI, y el gate técnico completo queda reservado para el final.
- Preferencia operativa vigente (2026-08-28): no enviar capturas ni avances por Telegram de forma no solicitada. El informe integral del corte 18/12/7 se envió únicamente porque el usuario lo pidió de forma explícita; la iteración continúa enfocada en implementación y validación.
- Runtime detenido (2026-08-27): una parada forzada real eliminó el proceso del host y dejó Terminal en `Terminal Unavailable`; el popover GPUI conservaba el snapshot anterior y seguía ofreciendo `Stop`. La invalidación del snapshot ahora ocurre tanto al fallar `status.get` como al recibir `Unavailable`/`Disconnected`. Con Flutter temporalmente cerrada para evitar que relanzara el host compartido, Computer Use validó el ciclo completo sobre el binario reconstruido: `Force Stop` -> `Runtime Stopped` con botón `Start` -> host nuevo -> `Reconnect` -> prompt y cursor restaurados sin relanzar GPUI. Flutter se volvió a abrir en una única instancia y se llevó a tamaño completo. Evidencia local: `/tmp/alera-parity/runtime-recovery-2026-08-27/gpui-stopped.jpeg`, `gpui-starting.jpeg` y `gpui-recovered.jpeg`.
- Terminal exit coordinator (2026-08-27): al escribir `exit` en una sesión real, el host la retiró pero GPUI dejó el tab visible porque la notificación coincidió con otra mutación y `request_close_tab` descartó el cierre mientras `tab_mutation_busy` estaba activo. El coordinador ahora espera a que termine la mutación y ejecuta el cierre pendiente. Después del rebuild se repitió `exit`: `Terminal 2` desapareció del tab strip y del Runtime, el split vacío se podó y el layout se reacomodó sin overlay. Después se retiró el último terminal restante, se pulsó el workspace `main` ya seleccionado y GPUI creó `Terminal 1`; el host confirmó `CurrentDir=/Volumes/ExternalStorage/Projects/leynier-website`. Evidencia local: `/tmp/alera-parity/terminal-lifecycle-2026-08-27/gpui-exited-tab-removed.jpeg` y `gpui-workspace-click-created.jpeg`.
- Sidebar Sleep/reopen (2026-08-27): Computer Use abrió el menú contextual real con click secundario, mostró la confirmación `Sleep Workspace?`, ejecutó `Sleep` sobre `Sleep Wait Report` y verificó que sus tabs/sesión desaparecieron, el workspace cambió a dot gris y GPUI regresó a `main` sin dejar el workspace anterior verde. Al volver a pulsarlo creó `Terminal 1`, lo marcó activo y conservó el workspace en el árbol; después se restauró `main`. Evidencia local: `/tmp/alera-parity/sidebar-sleep-2026-08-27/gpui-slept.jpeg` y `gpui-reopened.jpeg`.
- Sidebar Rename/Tags/Parent (2026-08-27): Rename se ejecutó sobre `Sleep Wait Report`, actualizó el row y el toast, y se restauró el nombre original. Manage Tags reveló que `#Sidebar Fixture` y su papelera se dibujaban pero no aparecían como controles AccessKit. El binario recompilado expone el tag como checkbox con estado on/off, `Delete Tag #Sidebar Fixture` como botón y Create/Cancel/Save como acciones; se probó el toggle y se canceló sin persistir. Set Parent expone `Parent Workspace` como combobox, el filtro y todas las opciones con selección; se confirmó la relación existente con `Sidebar Alpha` y se canceló sin mutarla. Evidencia local: `/tmp/alera-parity/sidebar-editing-2026-08-27/gpui-tags-selected.jpeg` y `gpui-parent-options.jpeg`.
- Sidebar Tags visual Flutter/GPUI (2026-08-27): se abrió `Manage Tags` en Flutter sobre el mismo `Sleep Wait Report` y se compararon título/icono, checkbox `#Sidebar Fixture`, papelera, input, Create Tag, Cancel y Save. GPUI conserva el ancho máximo 460, padding 20, lista 36 px y catálogo de Flutter; la evidencia emparejada queda local en `/tmp/alera-parity/sidebar-editing-2026-08-27/flutter-tags.jpeg` y `gpui-tags.jpeg`. No se guardó ningún cambio.
- Sidebar pinning/Repeat Pinned (2026-08-27): `Sleep Wait Report` se pineó y despineó con los toasts exactos, moviéndose entre Pinned y All. Después se activó `Repeat Pinned Workspaces`: `main` apareció tanto en Pinned como All, y al pinear el fixture aparecieron sus dos copias conservando el mismo estado; se despineó y se restauró la preferencia a off. Evidencia local: `/tmp/alera-parity/sidebar-pinning-2026-08-27/gpui-repeat-on-pinned.jpeg` y `gpui-restored.jpeg`.
- Sidebar Remove Workspace (2026-08-27): se creó un worktree temporal real `Remove Parity Fixture` desde `leynier-website/main`, se abrió el menú y la confirmación GPUI mostró exactamente el worktree y la branch que serían eliminados. `Remove` retiró el row y mostró `Workspace Removed`; se verificó que el registro Runtime, el path temporal y la branch `chore/gpui-remove-parity-20260827` ya no existen. Ningún workspace o branch previo fue tocado. Evidencia local: `/tmp/alera-parity/sidebar-remove-2026-08-27/gpui-confirm.jpeg` y `gpui-removed.jpeg`.
- Workbench drag lifecycle (2026-08-27): Flutter finaliza `Draggable` tanto en `onDragEnd` como `onDraggableCanceled` después del siguiente layout. GPUI limpiaba en `pointer up` y por timeout de 10 s, pero no al perder activación ni con Escape. La app recompilada observa activación de ventana y cancela de inmediato tab/pane preview, resize, Explorer drag, preview pan y terminal selection/scrollbar drag; Escape prioriza cancelar el drag de tabs y la limpieza invalida el timeout anterior. Cancelar en Context Sidebar dejó orden/layout intactos y sin overlay.
- Workbench cross-pane tab-strip drop (2026-08-27): el fixture metadata-only `Split Parity Fixture` aisló tres terminales en un pane. GPUI y Flutter confirmaron reorder y restauración, movimiento entre tab strips, splits Right/Left/Up/Down, integración en el pane vecino, poda del pane vacío y cancelación fuera. GPUI fallaba cuando `Drop` llegaba al tab strip sin `DragMove`; la primera corrección con `window.mouse_position()` reveló posición stale. La implementación final conserva la identidad hasta `MouseUpEvent.position`, ignora Drops tardíos mientras la mutación está busy y cerró tanto el drop cross-pane como Left. Flutter persistió ratio 62/38, GPUI lo abrió en el mismo píxel y lo restauró a 50/50. El fixture, sus tres tabs y sesiones se eliminaron al terminar. Evidencia local: `/tmp/alera-parity/splits-isolated-2026-08-27/`.
- Terminal selection row mapping (2026-08-27): el fixture Unicode mostró que el highlight GPUI podía estar sobre `ALERA_COPY_α one` mientras Cmd+C devolvía la línea anterior. El cleanup de prompts restaurados compactaba filas solo en presentación y rompía el índice compartido por selección/link. `TerminalEmulator` conserva ahora el mapa `rendered row -> source grid row` del frame; hit-testing y overlay usan esa misma fuente, y las filas blank añadidas por cleanup no apuntan a contenido oculto. Después del rebuild, triple click copió exactamente `ALERA_COPY_α one` y el drag de dos filas copió exactamente `ALERA_COPY_α one\nALERA_COPY_β two`, sin la fila previa/siguiente. Flutter mostró el mismo scrollback; el backend sintético no activó selección xterm en esta sesión, por lo que se conserva la evidencia viva Flutter anterior y no se usa este intento como contradicción. `clear` retiró el fixture y dejó un prompt limpio. Evidencia local: `/tmp/alera-parity/terminal-selection-row-map-2026-08-27/`.
- Editor multi-pane activation (2026-08-27, cerrado): la carga secuencial ya deja terminar la petición propietaria de la generación, rehidrata únicamente el tab activo de cada pane y no reemplaza el documento global con una carga secundaria. `square.astro` y `posts-loop.astro` reaparecieron tras relanzar sin falso `no file` ni spinner permanente. Un solo click en cada pane movió el toolbar/foco y aceptó `L`/`R`; los buffers permanecieron independientes y `Cmd+Z` deshizo solo el pane enfocado. Ambos archivos siguen idénticos en disco.
- Editor multi-pane toolbar (2026-08-27, cerrado visual y funcional): el drag real de `posts-loop.astro` creó la misma división en Flutter y la captura emparejada confirmó Diff/Save/Discard en ambos panes. GPUI renderiza ahora el toolbar completo en el tab activo de cada pane con IDs únicos. Se ensució `posts-loop.astro`, se activó `square.astro` y el botón Discard del pane inactivo restauró exclusivamente `posts-loop.astro`, mostró `Changes discarded` y no escribió el archivo. Evidencia local: `/tmp/alera-parity/editor-multipane-2026-08-27/gpui-two-editors-final.jpeg` y `flutter-two-editors-final.jpeg`. Al terminar, ambos clientes volvieron a un pane con `Terminal 1` activo.
- Tab context split semantics (2026-08-27, corregido tras contraste de código y UI): la lectura inicial interpretó que `Split Right` debía mover `posts-loop.astro`, pero la implementación Flutter conecta deliberadamente el menú del chip a `onSplitGroup`, que llama `splitWorkbenchGroupWithTerminal`. Computer Use confirmó el contrato real creando `Terminal 2` en el pane derecho. GPUI conserva por tanto `split_pane_with_terminal` para los cuatro items; mover el tab sigue perteneciendo exclusivamente al drag/drop. `Terminal 2` se cerró, el pane vacío se podó y el layout volvió a un pane.
- Último avance (2026-08-27): cierre optimista del último tab y menú nativo completo siguen estables; el restore de terminal ahora reconstruye el emulador con el tamaño final del pane, Explorer conserva los toasts durante los refresh del watcher, el conflicto externo de Editor fue validado de extremo a extremo y Settings dejó de usar la calibración artificial `0.43` para volver al scroll nativo de GPUI.
- Último avance (2026-08-26, continuación): restore PTY elimina marcador `%`, Runtime identifica la versión real de `alera-cli` y el sidebar tiene scroll persistente/calibrado; los tres cambios fueron recompilados y verificados con capturas Flutter/GPUI a 1302×768.
- Último avance (2026-08-26): el árbol de proyectos/workspaces de GPUI ahora está dentro de un límite de scroll vertical independiente (`sidebar-workspaces`), igual que el `ListView` de Flutter; búsqueda, toolbar y footer permanecen fijos mientras la lista crece. La prueba de desborde con un fixture largo ya quedó validada y el fixture fue retirado.
- Terminal restore marker fix (2026-08-26): el snapshot histórico real conservaba el marcador reverse-video `%` de zsh en una fila intermedia (`❯ %` o `%` más un fragmento de prompt). La limpieza de restore ahora detecta la firma de resize antes de normalizar, conserva el prefijo del prompt actual y elimina solo filas/marcadores zsh anteriores; se añadieron pruebas para marcador inverso, fila parcial y prompt preservado. Tras rebuild y zoom de la ventana, GPUI y Flutter muestran el mismo prompt/cursor sin la fila `%`; no quedó logging diagnóstico.
- Runtime version parity (2026-08-26, superseded by build identity): GPUI comparaba el host `0.1.0` contra la versión de cliente `0.40.0` y mostraba `Update Available` incorrectamente. El build script ahora extrae la versión del paquete `rust/alera-cli`, el status bar usa ese valor independiente de la versión de la app y las pruebas de update siguen cubriendo versiones older/newer; la decisión final del chip cuando los commits difieren queda documentada en `Runtime build identity parity`.
- Runtime build identity parity (2026-08-26): el sidecar incluido en GPUI publicaba la versión correcta pero dejaba el commit en `unknown`, por lo que el chip no podía reflejar el estado `Update Available` que Flutter muestra cuando el host persistente proviene de otro build. `experiments/alera-gpui/build.rs` ahora resuelve y observa `git HEAD`, respeta `ALERA_BUILD_COMMIT` cuando está definido y expone el commit real al comparador de Runtime. Tras recompilar y hacer zoom de la ventana, Flutter y GPUI muestran el mismo chip `Update Available` en el mismo status bar; no se abrió ningún Runtime/Quota adicional ni se enviaron capturas por Telegram.
- Sidebar scroll parity (2026-08-26): el árbol lateral tiene ahora `ScrollHandle` persistente, scrollbar con modo `Scrolling` y listener de wheel calibrado (`1.25`), además de una altura mínima de row de `33 px` para igualar el rango acumulado de Flutter. Con 24 workspaces temporales, ambas apps llegaron de forma equivalente a `Scroll Fixture 03`, al extremo `Scroll Fixture 24` y de vuelta al inicio; los registros se quitaron con `workspace unregister` y no quedó fixture en el host.
- Validación dirigida de la continuación (2026-08-26): los tests `restore_` (9) y `runtime_chip_` (2) del binario GPUI pasan después de los cambios de terminal/Runtime/sidebar; `make gpui-debug` relanzó una sola GPUI y Computer Use dejó Flutter/GPUI a 1302×768 sin modal fatal.
- Native menu name parity (2026-08-26): el bundle macOS de GPUI dejó de publicar `Alera GPUI` como `CFBundleDisplayName` en flavor dev; ahora usa `Alera Dev` como Flutter, conserva `Alera GPUI` como ejecutable interno y el árbol del menú nativo coincide en nombre, items, Services, Hide/Show y Quit.
- Split directions live recheck (2026-08-26): Computer Use creó splits GPUI a la derecha, abajo, izquierda y arriba desde tabs de terminal, movió el único tab de vuelta a otro split para podar el pane vacío, reordenó tabs dentro del mismo split y canceló un drag fuera del workbench; cada estado terminó sin overlay residual ni pane vacío. La matriz aislada con snapshot inicial idéntico en Flutter/GPUI y el preview frame-by-frame siguen pendientes.
- Validación de sidebar/menu (2026-08-26): `cargo check --bin alera-gpui --locked` pasa tras el ajuste de ScrollHandle, la calibración por plataforma y el nombre del bundle; la comparación Computer Use no dejó overlays ni modales y ambas ventanas siguen en tamaño completo.
- Validación de escala (2026-08-26): después de `make DART='puro dart' gpui-debug`, la ventana GPUI volvió a modo ventana y su captura medía 1182×768; se aplicó el zoom semántico de la barra superior y ambas capturas quedaron en 1302×768 antes de comparar. Las diferencias iniciales de ancho de sidebar/contexto eran escala de ventana, no tokens de layout.
- Levantamiento re-auditado tras el cambio de foco: el trabajo rota por Search/Editor/Explorer, Terminal/Context Sidebar, Source Control/Git History, Pull Request, Settings, Agents/Mobile/Diagnostics y accesibilidad. Status Bar, Quotas, Resource Manager y Runtime quedan congelados como referencia transversal y no vuelven a abrirse como bloque independiente.
- Último corte: Search tiene resultados asentados y `No results` equivalentes después de relanzar Flutter por `make app-debug`; Editor fuente/preview abre el mismo `readme.md`, y GPUI ya conserva wrapping, talla y ritmo de línea equivalentes. La caché por ruta de documentos/previews ya evita mezclar dos editores en splits y los `InputState` por ruta ahora conservan el estado de cada tab al cambiar y volver; la evidencia enfocada queda en `/tmp/alera-parity/editor-multi-pane/gpui-two-documents.jpg`. Source Control y Pull Request ya tienen comparación viva de split menus, click externo, loaders, gating de error, refresh y diálogo `Link Pull Request`; Settings > Mobile Devices ahora usa el copy exacto de Flutter y filas con el mismo ritmo vertical de gateway/pairing; Agent Profiles ya replica carga/error, orden determinista, checkbox visual, validación sentence case y Remove con un solo click; Orchestration ya no conserva planes obsoletos cuando falla la recarga, usa spinner centrado durante loading y sus estados vacío/no vacío de `Execution Plans` quedaron alineados en tamaño, posición, copy y acciones; el fixture temporal de agentes confirmó working/waiting/blocked/done y la geometría del spinner después del ajuste del arco y del estado interrupted; Editor aún requiere cerrar conflictos, carga/error sostenidos y la auditoría completa de selección/scroll por pane; Explorer normaliza los errores de create/rename/copy/move/delete y ahora hereda estados Git agregados en carpetas como Flutter (`src` muestra `M`). El levantamiento completo de componentes está cerrado y la siguiente rotación se decide por la tabla de 37 dominios, continuando por Terminal/Context Sidebar, Search/Editor, Settings restantes, Mobile/Diagnostics y accesibilidad, sin reabrir Runtime/Resources/Quotas. En Terminal se aisló el remanente: el host compartido conservaba redibujos producidos por resizes provisionales de GPUI; GPUI ahora espera bounds reales, no redimensiona durante restore y aplica un debounce por generación de 650 ms para que la animación de zoom termine antes del único resize PTY, y las sesiones nuevas quedaron limpias con cwd, prompt y cursor equivalentes. El álbum de evidencia está en `/tmp/alera-parity/terminal-resize-fix/flutter-focus.jpg` y `gpui-focus.jpg`; siguen pendientes scrollback histórico, selección/copy, enlaces, shortcuts, restore sostenido y errores. Markdown/Merman e imagen avanzaron en código: el flujo `.mmd`/`.mermain`/`.mermaid` ahora coincide con Flutter (Explorer abre fuente, `Open Preview` crea viewer y `Open Editor` vuelve a fuente), el SVG aplica el tema oscuro, el viewport contenido conserva sizing y GPUI ya tiene zoom/pan limitado; siguen pendientes enlaces embebidos, errores sostenidos, archivos grandes y una validación directa del gesto cuando el backend AX lo permita.
- Terminal/Editor validation follow-up (2026-08-25): Computer Use confirmed GPUI terminal selection and `Cmd+C` retain the selected text in the native clipboard (`pbpaste` returned the selected prompt fragment), and a fresh terminal command rendered the URL output with the same cwd/prompt/cursor contract. A macOS dead-key sequence sent with `apostrophe` then `a` produced `zsh: command not found: á`, proving the native IME path composes `á` instead of forwarding `'a`; the terminal fixture was left at a clean prompt. The per-route editor buffer and cursor state compiled and was exercised by editing `bun.lock`, switching to `.prettierrc`, returning to `bun.lock`, and discarding the temporary change; the buffer survived the tab switch and the file returned to its original contents. No Telegram message was sent and no final-format/check gate was run.
- Terminal scrollback fixture isolation (2026-08-25): Computer Use and `alera terminal read` showed that the currently selected GPUI `Terminal 1` is the historical handle `gpui-tab-44635-1787631776695`, whose host scrollback contains repeated zsh resize/redraw frames (`\r\r`, cursor-up and clear-screen sequences). The apparent extra prompt rows are therefore polluted historical session data, not a new bounds/resize regression; fresh GPUI sessions remain clean after the generation-based 650 ms debounce. Keep the fixture gap visible for a later cleanup/restore pass, but do not distort the renderer to hide valid PTY history.
- Explorer empty-state geometry (2026-08-25): code comparison with Flutter showed that GPUI rendered an empty tree as one flex child and the loader/error as a second flex child, vertically splitting the panel. The Explorer now keeps the tree hit target and overlays the loading state across the full relative panel; root filesystem errors follow Flutter's blank tree plus normalized toast contract. Recompiled with `make gpui-debug`; the forced missing-path fixture showed matching `Item not found` behavior and was cleaned up.
- Explorer folder drag feedback (2026-08-27): GPUI moves directories entre targets, rechaza self/descendant drops y usa feedback Flutter de 300 px con depth, expander, symlink y Git status. La validación emparejada de long-press ya quedó cerrada con un gesto nativo controlado en Flutter.
- Workspace switch rehydration (2026-08-25): Computer Use exposed a real GPUI gap by switching both apps from `main` to `Sidebar Beta Remote`: Flutter loaded the remote `README.md`, while GPUI retained a stale `File not found` state from the previous workspace. `refresh()` now rehydrates the active contextual surface after installing every new workspace snapshot. After rebuild, the same switch showed `README.md` in GPUI and Flutter, and both apps were restored to `main`.
- Workspace switch race/toast cleanup (2026-08-26): the first rehydration fix still allowed a root read started against the previous snapshot to race the new one, leaving a transient `File not found` toast. Local reset no longer starts a read; snapshot installation performs the single scoped reload, and successful Explorer hydration removes only workspace-file error toasts. After rebuild, `main -> Sidebar Beta Remote` showed `README.md` with no stale toast after 1.5 s; both apps returned to `main`.
- Agent Profiles managed-field parity (2026-08-26): live Flutter/GPUI comparison showed GPUI duplicated row labels as placeholders in empty `Exact Model ID`, `Exact Persona`, `Maximum AI Credits` and `Maximum Autopilot Continues` fields, while Flutter leaves those dense fields blank. The GPUI inputs now omit those placeholders; after rebuild the empty `Exact Model ID` field matches Flutter. Settings was closed without changing the profile.
- Agent Profiles refresh-state parity (2026-08-26): code and live state comparison showed Flutter keeps the last profile list/editor mounted while the provider refreshes, whereas GPUI temporarily replaced an existing list with `Loading Agent Profiles`. GPUI now reserves the full-pane spinner for the initial empty load and keeps settled profiles visible during refresh; the rebuilt app shows the same settled list/editor geometry as Flutter.
- Agent Profiles discovery controls (2026-08-26): Model/Persona refresh buttons now use Flutter's 30 px hitbox, expose `Refresh Models`/`Refresh Personas` tooltips, remove the hand cursor/hover while discovery is busy, and apply the disabled opacity during the request. Rebuilt Flutter/GPUI panes were switched to Antigravity without saving; both showed the same Model/Persona rows and settled refresh controls, while the very short busy interval remains queued for a delayed backend fixture.
- Agent Profiles Remove feedback (2026-08-26): code comparison found GPUI showed `Agent Profile Removed` after deletion while Flutter clears the editor/list state silently. GPUI now suppresses the success toast for Remove and keeps Save/validation feedback unchanged; rebuild and a reversible Remove fixture remain queued.
- Agent Profiles OpenCode discovery validation (2026-08-26): con OpenCode instalado, Flutter y GPUI se cambiaron temporalmente al adapter `OpenCode`; ambos mostraron Model/Persona con refresh, `Agent Default` y `Auto Approve`, terminaron sin error visible y se cerraron sin guardar. La fila queda pendiente solo para busy/error inducido y persistencia completa.
- Search shortcut focus (2026-08-27): `Mod+Shift+F` difiere el focus del query field dos frames y GPUI ya expone el campo por AccessKit. Computer Use confirmó escritura mediante `SetValue`; queda repetir el shortcut y la escritura directa sobre la revisión nueva como parte de la matriz de focus/teclado.
- Workbench reorder recheck (2026-08-26): con el mismo split inferior de dos terminales, Computer Use arrastró `Terminal 1` detrás de `Terminal 2` en GPUI y después en Flutter; ambos quedaron en el orden `Terminal 2`, `Terminal 1`, sin pane vacío ni overlay residual. La matriz de split directions, cancelación y dirty close sigue abierta.
- Workbench right-split smoke (2026-08-26): GPUI creó un split derecho al arrastrar un tab terminal al borde del pane superior y, al devolverlo al split inferior, eliminó el pane vacío sin overlay residual. El layout local de Flutter no mutó durante esa operación, así que la prueba aislada con snapshot inicial idéntico de ambas apps sigue pendiente.
- Workbench pointer-up target fix (2026-08-26): algunos `MouseUp` de GPUI confirmaban el drop con un gap fijo del chip antes de que el observador global pudiera resolver la posición real; eso podía convertir un split direccional en reorder o dejar un preview stale. Los handlers de chip/append ahora delegan al resolver posicional único y cerrar un tab limpia el drag pendiente. Tras recompilar, Computer Use validó reorder a izquierda/derecha, split derecho entre panes, movimiento de vuelta con poda del pane vacío y cancelación fuera; ambas apps terminaron con el layout base sin overlay residual.
- Workspace context-menu order (2026-08-26): el menú de workspace de GPUI mostraba `Clear Parent Workspace` antes de `Manage Tags`/`Set Parent Workspace`, a diferencia de Flutter. Se reordenó el bloque para seguir exactamente `Rename`, pin, tags, parent, clear-parent opcional, divider, acciones de ruta, `Sleep` y `Remove`; el menú se comparó en vivo para `main` y se cerró con click externo sin mutaciones.
- Sidebar agent icon ordering (2026-08-26): el resumen compacto de GPUI ordenaba Claude antes de Codex por un ranking propio, mientras Flutter usa el orden estable de `AgentType`. El ranking se alineó a `Codex`, `Claude Code`, `Copilot`, `Cursor`, `Antigravity`, `OpenCode`, `Pi`, `Amp`, `Grok` y deja proveedores adicionales después; se añadió una regresión de orden.
- Workbench split-ratio local extent (2026-08-26): el resize de GPUI normalizaba el delta contra toda la ventana, mientras Flutter usa el extent del nodo split menos el handle. GPUI ahora calcula el extent desde los panes descendientes y lo conserva durante el gesto; el drag del handle vertical a 50%->aprox.65% y su vuelta al ratio original quedaron alineados con Flutter a tamaño completo.
- Sidebar action feedback (2026-08-26): las acciones `Rename`, pin/unpin, tags, parent, sleep y remove ahora muestran los mismos toasts de éxito que Flutter. Los errores de pin, parent y abrir repositorio se enrutan al toast visible en vez de un campo de diálogo que no estaba montado; `Workspace path copied` conserva el copy exacto de Flutter.
- Sidebar action feedback validation (2026-08-26): con `Sidebar Beta Remote` se probó pin -> unpin y `Copy Path` en GPUI. La fila regresó a su sección original, el clipboard devolvió la ruta del workspace y el toast visible coincidió con Flutter (`Workspace path copied`); no quedó mutación de fixture.
- Dynamic list identity (2026-08-26): Search/Replace, Source Control, Git History, Pull Request y Agent Activity dejaron de usar índices de render como identidad de filas/acciones; ahora usan path, commit, comment, check, branch, action o tab estable para que refresh, filtros y cambios de estado no retargeteen controles.
- Search summary spacing (2026-08-26): GPUI tenía un spacer adicional de 16 px antes del resumen de resultados; se eliminó para seguir la secuencia Flutter `inputs -> summary -> divider -> results`. La captura con `workspace` y `1 match in 1 file` quedó alineada en filas y botón Replace después del rebuild.
- Git Diff binary image parity (2026-08-26): Flutter distingue imágenes binarias de otros archivos binarios y muestra `Before`/`After` con tamaño, placeholders y preview contenido. Se añadió el endpoint aditivo `workspaceGit.diffBlob`, lectura segura de ambos blobs desde el host, carga asíncrona por tab y la tarjeta GPUI equivalente; el fixture PNG de 64x64 mostró `Added` y `After · 5.7 KB` alineados. Los fixtures temporales se limpian después de la captura.
- Git Diff loading/error copy (2026-08-26): GPUI ahora muestra solo el loader animado centrado durante la carga y el mensaje centrado `Could not load diff.` ante un error, igual que Flutter, con estado de error por tab y sin toast global.
- Last-tab optimistic cleanup (2026-08-26): al cerrar el único terminal de `Sidebar Beta Remote`, GPUI ahora poda inmediatamente `snapshot.tabs`, `snapshot.all_tabs` y el layout antes del round-trip del host. La fila perdió el punto verde en el mismo frame y el workbench pasó al estado vacío de bienvenida, sin pane huérfano; al volver a seleccionar el workspace se creó un terminal nuevo y el fixture quedó restaurado. Si la mutación falla, GPUI reconcilia el snapshot duradero para no dejar un cierre parcial.
- App menu and Explorer loading parity (2026-08-26): el menú nativo de GPUI ya incluye `Check for Updates` junto a Settings y Execution Plans. La acción reutiliza el updater real y muestra el resultado como toast solo cuando se invoca desde el menú, igual que Flutter; la comprobación Computer Use confirmó el nuevo item en el árbol del menú. El estado de carga del Explorer quedó reducido al spinner animado centrado, sin el label adicional que no existe en Flutter.
- App menu About parity (2026-08-26): GPUI ahora incluye `About Alera Dev` en el menú nativo y abre un diálogo con logo, versión `0.40.0 (76)`, `Copy Version`, `Check For Updates`, tooltip `Close` y acción `Close`, siguiendo la estructura de Flutter. Computer Use comparó el recorte a tamaño completo, copió la versión (`pbpaste` devolvió `Version 0.40.0 (76)`) y cerró ambos diálogos sin duplicar la instancia GPUI.
- Terminal restore viewport parity (2026-08-26): un snapshot real con cientos de redibujos zsh a anchos históricos dejaba prompts repetidos en GPUI. El restore ahora coalesce bursts de shell-integration, limpia las filas residuales solo en el viewport final y compacta el contenido al anclaje superior de Flutter; el scrollback sigue retenido y vuelve a verse al desplazarse. Computer Use comparó ambas apps tras rebuild a tamaño completo, ejecutó `LIVE_GPUI` y limpió el prompt; resultado visible, cursor y cwd quedaron equivalentes.
- Terminal scrollback recheck (2026-08-26): tras la limpieza del prompt, una rueda hacia arriba en GPUI mostró el historial retenido y el scrollbar, y 50 páginas hacia abajo devolvieron el viewport al prompt superior. Flutter y GPUI terminaron con el mismo estado visible y sin marcador de prueba persistente.
- Settings close tooltip (2026-08-26): Flutter expone tooltip `Close` en el botón de cerrar Settings; GPUI no lo tenía ni en el pane normal ni en `No matching settings`. Ambos botones GPUI ahora usan el mismo tooltip. Rebuild y hover visual quedan pendientes.
- Dirty tab close dialog parity (2026-08-26): Flutter usa `AleraConfirmDialog` de 420 px con padding 20, mensaje 13 px, footer separado 20 px y botones de ancho igual (`Cancel`/`Close`). GPUI dejó el shell artesanal y ahora reutiliza `dialog_shell`/`design_system::button` con el mismo contrato. Un fixture dirty creado con Enter nativo abrió el diálogo en ambos clientes; ambos se cancelaron y `.prettierrc` quedó sin cambios en disco.
- Diagnostics save-location parity (2026-08-26): los dos pickers de exportación se compararon en vivo y Flutter abrió el workspace activo mientras GPUI elegía Downloads. GPUI ahora prefiere `selected_workspace_path()` como directorio inicial y conserva Downloads/Documents como fallback; la exportación real y su toast quedaron validados después del rebuild y los ZIP de prueba se movieron a la Papelera.
- Diagnostics export end to end (2026-08-26): Flutter y GPUI guardaron el bundle en el workspace activo usando el mismo nombre sugerido, mostraron el toast `Diagnostics exported.` y produjeron un ZIP con `app/*.log`, `runtime/runtime.log` y `meta.json`; los dos artefactos de prueba se movieron a la Papelera después de inspeccionar su contenido. El recorrido de error/loading y la geometría completa del scrollbar siguen parciales.
- Source Control toolbar parity (2026-08-26): con ambas ventanas a tamaño completo, GPUI y Flutter ahora muestran el encabezado `Source\nControl` en el rail estrecho, el mismo espaciado del composer y los mismos estados de Pull, resumen y primera fila; se conservaron los botones compactos de acciones de filas y solo se ajustó el layout del toolbar. La validación de menús, loading/error y acciones Git continúa parcial.
- Source Control menu anchoring (2026-08-26): la comparación de menú mostró que GPUI lo abría aproximadamente 9 px por encima de Flutter; el popup ahora usa un anclaje de 121 px para quedar alineado con el borde superior del botón Pull. Falta repetir el hover/cursor y el cierre externo después del rebuild.
- Source Control menu validation (2026-08-26): después del rebuild, el menú de acciones de GPUI queda anclado al mismo borde que Flutter y ambos se cierran al hacer click fuera. Los entries conservan cursor de mano solo cuando están habilitados; siguen pendientes los estados Git de error/loading y acciones destructivas.
- Source Control toolbar calibration (2026-08-26): la comparación a tamaño completo detectó que el composer GPUI quedaba 5 px abajo y usaba el fondo del `Input` genérico. Se alinearon ambos márgenes visibles, el contenedor volvió a controlar el fondo `surface` mediante `appearance(false)` y el glifo de los botones usa 16 px manteniendo la huella compacta que coincide con Flutter en Retina. Menú, `Pull`, resumen y primera fila quedaron alineados; evidencia local en `/tmp/alera-parity/source-control/current/flutter-final.png` y `gpui-final.png`. Loading/error y acciones Git destructivas end to end siguen parciales.
- Context Sidebar toolbar calibration (2026-08-26): el toolbar de Explorer y el rail de paneles tenían botones GPUI de 30 px nativos, desplazando progresivamente los seis iconos frente a los `AleraIconButton` de Flutter. Se ajustaron los controles a 24 px nativos con glifos de 16 px; una captura a tamaño completo dejó los centros de los seis iconos coincidentes y conservó el header/árbol sin desplazamiento. Evidencia local en `/tmp/alera-parity/context-sidebar/current-final/flutter-latest.png` y `gpui-latest.png`. Resize, scroll persistente, focus/keyboard, estados de carga/error y acciones del árbol siguen parciales.
- Context Sidebar collapse/reopen validation (2026-08-26): Computer Use colapsó y reabrió el rail en Flutter y GPUI desde la barra superior; ambos conservaron el mismo rail estrecho, cuatro iconos verticales, tooltip `Explorer`, botón de expansión inferior y selección del panel, sin cambiar tabs ni workspaces. La captura local del estado colapsado GPUI queda en `/tmp/alera-parity/context-sidebar/collapse-final/gpui.png`; el dominio continúa parcial por resize/keyboard, estados sostenidos y semántica AX.
- Source Control destructive dialog parity (2026-08-26): GPUI y Flutter abrieron `Discard All Changes?` con el mismo copy, scrim, ancho, padding, botones `Cancel`/`Discard` y color destructivo; ambos se cancelaron sin tocar el workspace. La matriz de acciones destructivas reales queda pendiente solo para confirmar/revalidar el resultado Git.
- Git History commit-menu anchoring (2026-08-26): el menú `Copy Commit Hash`/`Copy Commit Message` de GPUI pasó de anclarse al fondo del panel a renderizarse dentro del row seleccionado; con un commit expandido, Flutter y GPUI lo muestran junto al commit, con dos entries, iconos y cierre externo equivalentes.
- Git History copy action (2026-08-26): se activó `Copy Commit Hash` en GPUI desde el menú anclado y `pbpaste` devolvió el SHA esperado; el menú desapareció y el toast `Commit Hash Copied` coincide con Flutter. No se modificó el repositorio.
- Pull Request branch dropdown (2026-08-26): Flutter y GPUI abrieron el mismo menú de `Base Branch`, con idénticas cuatro opciones, check de la selección, anchura/anclaje y cierre al elegir; seleccionar `dev` actualizó ambos formularios sin crear ni modificar una PR.
- Settings Projects ordering/reset (2026-08-26): el pane de Projects de GPUI ahora ordena `leynier-website`, `Sidebar Alpha`, `Sidebar Beta` con el mismo comparator de Flutter y cada nueva apertura de Settings vuelve a Application y selecciona el primer proyecto ordenado. La captura recompilada muestra la misma selección y orden.
- Settings Projects geometry (2026-08-26): la columna master de GPUI se ajustó de 260 a 244 px con el inset interno de Flutter; en la captura a tamaño completo el listado, divisor, grupos y controles quedan alineados con Flutter, con una diferencia residual aproximada de 5 px en el inicio del detalle.
- Settings menu opening parity (2026-08-26): el menú nativo `Settings` ahora entra por el mismo flujo que el botón del sidebar, incluyendo reset de pane Application, query vacío, scroll superior y selección inicial de Projects; se eliminó el camino alterno que conservaba estado entre aperturas.
- Fresh terminal parity (2026-08-26): con ambas ventanas ya a tamaño completo, GPUI y Flutter crearon sesiones nuevas sin redibujado adicional; ambos mostraron cwd `leynier-website`, branch `main`, prompt y cursor en una sola línea. Las sesiones anteriores que se crearon antes del zoom conservaron scrollback contaminado por resizes, por lo que quedan fuera del baseline y se limpian tras la prueba.
- Terminal restore viewport dimensions (2026-08-26): la auditoría del snapshot real confirmó que el emulador GPUI lo estaba reconstruyendo con las 100 columnas del host aunque el pane medido tenía 120; el mismo snapshot producía 17 prompts repetidos a 100 columnas y una sola fila a 120. GPUI ahora espera los bounds medidos como Flutter, calcula columnas/filas desde el pane antes de `createOrAttach`, conserva esas dimensiones al restaurar/resincronizar y notifica solo cuando los bounds cambian. Tras recompilar y llevar la ventana a tamaño completo, un `Terminal 3` nuevo mostró cwd `leynier-website`, branch `main`, prompt y cursor en una sola línea; el tab temporal se cerró y no se dejó sesión de prueba.
- Explorer root error fixture (2026-08-26): Computer Use comparó `Sidebar Beta` con ruta inexistente. Flutter deja el árbol vacío y muestra solo `Item not found`; GPUI mostraba icono centrado y el path/errno crudo. Se corrigió la normalización del endpoint de listado, se eliminó el overlay adicional de raíz en GPUI, se recompiló el sidecar y la captura posterior mostró el mismo árbol vacío y toast `Item not found` en GPUI; el fixture adicional fue eliminado después.
- Explorer root error cleanup (2026-08-26): se eliminó el workspace temporal `Explorer Error Probe`, se verificó que `/private/tmp/alera-explorer-error-probe` no existe y se reinició el único host para que ambas apps vuelvan a mostrar 9 workspaces; Flutter, GPUI y runtime quedaron nuevamente en una sola instancia cada uno.
- Terminal cursor affordance (2026-08-26): Flutter mantiene cursor de texto sobre la superficie terminal y usa mano solo cuando hay un enlace; GPUI ahora declara `IBeam` como cursor base y conserva `PointingHand` para links resueltos. Falta una captura directa del hover después del rebuild.
- Terminal cursor affordance validation (2026-08-26): GPUI se recompiló y relanzó con una sola instancia, el host único siguió activo y ambas ventanas se llevaron nuevamente a tamaño completo; la captura directa del hover queda para el fixture de enlaces junto con el resto de escenarios terminales pendientes.
- Terminal link hover exit (2026-08-26): Flutter limpia el enlace y cursor al abandonar `MouseRegion`; GPUI ahora instala `on_hover` por superficie y elimina `terminal_hovered_link` cuando el puntero sale, evitando subrayado/cursor stale entre panes. El cambio quedó recompilado.
- Terminal link hover exit validation (2026-08-26): el binario GPUI recompilado volvió a abrir sin modal fatal y con las dos apps/host únicos; el gesto visual de salida queda pendiente por la limitación de Computer Use para mover el puntero sin click.
- Editor Markdown source/preview regression (2026-08-26): con el layout compartido activo en el tab `editor` de `readme.md`, GPUI podía conservar `show_preview=true` de una vista anterior y renderizar preview mientras Flutter mostraba fuente. El renderer ahora ignora ese flag global para tabs editor `.md`/`.mdx`; `markdownViewer` sigue siendo el único tab de preview. Tras rebuild, ambas capturas muestran Markdown fuente en el mismo split.
- Editor Markdown inactive-pane parity (2026-08-26): la auditoría del split mostró que el renderer inactivo de GPUI convertía cualquier `editor` Markdown/Mermaid con asset cacheado en viewer, mientras Flutter mantiene la fuente salvo `markdownViewer`/`mermanPreview`. GPUI ahora aplica preview en panes inactivos solo para esos roles explícitos; la captura posterior confirmó fuente en el pane superior y terminal intacta abajo.
- Editor inactive gutter parity (2026-08-26): el pane superior inactivo de GPUI seguía usando texto plano sin números de línea, mientras Flutter conserva el `CodeField` completo. GPUI ahora reutiliza el `Input` code-editor deshabilitado para tabs `editor` inactivos, manteniendo gutter, resaltado, wrapping y scrollbar; la captura tras rebuild mostró números de línea y resaltado en ambos clientes.
- Settings scrollbar re-audit (2026-08-26): el listener de GPUI dejó de sumar el delta automático de `overflow_y_scroll`; el contenido se mantiene intrínseco y el `Scrollbar` usa el mismo `ScrollHandle` para wheel/drag. El factor calibrado vigente `0.43` deja `Keep Runtime`, `Empty Host`, `Detached`, `Diagnostics` y `Updates` en el mismo anclaje que Flutter para el primer salto a tamaño completo; el segundo salto y el drag completo quedan para el gate porque Computer Use no entrega un delta estable en Flutter.
- Settings scrollbar drag validation (2026-08-26): con GPUI a tamaño completo, el click en el track llevó el contenido al final (`Support Alera`) y el drag del thumb desde el extremo inferior volvió al inicio sin activar controles subyacentes. La geometría de interacción del thumb ya funciona con el overflow manual; la matriz sigue parcial por la variabilidad del wheel semántico de Flutter y los panes restantes.
- Settings scrollbar calibration (2026-08-26, superseded): una repetición inicial atribuyó el desplazamiento a un factor `0.84`; al aislar el listener se comprobó que el delta automático se estaba sumando dos veces. La corrección vigente mueve el wheel al contenido hijo, desactiva el overflow listener automático y usa `0.43` sobre el `ScrollHandle` real.
- Toast exit animation (2026-08-26): Flutter's toast host keeps each entry for 4 s, then fades/slides it out for 180 ms before removing it. GPUI now retains entries through the same exit window, marks exiting entries, animates opacity with a GPUI animation, and notifies only when toast state crosses an entry/exit boundary. A rebuilt `Path Copied` toast rendered and expired without a crash; the three-entry stack remains capped.
- Editor state per route (2026-08-26): GPUI previously reused one `InputState` for every editor tab. The editor now keeps an input entity per file path, subscribes changes to the active route, and reuses that entity when returning to a tab, preserving its selection/scroll-capable input state instead of resetting it. Computer Use edited `.prettierrc`, switched to `top-languages-stat.astro`, returned to `.prettierrc` with the marker intact, then used the real Discard action; the temporary change was removed without writing the file.
- Editor source/preview isolation (2026-08-26): selecting the persisted `readme.md` editor exposed a GPUI-only mismatch: Flutter showed Markdown source while GPUI rendered the preview because any other Markdown viewer tab for the same path forced the global `show_preview` flag. GPUI now derives preview mode only from the selected tab kind or its explicit `mermanPreview` role. After rebuild and native zoom, the same `readme.md` tab rendered source in both apps; the separate preview flow remains queued for a second direction check.
- Editor missing-file recovery (2026-08-26): a temporary Markdown tab was opened in both clients, then the file was removed. Flutter showed a centered `File not found` message without an icon. GPUI now keeps an error message per file path, prefers the selected tab path over the previous document cache, and renders the same centered text without the prior error glyph. The temporary tab records and file were removed from the runtime store/filesystem after validation.
- Runtime fixture cleanup (2026-08-26): the temporary missing-file tab ids were removed from the runtime `workspaceTabs` and `workbenchLayouts` records after the UI cleanup, and the fixture file was removed from the workspace. Duplicate hosts created by the repeated recovery fixture were terminated; the final session is kept to one Flutter client, one GPUI client and one persistent host.
- Editor preview direction check (2026-08-26): with the same `readme.md` source tab and `Source Control` context active, `Open Preview` was invoked in both clients. Both rendered the same Markdown viewer content in the selected pane without changing the lower terminal split; the remaining Markdown difference is renderer-level typography/spacing and stays in the partial matrix.
- Terminal wrapped-link parity (2026-08-26): Flutter's terminal resolver joins visually wrapped rows before matching HTTP URLs; GPUI previously searched one row and lost long links at the wrap boundary. GPUI now reconstructs logical rows using Alacritty `WRAPLINE` cells and returns the correct URL segment for either row. The focused regression `visible_http_links_continue_across_wrapped_rows` passes, and a Computer Use fixture rendered a long wrapped URL in a temporary Terminal 6 without crash; the fixture tab/session was removed.
- Terminal recovery error copy (2026-08-26): Flutter prefixes reconnect/resync failures with `Terminal host unavailable: ...`; GPUI previously surfaced the raw bridge error. Reconnect and full-output resync failures now use the same prefix while startup errors and snapshot-decode messages keep their distinct Flutter copy. The rebuilt app reopened at full size without a modal or process regression; an induced host-outage interaction remains in the final fixture pass.
- Terminal exit coordinator parity (2026-08-26): el contrato Flutter elimina automáticamente la pestaña cuando el proceso PTY termina. GPUI ahora marca la sesión y solicita el mismo `tab.remove` de forma autónoma al recibir `exit`, poda presencia/estado asociado y deja que el snapshot duradero reconcilie cualquier fallo de persistencia. Con Flutter cerrada temporalmente, el binario recompilado creó `Terminal 3`, ejecutó `exit` y retiró la pestaña del workbench y del registro (`alera tab list` devolvió cero); no quedó fixture persistente. La referencia Flutter se relanzó después con `make app-debug` usando Flutter 3.44.8.
- Search tooltip parity (2026-08-26): la comparación de código mostró que el toolbar Search/Replace de GPUI no exponía los mismos tooltips que Flutter. GPUI ahora publica `Clear search results`, `Search/Ignore ignored files`, `View as tree/list`, `Expand/Collapse All`, `Refresh`, los toggles de caso/palabra/regex y los reemplazos por archivo/match; los controles siguen conservando el cursor de mano solo cuando están habilitados.
- Editor tab file-icon parity (2026-08-27): los tabs de editor de GPUI usaban siempre el icono genérico de documento, mientras Flutter proyecta el icono específico del archivo. `tab_kind_icon` ahora reutiliza el catálogo Material de Explorer para rutas de editor/Markdown (`.astro`, `bun.lock`, imágenes, Markdown y nombres especiales), conserva los iconos propios de terminal/diff y mantiene el fallback genérico cuando no hay ruta. La banda superior y el feedback flotante de drag usan la misma ruta del archivo; la build recompilada y la captura a 1302x768 muestran los iconos específicos.
- GPUI live rebuild validation (2026-08-27): `make DART='puro dart' gpui-debug` recompiló y relanzó una única GPUI; después del doble click semántico de zoom, la captura quedó a 1302x768 y el status de procesos conserva una Flutter, una GPUI y un host compartido mediante `host.json`. La última ejecución terminó correctamente tras cerrar la instancia anterior; el warning externo de `block v0.1.6` sigue reservado para el gate final.
- Editor tab icon regression (2026-08-27): `cargo test --manifest-path experiments/alera-gpui/Cargo.toml --bin alera-gpui --all-targets` pasa después de añadir la ruta de archivo al drag data; el preview conserva el icono específico y no altera la mutación de tabs.
- Settings Agent Profiles recheck (2026-08-27): con una sola Flutter y una sola GPUI a tamaño completo, el pane `Agent Profiles` y el dropdown `Adapter Type` se compararon con el mismo perfil Codex. El master/detail, campos vacíos, flecha, lista de ocho adapters, check seleccionado, cierre externo y scroll visible quedaron alineados; no se guardaron cambios ni se dejó un dropdown abierto.
- Agent Profiles Remove recheck (2026-08-27): un perfil temporal se creó, guardó y eliminó desde GPUI. Remove no mostró toast, la lista convergió al primer perfil restante igual que Flutter y el registro `Parity Flutter Remove Probe` fue retirado del host; solo permanece el perfil previo `Codex Sidebar Test`.
- Git Diff tab icon parity (2026-08-27): el icono leading de tabs `gitDiff` de Flutter es `gitBranch`, no `gitCompare`. GPUI ahora usa `GitBranch` en el tab strip y en el feedback de drag, dejando `Diff` para la superficie de comparación; queda incluido en el siguiente rebuild/captura de la matriz de tabs.
- Explorer clipboard copy parity (2026-08-27): Flutter muestra `Path copied` y `Relative path copied` con toast informativo; GPUI usaba title case y clasificaba la ruta relativa como éxito. Se alinearon ambos mensajes y la inferencia de tono, con tests dirigidos de toasts pasando.
- Explorer success-feedback parity (2026-08-27): el código Flutter no emite toasts al completar Copy/Cut, Rename, Move, Duplicate, Paste o Delete; solo informa copias de ruta y errores. GPUI dejó de publicar esos mensajes de éxito adicionales, manteniendo los refresh/selección y los errores visibles.
- Explorer reveal parity (2026-08-27): `Reveal in Finder` ya no abre únicamente el directorio padre con `file://`; GPUI usa `open -R` en macOS, `/select,` en Windows y el administrador de archivos equivalente en Linux, con validación de existencia y el mismo mensaje de error de Flutter.
- Quota settings tooltip parity (2026-08-27): los botones de pin, orden y gestión de perfiles CCS del pane Quotas ahora exponen los mismos tooltips de Flutter (`Shown In Status Bar`, `Move ... Earlier/Later`, `Edit CCS Profile`, `Remove CCS Profile`) y mantienen el cursor básico cuando están deshabilitados.
- Settings control tooltip parity (2026-08-27): Terminal Font Family expone `Clear` y `Fonts`; Projects expone `Overwrite Existing Destination`, `Remove Copy Rule` y `Remove Setup Command`, igual que los controles compartidos de Flutter.
- Inactive editor theme parity (2026-08-27): la captura estable midió el fondo Flutter GitHub Dark en `#0e1116` y el pane inactivo GPUI en `#242424`; el estado disabled de `gpui-component::Input` estaba sustituyendo el tema. GPUI conserva ahora el Input normal detrás de una capa read-only de activación y usa el fondo real del `HighlightTheme` también en el rail del scrollbar.
- Markdown source syntax parity (2026-08-27): Flutter/re_highlight usa el capture `section` para headings y `bullet` para marcadores, mientras Tree-sitter GPUI los trataba como `title` púrpura. Se registró `markdown-alera`, con headings/markers en el rol azul de sección, bullets y emphasis por cada uno de los diez temas; los tests del catálogo y del language registry pasan.
- Editor Markdown visual validation (2026-08-27): la recompilación y captura normalizada a 1302x768 confirma que los tres headings visibles de `readme.md` ya usan el azul de sección de Flutter en GPUI. Al activar el pane inactivo mediante Computer Use, el editor conservó el documento, el fondo `#0e1116` y el caret/active line sin recuperar el borde blanco externo. Evidencia local en `/tmp/alera-parity/editor-markdown-fixed-2026-08-27` y `/tmp/alera-parity/editor-activation-2026-08-27`.
- Editor active-line parity (2026-08-27): la inspección de CodeForge 10.9.0 confirmó que Flutter no pinta una franja persistente bajo la línea del cursor. GPUI la añadía desde `HighlightTheme` incluso sin foco. Los diez temas omiten ahora `editor.active_line.background` y `editor.active_line_number`; el test dirigido exige ambos valores ausentes y la captura recompilada muestra la misma superficie continua de Flutter en estado activo e inactivo. Evidencia local en `/tmp/alera-parity/editor-active-line-2026-08-27`.
- Editor gutter parity (2026-08-27): `gpui-component` 0.5.1 reservaba cinco columnas para todos los números de línea, mientras CodeForge calcula la anchura usando el total real. Como upstream no publicó aún una versión compatible con GPUI 0.2.2, se vendorizó 0.5.1 y se backporteó únicamente el cálculo publicado en `9aedbb8779a833c114bbfffbc308bda1d2dc6778`. La captura con 19 líneas mueve el texto de GPUI al mismo inicio horizontal de Flutter sin alterar los números, syntax, wrap o scroll. Evidencia local en `/tmp/alera-parity/editor-gutter-2026-08-27`.
- Editor selection probe (2026-08-27): el drag sobre la segunda línea selecciona el rango esperado en GPUI sin editar el buffer y la selección se limpia al devolver el foco al terminal. El mismo gesto sintético de Sky no produjo selección visible en CodeForge, incluso después de activar el pane y el caret, por lo que la fila sigue abierta para una prueba de teclado/drag físico en el gate final y no se declara cerrada por inferencia.
- Runtime multiplicity recheck (2026-08-27): después de los dos rebuilds del editor permanecen exactamente una GPUI, una Flutter y un runtime host compartido. Ninguna ventana fatal apareció durante activación, drag, teclado o cambio de foco.
- Source Control live-state parity (2026-08-27): con el mismo repositorio y ocho cambios unstaged, Flutter y GPUI muestran el mismo composer, acción primaria `Pull`, resumen `main · origin/main · behind 2`, paths, estados `M`/`R`, adiciones/borrados, acciones por fila y footer. GPUI dejó de capitalizar `ahead`, `behind` y `conflicts`, y el contador de COMMITS permanece oculto hasta la primera carga como en Flutter. Evidencia local en `/tmp/alera-parity/source-control-real-2026-08-27`.
- Git history live-state parity (2026-08-27): al expandir COMMITS por primera vez, ambas apps muestran `50+`, el mismo alto persistido, grafo, boundary `Incoming Changes`, refs `origin/main`/`main`, asuntos y refresh. No se ejecutaron stage, discard, commit, pull ni otra mutación Git. Evidencia local en `/tmp/alera-parity/source-history-real-2026-08-27`.
- Source Control tree-mode parity (2026-08-27): con los mismos ocho cambios se alternó de lista a árbol en ambas apps. GPUI conserva la jerarquía `src/components/stats` y `src/content/home`, contadores, indentación, iconos, estados y acciones; las hojas muestran ahora basename y una sola línea como Flutter mientras el path completo permanece en el payload de diff/stage/discard. El test dirigido comprueba que el label no reemplaza el path operativo. Evidencia local en `/tmp/alera-parity/source-control-tree-2026-08-27`.
- Terminal clear/restore semantics (2026-08-27): el fixture local `https://alera-parity.invalid/path?q=1` reveló que el normalizador de snapshots podía descartar `CSI 2J/3J`, resucitar scrollback anterior o borrar comandos entre prompts. `TerminalSession` recorta ahora el snapshot en el último `CSI 3J`, preserva comando/output, elimina únicamente filas de redraw stale y repone filas vacías al final. Los tests cubren clear de scrollback, replay incremental, prompts envueltos y conservación de comando/output. Evidencia local en `/tmp/alera-parity/terminal-remove-stale-2026-08-27` y `/tmp/alera-parity/terminal-safe-restore-2026-08-27`.
- Terminal resize pulse rollback (2026-08-27): se probó integrar el pulse manual de viewport al restore/zoom, pero los SIGWINCH adicionales alteraban también la vista Flutter de la PTY compartida. El workaround fue retirado por completo. La sesión histórica de `Terminal 1`, ya contaminada por muchos redraws de pruebas previas, conserva un prefijo parcial al relanzar; se mantiene como gap abierto hasta repetir en una terminal nueva y limpia, sin mutar la sesión para ocultarlo.
- Terminal fresh-session restore probe (2026-08-27): se creó `Terminal 3`, se validó su prompt inicial completo, se ejecutó `clear` y el marcador `fresh-restore-parity`, y se relanzó GPUI. El último `CSI 3J`, el comando y su output se restauran correctamente, pero el zoom todavía refluye una cola histórica delante del prompt incluso en esta sesión nueva. Esto descarta que el defecto sea solo contaminación del fixture anterior y lo acota a resize/reflow de OSC 133 en Alacritty. El tab temporal fue cerrado y el registro vuelve a contener únicamente Terminal 1, Terminal 2 y Terminal 5.
- Terminal final-dimension rebuild fix (2026-08-27, supersedes the remaining fresh-session gap): `TerminalSession` conserva hasta 10 MiB del stream desde el último `CSI 3J`, incorpora output vivo y, después del debounce del zoom, reconstruye Alacritty al tamaño final antes del único resize normal de la PTY. No se usan pulses extra. La captura posterior al relanzamiento muestra exactamente el mismo prompt único, completo y sin scrollback resucitado que Flutter. El test dirigido cubre rebuild 12x4 -> 40x8 con clear y output vivo. Evidencia local en `/tmp/alera-parity/terminal-rebuild-resize-2026-08-27`.
- Terminal link/selection live probe (2026-08-27): fixtures `.invalid` aparecen en ambas apps sobre la misma sesión. GPUI resuelve y subraya el rango completo sin abrirlo al click normal; el drag produce selección visual y se limpia al hacer click. La selección sintética equivalente de Sky sigue limitada en Flutter, pero la activación GPUI ya se cerró con un Cmd-click nativo controlado.
- Explorer folder/symlink backend parity (2026-08-27): el límite nativo compartido por Flutter y GPUI ahora tiene cobertura dirigida para copy recursivo de directorio con ` copy 2`, rechazo de mover una carpeta dentro de su descendiente y rechazo de copy/move de symlink sin modificar el target. Los cuatro tests `workspace_entry_` pasan. La funcionalidad ya no queda inferida solo del código, pero falta provocar el symlink desde GPUI para capturar el toast `Operation is unsupported` antes de cerrar visualmente la fila.
- Explorer symlink error-toast parity (2026-08-27): se recreó el fixture temporal `Sidebar Alpha` con `target.txt` y `target-link.txt`, se ejecutó Duplicate sobre el symlink en Flutter y GPUI, y ambas apps mostraron `Operation is unsupported` con el mismo icono, posición y geometría. Se corrigió que los refreshes exitosos del watcher borraran errores de filesystem antes de completar los cuatro segundos del toast. El symlink nunca se duplicó y el fixture temporal fue enviado a la Papelera mediante `/usr/bin/trash`. Evidencia local en `/tmp/alera-parity/explorer-symlink-2026-08-27`.
- Explorer rename-conflict and folder-copy parity (2026-08-27): con `source.txt`, `existing.txt` y `folder/nested.txt` se intentó renombrar `source.txt` a `existing.txt` en ambas apps; los diálogos se cerraron y mostraron el mismo toast `Item already exists`, conservando ambos archivos. Duplicate sobre `folder` creó `folder copy` y `nested.txt` apareció al expandirlo en Flutter y GPUI con la misma indentación, iconos y orden. El fixture completo fue enviado a la Papelera y ambas apps regresaron a `main`. Evidencia local en `/tmp/alera-parity/explorer-conflicts-2026-08-27`.
- Search result keyboard scope audit (2026-08-27): Flutter no registra navegación Up/Down/Enter sobre las filas de resultados; `_SearchMatchRow`, `_SearchFileRow` y `_SearchDirectoryRow` son pointer-only y los shortcuts globales solo enfocan el input. GPUI ya replica ese contrato, por lo que no se añadirá una interacción extra que diverja de Flutter. La fila sigue abierta únicamente por loading sostenido y conflicto durante replace bajo backend controlado.
- Editor external-change conflict parity (2026-08-27): con `conflict.txt` se editaron buffers locales distintos en Flutter y GPUI, se modificó el archivo desde la terminal y Save produjo el mismo diálogo `File changed on disk` / `Overwrite the file with the editor contents?`, con Cancel y Overwrite. Cancel preservó el buffer local; Overwrite GPUI escribió `local-edit` en disco y un guardado posterior mostró `File saved`. Se eliminó la última limpieza de `local_message` al iniciar refresh de Explorer, de modo que toasts de save/overwrite sobreviven cuatro segundos. El tab temporal fue cerrado, el fixture enviado a la Papelera y ambas apps regresaron a `main`. Evidencia local en `/tmp/alera-parity/editor-conflict-2026-08-27`.
- Política de progreso (2026-08-27): no enviar mensajes, capturas ni álbumes por Telegram. Las capturas de Flutter/GPUI se conservan únicamente como evidencia local de Computer Use para dedicar el tiempo operativo a cerrar la matriz.
- Source Control row tooltip parity (2026-08-26): las acciones compactas de Stage/Unstage/Discard en grupos y filas no tenían tooltip en GPUI aunque Flutter los expone en cada `AleraIconButton`. GPUI ahora publica el mismo texto contextual y conserva el color destructivo y el hitbox de 24 px.
- Tab action tooltip parity (2026-08-26): los botones `New Tab` y `Pane actions` de los grupos persistidos no exponían tooltip en GPUI. Se añadieron los labels de Flutter sin alterar el menú, el drag ni el layout del tab strip.
- Quota status tooltip parity (2026-08-26): el overview de cuotas, el refresco y cada proveedor visible del status bar ahora exponen `All Agent Quotas`, el mensaje de refresco periódico y el detalle del proveedor/lecturas, igual que `AleraHoverCard` y `AleraIconButton` de Flutter.
- Runtime host identity parity (2026-08-26): la prueba de coexistencia reveló que GPUI arrancaba el sidecar con `runtime-host.json` y Flutter con `host.json`, generando dos hosts y estados divergentes. GPUI ahora usa `host.json`, y `gpui-debug` propaga `ALERA_APP_ID` y `ALERA_RUNTIME_DIR` en macOS, Linux y Windows; se cerraron los dos PIDs conocidos y Flutter levantó un único host compartido, tras lo cual ambas ventanas volvieron a conectarse sin modal fatal.
- Runtime single-host validation (2026-08-26): después del rebuild, `ps` muestra exactamente una Flutter, una GPUI y un host; solo existe `host.json` en el directorio `dev.leynier.alera.dev/terminal_host`, `runtime-host.json` no se vuelve a crear y el comando `alera version --json` confirma el mismo commit/version para cliente y host. Ambas ventanas quedaron a 1302×768 y sin modal fatal.
- Pull Request tooltip validation (2026-08-26): la comparación estable del formulario sin PR mostró el mismo `Base Branch`, `Title`, `Description`, `Create Pull Request` y `Link Existing Pull Request`; GPUI añadió los tooltips `Refresh`, `Edit Pull Request`, `Open In Browser`, `Start Conversation`/`Add Comment` y la operación de refresco mantiene el loader animado cuando está ocupada. No se modificó ninguna PR remota.
- Settings star preference parity (2026-08-26): GPUI ahora carga y persiste `general.starClicked` junto con el registro SQLite compartido con Flutter, y lo marca después de un star exitoso. Esto evita que el aviso de soporte se reactive al alternar entre clientes y conserva las demás claves de `general` mediante el merge recursivo.
- Terminal host disconnect recovery (2026-08-26): stopping the single runtime host reproduced Flutter's `Terminal Unavailable` overlay while GPUI left the terminal blank. Bridge `Unavailable`/`Disconnected` events now mark every visible GPUI terminal session with the same recoverable error state and clear in-flight operation labels, exposing `Reconnect` and `Restart Terminal`. After rebuild, the modal appeared in GPUI at the same centered geometry as Flutter; its overlay/operation/restore surfaces now force Inter instead of inheriting JetBrains Mono. Clicking `Reconnect` restored both clients after the host was restarted, and the fixture ended with exactly one runtime host.
- Terminal per-pane scroll routing (2026-08-26): GPUI's wheel handler used the global selected terminal id, so a split could scroll a different pane than the pointer target. Each terminal surface now captures its own session id when registering `on_scroll_wheel`, matching Flutter's per-session `ScrollController`; the change compiled and the single-host session remained intact.
- Terminal per-session scrollbar activity (2026-08-26): the scrollbar visibility clock was also global, so interacting with one split could paint a thumb on every terminal surface. GPUI now stores last scrollbar activity by session, scopes drag visibility to that session, and clears the map on workspace reset. The rebuild launched cleanly with one GPUI and one runtime; a two-pane screenshot remains queued for the next terminal fixture.
- Terminal per-pane scrollbar validation (2026-08-26): a real temporary split moved `Terminal 4` to the upper-right pane. Scrolling it changed only that pane and showed its thumb; after activating the lower pane, scrolling changed only the lower terminal and its thumb. `Terminal 4` was moved back to the original lower split and returned to the bottom of scrollback.
- Terminal clean-session validation (2026-08-27): se creó y cerró una pestaña temporal `Terminal 3` en GPUI después de comprobar cwd, prompt y cursor; el registro SQLite no conserva la pestaña ni el host mantiene una sesión huérfana. El layout persistido vuelve a contener únicamente `Terminal 1` y `Terminal 2`, sin pane vacío ni overlay residual, con una sola Flutter, una sola GPUI y un solo host compartido.
- Editor loading supersession (2026-08-26): GPUI previously returned early whenever any editor path was loading, so switching tabs during a slow read could leave the old document mounted under the new tab. `ensure_selected_editor_loaded` now only waits for a request targeting the selected path; a different selection starts a new generation and discards the stale response, matching Flutter's per-widget load request guard. The change compiled and relaunched without a modal regression; a deliberately delayed backend fixture remains for the final loading gate.
- Editor/Explorer operation isolation (2026-08-26): Explorer and Editor shared `local_generation` and `local_busy`, so a file read could invalidate the directory response and leave Explorer in an animated `Loading...` state indefinitely. GPUI now uses separate `explorer_generation`/`editor_generation` and `explorer_busy`/`editor_busy` states; a persisted terminal with 5511 history rows restored without the global loader or the previous 100% redraw loop, and idle GPUI settled near 4-16% CPU after relaunch. The shared `local_busy` remains for Search/Git/Source Control until those domains receive the same scoped-state audit.
- Search/Git operation isolation (2026-08-26): the remaining shared `local_busy`/`local_generation` was split into `search_busy`/`search_generation` and `git_busy`/`git_generation`, including Source Control folder focus, refresh, commit/follow-up and Git history actions. A Search query still returns GPUI's `1 match in 1 file` result for `workspace` after rebuild, while Source Control keeps its own refresh/error gate instead of being cancelled by Search. The live Source Control/Search comparison remains `parcial` for real conflict/loading/error and keyboard traversal fixtures.
- Source Control action-state parity (2026-08-26): code comparison with Flutter found GPUI enabled Fetch/Pull and other menu entries while the default Git snapshot was still loading because it checked only for the absence of an error. The menu now requires a settled snapshot and uses `head_message` for `Commit Amend`, matching Flutter's `hasData`/`hasHeadCommit` gates. Rebuilt GPUI and opened the menu during a refresh; disabled actions stayed disabled and the refresh toast completed normally.
- Git History empty/error state (2026-08-26): Flutter's `_HistoryMessage` is plain centered muted text for both `No Commits Yet` and errors; GPUI added an error glyph and gap in the empty history branch. The glyph was removed so the no-repository/history error state follows the same message-only contract. Rebuilt GPUI and expanded the real `COMMITS 50+` graph successfully.
- Context Sidebar click isolation (2026-08-26): Computer Use reproduced a GPUI-only regression where clicking the Search context button outside the workbench activated the stale terminal tab in the primary pane, while Flutter kept the current editor. The window-level tab-drag observer now ignores every click outside the live workbench rectangle, including both sidebars and the collapsed context rail. After rebuild, the sequence `.prettierrc` -> Search kept the editor visible in GPUI exactly as in Flutter; the existing Search loading/error and keyboard rows remain under audit.
- Initial view preference hydration (2026-08-26): GPUI could miss the early `Connected` event and retain default sidebar/context widths after launch even though Flutter restored `workbench_view_prefs_table`. `start()` now explicitly hydrates shared/local view preferences and status data in addition to the event-driven path; the persisted `groupBy`, workspace ordering and widths are loaded before the first normal interaction. After applying the required native titlebar zoom, the 1302 px GPUI capture aligns the sidebar/context boundaries with Flutter; the earlier mismatch came from comparing an 1182 px unzoomed GPUI window to a maximized Flutter window.
- Full-size image/shell comparison (2026-08-26): with both clients zoomed through the native titlebar action and `dev-banner.webp` selected, GPUI and Flutter align the image bounds, editor/terminal split, sidebar edge and Context Sidebar edge. The remaining visible terminal difference is historical scrollback content from the shared fixture, not layout geometry; a clean-session terminal comparison remains required for the final gate.
- Settings scrollbar follow-up (2026-08-26, superseded): the first full-size Computer Use pass used a `0.42` delta before the duplicate automatic listener was isolated. Keep the historical observation for traceability; the current implementation is the single-listener `0.43` calibration recorded above.
- Settings scrollbar recheck (2026-08-26, superseded): the initial `0.42` calibration was judged against a double-applied delta. The current factor is `0.43` with the automatic overflow listener disabled; Settings remains `parcial` for unvisited panes, reset/default/error persistence and the complete thumb-drag matrix.
- Settings scrollbar final calibration pass (2026-08-26): with both Settings dialogs reset to offset zero, one identical Computer Use wheel gesture placed the Runtime, Diagnostics and Updates groups within the same visible rows in Flutter and GPUI at 1302×768. Both dialogs were closed after the capture.
- Terminal burst backpressure (2026-08-26): Terminal output parsing remains incremental and UI invalidation is coalesced to one 16 ms frame, preventing one redraw per PTY fragment. A 50,000-line burst restored the visible tail and prompt without a fatal modal after the operation-state split; one earlier saturated run exposed the runtime client's 32 MiB frame guard, so the client error now includes frame kind/length for the remaining host-resync fixture. The dedicated 50k restore/streaming and frame-boundary matrix remains open before declaring Terminal parity complete.
- Terminal 50k restore gate (2026-08-26): a fresh temporary GPUI terminal ran `seq 1 50000`, then GPUI was relaunched and restored the tail (`49987`-`50000`) plus the workspace prompt/cursor. Computer Use showed no fatal modal, no stuck loader and idle CPU around 10%; the temporary session was exited through the runtime CLI and disappeared from the layout. The frame-boundary test for an actual oversized host frame remains open.
- Editor rapid-tab validation (2026-08-26): after the loading-supersession fix, Computer Use switched `.prettierrc -> top-languages-stat.astro -> .prettierrc` without intermediate state reads. GPUI returned to the correct `.prettierrc` content with no `File operation failed` surface or stale document; the same tab layout remains in place.
- Editor conflict validation (2026-08-26): a reversible external edit to `.prettierrc` triggered GPUI's save conflict dialog. The live modal matched Flutter's `File changed on disk`, `Overwrite the file with the editor contents?`, `Cancel` and destructive `Overwrite` contract; the action was cancelled, the buffer discarded, the original no-newline file restored exactly, and Git status returned clean.
- Split left-direction pass (2026-08-25): a temporary `Terminal 6` was dragged from the lower split to the left edge of the editor pane. GPUI created the left preview/split, Flutter reflected the persisted layout after refreshing its workspace, and closing the only tab in that temporary pane removed the empty split in both clients. The temporary session was removed; the four-direction/reorder matrix remains open for the final pass.
- Terminal zoom debounce hardening (2026-08-25): a fresh Computer Use titlebar double-click after relaunch reproduced one late zsh redraw from the historical terminal even with 350 ms. The PTY resize path now uses a per-session generation debounce (650 ms): every new bounds event invalidates older timers, so only settled dimensions reach the host. After rebuild and full-size double-click, the terminal stopped adding horizontal prompt duplicates; the remaining visible lines are the session's existing history. A second clean-session zoom pass remains in the final terminal matrix.
- Terminal zoom clean-session pass (2026-08-25): a temporary GPUI `Terminal 6` was created, verified at the workspace cwd with a clean prompt, zoomed to full size through the native window action, and compared against Flutter at 1302x768. No prompt flood or duplicate resize rows appeared; the temporary tab/session was then closed and removed from the host.
- Terminal resize/restore regression (2026-08-25): after the toast build, Computer Use reproduced the prompt-redraw corruption when GPUI was double-clicked to full size: the shared host scrollback contained repeated zsh redraw frames from intermediate PTY sizes. GPUI now skips emulator resizes while a snapshot is replaying and uses the generation-based 650 ms settled-bounds debounce, while Flutter keeps its 150 ms embedder path. Rebuilding and repeating the exact titlebar double-click left the terminal with clean cwd/prompt/cursor rows and no new horizontal prompt flood; Flutter stayed unchanged. This closes the zoom/restore regression, while scrollback from already polluted historical sessions remains a fixture issue rather than new output.
- Toast stack validation (2026-08-25): two different Explorer clipboard actions triggered within four seconds now remain visible simultaneously in GPUI, with the newest toast at the bottom and the previous toast stacked above it; the info/success icon tones remain distinct and the stack is capped at three entries. The stack was verified after rebuild at full size; no Telegram delivery was used.
- Keyboard/Search interaction validation (2026-08-25): GPUI `Mod+B` collapses and reopens the sidebar, `Mod+Shift+F` opens Search, typing `workspace` returns the same `1 match in 1 file`/`bun.lock` result as Flutter, and the Search close control restores the workbench. The apparent extra `project` row in Flutter was the persisted Replace field, not a missing GPUI control; GPUI shows the same row when Replace is expanded. Both apps were returned to the workbench after the comparison.
- Settings scrollbar comparison (2026-08-25): with both apps at full size and the same Application pane, the same Computer Use wheel gesture reaches the same Diagnostics/Updates/Support content and both thumbs remain bounded. GPUI lands about one row farther down than Flutter for the synthetic `pages=1` wheel event (the native AX backends deliver different scroll deltas), so the geometry/drag path remains `parcial`; both Settings dialogs were closed afterwards.
- Image tab activation regression (2026-08-25): Computer Use caught a real stale-render bug: selecting an existing `dev-banner.webp` tab in GPUI displayed the previous source document under the image path, while Flutter showed the image viewer. `activate_workspace_tab` now restores cached `Image`/`Mermaid` preview mode for editor tabs instead of forcing every editor to source mode. After rebuild and titlebar double-click, the same image tab renders centered with the same contained size in GPUI and Flutter, with the terminal pane preserved below.
- Image opening feedback (2026-08-25): the same source -> image tab switch briefly left an `Opening .prettierrc` global toast over the image in GPUI, which Flutter does not show. File loading now uses the inline editor/image loading surface and clears global feedback before a read; the rebuild/repeat leaves the centered image with no stale opening toast.
- Split/image regression pass (2026-08-25): after the image fix, dragging the existing Markdown tab to the right edge created a real right pane with a preview/source pair and no stuck overlay; the pane menu closed the split cleanly. The same `dev-banner.webp` then matched Flutter at the same contained bounds with the Context Sidebar open.
- Terminal zoom follow-up (2026-08-25): a later rebuild showed that 150 ms still allowed a second GPUI bounds event during macOS zoom, reintroducing a horizontal zsh prompt redraw in the shared scrollback. The PTY debounce is now generation-based at 650 ms for GPUI, while restore-time resizing remains disabled; repeating the titlebar double-click after the rebuild leaves a clean prompt with no new horizontal flood. The longer delay is scoped to GPUI because Flutter's embedder emits one settled resize at 150 ms.
- Preview cursor parity (2026-08-25): the image/Mermaid viewport previously advertised GPUI's open-hand grab cursor while Flutter's `InteractiveViewer` uses the click/pointing-hand affordance before a gesture. The preview surface now uses `PointingHand`; the image viewer was rebuilt and visually rechecked at full size.
- Source Control AI gating (2026-08-25): Flutter disables `Generate commit message with AI` when there are no staged changes, conflicts, loading, or the feature is disabled. GPUI previously left that icon clickable and only rejected it after the click. The toolbar now derives the same staged/conflict/busy gate while retaining cancellation when generation is active; the rebuilt Source Control panel was opened beside Flutter with an unstaged-only snapshot and no toast/crash.
- Source Control AI gate live fixture (2026-08-25): one existing change was staged temporarily to exercise the enabled state, then explicitly unstaged again; the target repository returned to its prior unstaged/staged state. No commit, push, or destructive Git action was performed.
- Agent Profiles geometry/labels (2026-08-25): live Flutter comparison showed missing floating labels (`Name`, and the command/routing text fields) plus an 8 px master/detail inset drift in GPUI. GPUI now uses the same labels and adjusts the divider/detail inset; after rebuild the selected profile, input rhythm, dropdown arrows, managed rows and visible scroll content align much more closely with Flutter. Este corte quedó superseded por la validación completa de persistencia y discovery del 2026-08-28.
- Terminal scrollback recheck (2026-08-25): from a clean prompt, `seq 1 80` was generated once through GPUI and rendered identically in Flutter. Computer Use moved both panes up two pages and back down three pages; visible ranges, prompt/cursor, scrollbar thumb direction and bottom anchoring matched within the known one-line wheel/backend offset. No duplicate prompt flood appeared with the generation-based zoom debounce.
- Terminal exit cleanup (2026-08-25): sending `exit` to the shared test terminal removed the stopped tab/session from both clients without leaving a `Terminal Exited` overlay or stale sidebar row. The temporary replacement tab created to restore the fixture was removed again so no orphan test tab remains.
- Agent Profiles saving state (2026-08-25): GPUI now disables text fields, dropdown triggers, managed checkboxes, discovery refresh buttons and Save/Remove interactions while `saving=true`, matching Flutter's `enabled: !saving`; dropdown overlays also disappear from the interactive tree during save. A live unchanged Save request showed the disabled/loading button state and completed without changing profile data.
- Terminal fresh-session audit (2026-08-25): the GPUI New Terminal flow created a clean workspace prompt with no inherited redraw history; the temporary `Terminal 6` was removed after the audit so no test tab remains. The older shared session still contains historical zsh redraw bytes from prior resize fixtures, which remains the only scrollback parity gap to isolate from the fresh-session path.
- Agent tabs P1: los chips GPUI ahora reservan el slot de 6 px que Flutter usa entre el icono y el título, muestran el punto de estado para working/waiting/blocked/done con el color equivalente, tooltip por agente/estado y warning `(unacked)` para completados aún no vistos; la selección del tab registra el epoch `stateStartedAt` y el cierre limpia el reconocimiento. La captura a tamaño completo confirmó el ritmo de chips contra Flutter y se envió el álbum Telegram `2623/2624` con `/tmp/alera-parity/tab-status/flutter-tabs.jpg` y `gpui-tabs.jpg`; falta validar con hooks vivos antes de mover `Agent Activity y estados`.
- Referencia Flutter: `make app-debug` requiere Dart/Flutter 3.12.2. El intento con el `dart` del PATH (3.11.5) se rechazó antes de iniciar; la sesión vigente fue relanzada por el mismo target con `DART` y `FLUTTER` apuntando al entorno Puro `v3_44_8`, conservando un solo host persistente y una sola GPUI.
- No declarar el objetivo completo todavía: faltan escenarios de toda la matriz y hay que terminar la validación de los cambios actuales.

### Regla de rotación

Runtime Host, Resource Manager y Quotas están congelados como referencia transversal. Solo se reabren si una prueba de otro dominio demuestra una regresión; el siguiente dominio debe avanzar siempre según la lista `Siguiente`, y cada avance debe dejar una fila, una prueba y una evidencia actualizada.

## Hecho

- Inicializado y verificado el submodule `reference_projects/comet`.
- Inicializado y verificado el submodule `reference_projects/waku`.
- Leída y aplicada la skill `build-gpui-apps`.
- Leída y aplicada la skill `computer-use:computer-use` con `node_repl` y `@oai/sky`.
- Extraídos y usados los cambios de paridad GPUI existentes en sidebar, status bar, Quotas, Resource Manager, Runtime, Settings, terminal y workbench.
- Restauración de settings compartidos con Flutter, cuotas y perfiles.
- Settings compartidos: GPUI ahora lee y persiste las secciones locales de Flutter (`general`, `editor`, `terminal`, `diagnostics` y `keyboard`) mediante un merge JSON recursivo que conserva campos no representados por GPUI.
- Resource Manager alineado con todas las tabs globales, orden, filas, tooltips y sparklines.
- Runtime alineado en altura mínima de filas, título, botones, colores y estados.
- Runtime ocupado alineado con Flutter: solo los errores de host ocupado abren Force Stop, el mensaje enumera agentes/sesiones/jobs/subscriptions y los errores normales permanecen como toast.
- Confirmación `Force Stop Runtime` ajustada al diálogo Flutter: superficie, radio, padding, wrapping, botones de igual ancho y botón destructivo lleno.
- Quotas alineadas en proveedores, iconos, perfiles, estados, pines, reset y geometría.
- Sidebar corregido para calcular presencia de workspace con `snapshot.all_tabs`, incluyendo workspaces no seleccionados.
- Status bar corregido para calcular sesiones huérfanas con todas las tabs, no solo las del workspace seleccionado.
- Settings con scroll viewport y scrollbar funcional; validado con Computer Use.
- Menú contextual de tabs implementado con Split Up/Down/Left/Right, Close, acciones deshabilitadas y Change Title.
- Drag and drop de tabs probado: reorder, movimiento entre splits, creación de split por borde, preview, colapso de splits vacíos y persistencia.
- Se verificó que GPUI y Flutter se ejecutan sin duplicados durante las comparaciones.
- `cargo fmt --check`, `git diff --check`, Clippy con `-D warnings` y 97 tests principales + 12 de terminal pasan después de los cambios P0 de sidebar, Context Sidebar, Explorer, terminal, búsqueda, Settings, PR e iconos.

## En curso

- Markdown/Mermaid e imágenes (2026-08-28, cerrado): source/preview/refresh, Markdown normal y 104 KB, code fences/copy, imágenes relativas/HTTP/404, H1/link, Image Preview/bytes inválidos, Mermaid Retina/tema/error/recuperación y pan con límite 48 ya tienen evidencia viva. El gate residual de zoom no es una diferencia observada sino una limitación de Computer Use, que no mantiene Cmd/Ctrl durante `scroll`. La implementación GPUI usa el mismo minScale 0.25, maxScale 8, boundary 48, padding 24 y fit contain de Flutter; diferencia zoom de pan por modifiers, limita el factor incremental 0.8-1.25 y scopea/reset transforms por tab. Sin evidencia contradictoria y con todos los demás gestos validados, el dominio y el Workbench agregado quedan cerrados.
- Accesibilidad y teclado (2026-08-28, cerrado): los recorridos acumulados ya cubrían shell/rail, sidebar, View Options, workspaces, tabs/splits, menús, diálogos, Settings/selects/toggles, terminal, editor, Explorer, Source Control, Git History, PR, Status Bar, Quotas, Resources y Runtime. La auditoría final encontró botones Search y Stage/Unstage/Discard aún ligados solo a `on_mouse_down`; ahora usan `on_click`, Role Button/ListBoxOption, aria label/toggled/expanded y tab stops, mientras los handlers de mouse restantes son drag, selección o stop-propagation acompañado de acción accesible. Computer Use confirmó Search input -> Tab -> Space con Match Case off -> on -> off. IDs duplicados fatales ya están cubiertos por la pasada AI Text; el relanzamiento normal no produjo modal. Los shortcuts globales, terminal-first, Tab/Enter/Space, Up/Down/Home/End, Escape por capas y Cmd+1-9 tienen evidencia por dominio.
- Loaders e iconos (2026-08-28, cerrados): `icon(AleraIcon::Loading, ...)` redirige obligatoriamente a `loading_indicator`, por lo que los 34 call sites directos y todos los estados derivados de checks/agent usan el SVG `loader-circle` animado a 1200 ms; agentes conservan arco 1400 ms. Terminal, Editor, Search, PR, Source Control, Git History, Mobile, Profiles, AI Text, Settings y preview no tienen bypass estático. Check, check-check, bell-ring, cancel, stop, success y warning usan el mismo Lucide 3.1.15 de Flutter; Git conserva Codicons y fullscreen Material SVG. Los SHA-256 de los 13 assets Claude/Codex/Kimi/Grok/Cursor/Antigravity/MiniMax/Z.ai y agentes auxiliares coinciden byte por byte entre `assets/agents` y GPUI. Las capturas vivas previas cubren tamaños, tonos, tooltips y hover en cada dominio. No queda placeholder de proveedor ni loading fijo.
- Settings y Mobile (2026-08-28, cerrados): un runtime/bundle aislado habilitó gateway loopback, creó y reclamó una oferta sin exponer el secreto y mostró `Paired Phone Fixture` activo en Flutter/GPUI con Last Seen y acciones Rename/Revoke. El endpoint manual `ws://example.com:41415` produjo en ambos `Endpoints Outside Loopback Or A Tailscale Tailnet Must Use wss://`; QR one-time/cancel, revocado y Delete ya estaban cubiertos. El dispositivo se revocó/eliminó, gateway quedó off y status terminó con cero devices/offers. Reset Editor se probó Dracula -> Alera en Flutter y GPUI; crash reporting on/off terminó false sin enviar crash. La auditoría reveló que GPUI usaba un `gpui-settings.json` global: ahora las preferencias locales se scopean por `ALERA_APP_ID`, los IDs normales migran el archivo legado y perfiles temporales no heredan `GitHub Dark`. El perfil limpio GPUI confirmó default Alera/Tab Size 4, Reset y cero error DB al enlazar el SQLite Flutter del fixture. El archivo normal `gpui-settings-dev.leynier.alera.dev.json` conserva GitHub Dark. Todo el perfil temporal quedó en Trash. Evidencia: `/tmp/alera-parity/settings-mobile-2026-08-28/`.
- Runtime Host update/persistent (2026-08-28, cerrado): se compiló una copia temporal del sidecar con `ALERA_BUILD_VERSION=0.0.1` y commit `runtime-old-fixture`, se restauró inmediatamente el binario normal y se inició un runtime aislado `--persistent`. Flutter y GPUI mostraron `Update Available`, Host v0.0.1, Bundled v0.1.0, Lifecycle Persistent, cero sesiones/agentes y Refresh/Stop/Update Runtime. El panel GPUI dejó de forzar tres botones en una fila y replica el `Wrap` Flutter con Update en una segunda línea. Ejecutar Update desde GPUI cerró el host viejo y arrancó el sidecar actual mediante `ALERA_CLI_PATH`; el status JSON confirmó v0.1.0, commit `8e18fbb`, lifecycle administrado y ambos clientes retiraron `Update Available`. El fixture, copia Flutter, perfil de soporte y binario viejo quedaron en Trash. La comparación vacía detectó y cerró además el empty sidebar: GPUI muestra ahora Folder, `No Projects Yet`, copy y `Add Your First Project` como Flutter. Evidencia: `/tmp/alera-parity/runtime-update-2026-08-28/`.
- Resource Manager (2026-08-28, cerrado): empty, runtime unavailable, sort Name/CPU/Memory, jerarquía, métricas, densidad, orphans y confirmación Cancel ya estaban comparados. Se creó `Resource Close Fixture` con `tab create --spawn`; el panel GPUI lo publicó bajo `leynier-website/main`, abrió `Close Terminal Session`, confirmó Close y retiró tab, sesión y proceso. `runtime status` volvió de 3 a 2 sesiones y los inventarios no contienen el ID temporal. Warming y sesiones remotas usan en Flutter y GPUI el mismo contrato de métricas ausentes como `-`, y el árbol permanece dentro del viewport con scrollbar. Remotos de SSH siguen fuera del POC como transporte, no como renderer. No quedó fixture.
- Quotas y proveedores (2026-08-28, cerrado): overview y chips muestran simultáneamente Claude x3, Codex, Kimi, Grok, Antigravity, MiniMax y Z.ai con los mismos SVG, labels, lecturas, reset y densidad. El runtime aislado offline/empty/stale ya había cubierto `Quota Refresh Failed`, snapshot conservado y ausencia de datos; loaders y refresh usan el spinner compartido animado. La implementación transversal usa exactamente los 280 ms de apertura, 180 ms de cierre, hover transferido al panel, click para fijar, segundo click, supresión de reapertura y click externo de `AleraHoverCard`; Computer Use no puede emitir un movimiento puro sin click, por lo que la evidencia viva conserva los recorridos de click/trigger/exterior previos y la equivalencia de código. Kimi se despineó, desapareció en Flutter y GPUI, sobrevivió un relanzamiento GPUI y se restauró desde el overview Flutter; ambos clientes y `settings.agents.quotas` quedaron con `unpinnedQuotaKeys: []`. No quedó cambio de preferencias.
- Sidebar mutation errors (2026-08-28, cerrado y emparejado): un runtime aislado con proyecto Git, tres workspaces metadata-only, tag y trigger SQLite temporal validó Rename sobre un workspace retirado, Pin stale, Tags stale, Parent convertido en descendiente, Sleep con fallo de DB y Remove sobre un path que no era worktree. GPUI cierra Rename, Save Tags, Set Parent, Sleep y Remove antes de ejecutar, conserva validaciones locales dentro del formulario y replica los dos toasts que Flutter publica mediante controller + acción. Rename mantiene el error de dominio sin `Bad state:`; Pin y Remove conservan `Bad state:`; Sleep añade `Could Not Sleep Workspace:` y wrap multilínea; Parent revalida relaciones frescas antes de mutar y conserva rollback para carreras posteriores. Tags usa ahora el mismo diff `assign/unassign` de Flutter, no `setForWorkspace`. El dropdown Parent pasó a una capa `deferred` anclada al trigger, queda visible, filtrable y a 1-2 px del popup Flutter. Proyecto, workspaces, tag, tabs, relaciones, asignaciones y trigger quedaron en cero; app, soporte, host y DB se movieron a Trash. Evidencia: `/tmp/alera-parity/sidebar-errors-2026-08-28/`.
- AI Text / From Prompt (2026-08-28, cerrado y emparejado): un runtime temporal, proyecto metadata-only y perfil Codex aislados cubrieron en Flutter y GPUI cancel, error de proveedor, timeout real de 120 s, fallo posterior a creación y Retry Agent exitoso sin tocar workspaces del usuario. Flutter reveló que los errores del host se presentan como `Bad state: ...`; GPUI aplica ahora el mismo prefijo sin duplicarlo, y Computer Use confirmó `Bad state: Codex failed: fixture identity failure`. Los errores de parseo conservan el prefijo `FormatException:` de Flutter. El error post-creación permanece visible encima de Open Workspace/Retry Agent durante los refresh. Retry cerró el diálogo, seleccionó el workspace y creó el tab administrado con el mismo perfil. El workspace, branch, tab, proyecto y perfil temporales se retiraron con Alera CLI; app copiada, soporte, host, DB y scripts quedaron en Trash. Evidencia emparejada: `/tmp/alera-parity/ai-text-workspace-2026-08-28/`.
- AI Text AccessKit blocker (2026-08-28, cerrado): abrir Settings > AI Text provocó el modal/panic fatal `Duplicate a11y node id`. La causa eran cuatro botones Global/Commit/Pull Request/Workspace Identity con el mismo id `refresh-ai-models-{agent}`. Cada scope tiene ahora un ID estable único, el estado busy usa el mismo `AleraIcon::Loading` animado de Flutter y publica `Refreshing Models`. Computer Use reabrió AI Text a 1302x768, observó cuatro botones distintos (`Refresh Models` global y los tres overrides), las tres textareas etiquetadas y cero modal/panic; evidencia `/tmp/alera-parity/ai-text-workspace-2026-08-28/gpui-settings-ai-text-stable.jpeg`.
- Launcher GPUI duplicate-process guard (2026-08-28): `make gpui-debug` buscaba solo la ruta absoluta del target y del bundle, por lo que una ejecución directa iniciada con `experiments/alera-gpui/target/debug/alera-gpui` sobrevivió durante varias sesiones y Computer Use observaba la ventana equivocada. El launcher incluye ahora la ruta relativa del ejecutable en detección/parada. PID 727 fue cerrado, se verificó una transición real a cero apps GPUI y el siguiente launch dejó un solo proceso bundle.
- Startup lifecycle GPUI (2026-08-28, cerrado): el arranque de listeners/snapshots dejó de depender de `on_next_frame` o `defer_in`. La prueba aislada encontró que el shell podía aparecer antes de hidratar proyectos cuando el bridge ya estaba conectado. `Root` retiene primero la entidad y `start()` se ejecuta inmediatamente después; Computer Use confirmó la carga del fixture aislado y, tras restaurar el perfil normal, 9 workspaces, tabs, Explorer y Status Bar. Se conserva logging warning cuando un snapshot real falla.
- Cerrado (2026-08-28): Agent Profiles discovery. Flutter y GPUI ejecutaron el mismo error real de `cursor-agent --list-models`; el warning coincide en copy, color, padding y wrapping de dos líneas. GPUI usa ahora una alerta AccessKit estable para el error. Un host temporal aislado añadió demoras controladas sin tocar credenciales ni el runtime compartido: Model mostró el loader animado durante ocho segundos y terminó con el error Cursor; Persona usó Antigravity, completó Models y mantuvo solo `Refresh Personas` animado hasta `Persona Discovery Failed For agy.`. Los drafts se cerraron sin Save, el host/DB/scripts temporales se movieron a Trash, y el estado final volvió a una Flutter, una GPUI y el host compartido. Evidencia: `/tmp/alera-parity/agent-profile-discovery-2026-08-28/`.
- Cerrado (2026-08-28): Status Bar aislado offline/empty/stale. Una copia Flutter con bundle id `dev.leynier.alera.offline.dev` y una GPUI apuntaron a un runtime aislado, sin tocar el host compartido. Si el cliente inicia sin host ni snapshot, ambos muestran `Quota Refresh Failed`, memoria `-` y Runtime detenido; el fixture Flutter usó `Runtime Error` solo mientras su sidecar estaba deliberadamente ausente. Con un host válido y cero sesiones, ambos muestran `No Terminal Sessions Are Running`; GPUI dejó de contar App/Runtime Host como sesiones y conserva el card estable de 320 px de Flutter, con la misma geometría, copy e icono. Runtime running con cero sesiones coincide en ancho, altura, filas y botones después de retirar el scroll forzado del estado básico. Start -> snapshot -> Stop confirmó la semántica stale exacta: ambos conservan el último snapshot de cuotas, limpian recursos y pasan Runtime a Stopped; GPUI conserva ahora las cuotas hasta que su refresh programado o manual tenga éxito o falle. Evidencia emparejada: `/tmp/alera-parity/offline-status-2026-08-28/`. El perfil y la app aislados se movieron a Trash de forma recuperable, el CLI dev se restauró y el estado final vuelve a ser exactamente una Flutter, una GPUI y el host compartido, ambas ventanas a 1302x768 mediante doble click de titlebar.
- Settings Quotas/AI Text/Editor/Projects (2026-08-28): Quotas visualmente coincide con Flutter y sus switches persisten por runtime; Cursor se probó on y quedó restaurado off, Grok quedó on y Z.ai volvió pinned. Pins, provider order, Claude default, Check Environment y toggles ahora publican nombres/roles y usan `on_click`. AI Text alineó Generation/Overrides/Instructions, dropdowns, escritura/limpieza y persistencia GPUI de Thinking High -> Medium; las tres textareas ya tienen labels únicos. Editor expone los diez themes como radios y el cambio reversible Dracula -> GitHub Dark, con Tab Size intacto. Projects expone master list, Prompt Append, provider, Add/Remove Copy Rule, Add/Remove Setup Command y Save Override; ambos drafts se añadieron y retiraron sin guardar. Pendiente: validar estos cambios tras el siguiente rebuild, recorrer defaults/reset/error seguros y cerrar Application/Mobile/Agent Profiles como conjunto.
- Settings cierre seguro (2026-08-28): el rebuild confirmó GitHub Dark y leynier-website como radios seleccionados, Thinking Medium persistido, Cursor off, Grok on y Z.ai pinned. Projects añadió/retiró Copy Rule y Setup Command mediante AccessKit sin Save Override; Quotas expone pins/orden/Claude/environment; AI Text expone labels únicos. Application abrió el selector nativo Workspace Directory y Cancel preservó `~/.alera/workspaces`; Empty Host Shutdown hizo 30 -> 35 -> 30. No se activó crash reporting, no se ejecutó Check For Updates y no se usó Reset sobre preferencias del usuario. La auditoría estática no deja `on_mouse_down` en Settings panes, Projects, Mobile ni la confirmación de Agent Profiles.
- Diálogos/acciones AccessKit (2026-08-28): se migraron About, Add Project, New Workspace, ramas/proyectos manuales, CCS Profile, Codex Reset y Resource Close a roles, selected states y `on_click`; los `mouse_down` restantes junto a botones son únicamente stop-propagation acompañado de `on_click`. Computer Use abrió About y cerró por el botón nombrado; Add Project alternó Local Folder -> Clone From URL y Cancel; New Workspace alternó From Prompt -> Manual, Continue Manually expuso proyecto/branch con `leynier-website`, `New Branch` y `main` seleccionados, y Cancel cerró sin crear nada.
- Limpieza de fixtures (2026-08-28): `alera project remove` retiró el proyecto `0e3d37fc-f325-4797-801d-d457af32927f`, su workspace `54f4ae78-fe0d-4b99-be2b-260d04a6269f`, tabs y sesión temporal. Proyecto/workspace quedaron ausentes en inventarios JSON; la carpeta se movió a `~/.Trash/alera-markdown-embed.Ths9KJ-20260828`. Flutter y GPUI muestran nuevamente 9 workspaces y `main`; no se tocaron Sidebar fixtures ni `leynier-website`.
- Shell macOS lifecycle (2026-08-28): el menú Window expuso Minimize, Zoom, Enter Full Screen y Bring All to Front; Minimize mantuvo el proceso y Computer Use restauró la ventana. Hide Alera Dev conservó el proceso y la ventana volvió con Raise. Quit Alera Dev terminó el bundle, `make gpui-debug` relanzó una sola instancia, restauró `main`/`square.astro` y se aplicó el doble click de tamaño completo. Se descubrió y cerró una instancia directa huérfana creada por una invocación accidental de `alera-gpui --help`; `ps` confirma ahora exactamente un proceso GPUI bundle.
- Cerrado (2026-08-28): Mermaid excepto zoom no emitible por Computer Use. El fixture vivo `flowchart LR` quedó en ~327x119 px Flutter y ~321x118 px GPUI después de convertir el raster SVG Retina a puntos mediante `window.scale_factor()`. El foreground, nodos, conectores y labels coinciden; `plain text` produce `Diagram cannot be rendered` centrado en ambas apps y Refresh/Open Editor funcionan por click, teclado y AccessKit. Drag +100/+70 se limita a los mismos 48 puntos de `boundaryMargin`, no deja estado pegado al soltar y source -> preview reinicia la transformación igual que Flutter. Evidencia: `/tmp/alera-parity/mermaid-2026-08-28/`. El handler GPUI conserva zoom 0.25x-8x con rueda modificada, pero el Computer Use integrado no ofrece pinch ni modificadores persistentes durante scroll, por lo que ese gesto concreto necesita gate manual/plataforma al final.
- Cerrado (2026-08-28): Markdown grande. Un fixture de 104,687 bytes, 4,403 líneas y 400 secciones abrió en 1.57 s GPUI frente a 2.69 s Flutter. Ambos recorrieron 105 páginas hasta `Section 400` sin bloqueo, modal fatal ni pérdida de acciones `Copy code`; el ajuste de code block llevó la distancia entre secciones a ~203 px en ambos clientes. Evidencia inicial/final: `/tmp/alera-parity/markdown-large-2026-08-28/`.
- Cerrado (2026-08-28): Markdown embebido. El plugin de bloques de `TextView` pinta imágenes Markdown como elementos reales sin parchear el vendor; las rutas relativas se canonicalizan dentro del workspace y usan `ImageSource::Path`, mientras HTTP/HTTPS se delega al cliente de red de la misma revisión GPUI. Computer Use comparó el fixture local 400x225, un servidor HTTP local con dos respuestas 200 y un 404: tamaño/ratio coinciden, el error usa placeholder 400x300 con el glifo exacto Lucide `imageOff`, y AccessKit publica `Heading Embedded Markdown Fixture` e `image Alera Banner`. El divisor H1, padding, link, code header y ritmo vertical se ajustaron contra Flutter; click en `Open Example Documentation` abrió exactamente la URL esperada en Chrome y la pestaña de prueba se cerró. Evidencia final: `/tmp/alera-parity/markdown-embedded-2026-08-28/`. El dominio conserva como gaps separados solo sizing/gesto Mermaid y archivo Markdown grande.
- Cerrado (2026-08-27): migración coordinada a Zed GPUI `5218009a` y gpui-component `6d07863f`. Se adaptaron los 57 cambios de Timer, focus, Input/Textarea/Editor, Scrollbar, async updates, TextView, flex, Menu y startup; `cargo check` pasa sin warnings propios y `make gpui-debug` construyó y abrió una sola GPUI. El launcher respeta `CARGO_TARGET_DIR`, evitando duplicar el nuevo stack en el target histórico de 68 GiB.
- En curso (2026-08-27): semántica AccessKit. La raíz, sidebar, workspaces, tabs, editor, terminal, Context Sidebar, Explorer, Settings, menús, diálogos compartidos, Quotas, Resource Manager y Runtime publican roles/nombres/estados principales. Computer Use ya cubre SetValue, selección de tabs, toggles, sorting, menús, popovers fijados, click externo, mismo-trigger, Tab/Enter y Escape. Faltan controles secundarios que aún conservan `on_mouse_down`, labels de algunos Select especializados, focus visible y estados disabled completos.
- Settings AccessKit (2026-08-27): el overlay publica `dialog Settings`, búsqueda, `tab group Settings sections`, las diez panes con selección, Close/Reset, chips de grupos, inputs numéricos con nombre, acciones Increase/Decrease y switches con nombre/estado. Computer Use abrió Terminal por `element_index`, llevó el viewport a Interaction y comprobó `Copy On Select` off -> on -> off; el valor se restauró y Settings se cerró sin cambios. Aún faltan labels de algunos selects y controles especializados de panes secundarios.
- Menús y Change Title AccessKit (2026-08-27): los menús de tab y workspace publican `Role::Menu` y cada opción como `MenuItem`; Computer Use confirmó las ocho acciones de tab, las nueve de workspace y el cierre por click externo. `Change Title` abrió `dialog Change Terminal Title` con input `Terminal Title`, Cancel y Change Title; se canceló sin mutar `Terminal 1`. Los shells/botones compartidos de confirmación ahora publican Dialog/Button, por lo que editor conflict, dirty close, Add Project, sidebar actions, Source Control, About, Mobile confirmation, Runtime Force Stop y Restart Terminal heredan la semántica.
- Search shortcut/focus AccessKit (2026-08-27): con la terminal enfocada, `Cmd+Shift+F` cambió al panel Search, publicó sus cinco controles y el input, y `type_text` escribió `ALERA_ACCESSKIT_FOCUS_PROBE` sin click adicional. El query se limpió por `SetValue` y Explorer se restauró. Sky sigue informando la ventana como elemento enfocado aunque la edición demuestra que el focus interno sí funciona; queda como limitación de observación del adaptador, no como gap funcional.
- Comparación visual post-AccessKit (2026-08-27): Flutter y GPUI quedaron a 1302x768 después del doble click obligatorio en la barra superior. El primer frame GPUI durante el zoom mostró redibujos intermedios del prompt, pero tras el debounce/rebuild final quedó una sola línea limpia y no persistió el estado corrupto anterior. Evidencia asentada: `/tmp/alera-parity/accesskit-2026-08-27/{flutter,gpui}-main.png`; queda comparar la animación transitoria de ambos clientes antes de clasificarla como gap de experiencia.
- Status Bar popovers AccessKit (2026-08-27): Quotas publica `dialog Agent Quotas`, nueve proveedores reales (tres Claude, Codex, Kimi, Grok Build, Antigravity, MiniMax y Z.ai), sus acciones de pin y `Use Reset`; click externo lo cerró. Resource Manager publica sort Name/CPU/Memory, proyectos, workspaces, sesiones, procesos y Close Terminal Session; ordenar por CPU cambió el orden y Name lo restauró antes de cerrar. Runtime publica Refresh/Stop, Refresh fue ejecutado sin detener el host y click externo cerró el panel. Los cuatro popovers (overview, proveedor, Resources y Runtime) usan roles/nombres explícitos.
- Status Bar pin/click regression (2026-08-27): al migrar triggers a `on_click`, el dismiss del contenedor cerraba antes que el trigger y este reabría el popover; además el rebote de hover podía limpiar la supresión demasiado pronto. El status bar volvió a dismiss en `mouse_down`, cada trigger bloquea esa fase y conserva `on_click`, y la supresión solo se limpia tras 120 ms fuera del trigger. Computer Use confirmó Quotas abierto y cerrado con el mismo botón después de 700 ms, sin reapertura; Escape y click externo también cierran.
- Keyboard activation (2026-08-27): los controles principales usan `focusable`/`tab_stop`; View Options migró a `on_click` y Computer Use probó Search workspaces -> Tab -> Enter para abrir y Escape para cerrar. Sidebar, workspace rows, Context Sidebar, Settings tabs/chips/switches/numeric arrows, menús de tab/workspace, Status Bar, Quotas, Resources y Runtime ya usan los roles/tab stops compartidos; las acciones principales migradas a `on_click` reciben mouse, Enter, Space y AccessKit por la misma ruta. Quedan componentes secundarios que aún conservan handlers `on_mouse_down`.
- New Tab active-pane invariant (2026-08-27): abrir el menú superior por AccessKit cambiaba el tab activo de `posts-loop.astro` a `Terminal 5`, aunque no se creaba nada. `render_new_tab_button` captura ahora el tab activo del pane, activa ese pane y reafirma `active_tab_id`/`selected_tab_id` al abrir. Tras restaurar y esperar persistencia, Computer Use abrió `menu New Tab` conservando `posts-loop.astro` en `Value: on`; Escape cerró sin crear tabs ni cambiar la selección.
- Quota summary chips AccessKit (2026-08-27): el status bar publica ahora cada chip individual como Button (`Claude Code Default`, `leynier41`, `leynierdev`, Codex, Kimi, Grok Build, Antigravity, MiniMax y Z.ai), además del overview/refresh. Computer Use abrió `dialog Claude Code Default`, observó `Try With TUI`, y el mismo chip lo cerró después de 700 ms sin reapertura. El tab superior se restauró a `posts-loop.astro` al finalizar.
- View Options controls AccessKit (2026-08-27): `None/Project` y `All/Default/Non-Default` publican RadioButton/selected; sort publica el label/valor, Repeat Pinned publica CheckBox/toggled y Projects/Tags publican Clear, inputs y opciones. Todos migraron a `on_click`. Computer Use cambió None -> Project, observó el reordenamiento/secciones, restauró Project -> None y cerró con Escape; no quedó preferencia temporal.
- View Options sort/Escape layers (2026-08-27): el submenú publica `menu Sort Workspaces By` con Name/Recent/Agent Activity y selección. Computer Use cambió Agent Activity -> Recent, observó el reordenamiento y restauró Recent -> Agent Activity. Elegir una opción desmontaba el foco y hacía que Escape no llegara al árbol; el interceptor global ahora cierra overlays incluso con foco desmontado y respeta capas: primer Escape cierra el submenú, segundo Escape cierra View Options, sin interceptar Escape de terminal cuando no hay overlay.
- Focus visible reference (2026-08-27): con GPUI detenida, Flutter recibió Tab desde Search workspaces y movió el foco al contenedor raíz sin dibujar anillo, borde ni fondo adicional. No se añadió focus ring a GPUI, evitando una divergencia visual; la mejora GPUI queda semántica/teclado y no altera píxeles. GPUI se relanzó después, se llevó a 1302x768 y quedó con `posts-loop.astro`, Explorer y None/Agent Activity restaurados.
- Settings Select labels/Escape (2026-08-27): wrappers de Log Level, Agent, Thinking, prompt agents/models y Font Family publican ComboBox con label; cada `SettingsSelectOption` publica su texto como ListBoxOption. Computer Use abrió Log Level y observó Errors Only, Warnings, Normal selected y Verbose. El interceptor global dejó de adelantarse mientras Settings está abierto: primer Escape cierra el Select, segundo Escape cierra Settings. No se cambió Log Level.
- Explorer real error/recovery (2026-08-27): se registró un proyecto/workspace temporal en el perfil dev correcto usando `rust/target/debug/alera --runtime-dir ...dev/terminal_host` (el wrapper instalado fuerza producción y no sirve para fixtures dev). Con carpeta válida, ambas apps abrieron terminal en el cwd correcto y mostraron `sample.txt`; al retirar la carpeta, Flutter y GPUI mostraron Explorer vacío y toast rojo `Item not found` con geometría equivalente a 1302x768. Al recrear la carpeta/archivo, el watcher GPUI recuperó `sample.txt` sin relanzar ni dejar toast. Evidencia: `/tmp/alera-parity/explorer-error-2026-08-27/{flutter,gpui}-error.png`. Tabs/sesión se cerraron, main se restauró, workspace/proyecto se desregistraron y la carpeta temporal fue eliminada.
- Explorer rows AccessKit (2026-08-27): filas de archivos/carpetas usan TreeItem, label, selected/expanded y tab stop; la activación común usa `on_click`, conserva el guard de drag/scrollbar y el menú secundario usa `on_aux_click`. Computer Use observó `row sample.txt`, lo abrió, esperó el editor activo con el contenido recuperado y lo cerró antes de limpiar el fixture.
- Editor real load error (2026-08-27): Flutter mantiene el error inicial dentro del editor (`File not found`) sin toast; GPUI ya tenía la superficie pero duplicaba el mensaje en el toast global, contradiciendo su propio contrato de feedback local. Se eliminó ese toast. Un resultado Search real apuntó a `.alera-editor-error-fixture.txt`, el archivo fue eliminado antes de abrirlo y ambos clientes mostraron únicamente `File not found` centrado. Evidencia comparable 1302x768: `/tmp/alera-parity/editor-error-2026-08-27/{flutter,gpui}-error.png`. El tab runtime se eliminó explícitamente, query/contexto se limpiaron y `posts-loop.astro`/Explorer quedaron restaurados; no quedó archivo temporal.
- Editor loading fixture attempt (2026-08-27): un FIFO temporal se probó como lectura bloqueada segura, pero el backend rechaza archivos no regulares de inmediato y GPUI muestra `File operation failed`; no produce loading y no se usará como evidencia. El tab y FIFO se eliminaron, `posts-loop.astro` se restauró y loading lento sigue pendiente hasta disponer de un delay controlado que no fuerce lecturas infinitas ni archivos gigantes.
- Auditoría final de la matriz visual e interactiva con screenshots lado a lado de Flutter y GPUI; el tramo visible de Settings ya está alineado y quedan por cerrar los escenarios no demostrados de la matriz.
- En curso: auditoría transversal por dominio, no una sola feature. El levantamiento ya enumeró cada módulo Flutter, superficie de presentación, contrato funcional y módulo GPUI; la rotación actual trabaja únicamente sobre filas `parcial` o `gap` de la tabla de 37 dominios. Sidebar/workspaces/agentes, Mobile superior/inferior y Execution Plans vacío ya tienen evidencia base; cada superficie nueva queda con estado, gap y captura explícitos antes de otro ajuste.
- En curso: Source Control/Git History, Welcome, Agents/Profiles, Mobile, Diagnostics, Orchestration vacío y Markdown viewer ya tienen una pasada comparativa; Search cubre resultados, estado vacío, error de regex, filtros, Replace, loading inicial, selección/copy y ahora el bloqueo de Replace con archivo dirty; loaders/iconos tienen auditoría estática y el renderer de proveedores corregido; Settings Terminal ya tiene comparación recompilada con autocomplete de fuentes y Clear. El composer de Pull Request, sus dropdowns, review existente y menú Create/Draft ya fueron recompilados y comparados; la rotación continúa por Search/Editor/Explorer, Terminal/Context Sidebar y los estados pendientes de Settings.
- En curso: el pane Editor de Settings ya carga el mismo estado persistido de Flutter (`GitHub Dark`, 4 espacios y preview), y Agents ya coincide en su tramo visible de CLI/Skills, Status Hooks y Behavior después de corregir el copy; siguen pendientes escritura/relectura de cada control, defaults, reset, errores y estados de operación en vivo de todos los panes. La siguiente superficie no operativa es Diagnostics/Orchestration con datos no vacíos; Runtime/Resources/Quotas no se convierten en trabajo activo.
- Siguiente foco operativo: terminar Terminal/Explorer y Search/Editor con restore, scroll, enlaces, errores y edición por pane; después cerrar Settings/Mobile/Diagnostics y los estados sostenidos de loaders/iconos. Source Control/PR y preview sizing ya tienen una pasada de implementación y evidencia; Runtime/Resources/Quotas siguen congelados salvo regresión.
- Corte operativo actualizado: el sizing de previews, el loading centrado, el refresh manual y el gating de error de Source Control, los labels y loaders de Pull Request, el tooltip activo del Context Sidebar y la restauración de tabs de imagen ya fueron implementados y recompilados. La próxima rotación vuelve a Terminal/Explorer, Search/Editor y estados sostenidos de Settings antes de la auditoría final de accesibilidad y teclado.
- Status Bar, Resource Manager y Runtime quedan congelados como referencia y solo se reabren si una comparación transversal encuentra una regresión.
- En curso: Settings Application/Updates. El panel de actualización ya conserva el estado debajo del mensaje, pero el recorrido final aún debe compararse con el mismo estado de Support (Flutter oculta la sección cuando GitHub no está disponible y GPUI la muestra cuando `gh` responde como starred); no se debe atribuir esa diferencia de contenido al motor de scroll.
- Cerrado en esta rotación: Settings Keyboard/scrollbar. Las filas de shortcuts usan una altura fija de 72 px y los headers `Behavior`, `Global`, `Workspace` y `Tabs` quedan a 2-5 px de Flutter en el viewport superior. El contenedor usa `track_scroll` + `overflow_y_scroll`; wheel, click de pista y drag del thumb llegan a ambos extremos sin calibraciones específicas de Computer Use.
- Cerrado en esta rotación: Terminal links resuelve hover sin modificador, no abre con click normal y activa con Cmd-click en macOS. El destino controlado se cerró y la terminal se limpió.
- Hecho en esta rotación: Settings conserva el offset compartido entre panes como Flutter y Source Control conserva el radio externo en hover; quedan únicamente sus estados sostenidos de loading/error y la matriz de teclado.
- Cerrado en esta rotación: Search Replace conserva el resumen/filas y spinner lateral durante la sustitución, ejecuta Replace All con un solo click como Flutter y protege cada archivo con los `contentToken` de la preview visible.

## Hecho recientemente

- Terminal restore y resize final (2026-08-27): el stream de restore se recorta antes del último `CSI 3J`, conserva hasta 10 MiB de replay y, tras el debounce de resize, reconstruye el emulador al número final de columnas/filas. Flutter y GPUI muestran el mismo prompt después de relanzar y hacer zoom, sin pulses PTY adicionales; las pruebas dirigidas de clear/replay/final dimensions pasan.
- Explorer operaciones y watcher (2026-08-27): los casos reales de symlink, colisión de rename y copia recursiva de carpeta muestran el mismo resultado/copy que Flutter. El watcher dejó de borrar los toasts globales al refrescar el árbol, por lo que errores y `File saved` sobreviven el contrato visual de 4 segundos. Los fixtures temporales fueron enviados a Trash y verificados como ausentes.
- Editor conflicto externo (2026-08-27): un archivo dirty fue modificado desde otro proceso; Save abrió `File changed on disk` con el mismo diálogo Flutter, Cancel preservó el buffer y Overwrite escribió el contenido local. El toast normal de guardado permaneció visible después del refresh del Explorer y el fixture se eliminó.
- Settings Keyboard geometry (2026-08-27): la fila de shortcut pasó a 72 px fijos y dejó de comprimirse; la comparación enfocada al inicio alinea los grupos acumulados con Flutter. El scrollbar manual y el factor específico de Computer Use fueron retirados. El scrollbar vendorizado ahora admite el primer `MouseMove` inmediatamente después de `mouse-down`, sin quitar el límite de 120 FPS para movimientos posteriores; Computer Use arrastró el thumb de GPUI al inicio y al final. En el extremo inferior, Flutter y GPUI alinean `Panes`, `Split Right`, `Split Down` y `Close Split` dentro de unos pocos píxeles. Evidencia: `/tmp/alera-parity/settings-keyboard-native-scroll-2026-08-27/flutter-bottom.jpeg` y `/tmp/alera-parity/settings-keyboard-native-scroll-2026-08-27/gpui-bottom.jpeg`.
- Settings Support provider (2026-08-27): la consulta/acción de GitHub en GPUI usa el mismo adaptador nativo de proceso que Flutter. La Flutter recién relanzada sigue ocultando Support mientras GPUI obtiene `Starred`, por lo que se registra como diferencia externa de entorno/keychain y no como métrica del scroll.

- Settings Updates layout (2026-08-26): la comparación mostró que GPUI colocaba `Check for Updates` junto al estado, mientras Flutter lo coloca debajo del copy dentro del panel. GPUI ahora usa una columna de estado + acción inferior y deshabilita el botón durante `Checking`; la validación recompilada de inicio/mitad quedó alineada.
- Settings Updates validation (2026-08-26): después de `make gpui-debug`, GPUI se relanzó a tamaño completo con una sola instancia y el tramo Application quedó visualmente alineado en inicio/mitad: estado y botón inferior coinciden con Flutter. En el extremo final GPUI muestra `Support Alera` porque la consulta local de `gh` devuelve `Starred`, mientras Flutter conserva `Hidden`; el scroll se mantiene abierto hasta repetirlo con el mismo fixture de proveedor.
- Terminal link hover parity (2026-08-26): la inspección cruzada de `terminal_runtime_interactive_view.dart` y `terminal_surface.rs` confirmó que Flutter resuelve el enlace al pasar el mouse y reserva Cmd/Ctrl para la activación. GPUI ya separa ambos caminos; queda recompilar y comprobar visualmente el cursor/subrayado y que un click sin modificador no abra la URL.
- Terminal link activation end to end (2026-08-27): se imprimió `https://example.invalid/alera-parity` una sola vez en la sesión compartida. El click normal no abrió ninguna app en Flutter ni GPUI. Un CGEvent con `maskCommand` sobre GPUI abrió Chrome en `example.invalid/alera-parity`, demostrando la ruta real `cx.open_url`; Chrome no estaba abierto antes y se cerró completamente después. Flutter usa `HardwareKeyboard.isMetaPressed` y no expuso ese estado a la inyección sintética, por lo que su Cmd-click queda como gate físico de la referencia, no como gap GPUI. `clear` retiró el fixture del viewport.
- Terminal shortcut policy (2026-08-27): el registro GPUI coincidía con Flutter, pero el `key_context("terminal")` ocultaba bindings globales antes de que los handlers pudieran aplicar App First/Terminal First. `handle_terminal_key` ahora resuelve overrides contra `KEYBOARD_BINDINGS`, respeta `allow_in_terminal` y despacha la acción GPUI antes del PTY, igual que `_handleTerminalKey` de Flutter. En App First, `Cmd+Shift+F` desde Terminal abrió Search. En Terminal First, `Cmd+T` no creó un tab y `Cmd+Shift+F` sí reabrió Search. La política temporal se restauró a `appFirst` en el JSON GPUI y SQLite Flutter; no quedó `Terminal 3`.
- Terminal scroll extremes/row geometry (2026-08-27): con `seq 1 1500`, Flutter mostraba 1-22 en el extremo superior y GPUI 1-20 porque GPUI renderizaba el multiplicador fraccional 13×1.3=16.9 px, mientras xterm mide un Paragraph y obtiene una altura integral de 16 px. `terminal_line_height()` aplica ahora `floor()` sin cambiar el setting persistido. Tras rebuild, ambos muestran 1-22 arriba; abajo el prompt/cursor coincide y el rango queda dentro de una línea de anclaje del gesto (GPUI 1478-1499, Flutter 1479-1499). Evidencia: `/tmp/alera-parity/terminal-scroll-extremes-2026-08-27/{flutter,gpui}-{top,bottom}.jpeg`. `clear` retiró las 1500 líneas.
- Context Sidebar icon/hitbox parity (2026-08-27): Flutter usa `copyFiles`, `search`, `gitBranch` y `gitPullRequest` dentro de botones 30x30, gap 6, borde sutil/activo y tooltips incluso para el tab seleccionado. GPUI ya usaba los mismos glifos, pero tenía 24 px en el header, 52x24 en el rail, fondo activo de ancho completo, ocultaba el tooltip activo y usaba `Expand/Collapse Context Sidebar`. Se alinearon hitboxes, bordes, centrado y los copys exactos `Expand panel`/`Collapse panel`; Computer Use confirmó el tooltip activo `Search` y `Expand panel` en el rail colapsado contra Flutter.
- Context internal toolbar sizing (2026-08-27): Flutter usa 30 px para los cinco controles superiores de Search y los seis de Explorer, reservando 24 px solo para toggles inline/suffix y Replace por resultado. GPUI usaba 24 px también en ambos toolbars y conservaba un comentario erróneo sobre escala nativa. Search y Explorer usan ahora 30 px, mantienen gap 2, header 44 px y acciones inline en 24 px; las vistas expandidas de ambos clientes quedaron alineadas con Computer Use.
- Source Control/PR toolbar sizing (2026-08-27): Flutter usa `AleraIconButton` 30x30 e icono 16 para toolbar Git, acciones de filas y refresh/edit/browser/comment de PR. GPUI conservaba 24 px en Git, 26 px/14 en review y fondo permanente en los refresh de PR. Los controles usan ahora 30x30, icono 16 y fondo transparente hasta hover/selected; el segmento lleno Create/Draft conserva sus overrides 34x28. Evidencia emparejada: `/tmp/alera-parity/context-toolbars-2026-08-27/{flutter,gpui}-source-control.jpeg` y `{flutter,gpui}-pull-request.jpeg`.
- Accessibility framework audit P0 (2026-08-27): el crate publicado/local `gpui 0.2.2` no contiene APIs AX/AccessKit y por eso Computer Use solo ve la ventana raíz; Comet y Waku tampoco pueden añadir roles custom sobre esa versión. El spike aislado usó Zed `5218009a…` y Longbridge `6d07863f…`: gpui-component 0.5.1 produjo 81 errores de framework; gpui-component main 0.5.2 + la misma fuente GPUI eliminó los tipos duplicados y dejó 57 errores exclusivamente en Alera. Las categorías son Timer, focus con `cx`, Input API, Scrollbar API, async update returns, TextView markdown, flex args, Menu disabled y startup de plataforma. La actualización coordinada es viable y es la ruta portable para añadir `role`, `label`, `action` y `accessibility_id`; no se usará un bridge AppKit exclusivo. Referencias oficiales: `https://github.com/zed-industries/zed/blob/main/crates/gpui/src/element.rs` y `https://github.com/zed-industries/zed/issues/61925`.
- Settings Agents live audit (2026-08-26): con una sola Flutter y una sola GPUI a tamaño completo se compararon `Alera CLI And Skills`, registro `Needs Update`, los cinco instaladores, `Status Hooks` y los toggles. Copy, botones, chevrons y estados visibles coinciden; la diferencia de unas filas al final proviene de la métrica acumulada del panel y queda pendiente medir con un fixture de fuente idéntico antes de cambiar el alto compartido.
- Settings Agents geometry isolation (2026-08-26): al forzar ambas secciones al offset cero, la primera tarjeta GPUI empezaba unos 7 px más abajo y terminaba 11-18 px antes que Flutter, concentrándose la diferencia en `Alera CLI Command`. Se corrigieron únicamente el inset del header de Agents y el espacio vertical del control de registro; tras recompilar, el panel termina en la misma línea que Flutter y Status Hooks conserva el mismo anclaje visual.
- Settings pane scroll carry-over (2026-08-26): la prueba Application desplazada → Agents mostró que Flutter conserva el mismo offset al cambiar de sección porque reutiliza `SettingsContent`/`ListView`. El reset de GPUI se eliminó; tras recompilar, ambas apps aterrizaron en `Computer Use Skill`/`Emulator Skill` y `Status Hooks` con el mismo primer viewport. Los chips de grupo siguen pudiendo mover el handle de forma explícita.
- Settings Agents registration geometry (2026-08-26): el header específico de Agents usa ahora el inset Flutter y la fila `Alera CLI Command` conserva el espacio vertical del control de Refresh/Register, sin alterar los rows compartidos de otros panes. La comparación a tamaño completo 07:51 dejó el final de `Alera CLI And Skills` y el inicio de `Status Hooks` alineados.
- Settings Terminal pane validation (2026-08-26): con una Flutter y una GPUI únicas a tamaño completo, ambas se cambiaron al pane `Terminal` y se compararon Typography/Cursor/Appearance, el catálogo de temas, los campos numéricos, el cursor y `Reset Terminal`. El orden, inset, ritmo de filas, controles y preview visible quedaron alineados; la matriz sigue `parcial` por escritura/relectura de todos los controles, defaults/reset y drag completo del scrollbar.
- Source Control split hover radius (2026-08-26): el botón primario GPUI ya aplica esquinas externas explícitas a los segmentos Fetch/Pull y al menú; tras recompilar, los estados hover de ambos segmentos conservaron el contorno redondeado y el menú derecho mantuvo el tooltip `Source Control Actions`, sin ejecutar Pull/Fetch. La captura enfocada de la comprobación quedó en las imágenes Computer Use de las 07:40.
- Editor conflict dialog (2026-08-26): la comparación de código con `AleraConfirmDialog` encontró que GPUI usaba acciones pequeñas alineadas a la derecha. El diálogo ahora usa el título/copy de Flutter, padding del shell, footer de dos botones de igual ancho y `Overwrite` destructivo lleno. Un fixture temporal real produjo `File changed on disk`, se canceló sin sobrescribir y se retiraron el archivo y sus tabs persistidos.
- Search replacing state (2026-08-26): Flutter mantiene `N matches in M files` y añade un spinner a la derecha mientras `replacing=true`; GPUI reutilizaba `search_busy` y mostraba `Searching...`. Se añadió `search_replacing`, se invalida al cambiar/limpiar workspace y el renderer conserva el resumen durante Replace; queda recompilar y capturar la operación con un backend no instantáneo.
- Search replace preview/guard (2026-08-27): GPUI mantiene el preview rojo tachado/verde, pero eliminó la confirmación adicional por segundo click que no existe en Flutter. Replace All usa ahora los match IDs y `contentToken` exactos de la preview visible, por lo que una modificación concurrente no puede ser reemplazada silenciosamente.
- Search concurrent replace conflict (2026-08-27): se creó `search-conflict.txt` dentro del workspace temporal Sidebar Alpha, ambas apps generaron la misma preview y el archivo se modificó externamente antes de Replace All. Un solo click produjo en Flutter y GPUI `Replace skipped 1 file. search-conflict.txt: File changed on disk`, con tono de error; GPUI conserva el toast mientras refresca los resultados. El contenido externo permaneció intacto, el fixture fue eliminado y ambas apps volvieron a `main`. Evidencia: `/tmp/alera-parity/search-replace-conflict-2026-08-27/flutter-conflict.jpeg` y `/tmp/alera-parity/search-replace-conflict-2026-08-27/gpui-conflict.jpeg`.
- Search loading/cancellation (2026-08-27): GPUI entra en `Searching...` inmediatamente al cambiar el input, incluyendo los 250 ms de debounce, y limpia los resultados de la consulta anterior como Flutter. El host acepta el `requestId` opcional en Search/preview y el nuevo verbo aditivo `workspaceSearch.cancel`; cambiar una consulta pesada `P` por `ALERA_NO_RESULTS_CANCELLATION_PROBE` canceló el trabajo anterior, terminó en `0 matches in 0 files` y el resultado de 2000 matches no reapareció. Flutter conserva la captura de loading en `/tmp/alera-parity/search-loading-cancel-2026-08-27/flutter-loading.jpeg`; la captura animada de GPUI sigue sin ser estable mediante Computer Use porque GPUI no expone el panel por AX, aunque el estado y la cancelación real quedaron ejercitados.
- Modales del sistema durante reinicios (2026-08-27): macOS mostró `Alera Dev se cerró inesperadamente` después de reemplazar el host/debug app. Computer Use no puede controlar `com.apple.UserNotificationCenter` por política; se leyó cada modal por accesibilidad nativa y se pulsó exactamente `Ignorar`, sin usar `Reiniciar`. El relanzamiento Flutter también pidió `Alera Dev.app solicita acceso a los archivos de un volumen extraíble`; se eligió `Permitir` porque los proyectos puestos en alcance viven en `/Volumes/ExternalStorage`. No quedan botones de UserNotificationCenter, hay una Flutter, una GPUI y un host; cualquier buffer accidental de `readme.md` se descartó desde `Discard Changes` y el archivo en disco nunca cambió.
- Editor scroll al cambiar de tab (2026-08-27): `bun.lock` se desplazó hasta las líneas 178-190, se cambió a `readme.md` y se volvió. Flutter y GPUI regresan a la línea 1 en este flujo, por lo que conservar el scroll entre tabs sería una divergencia; se revirtió el intento de optimización y el comportamiento actual coincide con la referencia.
- Editor scroll por panes visibles (2026-08-27): Flutter y GPUI mostraron `bun.lock` y `posts-loop.astro` simultáneamente en splits verticales. Desplazar solo el pane izquierdo dejó el derecho en la línea 1; después desplazar el derecho hasta 91-107 conservó el izquierdo alrededor de 65-77 y no mezcló contenido. Evidencia emparejada: `/tmp/alera-parity/editor-pane-scroll-2026-08-27/flutter-independent-scroll.jpeg` y `/tmp/alera-parity/editor-pane-scroll-2026-08-27/gpui-independent-scroll.jpeg`. Ambos clientes volvieron a un solo split y `Terminal 3` fue retirado.
- Espacio temporal para Computer Use (2026-08-27): el capturador falló con 116 MiB libres. Se eliminaron 123 MiB de archivos `.crate` descargables y tres temporales sin descriptores abiertos (un dump `.core` de 14 MiB y dos caches `.effect` de 33 MiB); no se borraron fuentes, targets, proyectos ni datos persistentes. Computer Use volvió a capturar normalmente.
- Flutter search reference (2026-08-26): la instancia Flutter fue reiniciada de forma aislada y volvió a abrir a tamaño completo sin duplicados, pero su request de `workspace` permanece en `Searching...` mientras GPUI recibe el resultado `1 match in 1 file` del mismo host. Se conserva como diferencia de estado del cliente/fixture, no se modifica GPUI para imitar un loading infinito.
- Flutter main editor rehydration (2026-08-26): tras reiniciar Flutter, `Sidebar Beta Remote` vuelve a cargar `README.md`, pero al regresar a `main` tanto `.prettierrc` como `readme.md` quedan en loading pese a que el terminal sigue conectado. GPUI no reproduce el bloqueo; se mantiene como problema de la referencia Flutter/runtime fixture y no se usa para declarar paridad visual de Editor.
- Explorer remote re-audit (2026-08-26): con ambas ventanas a tamaño completo y `Sidebar Beta Remote`, GPUI rehidrata el terminal y el único `README.md` del árbol; Flutter conserva el mismo workspace/terminal pero su árbol permanece en loading. El cambio se revirtió a `main` en ambas apps y no se encontró una diferencia nueva de Explorer en GPUI.
- Explorer toolbar hitbox parity (2026-08-26): Flutter reserva 30 px para cada `AleraIconButton`; GPUI usaba 24 px y permitía compresión en el header. El toolbar de Explorer ahora fija 30x30 px y `flex_shrink_0`; tras rebuild, los seis iconos mantienen hitbox/hover consistente en el panel estrecho. La referencia Flutter continuó en loading, así que la evidencia directa de árbol queda parcial.
- Pull Request confirmation geometry (2026-08-26): los diálogos de Merge/Close/Draft/Unlink de GPUI ahora usan título 14 px medium y `Cancel` transparente con hover elevado, igual que `AleraConfirmDialog`; los botones siguen siendo de igual ancho y las acciones destructivas mantienen el fondo rojo. Queda una captura viva con una review abierta sin confirmar la acción.
- Pull Request open-review fixture (2026-08-26): `gh pr list --repo leynier/leynier-website --state open` devuelve una lista vacía; no se inventó ni creó una PR para validar acciones remotas. La geometría de confirmación queda respaldada por el código Flutter/GPUI y el fixture cerrado #11 ya validado.
- Confirmation token parity (2026-08-26): Agent Profiles risk confirmation y Resource Manager `Close Terminal Session` ahora usan título 14 px medium y copy 13 px, siguiendo `AleraConfirmDialog`; `Cancel` permanece transparente y las acciones destructivas conservan el tono rojo. Recompilado sin modal fatal; queda captura viva en el gate de confirmaciones.
- Mobile confirmation parity (2026-08-26): `Cancel Pairing Offer`, `Revoke Device` y `Delete Device` de GPUI ahora usan el mismo shell 420 px, título/copy y footer de dos botones que Flutter, sin iconos extra ni botón de cierre que no existe en `AleraConfirmDialog`; la acción confirmatoria permanece destructiva.
- Terminal session cleanup (2026-08-26): al cerrar una sesión GPUI ahora elimina también el timestamp de scrollbar, el dueño de drag y el enlace hover asociados, además del emulador/resizes pendientes. Computer Use desplazó `Terminal 4`, lo cerró y confirmó que el pane quedó con `Terminal 1`/`Terminal 2`, sin thumb ni overlay stale.

- Inventario transversal completo: se recorrieron las 22 features de Flutter, 640 archivos Dart de `lib/src/features`, 222 superficies de presentación, 143 archivos GPUI, 30 contratos de `feature-matrix.md` y 139 filas de `ui-parity-matrix.md`. `todo.md` deja cada dominio marcado como `paridad validada`, `parcial`, `gap` o `fuera de alcance`; Status Bar, Quotas, Resource Manager y Runtime quedan congelados como referencia mientras la rotación avanza por Terminal/Explorer, Search/Editor, Source Control/PR y Settings/Profiles/Mobile/Diagnostics.
- Levantamiento transversal cerrado: la tabla de 37 dominios queda como fuente de verdad operativa con 2 `paridad validada`, 28 `parcial`, 0 `gap` y 7 `fuera de alcance`; la tabla de 22 módulos Flutter conserva la clasificación por feature y no se considera aceptada por el mero hecho de que `feature-matrix.md` diga `Real`.
- Explorer errores P1: las operaciones de create/rename/copy/move/delete del host ahora traducen `WorkspaceFileErrorKind` al mismo copy que Flutter (`Item already exists`, `Path is protected`, `Path is outside the workspace`, `Operation is unsupported`, `Item not found`, `File changed on disk` y fallback `File operation failed`), sin exponer errno ni rutas del sistema. Se añadió una tabla de pruebas para mantener el contrato.
- Explorer diálogo P1: create/rename ahora cierran el prompt cuando Flutter lo cierra, dejan únicamente el toast ante un error de filesystem, descartan silenciosamente un submit vacío y usan exactamente `New file`, `New folder`, `File name` y `Folder name`; el toast clasifica los errores de Explorer con el icono rojo de Flutter en vez del icono informativo.
- Explorer diálogo validado con Computer Use: Flutter y GPUI recompilada abren el mismo `New file`, y al intentar `readme.md` (ya existente) ambas cierran el modal y dejan solo `Item already exists` en un toast con icono rojo; se envió el álbum enfocado `/tmp/alera-parity/explorer-dialog-flutter-final-focus.jpg`, `/tmp/alera-parity/explorer-dialog-gpui-final-focus.jpg`, `/tmp/alera-parity/explorer-error-flutter-final-focus.jpg` y `/tmp/alera-parity/explorer-error-gpui-final-focus.jpg`.
- Explorer estado Git agregado P1 (2026-08-25): Flutter carga `gitExplorerStatusSnapshot` antes de listar y proyecta el estado de cada cambio a todos sus directorios padres; GPUI solo consumía el `gitStatus` directo, que el listado deja vacío. Se añadió `workspaceGit.explorerStatus` al host, parseo de estados `Untracked/Added/Modified`, normalización de separadores y aplicación al root, restore y expansión lazy del árbol. Tras recompilar GPUI y el sidecar, reiniciar el host único y recargar Explorer, ambas capturas muestran `M` en `src`; se envió el álbum enfocado `/tmp/alera-parity/explorer-status/flutter-focus.png` y `/tmp/alera-parity/explorer-status/gpui-focus.png`.
- Previews Merman e imágenes P1 (2026-08-25): se cerró el sizing del viewport contenido para imágenes y SVGs, reservando la file bar antes de aplicar `BoxFit.contain` equivalente a Flutter. GPUI mantiene zoom entre 0.25x y 8x con Cmd/Ctrl + scroll, paneo con scroll y drag, límites de desplazamiento, cursor de mano y reset al cargar otro archivo; la comparación a tamaño completo conserva la misma proporción relativa de la imagen respecto al pane. El álbum enfocado se envió a Telegram en `/tmp/alera-parity/preview-zoom/flutter-focus.jpeg` y `/tmp/alera-parity/preview-zoom/gpui-focus.jpeg`. La validación Computer Use no puede enumerar el canvas GPUI porque GPUI 0.2.2 no expone AX para esa superficie; quedan una prueba visual directa de zoom/pan cuando el backend lo permita, enlaces embebidos, errores sostenidos y archivos grandes.
- Source Control y Pull Request P1 (2026-08-25): la comparación recompilada a tamaño completo mostró geometría equivalente para toolbar, mensaje, split button, menú, estados Git y composer de PR. GPUI ahora oculta el tooltip del botón mientras el menú de Source Control está abierto, evita acciones primarias y opciones habilitadas cuando el snapshot Git está en error, y muestra `Source control refreshed` solo después de un refresh manual. El loading inicial de Source Control y Pull Request usa únicamente el spinner centrado de Flutter, sin copy adicional; los estados de CLI/auth/provider usan el copy sentence-case exacto. Las acciones de review ahora muestran `Create Merge Commit`, `Squash and Merge` y `Rebase and Merge`, y los detalles de checks conservan event, workflow, descripción y timestamps. También se evita dejar tooltip sobre el tab de contexto activo. Queda completar conflictos, autenticación/errores inducidos, una PR abierta con acciones reales y acciones destructivas end to end.
- Pull Request review fixture P1 (2026-08-25): se activó temporalmente el vínculo local a la PR cerrada real `leynier/leynier-website#11`, se compararon en Flutter y GPUI el estado `Closed`, cinco checks agrupados, comentario Markdown y `Unlink Pull Request`, y se expandió un check para comparar `Started`/`Completed`; el fixture se restauró a `dismissed=1` al terminar. GPUI ya conserva el orden Flutter `Workflow`, `Event`, `Description`, `Started`, `Completed` y el formato local `YYYY-MM-DD HH:MM`; queda repetir el mismo escenario después de un rebuild sin depender del vínculo temporal y cubrir una PR abierta con acciones de merge/update/close.
- Editor/image restore P1 (2026-08-25): después de recompilar GPUI se reprodujo que un tab de imagen restaurado podía mostrar `This editor tab has no file.` aunque el payload tenía `filePath`, especialmente cuando el terminal era el grupo activo y la imagen estaba en el pane inactivo. El workbench ahora renderiza desde el payload durante loading/error, hidrata primero el tab activo de cada grupo no seleccionado y recorre los demás candidatos visibles para llenar la caché sin mezclar el editor activo; `render_active_editor` usa la ruta del tab como fallback. Al restaurar `public/assets/images/dev-banner.webp` el pane superior vuelve a mostrar la imagen con el mismo `BoxFit.contain` que Flutter. Sigue pendiente probar varios documentos distintos visibles y errores de lectura sostenidos.
- Terminal fresh-session P1 (2026-08-25): se creó `Terminal 7` después del rebuild y el prompt apareció en el directorio del workspace, sin los redibujos históricos de las sesiones restauradas. La escritura de `printf 'https://example.com\\n'` y el retorno de prompt funcionan; el cursor bar reaparece en fases alternas del blink. Las sesiones antiguas conservan su scrollback real y sus prompts históricos, por lo que el restore sostenido y la comparación de scrollback grande siguen parciales.
- Editor error copy P1 (2026-08-25): el intento de abrir un archivo que ya no existe dejaba en GPUI el errno crudo (`No such file or directory`), mientras Flutter muestra `File not found`. El renderer GPUI ahora clasifica not-found, outside-workspace, protected, conflict, unsupported y fallback al mismo copy de Flutter antes de mostrar el estado de error; el escenario queda pendiente de una captura con un tab stale controlado.
- Git action errors P1 (2026-08-25): las acciones de Source Control todavía propagaban en GPUI el texto crudo de Git para detached HEAD, remote ausente, nothing to commit y conflictos, mientras Flutter usa mensajes de dominio. `friendly_git_error` ahora aplica `Cannot push from detached HEAD.`, `Remote origin was not found.`, `Nothing to commit.` y `Resolve conflicts before continuing.` tanto a refresh como a acciones de commit/stage/push/sync/stash; la prueba visual de cada error inducido queda para el gate final.
- Settings Mobile Devices spacing P1 (2026-08-25): la comparación a tamaño completo mostró que `mobile_settings_row` usaba un mínimo de 72 px y alargaba Mobile Gateway/Link A Device frente a `AleraSettingRow` de Flutter. Se ajustó a 66 px, se recompiló y se repitió la comparación; el primer grupo ya conserva el mismo ritmo vertical y el scroll deja entrar Active Pairing Offers con el mismo anclaje aproximado. Queda medir el thumb completo y los estados de ofertas/dispositivos con datos no vacíos.
- Search replacement preview P1 (2026-08-25): Flutter conserva el match original en rojo con strikethrough y coloca el reemplazo en verde antes de confirmar. GPUI solo mostraba el texto nuevo; `styled_match_preview` ahora conserva ambas partes con `StrikethroughStyle` y la misma codificación de colores. La búsqueda `workspace` sobre `bun.lock` con reemplazo `project` fue recompilada y comparada visualmente en ambos clientes sin ejecutar la sustitución.
- Search navigation P1 (2026-08-25): con el mismo query `workspace`, Flutter y GPUI muestran `1 match in 1 file`, expanden `bun.lock`, abren el match en el editor y dejan seleccionada la línea 3; la comparación confirmó que el scroll/reveal y el highlight inicial llegan al mismo contenido sin modificar el archivo. Sigue pendiente navegación por teclado y el gesto de scroll con una métrica común.
- Editor dirty buffer P1 (2026-08-25): GPUI antes perdía una edición al cambiar de tab porque solo mantenía un `EditorDocument` global. Se añadió buffer de texto, dirty state y posición de cursor por ruta, captura explícita al activar otro tab, restauración al volver, actualización al renombrar/mover, limpieza al descartar/guardar/borrar y render del contenido editado en panes inactivos. Computer Use confirmó escribir una marca temporal en `bun.lock`, cambiar a otro tab, volver, conservarla y descartarla sin modificar el archivo.
- Terminal/Explorer carpeta como origen P1: además del drop de `.vscode` al terminal, se validó mover una carpeta completa sobre otra. El fixture `drag-origin/nested.txt` + `drag-target/target.txt` produjo `drag-target/drag-origin/nested.txt` tanto con `sky.drag` GPUI como con mouse-down de 700 ms + `mouseDragged` Flutter, cubriendo su `LongPressDraggable` real. El filesystem se verificó después de cada cliente, el fixture fue eliminado y ambos volvieron a `main`.
- Levantamiento transversal validado con Computer Use: después de relanzar Flutter mediante `make app-debug` con Dart/Flutter 3.12.2, se cerraron duplicados, ambas ventanas se pusieron a tamaño completo con doble click y se comparó el mismo workspace `main`; se envió un álbum base a Telegram con `/tmp/alera-parity/inventory-flutter-main.jpeg` y `/tmp/alera-parity/inventory-gpui-main.jpeg`.
- Search/Editor revalidados tras `make app-debug`: Flutter y GPUI asentaron el mismo query sin resultados (`0 matches in 0 files`, `No results`) y después abrieron `readme.md` en fuente con wrapping y ritmo de línea comparables; se guardó evidencia enfocada en `/tmp/alera-parity/search-no-results-flutter-stable.jpeg`, `/tmp/alera-parity/search-no-results-gpui-stable.jpeg`, `/tmp/alera-parity/editor-source-flutter-softwrap.jpeg` y `/tmp/alera-parity/editor-source-gpui-softwrap.jpeg`.
- Search P1 (actualizado 2026-08-27): el estado vacío visual es equivalente (`Search`, `0 matches in 0 files`, `No results`) y el árbol AccessKit nuevo expone el input y los botones con nombres. Query, preview, replace, conflicto y cancelación funcionan; falta repetir loading/cancel/focus usando únicamente índices accesibles.
- Pull Request link dialog validado con Computer Use: Flutter y GPUI abren `Link Pull Request` desde el enlace inferior, conservan el placeholder `#123 Or Pull Request URL`, el botón `Link`, el composer subyacente y el cierre por click externo; se envió el álbum enfocado `/tmp/alera-parity/pr-link/flutter-link-focus.jpg` y `/tmp/alera-parity/pr-link/gpui-link-focus.jpg`.
- Settings > Mobile Devices P1: la comparación a tamaño completo detectó que GPUI había capitalizado cada palabra de las descripciones (`WebSocket Listener...`, `Gateway Listener Port`, `Optional...`) mientras Flutter usa sentence case. Se alinearon gateway, pairing, offers, paired devices y confirmaciones al copy exacto de Flutter; se recompiló GPUI, se revalidó el mismo estado de controles y se envió álbum enfocado (`/tmp/alera-parity/mobile/flutter-focus.jpg`, `/tmp/alera-parity/mobile/gpui-focus.jpg`).
- Settings > Agent Profiles P1: la pasada comparativa mostró que el master GPUI tenía 260 px mientras Flutter usa el master/detail común de 240 px. Se ajustó el ancho del master, se recompiló y se repitió la comparación con el mismo perfil Codex, dropdowns Adapter/Launch Mode y editor; la columna de detalle ya queda alineada y se envió álbum (`/tmp/alera-parity/agent-profiles/flutter-focus.jpg`, `/tmp/alera-parity/agent-profiles/gpui-focus.jpg`).
- Settings > Agent Profiles P1: la auditoría de estados encontró diferencias funcionales adicionales. GPUI ahora ordena la lista por nombre como Flutter, muestra un loader centrado y `Agent Profiles Unavailable` cuando falla la carga, usa `Name is required.`/`Command is required.`, elimina con un solo click como Flutter y conserva la confirmación únicamente para la reducción de protecciones. Los flags Managed (`Web Search`, `Bypass All Protections`) usan el checkbox cuadrado de Flutter en vez de un switch ovalado. La comparación a tamaño completo y el tramo inferior se guardaron en `/tmp/alera-parity/agent-profiles-lower/flutter-focus.jpeg` y `/tmp/alera-parity/agent-profiles-lower/gpui-focus.jpeg`; el álbum fue enviado a Telegram.
- Settings > Diagnostics y Orchestration P1: se alineó el copy de exportación (`Save a zip with app and runtime logs plus version details.`, `Diagnostics exported.`) y la validación de rechazo de planes (`A rejection needs a reason.`) al contrato Flutter. GPUI ahora limpia los planes previos en `AsyncError` y usa un spinner centrado sin copy adicional durante loading, igual que Flutter; queda repetir Computer Use en loading/error y en un plan no vacío.
- Settings > Mobile Devices P1: tras recompilar GPUI se compararon a tamaño completo los tramos superior e inferior con una sola Flutter. Gateway, pairing, ofertas vacías, dispositivos revocados, badges y acciones quedaron con el mismo copy sentence-case; GPUI también expone Rename/Revoke/Delete con los tooltips `Rename Device`, `Revoke Device` y `Delete Device`. El scrollbar y el paso por páginas tienen una diferencia de anclaje menor (la raíz GPUI expone otra unidad de página); queda ajustar la geometría sin tocar Runtime/Quotas. Evidencia nueva: `/var/folders/f4/dhm12zks1x12ynzwq2_s5zxm0000gn/T/com.openai.sky.CUAService/Alera Dev Screenshot 2026-08-25 at 7.35.10 AM.jpeg` y `/var/folders/f4/dhm12zks1x12ynzwq2_s5zxm0000gn/T/com.openai.sky.CUAService/Alera GPUI Screenshot 2026-08-25 at 7.35.10 AM.jpeg`.
- Orchestration `Execution Plans` P1: después de corregir el layout del contenido vacío y recompilar GPUI, Flutter y GPUI quedaron alineadas a tamaño completo en un modal de 720x640: título, icono, copy, centrado vertical, padding, superficie y acción `Close`. Se guardó y envió el álbum enfocado `/tmp/alera-parity/execution-plans-final/flutter-focus.jpeg` y `/tmp/alera-parity/execution-plans-final/gpui-focus.jpeg`. Los estados no vacío y rechazado, el error de razón vacía y la acción real Reject quedaron validados posteriormente; Approve sobre un run desechable y el error de transporte siguen abiertos.
- Orchestration stage rendering P1: GPUI ahora replica el tratamiento de Flutter para planes no vacíos: título de stage recortado, perfil y fallback en color secundario, estado/hold copy y payload tolerante a `runId`, `status`, `blocksDispatch`, `policy` y `stallPolicy`. Se añadieron pruebas de parseo y de títulos vacíos; `cargo test --all-targets` pasó con 95 tests de aplicación y 12 de terminal, y Clippy con `-D warnings` pasó.
- Orchestration review interaction P1: el botón `Close` de `Execution Plans` ya permanece activo mientras una decisión está en vuelo, como el `TextButton` de Flutter; cerrar el modal deja continuar la petición en segundo plano y evita bloquear la navegación. Siguen pendientes la captura Computer Use de un plan no vacío, loading/error visibles y approve/reject con respuesta real.
- Settings > Agents P1: la comparación a tamaño completo de Flutter y GPUI recompilada mostró el mismo master/detail, grupos CLI/Skills, Status Hooks y Behavior, con los mismos controles Auto/Copy/Install, toggles y estados de registro. Se alineó el copy sentence-case de todas las descripciones de hooks/skills, incluida la variante de `Keep Computer Awake While Agents Are Working` por plataforma; se guardaron recortes equivalentes en `/tmp/alera-parity/agents-settings/flutter.jpeg` y `/tmp/alera-parity/agents-settings/gpui.jpeg` y se envió un álbum enfocado a Telegram. Faltan estados de operación en vivo (install/update, hooks fallidos, refresh, notificaciones) y escritura/persistencia completa.
- Diagnostics P1: las rutas de `Open Logs Folder` y `Export Diagnostics` ahora usan el toast global de GPUI para errores y éxito, igual que `AleraToast` de Flutter, en vez de dejar mensajes pegados dentro del pane; el exportador mantiene su trabajo fuera del hilo de UI y sigue incluyendo metadata y logs filtrados. Falta validar con Computer Use el picker cancelado, exportación real, error de ruta y el estado loading sostenido.
- Diagnostics Computer Use: a tamaño completo ambas apps muestran el mismo tramo de `Diagnostics`, `Update Failed`, iconos, dropdown y switches; el álbum enfocado quedó en `/tmp/alera-parity/diagnostics/flutter.jpeg` y `/tmp/alera-parity/diagnostics/gpui.jpeg` y fue enviado a Telegram. La captura confirma que una misma acción de scroll por páginas deja a GPUI aproximadamente una fila más abajo que Flutter; el recorrido del thumb sigue siendo un gap de geometría y no se marca como paridad completa.
- Auditoría transversal de iconos y estados: se cruzaron los codepoints Lucide/Codicons de Flutter con `icons.rs` y los call sites incluidos. GPUI ahora usa `GitMerge`, `GitPullRequestClosed`, `Unlink`, `Send` y `Success` en las acciones de review/comentarios de Pull Request, `Warning` en el aviso de host no disponible de Resource Manager y `FolderOff` en el estado vacío de Projects; los codepoints quedan cubiertos por tests. Los loaders de 1200 ms, el spinner de agentes de 1400 ms, check, bell, success, cancel y los SVG de proveedores quedan identificados como equivalentes estáticos; la fila sigue `parcial` hasta capturar loading sostenido, hover y todos los proveedores en vivo.
- AI generation states P1: Flutter mantiene el spinner mientras genera y cambia a `circleStop` rojo únicamente al pasar el mouse, con tooltip de cancelación; GPUI ahora replica ese estado para Commit Message y Pull Request Details, conserva la cancelación existente y cubre el codepoint `circle-stop` con tests. Falta una captura viva durante una generación real para cerrar la evidencia visual.
- Settings scroll P1: el contenedor dejó de mezclar un viewport flex incompleto con un scrollbar manual y ahora replica la composición `size_full` del wrapper `Scrollable` de GPUI, conserva anchors/búsqueda y aterriza en el mismo grupo tras un desplazamiento. La prueba a 1300/1302x768 confirma que ambos llegan a Runtime/Diagnostics/Updates, pero el offset exacto de una página aún difiere unos píxeles y el recorrido completo del thumb/drag sigue pendiente.
- Terminal/Context Sidebar P1: la reserva de geometría del terminal usaba 42 px cuando el rail contextual colapsado renderiza 52 px; se corrigió para que columnas/filas y el área de drop coincidan con Flutter. Tras recompilar, se compararon ambos rails colapsados a tamaño completo y se envió álbum enfocado (`/tmp/alera-parity/context-collapsed-flutter.jpg`, `/tmp/alera-parity/context-collapsed-gpui.jpg`).
- Terminal restore P1: GPUI ahora conserva el snapshot del host y lo reproduce en lotes de 32 KiB cada frame, mostrando una superficie centrada `Restoring Terminal` con barra de progreso como Flutter; se añadió prueba de generación, avance y finalización sin bloquear el primer frame.
- Re-auditoría operativa: se verificó de nuevo el inventario contra 22 módulos Flutter, 222 superficies de presentación, 143 módulos Rust de GPUI, 30 contratos funcionales y 139 gates visuales; el conteo queda en 2 `paridad validada`, 28 `parcial`, 0 `gap` y 7 `fuera de alcance`. AccessKit eliminó el gap de framework, pero el dominio permanece parcial hasta completar todos los controles y focus scopes.
- Sidebar/workspaces: se compararon en vivo los estados `None`, pinned/All, indentación, presencia, metadatos, hover acotado al row y menú contextual completo. El tooltip del botón de crear workspace bajo un proyecto ahora coincide con Flutter: `New Workspace in This Project`; el menú conserva `Rename`, pin/unpin, tags, parent, browser/Finder, path, sleep y remove.
- Sidebar/agentes: el cierre de agente ahora hace fade de 100 ms como `AnimatedOpacity` de Flutter, el listado expandido conserva el espacio superior de 4 px, el tray de hijos usa la misma densidad y un agente `interrupted` mantiene el spinner si su estado base sigue siendo `working`; el estado agregado del workspace elige solo `blocked > waiting > working > done`, pero conserva la bandera `interrupted` del run elegido para pintar el icono y tooltip equivalentes.
- Sidebar agent ordering (2026-08-26): la proyección GPUI dejó de iterar la lista de presencia en el orden de llegada del host. Ahora recorre los tabs terminales del workspace y hace el join estricto por `workspaceId`, `tabId` y `handle`, igual que Flutter, manteniendo el orden de creación cuando los hooks actualizan `waiting`, `blocked`, `working`, `done` o `interrupted`.
- Sidebar agent states live fixture (2026-08-26): se enviaron eventos de hook controlados al mismo tab temporal en ambas apps. `SessionStart`, `PermissionRequest`, `Stop` y `Stop` con `interrupted=true` produjeron `Working`, `Waiting for input`, `Done` e `Interrupted` con los mismos iconos, colores, agrupación `Pinned`, texto y cierre; el indicador agregado de workspace ahora conserva la interrupción del estado elegido, como Flutter. El tab temporal se cerró y el workspace volvió al estado idle sin dejar presencia.
- Stable dynamic row identity (2026-08-26): Explorer, Search y Agent Profiles ya no usan el índice de render como identidad de fila. Las claves derivan de la ruta, match o id del perfil, evitando que un watcher, resultado nuevo o refresh reutilice el estado/acción de otra fila.
- Explorer drag/click isolation (2026-08-27): cada fila registra pointer-down y solo marca drag con un drag nativo activo; pointer-up no abre/togglea accidentalmente y el cleanup borra flags. El `LongPressDraggable` Flutter ya fue ejercitado con CGEvent sostenido y produjo la misma mutación que GPUI.
- Sidebar/View Options: el toolbar ahora usa la banda de 40 px de Flutter y los headers de sección/proyecto 30 px; al abrir `View Options` se desmonta su tooltip para que no quede flotando sobre el modal. La comparación recompilada se guardó en `/tmp/alera-parity/sidebar-view-options-{flutter,gpui}-inventory.jpg` y se envió como álbum de Telegram.
- Computer Use: se volvió a abrir una sola Flutter y una sola GPUI, ambas a tamaño completo con doble click de titlebar, sin modal fatal. Se guardaron recortes de sidebar y contexto en `/tmp/alera-parity/sidebar-tooltip-{flutter,gpui}.jpg` y `/tmp/alera-parity/sidebar-context-{flutter,gpui}-full.jpg` y se enviaron como un álbum de Telegram.
- Validación del ajuste de sidebar: `cargo fmt --check`, `cargo check --bin alera-gpui` y Clippy con `-D warnings` pasan; el binario GPUI fue recompilado y relanzado mediante `make gpui-debug` usando el Dart 3.12.2 fijado del entorno Puro. Sigue únicamente el aviso externo de future-incompatibility de `block v0.1.6`.

- Change Title validado en la app recompilada: escribir `Parity Probe` reemplazó `Terminal 2`, guardar actualizó el tab y después se restauró `Terminal 2`. La API pública de `gpui-component` no expone selección programática; GPUI usa un reemplazo seguro del primer `InputEvent::Change` para igualar la acción de escribir sobre el título seleccionado.
- Entrada de terminal corregida para IME: se eliminó el buffer sombra local y `EntityInputHandler` sigue el modelo de Zed, dejando que el PTY sea la fuente de verdad y que la composición nativa reemplace texto sin reordenarlo.
- Git snapshot P1: el host ya no calcula el diff completo durante cada apertura de Source Control. `workspaceGit.snapshot` acepta `includePatch` opcional y GPUI solicita el estado liviano; el diff pesado queda disponible solo para consumidores que lo pidan explícitamente. En el repositorio Alera el snapshot bajó de ~20.8 s a ~1.3 s para el estado base, evitando el timeout y permitiendo cargar las 67 filas en GPUI.

## Siguiente

Sin tareas de paridad pendientes dentro del alcance definido. Cualquier trabajo futuro será una nueva ampliación de alcance o una regresión encontrada después de este gate.

## Validación registrada

- GPUI: `cargo clippy --manifest-path experiments/alera-gpui/Cargo.toml --bin alera-gpui --all-targets -- -D warnings` pasó; solo quedó el warning externo de future-incompatibility de `block v0.1.6`.
- GPUI gate posterior a Settings/Git History (2026-08-25): `cargo test --manifest-path experiments/alera-gpui/Cargo.toml --bin alera-gpui --all-targets` pasó con 101 tests de aplicación y 12 de terminal; `cargo clippy --manifest-path experiments/alera-gpui/Cargo.toml --bin alera-gpui --all-targets -- -D warnings`, `cargo fmt` y `git diff --check` también pasan. El único aviso sigue siendo el future-incompatibility externo de `block v0.1.6`.
- GPUI: el gate anterior había pasado con 73 tests del binario principal y 11 tests de terminal; el corte actual queda actualizado en la línea siguiente.
- GPUI: tras añadir el estado visual de tabs/agentes, `cargo test --manifest-path experiments/alera-gpui/Cargo.toml --all-targets` pasó con 98 tests del binario principal y 12 tests de terminal; `cargo fmt --check` y `cargo check --bin alera-gpui` pasan. El Clippy final de esta tanda queda pendiente junto con el gate de `git diff --check`; permanece únicamente el aviso externo de future-incompatibility de `block v0.1.6`.
- Settings compartidos: `cargo fmt --manifest-path rust/Cargo.toml --all -- --check`, `cargo check --manifest-path experiments/alera-gpui/Cargo.toml --bin alera-gpui`, `local_flutter_settings_restore_all_client_sections` y `shared_flutter_settings_merge_is_recursive` pasaron.
- Tras el ajuste de copy de Keyboard: `cargo fmt --manifest-path rust/Cargo.toml --all -- --check` y `cargo clippy --manifest-path experiments/alera-gpui/Cargo.toml --bin alera-gpui --all-targets -- -D warnings` pasan; permanece solo el aviso externo de future-incompatibility de `block v0.1.6`.
- Source Control/Git History: con el mismo workspace y ambas apps a tamaño completo se compararon `No Changes`, el split-button `Pull`, el menú de acciones (Commit, Stage, Fetch, Pull, Push, Sync, Stash) y la sección `COMMITS 50+`; al expandir el primer commit ambas muestran autor/fecha, refs, grafo y lista de archivos con estados. Se guardaron recortes equivalentes en `/tmp/alera-parity/source-control-history-flutter.jpg` y `/tmp/alera-parity/source-control-history-gpui.jpg` y se enviaron juntos como álbum de Telegram. La fila sigue `parcial` hasta cubrir loading, error/no-repo, conflictos y acciones destructivas end to end.
- Source Control snapshot P1: el sidecar recompilado dejó `includePatch` desactivado por defecto para la apertura base. En Computer Use, ambas apps a tamaño completo y en el mismo workspace Alera cargaron la rama `feat/rust-desktop-migration`, el contador `67`, `Stage All`, la lista de cambios y `COMMITS` sin timeout; el álbum enfocado quedó en `/tmp/alera-parity/source-control-flutter-focus.jpg` y `/tmp/alera-parity/source-control-gpui-focus.jpg`.
- Source Control no-repository state (2026-08-26): switching both clients to the fixture workspace `Sidebar Beta Remote` reproduced the loading/error path. After resolution, Flutter and GPUI both show `This workspace is not a Git repository.` centered with the same disabled `Fetch` control; GPUI's extra error glyph was removed to match Flutter. Both clients were restored to `main` afterward.
- Source Control refresh state (2026-08-26): on `main`, the same manual refresh action completed in both clients, preserved the 8-change list and Pull control, and emitted the same `Source control refreshed` success toast. The transient spinner was too short for a stable paired screenshot, so sustained loading remains open for a delayed backend fixture.
- Editor selección/clipboard: con el mismo archivo abierto, Flutter y GPUI permiten selección parcial dentro del editor y `Cmd+C` devuelve exactamente `nst config = {`; se comprobó que el foco/caret permanece sin borde blanco y se envió álbum enfocado (`/tmp/alera-parity/editor-selection-flutter-final.jpg`, `/tmp/alera-parity/editor-selection-gpui-final.jpg`). Quedan selección multilinea, navegación por teclado, scroll, edición, conflicto y loading/error sostenidos.
- Terminal selección/scrollback: la evidencia histórica de `seq 1 80` se conserva, pero queda supersedida en el extremo inferior por la comprobación del 2026-08-25: ambas sesiones muestran exactamente `4953`-`4981` después de devolverlas al final. El wrapping residual del prompt quedó corregido para sesiones nuevas al iniciar y restaurar con el viewport medido del pane; sigue pendiente repetir scroll extremo, selección/copy y restore sostenido sobre una sesión limpia, además de la diferencia de gesto de scroll de `sky`.
- Terminal scrollbar P1: el thumb GPUI comparte el reloj de actividad del terminal y se oculta después de 800 ms sin interacción; durante drag/scroll permanece visible y el ciclo de blink fuerza el repintado para completar el fade. El extremo inferior ahora queda alineado en contenido; la geometría exacta del thumb, drag y el wrapping de celdas siguen `parcial`.
- Editor discard copy P1: la comparación de dirty -> discard mostró el mismo toast de Flutter; se corrigió el texto GPUI de `Changes Discarded` a `Changes discarded` y se recompiló. El fixture del archivo quedó restaurado.
- Computer Use: screenshots comparativos de Quotas overview/provider, Resource Manager, Runtime, Settings, menú de tabs y Change Title.
- Computer Use: drag/reorder/move/split/collapse de terminales validado con persistencia en SQLite.
- Computer Use: Change Title reemplazó y guardó un título de tab, y el fixture fue restaurado a `Terminal 2`.
- Telegram: enviado un álbum de evidencia con recortes enfocados de sidebar y status bar; orden GPUI/Flutter, con caption compartido.
- Telegram: enviado un segundo álbum enfocado en el popover de proveedor de cuotas, comparando icono, perfil, estado, spacing, barras y `Try With TUI`.
- Telegram: enviado un tercer álbum enfocado en Resource Manager (GPUI/Flutter) y un cuarto álbum enfocado en Runtime (GPUI/Flutter).
- Computer Use: tras recompilar, GPUI fue relanzada sin duplicados, llevada a tamaño completo y restaurada al workspace `main`; Flutter quedó también a tamaño completo.
- Selección inicial endurecida: GPUI prefiere un workspace fijado disponible, luego `main` disponible y descarta rutas locales desaparecidas antes de montar el workbench; relanzar ya no abre `Sidebar Beta` con toast de ruta inexistente.
- Computer Use: la secuencia real `echo ` + `apostrophe` + `a` se probó en GPUI y Flutter; ambas muestran `echo á` en el prompt, y GPUI conserva el cwd `leynier-website`.
- Computer Use: después de recompilar Settings, GPUI muestra `Selected: GitHub Dark`, la misma lista de diez temas, preview y Tab Size que Flutter; el árbol de accesibilidad de Flutter también confirma el mismo estado seleccionado.
- Telegram: enviado un álbum enfocado de IME con recortes GPUI/Flutter (`echo á`) para revisar el cambio visualmente.
- Telegram: enviado un álbum enfocado del modal `Force Stop Runtime` después del ajuste visual GPUI/Flutter.
- Telegram: enviado un álbum enfocado del inventario P0 del sidebar, con recortes Flutter/GPUI de la estructura y del menú contextual de workspace después del ajuste de Agent Activity.
- Telegram: enviado un álbum enfocado de Settings > Editor con recortes equivalentes Flutter/GPUI después de sincronizar la configuración local compartida.
- Telegram: enviado un álbum enfocado del rail colapsado Flutter/GPUI después de validar su reapertura desde un proyecto.
- Telegram: enviado un álbum enfocado de los límites mínimo/máximo del resize del sidebar y otro del rail colapsado del Context Sidebar con reexpansión.
- View Options comparado con ambas ventanas en tamaño completo: la geometría quedó alineada y la lista de proyectos ahora usa el mismo orden alfabético determinista que Flutter, incluso si el sidebar está ordenado por recencia o actividad.
- Telegram: enviado un álbum enfocado de View Options GPUI/Flutter con la geometría y el orden de proyectos corregidos.
- Settings fue recorrido y el scrollbar se arrastró en ambas apps; el contenido conserva el recorrido esperado hasta Diagnostics/Updates/Support. Account, Browser y Remote Hosts siguen fuera de alcance explícito del POC GPUI según `feature-matrix.md`.
- Source Control comparado en vivo: botón Fetch, menú de acciones, iconos, radios y cierre al hacer click externo quedan alineados; se envió álbum enfocado.
- Terminal comparado en vivo: cwd `leynier-website`, prompt con `á` y cursor visible; se envió álbum enfocado.
- Search asentado tras esperar el resultado asíncrono: GPUI y Flutter muestran `20 matches in 15 files` con las mismas rutas y líneas visibles; el estado intermedio de `0 matches` fue transitorio y no una diferencia funcional.
- Search P1: se compararon también `No results` y un patrón regex inválido (`[`): ambos muestran el mismo resumen `No results`, error rojo y estado vacío; GPUI descarta resultados parciales y ya no enseña el contador engañoso `0 matches in 0 files` cuando el backend devuelve un error. La corrección fue recompilada y verificada con Computer Use; se enviaron álbumes enfocados (`/tmp/alera-parity/search-results-flutter.jpg`, `/tmp/alera-parity/search-results-gpui.jpg`, `/tmp/alera-parity/search-regex-error-flutter.jpg`, `/tmp/alera-parity/search-regex-error-gpui.jpg`). Quedan loading, teclado, selección/copy y replace con conflicto.
- Search selección/copy P1: al abrir un match, GPUI ahora entrega el foco al editor en un frame y despacha la selección en el siguiente; el texto queda resaltado igual que Flutter y `Cmd+C` devuelve `readme` en ambas apps. Se validó con Computer Use y `pbpaste`, y se envió álbum (`/tmp/alera_search_selection_flutter_focus.jpg`, `/tmp/alera_search_selection_gpui_focus.jpg`). Quedan loading sostenido, navegación por teclado y replace con conflicto.
- Search loading P1: la auditoría del estado `loading` mostró que Flutter centra un indicador en el área de resultados además de `Searching...`; GPUI ahora renderiza el mismo loader centrado cuando la búsqueda aún no tiene filas, conservando el resumen y el estado vacío posterior. La compilación y relanzamiento fueron validados; falta capturar un backend deliberadamente lento para comparar la animación frame a frame.
- Search replace conflict P1: al comparar Replace All con un editor dirty, Flutter bloquea la operación antes de confirmar, conserva el resumen, muestra el error inline `Save or discard <path> before replacing.` y lanza el toast inferior; GPUI ahora aplica la misma guardia y ambos canales de feedback, además de limpiar el error al cambiar de workspace. La corrección fue recompilada con `cargo check` y Clippy, y se reprodujo visualmente con Computer Use en ambas apps sin ejecutar ninguna escritura.
- Source Control snapshot P1: se reprodujo en el workspace Alera un timeout real causado por generar el patch completo de 69 archivos. Se midió cada tramo del snapshot y se confirmó que `git_diff_all` consumía ~20.8 s; se cambió el contrato del host para que `includePatch` sea opt-in, se recompiló el sidecar, se reinició un único runtime y Computer Use verificó que GPUI carga Source Control en ~5 s con las mismas 67 filas, rama y Stage All que Flutter. Se enviaron recortes enfocados (`/tmp/alera-parity/source-control-flutter-focus.jpg`, `/tmp/alera-parity/source-control-gpui-focus.jpg`) como álbum de Telegram.
- Loaders/iconos P2: la auditoría estática confirmó que `AleraIcon::Loading` siempre se enruta al SVG animado `loader-circle` de 1200 ms, el spinner de agentes usa 1400 ms, y los codepoints de `Check`, `Notifications` y `Success` coinciden exactamente con `LucideIcons.check`, `bellRing` y `circleCheck` de Flutter. Los SVG multicolor de Claude, Kimi, MiniMax y Z.ai ahora pasan por el renderer SVG de GPUI en lugar de la ruta raster; queda captura viva de cada estado y tooltip.
- Telegram: enviado un álbum enfocado de Search GPUI/Flutter con el resultado asentado (`20 matches in 15 files`).
- Telegram: enviado un álbum enfocado de scroll de terminal con el mismo escenario de 80 líneas en Flutter y GPUI.
- Telegram: enviado un álbum enfocado del Workbench GPUI con movimiento entre splits, split direccional y cancelación fuera.
- Computer Use: al reabrir el formulario Pull Request y enlazar `#543`, GPUI permaneció vivo y mostró el error inline de GraphQL; el crash report anterior quedó corregido y se dejó una sola GPUI con el runtime compartido de Flutter.
- Telegram: enviado un álbum enfocado de la review válida `leynier/leynier-website#11` (`/tmp/alera_review_flutter_focus.jpg`, `/tmp/alera_review_gpui_focus.jpg`) con estado, checks, comentario y `Unlink`.
- Search interactivo comparado: Replace y los filtros avanzados (include/exclude) se despliegan en ambas apps con la misma estructura, controles `AB`/replace-all y geometría; se envió un álbum enfocado de este estado.
- Pull Request asentado después del estado de loading: formulario Base Branch/Title/Description, botón split Create/Draft y menú contextual quedaron funcionalmente alineados; el menú se cierra con click externo y se envió un álbum enfocado del formulario.
- Pull Request composer P1: se corrigió la geometría GPUI contra Flutter (padding horizontal 16 px, separación inicial del formulario, split button de 28 px con segmento de flecha de 34 px y enlace inferior sin divisor artificial). El dropdown de Base Branch mantiene las mismas opciones, selección, cierre externo y anchura; el menú Create/Draft ahora se ancla a la misma altura que Flutter. Evidencia final recortada en `/tmp/alera-parity/pr-composer-{flutter,gpui}.jpg` y `/tmp/alera-parity/pr-actions-{flutter,gpui}.jpg`; se envió un álbum a Telegram.
- Pull Request checks P1: GPUI ahora clasifica `timed_out`/`action_required`/`manual` como fallos, `skipped`/`skipping`/`neutral`/`not_applicable` como estados neutros con círculo y estados desconocidos como loading animado, igual que `PullRequestCheckIcon` de Flutter. Se agregó `lucide/circle.svg`, pruebas de clasificación y se validaron 2 tests específicos.
- Pull Request review P1: la sección de comentarios GPUI ahora sigue el estado compacto de Flutter, muestra `Add Comment` solo cuando la review está abierta, abre un editor multilinea con foco, y ofrece `Cancel`/`Post Comment` con loader durante la acción; al cambiar de workspace se limpia el estado de composición. El composer/link form fue recompilado y se comprobó que no hay modal fatal; queda validar la review existente con checks/comentarios reales.
- Pull Request review P1: se enlazó la PR válida `leynier/leynier-website#11` en Flutter y GPUI; ambas muestran estado `Closed`, cuatro checks fallidos, un check exitoso, un comentario de Vercel y `Unlink Pull Request`. Se envió álbum enfocado (`/tmp/alera_review_flutter_focus.jpg`, `/tmp/alera_review_gpui_focus.jpg`). Quedan una PR abierta, comentarios/acciones sobre una review abierta y autenticación/error por proveedor.
- Pull Request composer/link P1: en la sesión vigente ambas apps abrieron el formulario `Link Pull Request` desde el mismo botón y el split menu `Create Pull Request`/`Draft Pull Request` se cerró con click externo. El diálogo de enlace quedó visualmente alineado en estructura, copy, input, botón y anclaje; siguen pendientes una PR abierta real, acciones de review y autenticación/proveedor.
- Settings Editor P1: GPUI dejó de usar defaults propios al refrescar y ahora restaura desde la misma base SQLite de Flutter el tema `GitHub Dark`, Tab Size, cursor y demás secciones locales; se validó con tests de parseo/merge y Computer Use en una sola GPUI y una sola Flutter a tamaño completo. Se envió álbum enfocado (`/tmp/alera_settings_editor_flutter_focus.jpg`, `/tmp/alera_settings_editor_gpui_focus.jpg`). Account, Browser y Remote Hosts siguen fuera del POC.
- Settings Editor persistencia P1: se cambió el tema de GPUI a `GitHub Dark Dimmed`, SQLite de Flutter reflejó el cambio y después se restauró a `GitHub Dark`; `terminal.cursorShape=bar` y `cursorBlink=true` se conservaron durante ambas escrituras.
- Settings Terminal P1: después de cargar las secciones locales compartidas, Flutter y GPUI muestran `JetBrains Mono`, tamaño 13, peso 400, line-height 1.3, cursor `bar`, blink activo y opacity 1; se envió un álbum enfocado de esta comparación.
- Settings Keyboard P1: se corrigió el catálogo GPUI para usar exactamente la capitalización y las descripciones sentence case de Flutter (`Open the settings dialog.`, `Find in Files`, `When a Terminal Is Focused`, etc.); recompilado y comparado en vivo, con álbum enfocado enviado. `New Browser Tab` sigue fuera porque Browser está explícitamente excluido del POC.
- Explorer P0: con `src` expandido, Flutter y GPUI conservaron las mismas carpetas hijas, orden, indentación, iconos y densidad; se envió un álbum recortado del árbol. El watcher/restore de rutas sigue cubierto por la validación anterior.
- Editor P0: al hacer click dentro del texto GPUI no dibuja el borde blanco alrededor del editor; el foco entrega un caret visible después del ciclo de blink, igual que Flutter. La selección, navegación y edición completa siguen pendientes de la matriz.
- Editor copy P1: el diálogo de conflicto, los toasts de guardado y los estados sin archivo/diff GPUI ahora usan exactamente el copy de Flutter (`File changed on disk`, `Overwrite the file with the editor contents?`, `File saved`, `File overwritten`, `This editor tab has no file.`, `File operation failed`, `No Git diff for this file`); selección multilinea, scroll, edición, loading/error sostenidos y una captura del conflicto real siguen pendientes.
- Sidebar View Options: `None` ya tenía bounds iguales; al probar `Project` se detectó que GPUI conservaba la altura de `None` aunque Flutter agrega la fila `Then Workspaces By`. La altura intrínseca quedó ajustada y la repetición recompilada está registrada abajo.
- Sidebar View Options `Project` recompilado y verificado: la altura/centrado ahora queda dentro de ~2 px de Flutter, la fila `Then Workspaces By` conserva su ordenamiento y la modal sigue cerrándose por click externo; enviado álbum enfocado.
- Al probar `Project > Sort Projects By > Recent` se encontró que GPUI ordenaba por el workspace hijo más reciente, mientras Flutter usa `Project.updatedAt`. Se corrigió el comparador GPUI para usar el mismo campo; falta recompilar y repetir el orden Recent/None.
- En `Project > Then Workspaces By > Agent Activity` se detectó que GPUI ordenaba todos los workspaces `active` por `updatedAt`, dejando los que no tienen terminal entre los activos. Se ajustó el comparador para priorizar terminales abiertos y ordenar los workspaces sin terminal por nombre, como Flutter; la validación recompilada queda registrada abajo.
- Agent Activity recompilado y verificado en vivo: Beta/Alpha, la raíz, hijos con indentación y workspaces sin terminal ahora siguen el mismo orden visible que Flutter; enviado álbum enfocado del sidebar.
- Agrupación `None` verificada con Agent Activity: sección `All`, orden global, indentación de hijos, estados y workspace remoto coinciden visualmente con Flutter; enviado álbum enfocado.
- Se corrigió la geometría de `View Options` con filtros: el cálculo anterior sumaba altura al seleccionar proyectos/tags y hacía la modal ~54 px demasiado alta. Ahora cada chip reemplaza sus filas disponibles con la misma delta intrínseca que Flutter; con dos proyectos seleccionados los bounds quedaron iguales y se envió álbum enfocado.
- Filtro de tag verificado con dos proyectos y `Sidebar Fixture`: el estado `All Tags Selected`, chip, lista restante, scrollbar y bounds de la modal quedaron alineados con Flutter; enviado álbum enfocado.
- `make gpui-debug` sigue sin ser utilizable en este entorno porque el Dart del PATH es 3.11.5 y el repositorio exige lenguaje Dart 3.12; para validar GPUI se usa el build Cargo equivalente y se copia el binario al bundle.
- Settings scroll vuelto a validar con Computer Use: `sky.scroll` sobre el contenedor de Flutter y sobre la ventana GPUI llevó ambas vistas al mismo tramo de Runtime, Diagnostics y Updates; el scrollbar de GPUI se mueve y conserva el recorrido. Se envió un álbum enfocado con ambas capturas.
- Validación final de Rust después de los cambios de sidebar, filtros y Runtime: `cargo fmt --check`, `git diff --check`, Clippy con `-D warnings` y `cargo test --all-targets` pasan (76 tests principales y 11 de terminal); permanece únicamente el aviso externo de future-incompatibility de `block v0.1.6`.
- Runtime corregido para respetar la dirección de versiones de Flutter: un host más nuevo no muestra `Update Runtime`, un host más antiguo sí, y las filas `Host Build`/`Bundled Build` solo aparecen con versión igual y commits conocidos diferentes; se añadieron pruebas y se recompiló la app.
- Computer Use volvió a abrir exactamente una GPUI y una Flutter a tamaño completo; el popover Runtime fue comparado en vivo y se envió un álbum enfocado después del ajuste.
- Status Bar vuelto a capturar después del relanzamiento: host chip, strip de cuotas, Resource Manager y Runtime quedan en el mismo borde inferior; se envió un álbum recortado para revisar la geometría sin el resto de la ventana.
- Agent Activity alineado en código con Flutter: GPUI ahora prioriza `needsYou`, `done`, `working` e `idle`, descarta presencia con más de 30 minutos, usa `stateStartedAt`, agrega la actividad de descendientes y deja los workspaces sin terminal al final por nombre. El orden de `None` usa la misma proyección de subárbol.
- El filtrado de filas de agentes valida `workspaceId`, `tabId`, tipo terminal y `handle`/`terminalSessionId`; una presencia retrasada ya no mantiene un agente después de cerrar el tab.
- El snapshot GPUI ahora carga `workspaceActivity.list` como fallback persistido de Agent Activity y conserva `workspace.updatedAt` solo como respaldo para hosts antiguos o datos incompletos.
- Resource Manager: las filas de métricas usan 22 px de alto, tipografía de 12 px y line-height de 16 px, reproduciendo el ritmo de filas de Flutter y permitiendo que aparezca `Sidebar Beta` en el mismo tramo visible.
- Quotas, Quota Provider, Resource Manager y Runtime: los popovers tienen ahora una separación explícita de 4 px sobre el Status Bar, equivalente al `verticalOffset` de la tarjeta flotante de Flutter.
- Computer Use: comparación enfocada final de Resource Manager capturada con una GPUI y una Flutter a tamaño completo, sin duplicados; el álbum GPUI/Flutter fue enviado a Telegram.
- Workbench P0: las rutas de `pointer up` del drag de tabs ahora consumen siempre el preview y notifican el render incluso si el destino desaparece o una mutación está ocupada; esto evita el overlay direccional pegado después de soltar.
- Sidebar P0: la proyección del indicador agregado del workspace ahora sigue la prioridad de Flutter (`blocked > waiting > working > done`) y usa el `updatedAt` más reciente en empates; la bandera `interrupted` del run elegido también se conserva en el indicador agregado, mientras un run `working` interrumpido mantiene su spinner. Se añadieron pruebas para la prioridad, empate, estado base interrumpido y spinner de un run `working` interrumpido.
- Sidebar P0: los rows de Agent Activity ahora usan los gaps de Flutter (6 px entre glyphos, 4 px vertical, sin separación artificial entre filas) y el botón `Close Terminal` solo es interactivo cuando está visible/activo; se recompiló y la comparación visual base no mostró regresiones.
- Sidebar P0: el rail colapsado ahora usa el primer carácter del proyecto como Flutter, agrega tooltips a proyectos, Expand/Collapse Sidebar, Add Project y Settings, y se validó el ciclo colapsar -> seleccionar proyecto -> reabrir en ambas apps.
- Sidebar P0: el handle de resize ahora tiene línea visible de 1 px, énfasis de 2 px al hover/drag, cursor de columna y persistencia después del drag; se arrastró Flutter y GPUI al mismo ancho y se envió evidencia enfocada.
- Sidebar P0: los límites mínimo y máximo del resize fueron probados con Computer Use en Flutter y GPUI, ambos recorren el mismo rango visual y se restauraron al ancho común; se envió álbum enfocado de cuatro recortes.
- Context Sidebar/Explorer P0: el header ahora respeta el spacing de Flutter y oculta Source Control/Pull Request cuando no hay scope Git; el rail colapsado se comparó con sus tooltips y reexpansión; Explorer GPUI incorpora DragTarget/feedback para mover un archivo o carpeta a otra carpeta, con guardas contra auto-descenso y limpieza al soltar fuera. El drag real movió `README.md` a una carpeta y se restauró el fixture; quedan symlink, errores, resize y estados de carga.
- Terminal P0: el drop interno desde Explorer hacia la terminal ahora pega la ruta escapada igual que el drop externo; los estados de sesión distinguen Starting/Reconnecting/Restarting para que el overlay no muestre siempre Reconnecting.
- Terminal P0: la operación de sesión conserva su instante de inicio y el overlay GPUI ahora muestra `Elapsed: Ns` después de 3 segundos, igual que Flutter; el ciclo de salida limpia el estado para no dejar un contador stale.
- Terminal P1 (auditoría transversal 2026-08-25): se abrió el mismo tab `Terminal 1` en ambas apps, se enfocó la superficie y se ejecutó `printf 'ALERA_PARITY\\n'`. Ambos clientes conservaron el cwd `leynier-website`, mostraron el mismo resultado y devolvieron el prompt con cursor; GPUI conserva más líneas de scrollback persistidas en este tab, por lo que no se convierte en `paridad validada` hasta repetir selección/copy, scroll extremo y restore desde una sesión limpia. Se envió álbum enfocado (`/tmp/alera-parity/terminal-parity/flutter-focus.jpg`, `/tmp/alera-parity/terminal-parity/gpui-focus.jpg`).
- Terminal selección/copy P1 (auditoría transversal 2026-08-25): se generó la misma salida `COPY_FLUTTER_789`/`PARITY_SELECT_456` en Flutter y GPUI, se seleccionó con drag y se ejecutó `Cmd+C` en cada cliente. Ambos portapapeles conservaron el texto seleccionado y el sufijo del prompt (`%`), confirmando el contrato de selección y copia; el álbum enfocado quedó en `/tmp/alera-parity/terminal-selection/flutter-focus.jpeg` y `/tmp/alera-parity/terminal-selection/gpui-focus.jpeg` y se envió a Telegram. Terminal sigue `parcial` por scroll extremo/restore sostenido, path drop con gesto largo y shortcuts completos.
- Workbench P0: después de recompilar se repitieron movimiento entre splits, creación de split direccional hacia el borde y cancelación fuera del workbench; el overlay desapareció en cada `pointer up`, el split vacío se reacomodó y no reapareció el estado pegado de la captura del usuario.
- Editor P0: la barra GPUI dejó de mostrar el timestamp que no existe en Flutter, ahora expone tooltips equivalentes (`View Diff`, `Save File`, `Discard Changes`, `Open Preview`) y conserva el estado de guardado en el label; el preview Markdown ahora abre un tab `markdownViewer` separado con `Refresh Preview` y `Open Source File`, igual que Flutter.
- Editor P1: la comparación a tamaño completo del editor fuente detectó toggles de texto `Source/Preview` exclusivos de GPUI. Flutter mantiene una barra de acciones icon-only y abre el preview Markdown en un tab separado; GPUI eliminó esos toggles, conserva `View Diff`, `Save`, `Discard` y `Open Preview`, se recompiló y se revalidó sin modal fatal. Evidencia enfocada enviada como álbum (`/tmp/alera-parity/editor-source/flutter-focus.jpg`, `/tmp/alera-parity/editor-source/gpui-focus.jpg`). Persisten selección/scroll, conflictos y carga/error sostenidos.
- Git History P1: el panel GPUI ahora tiene handle de resize con límites de 96 a 520 px, drag con cleanup al soltar y altura ajustable, alineándose con el panel Flutter; queda validar visualmente el recorrido y los estados de loading/error.
- Source Control/Git History P1: GPUI ahora conserva un estado explícito de snapshot en carga/error, traduce worktrees rotos a `This workspace is not a Git repository.`, selecciona `Fetch` cuando no hay snapshot y muestra el mensaje de historial vacío al expandir `COMMITS`, sin toast duplicado.
- Welcome Dashboard P1: iconos de Quick Start igualados a Flutter (`New Folder`, `Git Fork`, `Settings`), logo/header con spacing y peso tipográfico corregidos, tarjetas con padding/altura de filas equivalentes y acciones deshabilitadas cuando no existe un proyecto Git.
- Welcome Dashboard P1: captura enfocada en tamaño completo comparada y enviada como álbum (`/tmp/alera_flutter_welcome_focus_final.jpg`, `/tmp/alera_gpui_welcome_focus_final.jpg`); ancho de contenido, tarjetas, iconos, shortcuts y centrado vertical quedaron alineados. Se mantiene `parcial` hasta probar loading y teclado en vivo.
- Settings Agents/Profiles: Status Hooks y Agent Profiles se compararon en vivo con una Flutter y una GPUI a tamaño completo; se verificaron filas de hooks, switches, master/detail, campos, dropdowns con flecha, opciones y cierre externo. Se envió álbum enfocado (`/tmp/alera_agents_flutter_focus.jpg`, `/tmp/alera_agents_gpui_focus.jpg`, `/tmp/alera_profiles_flutter_focus.jpg`, `/tmp/alera_profiles_gpui_focus.jpg`). Save/remove, risk confirmation, loading/error y descubrimiento de modelos quedan pendientes.
- Mobile Devices P2: gateway, connection mode, port, endpoint/device name, expiry, Generate Pairing QR y el modal Link Mobile Device se compararon en vivo; QR, expiración, Cancel Offer y Copy Pairing JSON quedaron funcionales en ambas apps y se envió álbum (`/tmp/alera_mobile_flutter_focus.jpg`, `/tmp/alera_mobile_gpui_focus.jpg`, `/tmp/alera_pair_flutter_focus.jpg`, `/tmp/alera_pair_gpui_focus.jpg`). Faltan dispositivos ya emparejados, rename/revoke/delete y errores de red.
- Diagnostics P2: el tramo de Settings se desplazó en ambas apps y se compararon Open Logs Folder, Export Diagnostics, Log Level, Send Crash Reports, Update Failed y Support; el thumb de scroll se mueve y el contenido conserva las acciones principales. Se envió álbum (`/tmp/alera_diagnostics_flutter_focus.jpg`, `/tmp/alera_diagnostics_gpui_focus.jpg`). Faltan exportación real, error/loading y validación del recorrido completo del scrollbar.
- Diagnostics P2 (auditoría transversal 2026-08-25): con una Flutter y una GPUI a tamaño completo se volvió a abrir el ancla `Diagnostics`; ambos clientes muestran el mismo bloque de Runtime/Diagnostics/Updates, copy, dropdown `Normal`, switches, botones `Open`/`Export`, error `Update Failed` y Support. El thumb GPUI aparece durante el anclaje y desaparece tras 4.5 s de inactividad, igual que el estado capturado de Flutter; no se abrió un nuevo gap visual. Se envió álbum enfocado (`/tmp/alera-parity/diagnostics/flutter-focus.jpg`, `/tmp/alera-parity/diagnostics/gpui-focus.jpg`). La fila permanece `parcial` por exportación real, error/loading inducidos y recorrido completo del scrollbar.
- Orchestration P2: `Execution Plans` en estado vacío se abrió desde el menú nativo en ambas apps a tamaño completo; modal, bounds, título, icono, copy, centrado y cierre quedaron prácticamente alineados. Se envió álbum enfocado (`/tmp/alera_execution_plans_flutter_focus.jpg`, `/tmp/alera_execution_plans_gpui_focus.jpg`). Faltan planes activos/no vacíos, loading/error y acciones de revisión.
- Markdown P1: Explorer ahora abre `readme.md` en el viewer como Flutter, no en un editor duplicado; la captura muestra heading, listas, fences anidados, lenguaje y `Copy code` en ambas apps. Se envió álbum enfocado (`/tmp/alera_markdown_flutter_focus.jpg`, `/tmp/alera_markdown_gpui_focus.jpg`). Faltan Mermaid/imágenes, zoom/pan, enlaces y errores.
- Markdown P1 (último corte): con ambas ventanas a tamaño completo se confirmó una diferencia visual concreta en los fences: GPUI heredaba el bloque elevado gris de `gpui-component`, mientras Flutter usa `CodeField` oscuro (`onInverseSurface`), radio 8 y padding 16. GPUI ahora fuerza fondo `#121212`, radio 8 y padding 16, y se recompiló sin warnings propios; el álbum enfocado posterior quedó en `/tmp/alera-parity/markdown-code-rhythm/flutter-code-focused.jpg` y `/tmp/alera-parity/markdown-code-rhythm/gpui-code-focused.jpg`. Sigue `parcial`: la primitiva pública de `TextView` aún coloca `language`/`Copy code` como overlay a la derecha en vez de la fila superior con divisor de Flutter; Mermaid, imágenes, enlaces, errores, SVG/raster, zoom/pan y archivos grandes siguen pendientes.
- Editor P1: la auditoría del ciclo de apertura detectó que Flutter centra un loader durante la lectura y un estado de error dentro de la superficie, mientras GPUI dejaba un input vacío y dependía de un mensaje global. GPUI ahora muestra loader centrado durante `editor_loading_path + local_busy` y error centrado con icono cuando la lectura falla; queda validar ambos estados con I/O lento y archivo inválido en Computer Use.
- Editor P1 (scroll/edición): Flutter mantiene transparente el scrollbar de CodeForge; GPUI mostraba el thumb de `gpui-component::Input` al hacer scroll. GPUI ahora cubre solo el rail derecho del editor con el fondo de la superficie, sin cambiar el tema global usado por Settings/Explorer/terminal. La edición, caret, undo/backspace y soft-wrap se probaron en ambos clientes sin guardar cambios; el delta de `sky.scroll` no es comparable entre backends (Flutter aterriza cerca de la línea 19 y GPUI cerca de la 38 con `pages=1`), por lo que la fila sigue `parcial` hasta validar el gesto/scrollbar con una métrica común.
- Pull Request P1: el formulario recompilado conserva la geometría de Flutter y ahora usa `Description` como placeholder; se añadió un banner de error persistente dentro del panel GPUI para igualar el `errorMessage` de Flutter en refresh/acciones. La comparación estable se volvió a capturar después del relanzamiento y se envió álbum (`/tmp/alera_pr_flutter_focus_latest.jpg`, `/tmp/alera_pr_gpui_focus_latest.jpg`). GPUI también ejecuta `git push` antes de `gh pr create`, por lo que el rechazo `Could not push the branch: ...` coincide con Flutter; el estado de Pull Request existente se probó con `#543` y muestra el error inline de GraphQL sin cerrar la app. Faltan checks/comentarios con datos reales, autenticación por proveedor y acciones de merge/close/update.
- Pull Request P0 de estabilidad: el reporte `Alera GPUI-2026-08-25-013138.ips` identificó un `debug_assert` de GPUI por registrar dos estilos `.hover()` sobre `context-link-pr` al abrir el formulario de enlace. Se eliminó el hover duplicado, se recompiló y se verificó con Computer Use que el formulario abre, acepta `#543`, muestra el error inline esperado y no vuelve a producir modal fatal ni terminar el proceso.
- Pull Request P1: el menú `Base Branch` ahora obtiene el catálogo completo de ramas del proyecto (incluidas remotas normalizadas), en vez de limitarse al historial local reciente; su popover se pinta fuera del panel y conserva el ancho intrínseco, igual que Flutter. Se verificaron las cuatro ramas visibles, la opción seleccionada, el cierre por click externo y la geometría a tamaño completo; se envió álbum enfocado (`/tmp/alera_pr_branch_flutter_focus.jpg`, `/tmp/alera_pr_branch_gpui_focus.jpg`). Faltan review existente, checks/comentarios con datos reales, autenticación por proveedor y acciones de merge/close/update.
- Pull Request dropdown audit P1 (2026-08-25): se repitió la comparación del formulario con el mismo catálogo de cuatro ramas, flecha, selección `main`, opciones y cierre externo en ambas apps después del rebuild de GPUI. El álbum enfocado `/tmp/alera-parity/pr-dropdown-focus/flutter.jpg` y `/tmp/alera-parity/pr-dropdown-focus/gpui.jpg` fue enviado a Telegram. La superficie permanece `parcial` por los estados de review abierta, autenticación/proveedor y acciones de merge/close/update.
- New Workspace / AI Text P1: la comparación completa del modal `From Prompt` mostró que GPUI ya replica proyecto, source branch, parent workspace, agent profile, dropdowns con búsqueda, `Create Another`, foco automático del prompt y estados de creación. Se corrigió el gap de Flutter que deja `Create And Start Agent` habilitado con prompt vacío para mostrar la validación inline; se envió álbum (`/tmp/alera_new_workspace_flutter_focus.jpg`, `/tmp/alera_new_workspace_gpui_focus.jpg`). Falta recorrer loading de branches/profiles, cancelación, colisión, retry del agente, creación manual y persistencia.
- Settings Search P1: `clipboard` ahora selecciona el pane `Terminal` y lleva automáticamente el primer grupo coincidente (`Interaction`) al viewport mediante `ScrollAnchor` en el siguiente frame; se eliminó una llamada redundante a `request_animation_frame` que provocaba un abort de GPUI durante el paint. Las descripciones de Terminal y el estado vacío de themes se alinearon con el copy de Flutter. La captura final a tamaño completo se envió en álbum (`/tmp/alera_settings_search_flutter_focus_final.jpg`, `/tmp/alera_settings_search_gpui_focus_final.jpg`); quedó verificado que el scrollbar hace fade después de la interacción.
- Settings Terminal P1: la fila `Font Family` ahora replica el autocomplete de Flutter con placeholder `SF Mono`, flecha, icono `Clear`, lista searchable de fuentes y confirmación de valor vacío como fuente del sistema; el renderer de terminal omite `font_family` cuando está vacío. Se probó limpiar, apertura automática del menú, seleccionar `JetBrains Mono`, persistencia y restauración del fixture. Se envió álbum enfocado con Flutter, GPUI y el menú (`/tmp/alera_settings_terminal_flutter_final.jpg`, `/tmp/alera_settings_terminal_gpui_final.jpg`, `/tmp/alera_settings_terminal_gpui_clear_menu.jpg`).
- Settings Support P1: GPUI consulta el mismo endpoint `gh api --silent -i user/starred/leynier/alera` que Flutter y replica `Loading`, `NotStarred`, `Starring…`, `Starred`, `Try Again` y `Hidden`; la captura recompilada mostró `Thanks for the support!` con estrella amarilla y la misma geometría de Flutter. Se envió álbum enfocado (`/tmp/alera_settings_support_flutter.jpeg`, `/tmp/alera_settings_support_gpui.jpeg`).
- Terminal + Explorer P0: tras volver a alinear el Context Sidebar en `Explorer`, Flutter y GPUI muestran el mismo workspace, cwd/prompt, cursor, tab terminal y árbol de archivos a tamaño completo; se envió álbum enfocado (`/tmp/alera_terminal_explorer_flutter_focus.jpg`, `/tmp/alera_terminal_explorer_gpui_focus.jpg`). Selección/copy, enlaces, drop de rutas y errores siguen pendientes de la matriz diaria completa.
- Explorer P0: el watcher conserva rutas expandidas, las restaura por profundidad y cancela reconstrucciones obsoletas. Carpeta como origen, symlink, copy/rename y errores principales ya están cerrados; quedan loading/error sostenidos y recuperación bajo backend lento.
- Auditoría transversal de Context Sidebar/Explorer: el default de `rightSidebarWidth` de GPUI estaba en 400 px frente a los 280 px de Flutter; se corrigió la inicialización y el fallback de preferencias. También se añadió el tooltip `Source Control Root` al icono de rama del Explorer. El resize persistido se volvió a ajustar visualmente a 333 px para repetir la comparación a tamaño completo.
- Markdown/Context Sidebar P1: el inventario confirmó que `markdownViewer` usa intencionalmente la barra de Flutter con iconos `Refresh Preview` y `Open Source File`, no los toggles de texto `Source/Preview` reservados al editor; GPUI conserva ese contrato. Se alineó el estado vivo de `Pull Request` en ambas apps y se comparó el formulario `Link Pull Request`, placeholder, botón `Link`, cierre externo y densidad del panel a tamaño completo. Evidencia enfocada enviada como álbum (`/tmp/alera-parity/context-sidebar-pr/flutter-focus.jpg`, `/tmp/alera-parity/context-sidebar-pr/gpui-focus.jpg`). Quedan Mermaid/imágenes, enlaces, errores, resize y focus/keyboard, que siguen en la matriz de dominios pendientes.
- Auditoría transversal de Settings: Application/Diagnostics/Updates se comparó con el mismo tramo funcional y el estado `Update Failed`; GPUI conserva controles, iconos, switches, dropdown y acciones. El contenedor ahora usa la misma composición `size_full`/contenido flexible que el `Scrollable` de GPUI y un `page down` aterriza en el mismo grupo `Runtime` que Flutter; el thumb de inicio/mitad/final y el arrastre siguen pendientes antes de cerrar la fila.
- Validación de tamaño completo del inventario: después de relanzar GPUI y hacer doble click en la barra superior, Flutter y GPUI quedaron en 1300/1302x768; el tramo Application de Settings se comparó desde el mismo inicio y se envió en un álbum enfocado. No hubo modal fatal y el proceso quedó con una sola Flutter, una GPUI y un runtime. El scroll/track sigue `parcial` hasta medir el thumb en inicio, mitad y final con el mismo gesto.
- Sidebar P0: la búsqueda recuperó la altura compacta de Flutter y View Options, Collapse/Expand All y New Workspace ahora tienen tooltips; los botones de New Workspace quedan deshabilitados en proyectos/carpetas que no soportan workspaces enlazados.

## Levantamiento completo de superficies (2026-08-25)

El levantamiento se cruzó en tres capas antes de continuar la implementación: (1) los 22 módulos bajo `lib/src/features`, (2) las 222 superficies Flutter bajo `presentation`, y (3) los 143 módulos Rust de GPUI junto con `feature-matrix.md` (30 contratos) y `ui-parity-matrix.md` (139 gates). La tabla siguiente es la clasificación operativa: `paridad validada` exige evidencia Computer Use, `parcial` indica implementación o evidencia incompleta, `gap` marca una diferencia bloqueante o transversal, y `fuera de alcance` conserva las exclusiones de `context.md`.

### Control de exhaustividad

- Recuento verificado en el checkout actual: `find lib/src/features -mindepth 1 -maxdepth 1 -type d` = 22 módulos; `rg --files lib/src/features` = 640 archivos; `rg --files lib/src/features | rg '/presentation/.*\\.dart$'` = 222 superficies; `rg --files experiments/alera-gpui/src | rg '\\.rs$'` = 143 archivos Rust.
- La matriz funcional conserva 30 contratos y la matriz visual conserva 139 gates documentales por sus secciones definidas. Sus etiquetas históricas `Real` y `Done` no sustituyen la clasificación operativa de esta sección.
- Cada módulo Flutter aparece una sola vez en la tabla siguiente. Las superficies compartidas de shell, feedback, iconos, accesibilidad, procesos y release se auditan como dominios transversales para no esconder gaps dentro de una feature grande.
- Corte de estado: 4 dominios con `paridad validada`, 26 `parcial`, 0 `gap`, 0 `sin validar` y 7 `fuera de alcance`. Los conteos se actualizan cuando se añade evidencia o se cierra un gap, no cuando solo cambia el código.

### Cobertura de los módulos Flutter

| Módulo Flutter | Dominio de la matriz | Estado | Nota de alcance o evidencia |
| --- | --- | --- | --- |
| `account` | Account/OAuth | fuera de alcance | Excluido explícitamente en `context.md`. |
| `agent_profiles` | Perfiles y edición de agentes | paridad validada | Master/detail, orden, carga/error, Save/remove, risk confirmation, descubrimiento, checkbox visual, creación de dos perfiles, relanzamiento, orden y eliminación están cerrados. Los estados inducidos de Model y Persona cubren loaders animados, controles disabled, error real, copy, wrapping y semántica accesible. |
| `agent_quota` | Quotas y proveedores | paridad validada | Chips, Claude x3 y los otros seis proveedores, pins persistidos, hover card, overview, reset, loading/error/offline/stale e iconos SVG están emparejados y restaurados. |
| `agent_status` | Agent Activity y estados | paridad validada | Working/waiting/blocked/done, multiagente, orden, navegación, cierre, expiración/fallback y overflow `+3` con cuatro agentes reales están validados. |
| `ai_text_generation` | AI Text y prompt workspace | paridad validada | Identidad/workspace/agente Codex real, validación, cancel, error, timeout 120 s, fallo post-creación, error persistente y Retry Agent exitoso están emparejados con runtime aislado y cleanup verificado. |
| `app_menu` | Shell, titlebar, menú y ventana | parcial | Menú nativo macOS coincide en catálogo y About/Settings/Execution Plans/Check for Updates están validados; faltan Edit/Window, hide/show/quit y otras plataformas. |
| `app_window` | Shell, titlebar, menú y ventana | parcial | Zoom/tamaño completo y coexistencia validados; falta focus, accesibilidad y matriz multiplataforma. |
| `browser` | Browser | fuera de alcance | Excluido explícitamente en el POC. |
| `diagnostics` | Diagnostics | paridad validada | Estado, copy, controles, Log Level, Open y Export comparados; exportación real guarda ZIP con logs/metadata y toast equivalente. Cancel y el fallo de escritura en `/System` quedaron alineados, sin crear archivos ni habilitar crash reporting; scrollbar y teclado también están cerrados. |
| `keyboard` | Accesibilidad y teclado | paridad validada | Registro, matching, terminal-first, roles/labels/states, focus scopes, diálogos, menús, toggles/disabled y matriz Tab/Enter/Space/Escape/shortcuts están cubiertos. |
| `mobile_devices` | Mobile devices y pairing | paridad validada | Gateway, QR/cancel/copy, activo/revocado, Rename/Revoke/Delete, errores endpoint/red y cleanup están emparejados en perfil aislado. |
| `mobile_emulator` | Emulator embedding | fuera de alcance | Embedding de emulador excluido; no confundirlo con el panel de pairing incluido. |
| `orchestration` | Orchestration y run policy | paridad validada | Empty, Awaiting Approval, Approved y Rejected, loading/error de razón, acciones e iconos están alineados. Approve/Reject reales se ejecutaron sobre runs controlados detenidos/cancelados, con cero dispatch; diálogo, grupos, inputs y botones están expuestos en AccessKit. |
| `projects` | Sidebar de proyectos y workspaces | paridad validada | Selección, árboles, grupos, orden, acciones, agentes, cierre, geometría y errores inducidos de Rename/Tags/Parent/pin/Sleep/Remove están emparejados con cleanup total. |
| `pull_requests` | Pull Request, Checks y comentarios | paridad validada | Composer, review cerrado y una PR pública abierta real tienen evidencia Flutter/GPUI; acciones externas se validaron hasta confirmación y Cancel, sin mutar GitHub. |
| `remote_hosts` | SSH/Remote Hosts | fuera de alcance | SSH excluido explícitamente en el POC. |
| `resource_manager` | Resource Manager | paridad validada | Snapshot, jerarquía, sort, empty/unavailable/warming/remoto, orphans, scrollbar y cierre real de sesión están cubiertos con cleanup verificado. |
| `runtime_host` | Runtime Host | paridad validada | Running/stopped/start/stop/reconnect, busy/force, persistent, update real old-to-current, refresh, hover/pin y versiones están cubiertos. |
| `settings` | Settings y búsqueda | paridad validada | Los diez panes en alcance, defaults, Reset, validaciones/errores, persistencia scoped, búsqueda/anclas, scroll y acciones externas seguras están cubiertos. |
| `shell` | Shell, titlebar, menú y ventana | parcial | Shell oscuro, welcome, rail y status base comparados; faltan acciones y accesibilidad completas. |
| `updater` | Updater | fuera de alcance | Excluido explícitamente en el POC. |
| `workbench` | Workbench, terminal, editor, explorer, Search, Git y previews | paridad validada | Los dominios diarios, estados/error, splits, PTY, editor, files, Search/Git/PR y previews están cerrados; zoom Mermaid coincide en código y el automatizador no puede emitir su gesto modificador. |

Los contratos históricos de `feature-matrix.md` permanecen marcados `Real` porque describen integración disponible, no aceptación visual. La clasificación anterior prevalece para decidir el siguiente trabajo y evitar que una superficie operativa vuelva a ocultar gaps de otra.

Estados usados: `paridad validada` significa que hay implementación, estados principales y evidencia Computer Use; `parcial` significa que hay implementación o una parte validada, pero faltan estados, interacciones o evidencia; `sin validar` significa que existe código o una referencia clara, pero aún no hay comparación completa; `fuera de alcance` conserva las exclusiones explícitas del POC.

| Dominio | Referencia Flutter | Estado GPUI | Evidencia y gap actual |
| --- | --- | --- | --- |
| Shell, titlebar, menú y ventana | `alera_shell_page.dart`, `app_menu`, `app_window` | paridad validada | macOS quedó cerrado con doble click/tamaño completo, tema, About/Settings/Execution Plans/Check for Updates, Window Minimize/restore, Hide/show, Quit/relaunch, foco, persistencia y menús nativos estándar. Release compiló con el código final en macOS, Linux x86_64 y Windows; la comparación visual obligatoria se realizó en la pantalla nativa de la MacBook. |
- App menu macOS (2026-08-27, cerrado para acciones reversibles): Flutter y GPUI exponen exactamente About Alera Dev, Settings, Execution Plans, Check for Updates, Services, Hide, Hide Others, Show All y Quit en el mismo orden/disabled state. About coincide en versión `0.40.0 (76)`, icono, botones y geometría a 1-2 px. Settings no abría porque `window_handle.update` ocurría reentrante durante el dispatch; ahora usa `cx.defer` y abre el diálogo completo. Execution Plans abre/cierra sin decidir nada, y Check for Updates produce en ambos el mismo error real `FormatException: The release descriptor does not match its update index entry.`. Edit dejó de usar `OsAction` y ahora enlaza las acciones gpui-component: con Search enfocado aparecen habilitadas y Select All reemplazó `sidebar` por `x`, luego se restauró vacío. Window evitó la inyección de tiling de macOS/duplicado Full Screen y coincide en Minimize, Zoom, Enter Full Screen y Bring All to Front. Evidencia: `/tmp/alera-parity/app-menu-2026-08-27/`.
| Welcome Dashboard | `welcome_dashboard*.dart` | paridad validada | Render, shortcuts dinámicos, acción New Workspace disabled, iconos, tarjetas, ancho/centrado y spacing fueron comparados. Un runtime aislado vacío mostró el dashboard real; desde Search workspaces, Tab omitió la tarjeta disabled, Return abrió `Open Settings` y `Add Project`, y ambos diálogos se cerraron por Cancel sin mutar estado. El perfil aislado se movió a Trash y GPUI volvió al runtime compartido con 9 workspaces. Evidencia: `/tmp/alera-parity/welcome-keyboard-2026-08-28/gpui-empty-runtime.jpg`. |
- Welcome selection lifecycle (2026-08-27, cerrado): al retirar el workspace temporal Flutter dejó `activeWorkspaceId=null` y mostró Welcome; GPUI elegía `main` por un fallback aplicado en cada refresh. Flutter `_resolveActiveWorkspaceId` confirma que selecciones stale deben quedar vacías. GPUI conserva ahora el fallback solo para el primer snapshot y limpia workspace/tab/pending al cerrar el último tab. Computer Use seleccionó `Sleep Wait Report`, cerró su única Terminal 1 y obtuvo Welcome, sin highlight/dot verde ni context sidebar; CLI confirmó cero tabs. Pulsar de nuevo el workspace creó una sola Terminal 1 y `terminal read` confirmó el cwd exacto `.../sleep-wait-report`. Evidencia: `/tmp/alera-parity/welcome-lifecycle-2026-08-27/gpui-after-last-tab.jpeg`.
- Welcome global tab dots (2026-08-27, cerrado): la comparación Welcome alineada mostró Flutter `main` verde por sus tabs persistentes y GPUI gris. `WorkbenchSnapshot::load` vaciaba `all_tabs` cuando no había workspace seleccionado; ahora carga los tabs globales también en Welcome. Computer Use cerró de nuevo el último tab de Sleep Wait Report: Welcome permaneció sin selección, `main` quedó verde por sus tabs globales y Sleep quedó gris. Pulsar Sleep recreó Terminal 1 y restauró el fixture. Evidencia: `/tmp/alera-parity/welcome-lifecycle-2026-08-27/flutter-aligned.jpeg`, `gpui-aligned-before-dot-fix.jpeg` y `gpui-aligned-dot-fixed.jpeg`.
| Sidebar de proyectos y workspaces | `project_workbench_sidebar*.dart`, `project_workbench_workspace_rows.dart` | paridad validada | Agrupaciones, orden, indentación, tags, agentes, presencia, tooltips, hover, Sleep/reopen, Rename, Tags, Parent, pin/unpin, Repeat Pinned y Remove están cerrados. Un fixture aislado añadió errores de Rename/Pin/Tags/Parent/Sleep/Remove, doble toast, rollback y dropdown Parent anclado, con evidencia Flutter/GPUI y cleanup total en `/tmp/alera-parity/sidebar-errors-2026-08-28/`. |
| Sidebar colapsado y resize | `project_workbench_collapsed_sidebar.dart`, `sidebar_resize_handle.dart` | paridad validada | Rail colapsado, tooltips, selección, reapertura, límites mínimo/máximo, restauración tras relanzamiento y drag del ancho fueron comparados. GPUI colapsa por Cmd+B, publica Expand/Projects/Add/Settings y restaura por botón; Flutter/GPUI quedan a 0-2 px. Expand recibe foco después del frame colapsado, Enter reabre, Tab + Enter activa el siguiente proyecto y fue limpiado/restaurado. Ambos handles se publican como Splitter con label, valor y rango 220-460; flechas cambian 8 px, persisten y se restauraron al valor inicial. |
- Sidebar rail shortcut (2026-08-27): Cmd+B colapsó GPUI y dejó Expand Sidebar, tres proyectos, Add Project y Settings como botones; el recorte coincide con Flutter a 0-2 px y ambos se restauraron. Computer Use no entregó el mismo shortcut a Flutter en esta sesión, por lo que no se usa ese intento como diferencia de producto. Evidencia: `/tmp/alera-parity/sidebar-collapsed-2026-08-27/`.
| View Options y filtros | `workbench_view_options_menu.dart`, `workbench_view_options_controls.dart` | paridad validada | `None`, `Project`, `Agent Activity`, orden Recent/Agent Activity, proyectos, tags, Repeat Pinned y click externo fueron comparados y restaurados. Radios/checkboxes/options publican selección; Search -> Tab -> Enter abre, los submenús responden Up/Down/Home/End/Enter/Space y Escape respeta capas. Clear y selecciones vacías quedan visibles sin crear un estado fantasma. |
| Workbench, tabs y splits | `workspace_workbench_view.dart`, `workspace_workbench_layout_view.dart`, `workspace_workbench_tab_strip_drop.dart` | paridad validada | La matriz aislada Flutter/GPUI cerró reorder, movimiento entre tab strips/panes, Right/Left/Up/Down, preview, poda, cancelación fuera, cleanup en pointer-up/desactivación/Escape y ratio compartido con resize/persistencia. El drop sin DragMove usa MouseUp real y close-during-drag limpia preview antes del diálogo dirty. Los tabs conservan ScrollHandle por pane y revelan el activo. Next/Previous/⌘1-9 se interceptan antes de Editor/Diff usando el matcher efectivo y respetan Terminal First; Computer Use verificó último -> tab 2 -> dos Next -> Previous con strip visible. |
| Context Sidebar y navegación | `workspace_context_sidebar.dart` | paridad validada | Estructura, tabs, selección, rail colapsado/reapertura, resize, menú Explorer, los cuatro iconos/tooltips/hitboxes y toolbars Explorer/Search/Git/PR coinciden con Flutter; tabs Git/PR se ocultan sin scope en ambos clientes. Los estados del contenido quedaron cubiertos por sus dominios: Explorer `Item not found` + recuperación, Search loading/no-results/regex, Source Control loading + repo inválido/conflicto y Pull Request error inline + refresh/composer. No queda un contrato propio adicional del contenedor sin validar. |
| Explorer | `workspace_explorer*.dart` | paridad validada | Árbol lazy, iconos, Git agregado, create/rename/delete/move, menús, refresh, drag/drop real y filas AccessKit fueron probados. El menú contextual publica sus 13 opciones, toma/restaura foco y cierra con Escape/click externo. Errores unsupported/existing/not-found y recuperación por watcher están cubiertos. Un folder de 3,000 archivos reveló el costo de materializar todos los divs; Explorer ahora usa `uniform_list` con el mismo scrollbar/ScrollHandle, abre en 1.58 s con 27 filas visibles, recorre 0001-3000 y conserva apertura/selección. El fixture se retiró completamente. |
| Search y Replace | `workspace_search_panel*.dart` | paridad validada | Resultado asentado, `No results`, loading inmediato, cancelación nativa, Replace, filtros include/exclude/ignored, regex, geometría, abrir/copy de match, bloqueo dirty y conflicto concurrente están cubiertos. Replace All usa un click y los tokens de la preview como Flutter; una modificación externa muestra el mismo toast y no escribe el archivo. GPUI publica `progress indicator Searching`, usa el loader central de 36 px y virtualiza hasta 2,000 filas con `ListState`; el fixture ignored + `a` respondió en 841 ms, scrolló y permaneció controlable. Evidencia: `/tmp/alera-parity/search-virtualized-2026-08-27/`. |
| Terminal PTY y superficie | `terminal_runtime*.dart`, `terminal_surface.dart` | paridad validada | Cwd, cursor, splits, múltiples sesiones, IME `'a` -> `á`, selección/link/scroll thumb, links HTTP, recuperación, restore incremental, `CSI 3J`, reconstrucción final, Cmd-click, shortcuts y scrollback 1500 líneas están cubiertos. La altura integral de fila alinea 1-22 y prompt/cursor con Flutter. El ciclo real de host detenido cubrió `Terminal Unavailable`, `Reconnect` y recuperación sin relanzar GPUI; `exit` elimina el tab/split vacío, pulsar un workspace sin terminales crea uno en el cwd correcto, y selección/copy simple y multilinea ya usan el mismo row map del frame. `Starting Terminal` y `Restarting Terminal` quedaron capturados como progress indicators de 36 px; Restarting muestra elapsed y ambas operaciones recuperan el prompt sin perder tab ni layout. La selección sintética de Flutter volvió a funcionar y confirmó highlight/copy de una y varias filas; el fixture se limpió y el clipboard previo se restauró. |
| Editor | `workspace_editor*.dart` | paridad validada | Source/Preview, barra, dirty, save/discard/diff, tooltips, preview Markdown, wrapping, syntax, gutter, conflicto externo, scroll por pane, restauración multi-pane, foco/edición/undo independiente, error real `File not found` y loading lento fueron comparados. GPUI usa el timeout de lectura de 10 segundos de Flutter, centra el loader de 36 px y recupera el contenido sin toast duplicado. Evidencia final: `/tmp/alera-parity/editor-multipane-2026-08-27/` y `/tmp/alera-parity/editor-loading-2026-08-27/`. |

- Editor loading lento (2026-08-27, cerrado): se aplicó temporalmente una demora de 9 segundos fuera de los hilos UI, se capturaron los loaders a tamaño completo y se verificó la recuperación de ambos clientes. GPUI pasó de 20 a 36 px para igualar la caja del `CircularProgressIndicator` Flutter; el centro, header y viewport coinciden. Toda instrumentación de demora y sus variables fueron retiradas antes del rebuild normal. Evidencia: `/tmp/alera-parity/editor-loading-2026-08-27/gpui-loading-final.jpeg`, `flutter-loading-final.jpeg`, `gpui-recovered.jpeg` y `flutter-recovered.jpeg`.
- Editor timeout de lectura (2026-08-27, cerrado): la demora determinista reveló que GPUI usaba el timeout genérico de 3 segundos mientras Flutter espera 10 segundos en el mismo cliente del host. `WorkspaceService::read` usa ahora 10 segundos para `workspaceFiles.readEditor`, evitando `File operation failed` prematuro en discos remotos/saturados. La prueba final recuperó el contenido dentro del plazo y sin modal fatal ni toast de error.
- Editor toolbar AccessKit (2026-08-27, cerrado): los toolbars activos e inactivos conservan IDs únicos y ahora publican `Role::Button`, nombre exacto y tab stop solo cuando la acción está habilitada. Computer Use ve `View Diff`, `Save File`/`Saving File` y `Discard Changes` junto al `text entry area`; Markdown/Merman publican además `Refresh Preview`, `Open Source File` y `Open Editor`. El layout se restauró a `Terminal 1` y ambos clientes se reconectaron al único host normal sin la instrumentación de loading.
- Diálogos y toggles AccessKit (2026-08-27, cerrado para el patrón compartido): Computer Use comparó `New Workspace`. GPUI publica ahora `dialog New Workspace`, Close, los radios `From Prompt`/`Manual`, textarea, cuatro comboboxes, `Create Another` con Value on/off y `Create And Start Agent`, igualando la información funcional de Flutter. El paso Manual expone diálogo Selection, radios estables por proyecto/branch y botones Cancel/Continue. Checkbox y radios se activaron por `element_index`, se restauraron y Escape/Close descartaron sin mutar datos. Settings publica dialog, tabs, switches con Value, botones Open/Export y Close; Confirm Project Removal se alternó y restauró. Los wrappers compartidos cubren además Settings, flags de perfiles, copy overwrite y Mobile Access. Evidencia local: `/tmp/alera-parity/accesskit-dialogs-2026-08-27/`. El menú contextual de tabs ya publica `menu Tab actions` y cierra con Escape; el foco inicial y navegación Up/Down/Enter siguen pendientes.
- Menús Workbench por teclado (2026-08-27, cerrado): siguiendo el patrón de `reference_projects/waku/src/ui/menu.rs`, New Tab, Pane Actions y Tab Actions conservan FocusHandle propio/anterior, índice resaltado y lista de items habilitados. Computer Use abrió `menu Tab actions`, verificó que Down movió el highlight de Split Up a Split Down, End seleccionó Change Title y Enter abrió `dialog Change Terminal Title`; Escape canceló sin cambiar el título y el foco/tab activo se restauró. Up/Down/Home/End saltan disabled, Enter/Space comparten las operaciones de click y click externo/Escape devuelven foco. Evidencia: `/tmp/alera-parity/accesskit-menus-2026-08-27/`.
- Diálogos secundarios AccessKit (2026-08-27, cerrado para el patrón): Explorer New/Rename/Delete, Resource Close Terminal y Add/Edit CCS Profile publican ahora Role::Dialog y botones nombrados/tabbable, conservando sus tamaños. Computer Use abrió `dialog New file`, vio File name/Cancel/Create y Cancel cerró sin crear nada. Source Control, Sidebar, Settings, tab conflict/dirty/rename y About ya usan `design_system::dialog_shell`, por lo que heredan el mismo contrato de diálogo.
- Search/Replace loading y escala (2026-08-27, cerrado): una consulta `a` con ignored activado sostuvo el loader, pero el renderer anterior materializó hasta 2,000 filas, alcanzó 100% CPU y Computer Use agotó el timeout; se reinició solo GPUI y se corrigió con `gpui::list` + `ListState`, preservando el límite completo. La misma consulta volvió con 2,000 matches/32 files en 841 ms, Computer Use y el scroll siguieron respondiendo y el muestreo bajó de 100% a 26.1% durante resultados, 15.4% tras limpiar. El loader central usa 36 px, publica `progress indicator Searching` y no agrega el spinner pequeño que Flutter no muestra. Los toggles Match case/whole word/regex/preserve case publican nombre/estado. Consulta e ignored quedaron restaurados. Evidencia: `/tmp/alera-parity/search-virtualized-2026-08-27/`.
- Terminal Starting/Restarting sostenidos (2026-08-27, cerrado): se abrió la confirmación real `Restart Terminal`, se detuvo temporalmente el único host y se confirmó la acción sin relanzar GPUI. La superficie publicó `progress indicator Restarting Terminal`, spinner de 36 px y `Elapsed: 8s`; al restaurar el host y ejecutar `Reconnect`, el prompt regresó sin perder el tab ni el layout. Para Starting se creó una sesión nueva con demora determinista fuera del hilo UI: Computer Use publicó `progress indicator Starting Terminal` y la sesión recuperó el prompt al terminar el attach. Toda la instrumentación temporal y su variable de entorno se retiraron del código y del launcher antes del rebuild normal. Evidencia: `/tmp/alera-parity/terminal-operations-2026-08-27/gpui-restarting.jpeg` y `gpui-starting.jpeg`.
- Terminal operación/cleanup final (2026-08-27): `make gpui-debug` con Dart 3.12.2 recompiló el binario normal sin la demora y abrió únicamente el PID GPUI 23405 junto a una Flutter y el host persistente. El tab `Terminal 2` usado como fixture fue retirado mediante `alera tab remove`, dejando solo `Terminal 1`; ambas ventanas quedaron en 1302x768 por doble click de titlebar y Flutter se reconectó al mismo host. El backend sintético de Computer Use volvió a no producir selección visible en xterm ni con triple click ni drag, aunque el contenido y clipboard GPUI ya están validados y Flutter conserva la evidencia manual anterior; esto se registra como limitación del automatizador, no como una diferencia observada entre clientes.
- Limpieza de compilaciones antiguas (2026-08-27): con Flutter, GPUI y el host detenidos se eliminaron únicamente targets regenerables: incrementales debug y perfiles release históricos de GPUI/Rust, el spike AccessKit, su Cargo home, el cache nativo de hooks y `Build/Intermediates.noindex` de Xcode. Se conservaron `deps` debug, productos, bundles y fuentes. El volumen externo pasó inicialmente de 489 GiB a 543 GiB libres, una recuperación de 54 GiB; después de reconstruir el host release, Flutter limpia y los nuevos incrementales GPUI quedó en 537 GiB libres, una ganancia neta de 48 GiB. El corte post-build es GPUI target 58 GiB, Rust target 31 GiB, `.dart_tool` 2 GiB y `build` 7.1 GiB. El DerivedData interno de Xcode estaba vacío.
- Source Control conflict/AccessKit (2026-08-27, cerrado): un repositorio temporal aislado produjo simultáneamente un archivo conflictivo, uno staged, uno unstaged y uno untracked. Flutter y GPUI coinciden en `main · conflicts`, acción primaria Fetch, grupos, badges, line stats, enabled/disabled y menú completo. GPUI publica ahora toolbar, acción primaria, trigger, grupos, filas, Stage/Unstage/Discard, COMMITS/Refresh y `menu Source Control Actions`; Up/Down/Home/End saltan acciones disabled, Enter/Space ejecutan la habilitada, Escape/click externo cierran y el foco anterior se restaura. Click sobre Commit disabled fue ignorado y collapse/expand de Staged se restauró. La Flutter limpia confirmó que libgit2 agrupa varias líneas de header en un `GitDiffLine`; GPUI aplica la misma visualización `maxLines: 1`, por lo que `old mode 0`/`new mode 100644` ya no se filtran al viewport. Evidencia: `/tmp/alera-parity/source-control-conflict-2026-08-27/` y `/tmp/alera-parity/source-control-conflict-2026-08-27-clean/`.
- Source Control mutaciones reales (2026-08-27, cerrado para paths): sobre el fixture conflictivo GPUI ejecutó Stage de `working.txt`, lo movió a Staged y mostró `Staged`; Unstage lo devolvió a Unstaged con `Unstaged`; Discard de untracked y unstaged abrió el diálogo destructivo accesible, retiró/restauró el archivo esperado y mostró `Change discarded`. Los mensajes de éxito ya no dependen del copy técnico del host: Stage/Unstage/Discard, Fetch/Pull/Push/Sync/Stash/Commit/Amend y publish usan las cadenas exactas de Flutter, incluidos `Branch published`, `Committed and pushed` y `Committed and synced`. Pull durante conflicto devolvió `Resolve conflicts before continuing.` igual que Flutter. Al terminar se seleccionó `main` en ambos clientes, se desregistró el workspace/proyecto y se eliminó el repositorio temporal; exactamente una Flutter, una GPUI y un host permanecen activos.
- Git History AccessKit/teclado (2026-08-27, cerrado para estado vivo): el panel de 50+ commits publica `list Commits`, cada commit como botón expandible con subject y estado expanded, su trigger `Commit Actions For ...`, archivos como opciones y `Open All Changes`. El menú publica Copy Commit Hash/Message, selección y foco propio; Down cambió el highlight a Message, Up/Home/End y Enter/Space comparten la acción, Escape/click externo cierran y restauran foco. Expandir el primer commit cargó 12 archivos y abrir `alera.webp` creó el diff histórico correcto; el tab temporal se retiró por el Runtime al terminar. Quedan los estados error/no-repo/vacío, filtros y una validación de clipboard que preserve el contenido previo del usuario.
- Pull Request composer AccessKit (2026-08-27, cerrado): la comparación Flutter/GPUI limpia confirmó la misma geometría de Base Branch, Title, Description, Create split-button y Link Existing. GPUI publica Refresh, combobox Base Branch con estado expanded, AI generation, Title/Description, Create/Draft, Pull Request Type y Link Existing; el dropdown de branches publica cuatro opciones y selección, cierra por click externo, y el menú Create/Draft publica selección equivalente. Computer Use alternó Create -> Draft -> Create y restauró la preferencia. Link Existing expone Refresh, input `#123 Or Pull Request URL`, Link y retorno Create; se abrió y restauró sin mutar Forge. Los botones compartidos usan Role Button, tab stop y `on_click`, y se eliminó el ID duplicado del header/link.
- Pull Request review contract (2026-08-27, estático/recompilado): los controles de PR existente usan ahora roles/nombres/teclado: Refresh/Edit/Open Browser, editor Base Branch y opciones, checks/group rows con expanded, links, Start/Add Comment, Cancel/Post, links de comentarios y split action Merge/Squash/Rebase/Close/Unlink con menú y selección. El composer real volvió a abrir y expuso Base Branch, Create, Pull Request Type y Link Existing después del rebuild. Falta una PR real para validar estos estados y acciones sin realizar una mutación externa no autorizada.
- Settings Agents controls/estado (2026-08-27, cerrado): Flutter y GPUI asentados muestran el mismo `Registration Needs Update`, path/detail, cinco filas de skills, runners Auto/npx/bunx, Copy/Install, Status Hooks y Behavior. GPUI publica Refresh/Register, runners con labels específicos, Copy/Install All/Update y todos los switches; el select All Alera Skills expuso la lista y selección Auto y Escape cerró sin cambiarla. Computer Use alternó Codex Hooks on -> off -> on y verificó la restauración. No se ejecutaron instalaciones ni Copy. Los helpers de buttons/selects usan roles, tab stops y `on_click`, por lo que las cinco filas heredan el mismo contrato.
- Mobile Devices AccessKit/icono/spacing (2026-08-27, cerrado para estado actual): Flutter y GPUI muestran el mismo gateway habilitado, This Device, port 6768, pairing inputs, `No Active Offers` y dos dispositivos revocados. GPUI publica Connection Mode como tres radios con selección, Apply/Generate, steppers, Delete Device y diálogos; Manual -> This Device se ejecutó y restauró. Delete de `Runtime Overlay Audit` abrió `dialog Delete Runtime Overlay Audit`, Cancel cerró y ambos registros permanecieron. GPUI usa ahora el mismo glifo `LucideIcons.qrCode` codepoint 57823 de Flutter, no una aproximación dibujada. El empty state replica padding 24, icono 28 y altura del `AleraEmptyState`; al ancla Offers la tarjeta difiere unos 3 px y las filas de dispositivos quedan a 3-5 px. Evidencia: `/tmp/alera-parity/mobile-devices-accesskit-2026-08-27/`.
- Orchestration AccessKit/error local (2026-08-27, cerrado para fixture): se verificó por CLI que `run_cd586ff5b9654f78` es el fixture detenido y su task está cancelada. GPUI publica `dialog Execution Plans`, group por run, Rejection Reason, Approve/Reject/Close y loader como progress indicator. Reject vacío reprodujo un bug: el error reemplazaba el plan por `Plans Unavailable` y se duplicaba abajo. La precedencia ahora conserva el plan y muestra una sola línea `A rejection needs a reason.`, como Flutter. Click externo cierra el diálogo sin mutar el fixture. La validación end to end posterior rechazó la policy con una razón real y dejó el run histórico en `stopped / rejected / cancelled`. Evidencia: `/tmp/alera-parity/orchestration-accesskit-2026-08-27/gpui-rejection-error.jpeg`.
- Image/Markdown Preview viewport/error/transform (2026-08-27, cerrado): `dev-banner.webp` mide y se centra igual en Flutter/GPUI, con fit contain y padding equivalente. GPUI ya no comparte zoom/pan globalmente: mantiene `PreviewTransform` por tab, usa IDs por tab y elimina la transformación al cerrar, igual que un `InteractiveViewer` por widget Flutter. Un `.png` temporal con bytes inválidos mostró que GPUI quedaba vacío; ahora valida metadata de PNG/JPEG/WebP/GIF/BMP/TIFF y raíz SVG antes de crear el asset, mapea decode failures a `Image cannot be opened` y publica el error como alert. Flutter y GPUI muestran el mismo copy/centro; el tab y archivo temporal se eliminaron, preservando todos los cambios previos de `leynier-website`. `Copy code` usa hash estable del contenido, Role Button, tab stop y click/teclado; el README vivo expone dos acciones Copy code junto a Refresh/Open Source. Evidencia: `/tmp/alera-parity/image-preview-error-2026-08-27/`.
- Mermaid transform fixture (2026-08-27): un flowchart temporal Alpha -> Beta -> Gamma confirmó que GPUI crea Source/Preview, publica Open Editor/Refresh Preview y renderiza el SVG oscuro centrado. La transformación ya queda aislada por tab igual que Image Preview. Flutter recibió ambos tabs compartidos, pero Computer Use no consiguió activar confiablemente el preview y seguía mostrando Source; no se usa esa sesión como evidencia visual comparativa ni se declara sizing cerrado. Los dos tabs y el archivo temporal se eliminaron. Evidencia GPUI: `/tmp/alera-parity/mermaid-transform-2026-08-27/gpui.jpeg`.
- Explorer context menu focus (2026-08-27, cerrado): el overlay conserva un `FocusHandle` propio/anterior, toma el foco al abrirse y lo devuelve al cerrarse, usando el mismo contrato ya validado en Workbench, Source Control e History. Computer Use abrió las 13 opciones sobre `readme.md`, Escape retiró el menú sin mutar el árbol y una segunda apertura ejecutó Refresh por `element_index`; el menú se cerró y el Explorer se recargó sin error. Evidencia: `/tmp/alera-parity/explorer-menu-keyboard-2026-08-27/gpui-open.jpeg` y `gpui-closed.jpeg`.
| Markdown, Mermaid e imágenes | `workspace_markdown_viewer_surface.dart`, `workspace_merman_viewer_surface.dart`, `workspace_image_preview_surface.dart` | paridad validada | Markdown normal/grande, source/preview/refresh, code/images/links/errors y Mermaid Retina/tema/error/pan/reset están comparados. Zoom coincide con InteractiveViewer en 0.25-8x, boundary 48, padding 24 y fit contain; Computer Use no puede emitir el modificador, lo que queda como limitación de automatización. |
| Source Control y Git diff | `workspace_git_diff_panel*.dart`, `workspace_git_diff_surface*.dart` | paridad validada | Fetch, split menu, radios, cierre externo, botones, loading, repo inválido, conflicto y geometría fueron comparados; Stage/Unstage/Discard, diff y disabled states están cubiertos. Un bare remote desechable cerró Commit, Push, Fetch, Pull, Stash, Stash Pop, Amend, error de Sync divergente, Sync con rebase exitoso y Commit & Push; hashes local/origin quedaron idénticos y sin cambios. El campo Message ahora se vacía solo tras commit exitoso, igual que Flutter, y se preserva en error. Credenciales reales dependen del helper del sistema y no se mutaron; la ruta de red usa el mismo backend Git/CLI ya validado. |
| Git history | `workspace_git_history_panel*.dart` | paridad validada | El estado vivo de 50+ commits y el repositorio vacío coinciden en carga lazy, contador, grafo, refs, incoming boundary, expansión visual y alto; GPUI soporta resize de 96-520 px, archivos, filas/menús AccessKit, teclado y apertura de diff. Copy Hash/Message se verificaron contra `git rev-parse HEAD` y `git log -1 --format=%B`, restaurando el clipboard previo. Un proyecto Folder no-Git confirmó el contrato común: ambos clientes ocultan Source Control/PR y no presentan un Git History inválido. El fix final evita que una opción del menú propague el click y expanda el commit. |
- Git History empty (2026-08-27, cerrado): se creó un proyecto Git temporal sin commits en `/private/tmp`; `project add` ya creó su main workspace y el registro linked duplicado se eliminó antes de probar. Flutter/GPUI muestran main, No changes, Publish Branch, `COMMITS 0` y No Commits Yet con geometría equivalente. GPUI expone No Commits Yet como Label y Loading Commits como ProgressIndicator. La terminal se cerró, el proyecto se eliminó, el directorio quedó ausente y ambos clientes regresaron a Welcome sin el fixture. Evidencia: `/tmp/alera-parity/git-history-empty-2026-08-27/flutter.jpeg`, `gpui-before.jpeg` y la recaptura final `gpui-final.jpeg`.
| Pull Request, Checks y comentarios | `workspace_pull_requests_panel.dart`, `pull_request_review*.dart` | paridad validada | Composer, base/title/description, Create/Draft, split menu, refresh, push previo, error inline y Link Existing estaban cerrados. El submódulo Waku añadió un fixture metadata-only con la PR pública abierta `egoist/waku#172`: Flutter/GPUI alinearon Open, título/autor/branches, dos checks exitosos, detalles Started/Completed, comments vacío y merge split menu. Edit y comment se abrieron/cancelaron; el menú expuso Merge/Squash/Rebase/Draft/Close/Unlink y Escape/click externo; Merge abrió `dialog Create Merge Commit PR #172?` con Cancel accesible. No se ejecutó ninguna escritura GitHub. El proyecto/workspace temporal se retiró y Waku quedó clean. Evidencia: `/tmp/alera-parity/pr-open-2026-08-28/`. |
| AI Text y prompt workspace | `prompt_workspace_dialog*.dart`, `context_*_ai.rs` | paridad validada | Modal, foco, selects, validación, toggle, fases y acciones coinciden. Un runtime aislado emparejó cancel, error de proveedor, timeout 120 s, fallo posterior a creación y Retry Agent. GPUI replica `Bad state:` y `FormatException:`, preserva el error durante refresh y crea el mismo tab administrado. Todo fixture fue retirado y el perfil normal quedó con una Flutter, una GPUI y un host. Evidencia: `/tmp/alera-parity/ai-text-workspace-2026-08-28/`. |
- New Workspace validación/acciones exactas (2026-08-27, cerrado): Flutter y GPUI muestran `Complete The Prompt, Project, Branch, And Agent Profile.` con el mismo proyecto/branch/parent/profile, geometría y botón; GPUI expone además el error en AccessKit. Se eliminó el fallback silencioso de branch y Cancel/Open Workspace/Retry Agent/Create And Start Agent, Create Another y Continue Manually comparten `on_click`; Create Another se alternó off -> on -> off en ambos. Al probar loading, la operación Codex terminó antes de poder cancelar: generó `Prompt Workspace Validation`, branch `test/prompt-workspace-loading-cancellation`, tab `Codex Sidebar Test` y entregó el prompt real, confirmando el flujo completo. Se inventariaron IDs antes de limpiar; tab y workspace se retiraron con `alera-cli`, y se verificaron cero registros, path/worktree/branch ausentes, total de workspaces restaurado a 10 y cambios previos de `leynier-website` intactos. Evidencia del estado vacío: `/tmp/alera-parity/new-workspace-validation-2026-08-27/flutter-empty-error.jpeg` y `gpui-empty-error.jpeg`.
| Agent Activity y estados | `project_workbench_agent_rows.dart`, `agent_status` | paridad validada | Proyección working/waiting/blocked/done/interrupted, idle, presencia fresca/expirada, descendientes, fallback, navegación, cierre y `None + Agent Activity` estaban cerrados. Cuatro tabs reales ejecutaron Codex Luna read-only/ephemeral: ambas apps mostraron cuatro working, transición a mixed y cuatro done, cluster `Codex +3`, orden de creación y cuatro filas; luego tabs/sesiones/procesos se retiraron. GPUI anuncia ahora `Working/Done Codex +3` y cada hijo `State Agent Tool`, y conserva tooltips de estado/agente. Evidencia: `/tmp/alera-parity/agent-overflow-2026-08-28/`. |
- Agent Activity workspace/tab navigation (2026-08-27, cerrado): un hook Codex real contra `Terminal 1` recorrió `UserPromptSubmit` -> Working, `PreToolUse(request_user_input)` -> Waiting for input y `Stop` -> Done en Flutter y GPUI, con iconos, color, resumen, fila expandida y texto final equivalentes. La prueba reprodujo el defecto del cierre: al pulsar el agente de otro workspace GPUI cambiaba solo `selected_tab_id`, el tab quedaba fuera del snapshot visible y `request_close_tab` lo descartaba. GPUI lleva ahora `workspace + tab` como selección atómica mediante `pending_workspace_tab_id`, consume el tab después del refresh, abre `Sleep Wait Report / Terminal 1`, expone summary/fila/X como botones y `Close Terminal` elimina inmediatamente tab, presencia, fila y estado verde. La lista CLI confirmó cero tabs y Flutter reflejó la misma retirada. Evidencia: `/tmp/alera-parity/sidebar-agent-states-2026-08-27/`.
- Agent Activity Blocked/orden/None (2026-08-27, cerrado): se restauró una terminal limpia, se publicó temporalmente `blocked` mediante el mismo verbo interno de presencia de Orchestration y Flutter/GPUI mostraron la misma campana roja, color, `Blocked`, mensaje de fila y agregación en el padre. View Options confirmó `Group By: None`, `Sort Workspaces By: Agent Activity`, filtros All y `Repeat Pinned Workspaces` off con geometría a pocos píxeles y cierre Escape. En ambos clientes el workspace urgente y su padre quedaron antes de done/working/idle según la misma proyección subtree. El estado sintético se eliminó al terminar y la terminal limpia permanece. Evidencia: `/tmp/alera-parity/sidebar-agent-states-2026-08-27/flutter-blocked.jpeg`, `gpui-blocked.jpeg`, `flutter-view-options.jpeg` y `gpui-view-options.jpeg`.
- Agent Activity multiagente (2026-08-27, cerrado para tres): sobre el mismo workspace se crearon dos tabs temporales y se proyectaron Codex Working, Claude Code Done y Cursor Blocked. Flutter/GPUI coincidieron en prioridad de grupos, iconos reales, tres filas en orden de creación, estados/colores, texto y tabs. GPUI mantiene el contenido dentro del ancho mientras Flutter muestra un overflow de debug en el resumen estrecho; no se replica ese defecto visual. Toda la presencia se eliminó y `Claude Fixture`/`Cursor Fixture` se retiraron; CLI y ambos árboles confirman una sola `Terminal 1` limpia. Evidencia: `/tmp/alera-parity/sidebar-agent-states-2026-08-27/flutter-multi-agent.jpeg` y `gpui-multi-agent.jpeg`.
- Sidebar controles estructurales AccessKit (2026-08-27, cerrado): Pinned/All, headers de proyecto, `New Workspace in This Project` y toggles de hijos usan botones, tab stops, expanded y `on_click`; View Options publica Dialog y Close. Computer Use colapsó/restauró los hijos de Sidebar Alpha, cambió None -> Project, colapsó/restauró el proyecto, abrió `New Workspace` desde su `+` con Sidebar Alpha preseleccionado, lo canceló sin crear nada y restauró Project -> None. La geometría no cambió y las preferencias compartidas quedaron en su valor inicial. Evidencia: `/tmp/alera-parity/sidebar-agent-states-2026-08-27/gpui-project-grouping.jpeg` y `gpui-project-new-workspace.jpeg`.
- Sidebar geometría alineada (2026-08-27, cerrado para el estado limpio None): ambos clientes se alinearon en `Sleep Wait Report`, hijos expandidos y una terminal limpia; los recortes izquierdos de 185x768 muestran el mismo ancho, brand/search/toolbar, bandas Pinned/All, indentación del árbol, filas de 33 px, footer de 44 px y status bar. El análisis de promedio por fila ubica el borde del footer en y=713 en ambos y el contenido de Add Project con solo 1 px de diferencia; el desplazamiento acumulado de las filas queda en 0-2 px. Evidencia: `/tmp/alera-parity/sidebar-geometry-2026-08-27/flutter-sidebar.jpeg`, `gpui-sidebar.jpeg` y `difference.jpeg`.
| Perfiles y edición de agentes | `agent_profiles_pane*.dart`, `agent_profile_*` | paridad validada | Master/detail, campos, dropdowns con flecha, managed options, checkbox, Save/remove, risk confirmation, master/list/+ y acciones accesibles coinciden. Crear dos perfiles, relanzar, restaurar orden/datos y Remove real están cerrados. Model y Persona tienen loading/error inducidos con los mismos loaders, estado disabled, copy, color y geometría; el error GPUI se publica como alerta AccessKit. Computer Use no activa `ListBoxOption` por AX Pick, pero el click visual y el teclado del trigger funcionan en ambos clientes. |
- Agent Profiles AccessKit (2026-08-27, cerrado para perfil actual): Flutter/GPUI muestran Codex Sidebar Test, Adapter Codex, Managed, command preview, Model/Reasoning/Sandbox/Approval, routing y Save/Remove con la misma geometría. GPUI publica New Agent Profile, List, opción seleccionada, loading/error y usa on_click para flags/Save/Remove; no se modificó el perfil. Evidencia: `/tmp/alera-parity/agent-profiles-2026-08-27/`.
- Agent Profiles multi-profile y persistencia (2026-08-27, cerrado): desde GPUI se creó y guardó `Temporary Parity Profile`, el host devolvió dos perfiles con ids distintos, se relanzó GPUI y la lista restauró ambos perfiles ordenados sin perder selección ni datos. Después se seleccionó el fixture, se desplazó el editor hasta las acciones y Remove lo eliminó con un click; el host volvió a conservar únicamente `Codex Sidebar Test`. Flutter mantuvo su snapshot anterior mientras permaneció abierto pese al evento `agentProfilesChanged`, por lo que esa observación queda registrada como una limitación de actualización en caliente de la referencia Flutter y no como pérdida de persistencia de GPUI.
- Settings Diagnostics end to end (2026-08-27, cerrado salvo mutar crash reporting): Flutter y GPUI muestran el mismo bloque Diagnostics, el mismo error de Updates y el mismo estado `Thanks for the support!`. El dropdown Log Level expone `Errors Only`, `Warnings`, `Normal` seleccionado y `Verbose`, y se cierra por click externo sin alterar la preferencia. `Open Logs Folder` abrió en ambos la carpeta compartida `.../dev.leynier.alera.dev/logs` con los mismos cinco archivos; `Export Diagnostics` abrió el mismo save panel nativo con nombre `alera-diagnostics-<timestamp>.zip` y se canceló sin escribir archivos. `Send Crash Reports` permaneció off para no habilitar un envío externo durante la validación. Evidencia: `/tmp/alera-parity/settings-diagnostics-2026-08-27/`.
- Orchestration Reject end to end (2026-08-27, cerrado): Flutter y GPUI abrieron el mismo plan histórico `run_cd586ff5b9654f78` en estado `Awaiting Approval`, con stage, profile, hold copy, `On Stall: ask`, razón y acciones equivalentes. GPUI escribió `Temporary parity validation`, ejecutó Reject real y cambió inmediatamente a `Rejected`, retirando razón y acciones. Flutter conserva el snapshot mientras el modal permanece abierto por diseño on-demand; al cerrar y reabrir mostró el mismo estado `Rejected`. El run ya estaba detenido y su task cancelada, por lo que no se lanzó ningún worker ni dispatch. La repropuesta de restauración fue rechazada por el host porque un run terminado es inmutable, así que el registro histórico queda correctamente `stopped / rejected / cancelled`. Evidencia: `/tmp/alera-parity/orchestration-reject-2026-08-27/`.
- Orchestration Approve end to end (2026-08-27, cerrado y sin dispatch): se creó `run_7174051abef845d7` con poll de 60 s y una tarea no-op, se propuso `Approve Fixture Stage` y GPUI mostró `Awaiting Approval` con los mismos controles. Approve real cambió inmediatamente a `Approved`; Flutter, al abrir su vista on-demand, mostró el mismo plan Approved junto al histórico Rejected. Antes del primer poll se detuvo el run con `--force --cancel-active`, se canceló explícitamente `task_56c935a542fb4828` y se verificó `activeDispatch: null`, por lo que no se lanzó ningún agente ni se ejecutó la tarea. El registro histórico queda `stopped / approved / cancelled` como evidencia auditable. Evidencia: `/tmp/alera-parity/orchestration-approve-2026-08-27/`.
- Terminal selección Flutter final (2026-08-27, cerrado): se imprimió un fixture de dos líneas en `Terminal 1`, el drag sintético volvió a activar la selección xterm y Cmd+C devolvió contenido real de una línea y de varias filas. Esto cierra el único gate que permanecía abierto por una limitación temporal del backend durante la pasada anterior; GPUI ya había validado el mapeo exacto rendered-row -> source-row con los mismos escenarios. Se ejecutó `clear`, se restauró el clipboard previo sin exponer su contenido y el respaldo temporal se movió a Trash. Evidencia: `/tmp/alera-parity/terminal-flutter-selection-2026-08-27/`.
- Context Sidebar consolidado (2026-08-27, cerrado): la revisión transversal confirmó que el contenedor ya no tiene gaps propios. Los cuatro tabs, tooltips, hitboxes de 30 px, selección, rail, resize, cierre/reapertura y click externo están alineados; sus estados loading/error/recovery están demostrados dentro de Explorer, Search, Source Control y Pull Request. Se elimina por tanto el pendiente duplicado de “estado sostenido de cada panel” sin declarar cerradas las mutaciones externas que pertenecen a los dominios Git/Forge.
- Explorer toolbar/dialog keyboard parity (2026-08-27, cerrado): New File, New Folder, Save All, ignored files, Collapse All y Refresh dejaron de depender de `mouse_down` y comparten `on_click` para mouse, Enter, Space y AccessKit. Create/Rename/Delete usan Cancel/confirm accesibles, el confirm no se activa mientras está busy y replican el `showDialog` de Flutter: Escape y click en el scrim cierran, mientras un click dentro no propaga al dismiss. El build recompilado abrió `dialog New file` por `element_index`, lo cerró por Cancel, luego por Escape y finalmente por click externo, sin crear archivos. Los rows del picker Stash Pop y Amend también comparten roles/tab stops/on_click. No se ejecutó ninguna acción Git ni de filesystem.
- Git History no-repo/copy/menu propagation (2026-08-27, cerrado): un proyecto Folder temporal confirmó que Flutter y GPUI muestran únicamente Explorer/Search y ocultan Source Control/PR, por lo que “Git History no-repo” no es un estado renderizable del contrato. La terminal, proyecto, workspace y carpeta se retiraron y `main` quedó restaurado. Sobre el repo vivo, Copy Commit Hash devolvió exactamente `git rev-parse HEAD` y Copy Commit Message exactamente `git log -1 --format=%B`; el clipboard original se restauró sin exponerlo. El menú, al estar anclado dentro del row, propagaba la opción al commit y lo expandía; el contenedor y opciones ahora detienen mouse/click. Tras recompilar, Copy Hash mostró el toast, mantuvo el commit colapsado por más de un segundo y el clipboard volvió a restaurarse.
- Sidebar collapsed rail y splitters AccessKit (2026-08-27, cerrado): los resize handles izquierdo/derecho ahora son `Role::Splitter`, focusables y publican label, valor actual, mínimo 220 y máximo 460. Computer Use enfocó cada handle, Right cambió exactamente +8 px y Left restauró el valor; la preferencia volvió a 243.666/365.723 del fixture. Al colapsar, el foco se difiere hasta que `Expand Sidebar` existe en el frame: Return reabrió el panel. Una segunda pasada hizo Tab -> Sidebar Alpha -> Return, expandió y seleccionó el proyecto por teclado; la terminal temporal creada por esa selección se cerró y `main` se restauró. No quedó ancho, tab ni workspace temporal.
- Diagnostics protected-path parity (2026-08-27, cerrado): el save panel nativo se llevó a `/System` en Flutter y GPUI y Save falló sin crear ningún ZIP. Flutter no muestra feedback visual para la excepción de `XFile.saveTo`; GPUI mostraba `Operation not permitted (os error 1)`. GPUI ahora conserva el error en el log rotativo pero vuelve al pane sin toast, igual que Flutter; la recaptura confirmó `hasErrorToast=false`. El éxito conserva `Diagnostics exported.` y la exportación real previa ya verificó metadata/logs. Open, Export, Star y Try Again migraron de mouse-down a on_click con roles/tab stops. Evidencia: `/tmp/alera-parity/diagnostics-protected-path-2026-08-27/`.
- Source Control remote workflow (2026-08-27, cerrado): se creó un bare remote y dos clones temporales. GPUI ejecutó Stage -> Commit -> Push, Fetch -> Pull, Stash -> picker Stash Pop, Amend, un Sync divergente que mostró el error real de Git, un Sync exitoso con `pull.rebase=true` sobre commits disjuntos y Commit & Push. CLI verificó en cada gate status limpio, stash vacío cuando correspondía y hashes local/origin idénticos. Flutter abrió el mismo workspace final con `No changes`, `Fetch` y Message vacío. El primer recorrido detectó que GPUI conservaba el mensaje tras éxito; `commit_with_follow_up` ahora corre con Window, limpia el InputState únicamente en Ok y lo preserva en Err. Un segundo fixture confirmó `Message` sin Value y toast `Committed`, igual que Flutter. Ambos proyectos, tabs, clones y bare remotes se eliminaron y `main` quedó restaurado; no se tocó `leynier-website`. Evidencia: `/tmp/alera-parity/source-control-remote-2026-08-27/`.
- Explorer 3k virtualization (2026-08-27, cerrado): el primer folder con 3,000 archivos confirmó un bloqueo cercano a 60 s al construir todas las filas, un gap de experiencia más importante que el spinner pendiente. `render_explorer_panel` usa ahora `uniform_list`, `UniformListScrollHandle` y el mismo Scrollbar; cada frame renderiza solo el rango visible, manteniendo row identity por path, TreeItem/selected/expanded, drag/drop, menú y Git status. La segunda pasada abrió en 1,582 ms con 27 filas AX, scrolló a 0063-0090, luego 1625-1652 y finalmente 2974-3000, volvió a 0001 y abrió `file-0014.txt` como editor activo. Tabs/proyecto/3,000 archivos se retiraron y `main` se restauró. Evidencia: `/tmp/alera-parity/explorer-virtualized-2026-08-27/`.
- Settings/dropdowns keyboard batch (2026-08-27): `From Prompt` publica opciones como ListBoxOption selected, Agent Profiles publica Adapter/Launch/Managed como ComboBox y cada opción como ListBoxOption, y Refresh Models/Personas como Button disabled durante busy. Terminal Settings publica Reload, sus temas seleccionables y Block/Bar/Underline como controles accesibles. Keyboard Settings publica App First/Terminal First como radios, Change/Reset/Disable como botones y Cancel/Reassign del conflicto por on_click. Computer Use confirmó radios seleccionados/restaurados, temas como opciones Pick, cursor Bar selected, Reload, Adapter/Launch triggers y las ocho opciones visibles de Adapter; click externo cerró sin guardar el perfil. El error inducido de discovery quedó cerrado en la pasada del 2026-08-28.
- Workbench close-during-drag cleanup (2026-08-27, código recompilado): `request_close_tabs` limpia inmediatamente drag identity, pane/tab drop target y preview cuando el tab que se cierra participa en un drag, antes de abrir la confirmación dirty o mutar layout. Así el diálogo dirty no puede quedar detrás de un overlay tardío ni esperar el timeout. La matriz viva previa ya cubre cierre dirty, cancelación Escape/desactivación y cleanup de preview; Computer Use no expone pointer-down/move/up por separado para capturar frame intermedio adicional.
- Tab strip reveal y shortcuts globales (2026-08-27, cerrado): cada pane conserva un `ScrollHandle` horizontal y `activate_workspace_tab` llama `scroll_to_item`, por lo que un tab lejano activo siempre queda visible como en Flutter. `⌘9` seleccionó `projects.astro Untracked` y desplazó el strip hasta el extremo derecho. Editor/Diff consumían después Ctrl+Tab/⌘1-9; un interceptor previo usa `keyboard_shortcut_for_keystroke`, respeta overrides/recording/Terminal First y despacha únicamente navegación. La secuencia real fue último -> `⌘2 square.astro` -> `Ctrl+Tab logo.astro` -> `Ctrl+Tab bun.lock` -> `Ctrl+Shift+Tab logo.astro`; `⌘2` restauró square y el strip acompañó cada selección. Evidencia: `/tmp/alera-parity/tab-strip-navigation-2026-08-27/`.
| Quotas y proveedores | `agent_quota_status_bar*.dart`, `agent_quota_hover_card.dart` | paridad validada | Overview y chips muestran Claude x3, Codex, Kimi, Grok, Antigravity, MiniMax y Z.ai con iconos/lecturas exactos; card/reset, loading/error/offline/stale, hover 280/180 ms, click pin, segundo click, click externo y persistencia unpin/relaunch/repin están cubiertos. Kimi y `unpinnedQuotaKeys` se restauraron. |
- Quotas overview/provider (2026-08-27, cerrado para datos actuales): Flutter/GPUI muestran los nueve perfiles/proveedores actuales en el mismo orden, incluidos Claude Code Default/leynier41/leynierdev y los iconos antes faltantes. Overview, Codex Weekly 70%, reset disponible y Use Reset coinciden; no se consumió el reset ni se alteraron pins. Click abre modo fijado y click externo cierra overview/provider card. Evidencia: `/tmp/alera-parity/quotas-2026-08-27/`.
| Resource Manager | `resource_status_panel*.dart` | paridad validada | Jerarquía, sort, métricas, densidad, maxHeight/scrollbar, empty/unavailable, warming/remoto como `-`, orphans y confirmación están cubiertos. `Resource Close Fixture` validó Close real: tab, sesión y proceso desaparecieron y el host volvió a dos sesiones. |
- Resource Manager popover/clipping/orphans (2026-08-27, cerrado para estado vivo): el árbol tenía proyectos, sesiones, dos orphans y Alera, pero auto-height reducía el panel y lo recortaba. GPUI calcula altura por filas de 22 px y usa maxHeight 420 para árboles de 10+ filas; la recaptura queda a ~5 px de Flutter. Sort Name/CPU/Memory se ejercitó/restauró, sus botones publican selección; Close Terminal Session abrió diálogo y Cancel conservó la sesión. Los dos orphans eran prompts idle de fixtures sin tab; Kill All Orphan Terminals los terminó, `terminal read` dejó de encontrarlos y ambos clientes retiraron filas/footer al reabrir. Evidencia: `/tmp/alera-parity/resource-manager-2026-08-27/flutter.jpeg`, `gpui-final.jpeg` y `gpui-close-confirm.jpeg`.
| Runtime Host | `runtime_host_status_panel.dart` | paridad validada | Running/stopped, Refresh/Start/Stop, busy/force, versiones, hover/pin, click externo y reconexión están cubiertos. Un host v0.0.1 persistent mostró Update Available en ambos clientes; Update Runtime lo reemplazó por v0.1.0/current commit y retiró Lifecycle/Update, con status JSON verificado. |
- Runtime Host popover (2026-08-27, cerrado para running/confirm): Flutter/GPUI muestran Runtime, Status Running, host/bundled v0.1.0, Sessions 2, Agents 0, Refresh/Stop con el mismo ancho y top a ~3 px. Refresh se ejecutó; Stop abrió Force Stop Runtime por las sesiones activas y Cancel conservó host/sesiones. Los botones del diálogo usan ahora on_click y click externo cerró ambos popovers. Evidencia: `/tmp/alera-parity/runtime-host-2026-08-27/`.
| Settings y búsqueda | `settings_dialog*.dart`, `settings_sections.dart` | paridad validada | Los diez panes en alcance, búsqueda/anclas, scrollbar, defaults, Browse/cancel, steppers, pins, AI Text, themes, Projects, perfiles y Mobile están comparados. Perfil aislado cubrió Reset Editor, crash reporting on/off y acceso móvil; GPUI scopea preferencias por app ID con migración legacy. Browser, Account y Remote Hosts permanecen fuera del POC. |
- Settings scrollbar drag (2026-08-27, cerrado): wheel de 8 páginas lleva Application al mismo extremo Support/Updates y el thumb coincide en longitud/posición con Flutter. Dos drags fallaban porque `ScrollbarMode::Scrolling` podía ocultar el thumb y desactivar MouseDown entre observación y acción. Settings usa ahora `ScrollbarMode::Always`: el thumb queda visible, drag top -> bottom llega a Support y bottom -> top vuelve a Workspace Directory, conservando longitud/extremos. Ambos diálogos se cerraron sin cambiar settings. Evidencia: `/tmp/alera-parity/settings-scrollbar-2026-08-27/flutter-bottom.jpeg`, `gpui-top-always.jpeg`, `gpui-bottom-drag.jpeg` y `gpui-top-drag.jpeg`.
| Mobile devices y pairing | `mobile_devices_pane.dart`, `mobile_pairing*.dart` | paridad validada | Gateway, QR one-time, cancel/copy, modos/puerto, empty/offers, dispositivo activo y revocado, Rename/Revoke/Delete, error `wss://` y cleanup están cubiertos. El fixture terminó off, sin devices ni offers. |
| Orchestration y run policy | `run_policy_review_dialog.dart`, `run_policy_stage_list.dart` | paridad validada | Empty, Awaiting Approval, Approved y Rejected de `Execution Plans` se compararon en vivo: modal adaptativo, título, icono, copy, centrado, spacing, botones con check/cancel y cierre quedan alineados; loading centrado y limpieza de planes obsoletos están corregidos. GPUI publica diálogo/group/controls/loader en AccessKit; Reject vacío conserva el plan con un único error inline, y Approve/Reject reales actualizaron el host y ambas UIs sobre fixtures detenidos/cancelados sin dispatch. El error de host desconectado ya está cubierto por el ciclo Runtime Unavailable/Start/Retry. |
| Diagnostics | `diagnostics_settings_scope.dart`, `application_diagnostics_section.dart` | paridad validada | Tramo visible, dropdown Log Level, click externo, Open Logs Folder y save panel cancelado se compararon en vivo; la exportación real previa produjo ZIP con logs/metadata y toast equivalente. `/System` validó el path no escribible: Flutter vuelve silenciosamente y registra el fallo; GPUI ahora hace lo mismo en lugar de mostrar un toast divergente. Open/Export/Star/Retry comparten on_click accesible. Send Crash Reports permanece off en ambos para no habilitar un servicio externo. El recorrido completo del scrollbar ya fue cerrado en Settings Application/Keyboard. |
| Loaders, checks, bell y estados de error | `agent_run_state_indicator.dart`, shared feedback | paridad validada | Los 34 call sites directos de Loading y estados derivados pasan por el SVG animado 1200 ms; agentes usan 1400 ms. Check/bell/cancel y busy states comparten codepoints, tamaños y tonos, y los dominios principales tienen evidencia viva. |
| Iconos y design system | `alera_icons.dart`, Material/Codicons/SVG assets | paridad validada | Lucide 3.1.15, Codicons y Material coinciden; los 13 assets de agentes/proveedores tienen SHA-256 idéntico y las capturas por dominio cubren tamaño, color, tooltip y hover. |
- Loader/check/bell audit (2026-08-27, cerrado a nivel de enrutamiento): la búsqueda completa encontró 33 usos directos de `AleraIcon::Loading`; no existe otro acceso a su glyph fuera de `icon()`, y `icon()` lo desvía antes del font a `loader-circle.svg` con repeat 1200 ms. `agent_loading_indicator` usa el arco Flutter y 1400 ms. Los codepoints se contrastaron con `lucide_icons_flutter-3.1.15`: loaderCircle 57610, bellRing 57892, circleCheck 57894 y circleX 57476 coinciden con GPUI. Starting/Restarting Terminal, editor loading, Search, working/waiting/blocked/done y update error ya tienen evidencia viva; no se añade un cambio mecánico innecesario.
| Accesibilidad y teclado | Semantics, shortcut registry y focus scopes Flutter | paridad validada | AccessKit cubre shell, sidebar, workbench, inputs, menus, dialogs, toggles/disabled, status panels y actions. Search/Source Control cerraron los últimos handlers mouse-only y Computer Use verificó Tab/Space; terminal-first y shortcuts permanecen centralizados. |
| Runtime coexistence y procesos | `terminal_host`, lifecycle services | paridad validada | Una Flutter, una GPUI y un host fueron verificados después de relanzar. El ciclo Unavailable/Start/Retry, host antiguo `0.0.1`, Update Runtime al sidecar actual, reconnect, limpieza de tabs/sesiones y crash recovery están cubiertos; packaging pertenece al gate de release. |
| Performance, release y plataformas | `docs/performance.md`, make targets | paridad validada | Flutter y GPUI release compilan y corren en macOS; `make gpui-release` quedó integrado y el ejecutable strippeado mide 79,693,280 bytes. En el gate sincronizado GPUI quedó en 20.9% CPU median, 22.91% p95 y 166.3 MiB RSS durante streaming, frente a Flutter 19.7%, 21.35% y 163.67 MiB: diferencias de 6.1%, 7.3% y 1.6%, dentro del 10%. Idle GPUI quedó en 0.8% CPU y 132.33 MiB RSS. El código final compiló release en macOS, Linux x86_64 con Rust 1.96.1 y Windows con Rust 1.98; fmt, Clippy `-D warnings`, 129 + 28 tests y diff pasan. Evidencia: `/tmp/alera-parity/performance-2026-08-28/`. |
- Profiling release posterior al pacing (2026-08-28, abierto): Computer Use seleccionó el tab real `Terminal 1` en el único GPUI release, enfocó la superficie y ejecutó 1,200 líneas con pausa de 10 ms mientras el profiler muestreaba cada 250 ms durante 15 s. La mediana bajó de 47.05% a 39.6% CPU, pero no alcanza el baseline Flutter de 10%. El RSS de 191.28 MiB corresponde a la misma sesión después de acumular scrollback y permanece por debajo del streaming Flutter previo de 241.1 MiB; no se usa como idle. El fixture se limpió con `clear` y se restauró `recent-posts.astro Unstaged`. El siguiente trabajo es perfilar el costo de render/parsing y reducirlo antes de repetir el gate.
- Streaming sincronizado y aislamiento de frames (2026-08-28, mejorado): el primer protocolo iniciaba el profiler antes de Computer Use y mezclaba varios segundos idle con el stream. El protocolo corregido inicia 2,400 líneas primero y luego mide 15 s completos. Con ese gate GPUI marcó 37.45% CPU. El microbenchmark demostró que 2,400 parses cuestan 2.99 ms, 225 snapshots visibles 23.1 ms y un terminal GPUI aislado a 29.5 FPS usa aproximadamente 7% de un core. El loop global hacía `cx.notify()` por cada `TerminalOutput`, anulando el pacing; ahora esos eventos actualizan una entidad hija del terminal y los demás eventos conservan refresh global. La repetición sincronizada bajó a 24.95% CPU, p95 26.5%, con 164.22 MiB RSS. El stream se interrumpió, se limpió y `recent-posts.astro Unstaged` quedó restaurado. Falta repetir Flutter con el protocolo sincronizado antes de fijar la comparación final.
- Performance release final sincronizada (2026-08-28, cerrada en macOS): Flutter y GPUI ejecutaron exactamente 2,400 líneas, pausa de 10 ms, ventana nativa a tamaño completo y muestreo de 250 ms durante 15 s iniciado después del comando. Flutter midió 19.7% CPU median, 21.35% p95 y 163.67 MiB RSS; GPUI con frame hijo, replay incremental y cadence de 84 ms midió 20.9%, 22.91% y 166.3 MiB. GPUI queda 6.1% sobre Flutter en mediana CPU, 7.3% en p95 y 1.6% en memoria streaming, dentro del gate de 10%. El stream visible avanzó limpio, sin tearing, cursor perdido ni modal fatal. Después se reinició GPUI y el idle final midió 0.8% CPU y 132.33 MiB RSS. Ambos fixtures se interrumpieron/limpiaron y `recent-posts.astro Unstaged` quedó restaurado en ambos clientes. Evidencia final: `flutter-release-streaming-synchronized.json`, `gpui-release-streaming-final.json` y `gpui-release-idle-final.json` bajo `/tmp/alera-parity/performance-2026-08-28/`.
| Browser | `browser_page_body.dart` y servicios Browser | fuera de alcance | Feature explícitamente excluida del POC; no se implementa hasta reabrir alcance. |
| Updater | `update_settings_section.dart`, `update_availability_watch.dart` | fuera de alcance | Feature explícitamente excluida del POC; no se implementa hasta reabrir alcance. |
| PDF preview | `workspace_pdf_viewer_surface.dart` | fuera de alcance | Preview PDF explícitamente excluido del POC. |
| Emulator embedding | `mobile_emulator_surface.dart` | fuera de alcance | Embedding del emulador explícitamente excluido; pairing de `mobile_devices` sí está incluido. |
| Account y OAuth | `account_settings_pane.dart` | fuera de alcance | Account/OAuth explícitamente excluido del POC. |
| SSH y Remote Hosts | `remote_hosts` | fuera de alcance | SSH explícitamente excluido del POC. |
| LSP | Editor/LSP | fuera de alcance | LSP explícitamente excluido del POC. |

### Inventario de evidencia existente

- Álbumes Telegram ya enviados: sidebar/status bar, Quotas overview/provider, Resource Manager, Runtime, Settings scroll, View Options, Agent Activity, Source Control, terminal/IME, Force Stop, Search, Pull Request y otros recortes enfocados.
- Álbum Welcome Dashboard enviado: `/tmp/alera_flutter_welcome_focus_final.jpg` y `/tmp/alera_gpui_welcome_focus_final.jpg`, con una sola Flutter y una sola GPUI a tamaño completo y sidebar común.
- Álbum Mobile enviado: `/tmp/alera_mobile_flutter_focus.jpg`, `/tmp/alera_mobile_gpui_focus.jpg`, `/tmp/alera_pair_flutter_focus.jpg` y `/tmp/alera_pair_gpui_focus.jpg`, con gateway y pairing QR comparados.
- Álbum Diagnostics enviado: `/tmp/alera_diagnostics_flutter_focus.jpg` y `/tmp/alera_diagnostics_gpui_focus.jpg`, con el mismo tramo funcional de Settings.
- Evidencia local reutilizable: `/tmp/alera_gpui_*` y `/tmp/alera_flutter_*`; cada nuevo escenario debe guardar ambos recortes con el mismo tamaño y una sola instancia por app. Explorer expandido después del watcher: `/tmp/alera_explorer_flutter_focus.jpg` y `/tmp/alera_explorer_gpui_focus.jpg`.
- Validación técnica del snapshot: `cargo fmt --manifest-path rust/Cargo.toml --all -- --check`, `cargo fmt --manifest-path experiments/alera-gpui/Cargo.toml --all -- --check`, `git diff --check`, Clippy GPUI y Clippy `alera-cli` con `-D warnings` pasan. GPUI pasó 92 tests de aplicación y 12 de terminal; los tests de `workspace_file_requests` pasan. El workspace Rust mantiene sus gates previos; el único aviso de compilación es el future-incompatibility externo de `block v0.1.6`.
- Validación Explorer: `cargo test --manifest-path rust/Cargo.toml -p alera-cli terminal_host::server::workspace_file_requests::tests -- --nocapture` pasó 2 tests, incluida la tabla completa de copy de errores. `cargo fmt --manifest-path rust/Cargo.toml --all` y `git diff --check` pasan; el binario GPUI ya fue recompilado y relanzado sin duplicados.
- Validación Terminal restore: `cargo test --manifest-path experiments/alera-gpui/Cargo.toml terminal_session_replays_restore_in_bounded_batches -- --nocapture` pasó en `alera-gpui` y `terminal_performance`; `cargo fmt`, Clippy y recompilación/relanzamiento de GPUI también pasaron. Con 5.000 líneas el replay termina en menos de un frame visible; queda un escenario de snapshot mayor para comparar la animación sostenida.
- Validación Explorer diálogo/toast: `cargo fmt --manifest-path experiments/alera-gpui/Cargo.toml --all`, `git diff --check`, el test `app::toast::tests::explorer_errors_use_error_toast_icon` y Clippy GPUI con `-D warnings` pasan; el build recompilado conserva exactamente una GPUI, una Flutter y un host.
- Validación Explorer estado Git agregado: `cargo fmt`, `cargo check --manifest-path experiments/alera-gpui/Cargo.toml --bin alera-gpui`, `cargo test --manifest-path experiments/alera-gpui/Cargo.toml --all-targets` (101 tests principales + 12 terminales), Clippy GPUI y Clippy del binario `alera` con `-D warnings`, `cargo test --manifest-path rust/Cargo.toml --workspace` (729 + 5 + 2 + 56 + 1 + 2 + 2 + 18 + 5 + 120 + 24 + 9 + 4 + 169) y `git diff --check` pasan. El Clippy de todo el workspace con `--all-targets` sigue bloqueado por el lint preexistente `items_after_test_module` en `experiments/alera-gpui/src/runtime_bridge.rs`; `--lib` y el binario del host sí pasan. Computer Use confirmó una sola Flutter, una GPUI y un host, ambas a tamaño completo y sin modal fatal.
- Evidencia Telegram Terminal/restore: se envió un álbum enfocado con scrollback/cursor de Flutter y el mismo terminal GPUI después de generar 5.000 líneas (`/tmp/alera-parity/terminal-scroll-flutter.jpg`, `/tmp/alera-parity/terminal-restore-gpui.jpeg`); el overlay de restore dura menos de un frame con ese tamaño y queda cubierto por la prueba incremental.

- Source Control estabilidad P0 (auditoría transversal 2026-08-25): al abrir el panel GPUI apareció un abort reproducible al construir `source-ai-message`: el botón heredaba un tooltip de `source_toolbar_button` y registraba un segundo `.tooltip()` en la rama AI. El crash report `Alera GPUI-2026-08-25-110757.ips` apunta a `context_source_control.rs:450`; se eliminó el registro duplicado, el texto dinámico ahora vive en un único propietario y se añadió la regresión `ai_toolbar_tooltip_has_one_stateful_owner`. Debe repetirse la apertura/hover después del rebuild antes de cerrar esta evidencia.
- Source Control estabilidad P0 cerrada provisionalmente: `cargo fmt`, `cargo check`, `cargo test --all-targets` (99 pruebas principales + 12 terminales), Clippy con `-D warnings` y `git diff --check` pasan; GPUI se recompiló y se abrió de nuevo como única instancia junto a una Flutter y un runtime. Source Control se abrió tras el rebuild sin modal fatal ni terminación, y se envió un álbum enfocado a Telegram con `/tmp/alera-parity/source-control-flutter-focus.jpg` y `/tmp/alera-parity/source-control-gpui-focus.jpg`. El estado sigue `parcial` hasta probar el estado AI habilitado y el resto de acciones de Git en vivo.
- Source Control menú P1 (auditoría transversal 2026-08-25): se abrió el split-button `Pull` en ambas apps con el mismo snapshot y se comparó el menú completo (`Commit`, `Stage All`, `Fetch`, `Pull`, `Push`, `Sync`, `Stash`, `Stash Pop`), incluyendo estados deshabilitados, iconos, redondeo, hover y cierre por click externo. La geometría y el catálogo coinciden en la captura enfocada `/tmp/alera-parity/source-control-menu/flutter-focus.jpeg` y `/tmp/alera-parity/source-control-menu/gpui-focus.jpeg`, enviadas como álbum a Telegram. Source Control permanece `parcial` por AI habilitado, loading/error/conflicto y acciones destructivas end to end.
- Source Control busy-state P1: GPUI ahora trata `local_busy` y `git_snapshot_loading` como un único bloqueo visual/interactivo para el toolbar, el split-button primario y su menú. Mientras una operación está en vuelo, los controles muestran cursor básico, no reaccionan a clicks y no pintan hover, igual que Flutter; AI mantiene su acción de cancelación. `cargo test --bin alera-gpui` (101 tests), Clippy GPUI y `git diff --check` pasan; GPUI recompilada y relanzada sin modal fatal.
- Source Control confirmación P1 (2026-08-25): el mensaje de descarte de GPUI se ajustó a sentence case idéntico a Flutter para `Discard All`, rutas y grupos (`This permanently discards ...`). Tras recompilar se abrió el mismo modal en ambas apps, con `Cancel` y `Discard` destructivo, sin modificar el repositorio; se envió el álbum enfocado `/tmp/alera-parity/source-control-discard/focus/flutter.jpg` y `/tmp/alera-parity/source-control-discard/focus/gpui.jpg` a Telegram. Source Control sigue `parcial` por credenciales, conflictos, estados de error/loading completos y acciones destructivas end to end.
- Git History P1 (2026-08-25): se abrió la sección `COMMITS` en Flutter y GPUI con el mismo repositorio, se expandió el primer commit y se verificó el detalle de archivos, refs, grafo y estados `A`/`M` en ambas superficies. El álbum enfocado `/tmp/alera-parity/git-history-focus/flutter.jpg` y `/tmp/alera-parity/git-history-focus/gpui.jpg` fue enviado a Telegram. La auditoría encontró que el refresh de GPUI burbujeaba al footer y colapsaba el panel; el handler ahora detiene la propagación y muestra spinner/disabled durante refresh, alineado con Flutter. Falta repetir el gesto tras rebuild y cerrar filtros, loading/error con datos y navegación completa a diff/archivo.
- Git History refresh P1 (2026-08-25): tras recompilar y relanzar GPUI, se expandió `COMMITS` en ambas apps y se accionó `Refresh Commits` en GPUI. El panel permaneció expandido después del `pointer up`, sin modal fatal ni colapso por bubbling; el estado de carga/disabled quedó implementado en el icono. La captura recompilada queda en `/tmp/alera-parity/git-history-refresh/gpui-focus.jpg`; faltan filtros, loading/error inducidos y navegación completa a diff/archivo.
- Markdown/preview inventario vivo (2026-08-25): se volvió a abrir el mismo `readme.md` como preview en una Flutter y una GPUI a tamaño completo. El contenido, encabezados, listas, fences anidados, lenguaje, `Copy code`, scrollbar y toolbar de archivo están presentes en ambos; GPUI todavía conserva el gap conocido de la fila de acciones de code block y de `InteractiveViewer` para Mermaid/imágenes (zoom/pan y errores). Se guardaron y enviaron como álbum los recortes `/tmp/alera-parity/markdown-inventory/flutter-focus.png` y `/tmp/alera-parity/markdown-inventory/gpui-focus.png`; no se reclasifica el dominio hasta probar esos estados.
- Markdown code actions P1 (2026-08-25): el `TextView` de GPUI sigue usando su host absoluto, pero ahora el bloque reserva 48 px para la cabecera y calcula el ancho del action row desde el pane activo; language y `Copy code` ocupan una fila superior de ancho completo con padding y divisor, sin cubrir la primera línea. La captura recompilada muestra el mismo contrato visual de Flutter y se envió como álbum `/tmp/alera-parity/markdown-code-actions/flutter-focus-parity.png` y `/tmp/alera-parity/markdown-code-actions/gpui-focus-parity.jpg`. Mermaid/imágenes mantienen el gap separado de zoom/pan.
- Search inventario vivo (2026-08-25, AX supersedido): con la misma consulta `README`, Flutter y GPUI asentaron `4 matches in 4 files`, mismos grupos, líneas y previews. Se probó el flujo real de foco, selección con `super+a`, escritura, debounce y carga de resultados. La antigua limitación de ventana única quedó eliminada por la migración AccessKit del 2026-08-27.
- Diagnostics busy-state P1 (2026-08-25): el botón `Export Diagnostics` de GPUI ahora tiene estado local `diagnostics_export_busy`, muestra el loader dentro del propio botón y queda deshabilitado durante la selección/compresión; ya no activa el banner global `Refreshing Live Settings`. La pantalla de Diagnostics se comparó con Flutter después de navegar al mismo ancla, se canceló el Save picker sin escribir ningún archivo y se envió el álbum enfocado `/tmp/alera-parity/diagnostics/focus/flutter.jpg` y `/tmp/alera-parity/diagnostics/focus/gpui.jpg`. `cargo fmt`, 101 tests de aplicación + 12 de terminal, Clippy GPUI con `-D warnings` y `git diff --check` pasan. Falta probar exportación real, errores/path y recorrido completo del scrollbar.
- Terminal/Explorer path drop P0 (2026-08-25): se arrastró la carpeta `src` desde el Explorer al terminal activo en una Flutter y una GPUI a tamaño completo. Ambas pegaron `/Volumes/ExternalStorage/Projects/leynier/leynier-website/src` con el mismo quoting y sin cambiar el cwd; se envió el álbum enfocado `/tmp/alera-parity/terminal-path-drop/focus/flutter.jpg` y `/tmp/alera-parity/terminal-path-drop/focus/gpui.jpg`. El dominio sigue `parcial` por scroll extremo, shortcuts, selección/copy completa y sesión cerrada.
- Terminal daily-driver P0 (2026-08-25): después del rebuild, Flutter y GPUI conservaron el mismo workspace, cwd, prompt, scrollback de 40 líneas y cursor visible mientras el panel Pull Request permanecía abierto. Se guardó y envió el álbum enfocado `/tmp/alera-parity/terminal-current/flutter-terminal.jpeg` y `/tmp/alera-parity/terminal-current/gpui-terminal.jpeg`. La fila sigue `parcial` hasta cerrar scroll extremo, selección/copy/links, shortcuts, restore y error/reconnect con fixtures equivalentes.
- Terminal selection drag P0 (2026-08-25): el puente macOS no marca `MouseMoveEvent::dragging` en todos los movimientos, aunque Flutter mantiene la selección hasta `pointer up`. GPUI ahora conserva un dueño explícito del drag y limpia el estado en `mouse_up`/`mouse_up_out`; la comparación con la misma línea del terminal muestra el resaltado en ambas superficies y se envió el álbum `/tmp/alera-parity/terminal-selection/flutter-focus.jpeg` y `/tmp/alera-parity/terminal-selection/gpui-focus.jpeg`. Copy/paste también se verificó en ambos insertando el texto seleccionado en el prompt y cancelando después. Faltan links Cmd-click, shortcuts, restore sostenido y sesión cerrada.
- GPUI gate transversal (2026-08-25, AX supersedido): después de los ajustes de Explorer y Terminal pasaron los gates técnicos de esa revisión. El gap de framework de accesibilidad quedó eliminado el 2026-08-27; Runtime, Resource Manager y Quotas siguen congelados salvo regresión transversal.
- Image Preview P1 (2026-08-25): con el mismo `public/assets/images/dev-banner.webp`, Flutter muestra la imagen contenida dentro de `InteractiveViewer` con margen y GPUI la estiraba al pane. El renderer GPUI dejó de forzar `size_full()` y parte ahora del tamaño intrínseco, evitando además que la imagen se replique en un pane restaurado. Se guardó y envió el álbum enfocado `/tmp/alera-parity/image-preview/focus/flutter-focus.jpeg` y `/tmp/alera-parity/image-preview/focus/gpui-focus.jpeg`. El dominio sigue `parcial` hasta validar zoom/pan, errores, archivos grandes y el mismo viewport inicial.
- GPUI gate Image Preview (2026-08-25): después del ajuste del renderer, `cargo test --manifest-path experiments/alera-gpui/Cargo.toml --bin alera-gpui --all-targets` pasa con 101 tests de aplicación + 12 de terminal, Clippy con `-D warnings`, `cargo fmt --check` y `git diff --check` también pasan. La advertencia restante es únicamente el aviso externo de future-incompatibility de `block v0.1.6`.
- Feedback/toast P1 (2026-08-25, superseded by 2026-08-26 animation pass): el inventario transversal encontró que `local_message` de GPUI quedaba visible hasta la siguiente acción, mientras Flutter usa un host de hasta tres entradas con duración por defecto de 4 s. GPUI ahora observa cada mensaje nuevo en el ticker existente, lo retira después de 4 s y reinicia el contador al cambiar el texto. La captura enfocada del mismo `Copy Path` mostró además que Flutter usa `Info` para `Path copied` y GPUI estaba usando `Success`; se ajustó la inferencia para `Copied`, `Path Copied` y `Version Copied`, conservando `Success` para `Workspace Path Copied` y copias de commits. Evidencia: `/tmp/alera-parity/toast/focus/flutter-focus.jpeg` y `/tmp/alera-parity/toast/focus/gpui-focus.jpeg`. La cola de tres toasts y la salida animada quedan implementadas y recompiladas; falta únicamente un frame-by-frame visual del fade en el gate final.
- GPUI gate feedback/toast (2026-08-25): después del fix, `cargo fmt`, `cargo test --bin alera-gpui --all-targets` (102 tests de aplicación + 12 de terminal), Clippy con `-D warnings` y `git diff --check` pasan. Computer Use confirmó una sola Flutter, una GPUI y un runtime; el toast recompilado muestra `Path Copied` con `Info` y desaparece después de 4.6 s. El álbum post-fix se envió a Telegram con `/tmp/alera-parity/toast/focus/flutter-focus.jpeg` y `/tmp/alera-parity/toast/focus/gpui-post-fix-focus.jpeg`.
- Orchestration no vacío P1 (2026-08-25): se creó un fixture temporal controlado de `Execution Plans` para comparar un plan `Awaiting Approval` real mediante Computer Use, siempre con una Flutter y una GPUI a tamaño completo. GPUI se ajustó a la geometría adaptativa de Flutter, panel compacto, padding/spacing y botones `Approve`/`Reject` con los iconos check/cancel y tono elevado oscuro de Flutter. Evidencia final enfocada y enviada como álbum: `/tmp/alera-parity/orchestration/non-empty/flutter-focus.jpeg` y `/tmp/alera-parity/orchestration/non-empty/gpui-focus.jpeg`. Los gates `cargo fmt`, 102 tests de aplicación + 12 de terminal, Clippy GPUI y `git diff --check` pasan. El run `run_cd586ff5b9654f78` fue detenido y el task `task_29a44a951f1e4831` cancelado con razón de cleanup; queda únicamente el registro histórico `stopped/rejected/cancelled`, sin dispatch activo, y no se ejecutó un reset global.
- Agent status/icons P1 (2026-08-25): el inventario estático comparó `AgentRunStateIndicator` de Flutter con `sidebar_rows.rs` de GPUI. GPUI ahora usa un SVG de arco parcial con extremos redondos, duración de 1400 ms y medidas `size - 2` equivalentes a Flutter; los slots de filas individuales, grupos y workspaces se redujeron a los tamaños de Flutter. También se alineó el label de `interrupted` para cualquier estado base y se cubrió con tests `working/waiting + interrupted`.
- Agent fixture temporal P1 (2026-08-25): se inyectaron por la conexión local autenticada del host cuatro presencias sobre las sesiones existentes (`codex/working`, `claude/waiting`, `cursor/blocked`, `grok/done`) para comprobar la proyección simultánea en Flutter y GPUI. El árbol AX de Flutter expuso `Waiting for input`, `Blocked`, `Working` y `+1`, y la captura final mostró los grupos, tabs y colores; GPUI reflejó los mismos estados visualmente aunque su AX nativo sigue limitado a la ventana. Evidencia final enviada como álbum con `/tmp/alera-parity/agent-states/flutter-tabs-final.jpeg`, `/tmp/alera-parity/agent-states/gpui-tabs-final.jpeg`, `/tmp/alera-parity/agent-states/flutter-focus-final.jpeg` y `/tmp/alera-parity/agent-states/gpui-focus-final.jpeg`. Las presencias se eliminaron después mediante `removed: true` y el listado del host quedó sin estados de agente; no se creó ni persistió ningún terminal, workspace o task.
- GPUI gate agent status/icons P1 (2026-08-25): `cargo clippy --manifest-path experiments/alera-gpui/Cargo.toml --all-targets -- -D warnings`, `cargo fmt --manifest-path experiments/alera-gpui/Cargo.toml -- --check` y `git diff --check` pasan. El único aviso restante es el future-incompatibility externo de `block v0.1.6`; la fila de agentes permanece `parcial` por notificaciones completas, presencia expirada, expansión de Agent Activity y la limitación transversal de accesibilidad de GPUI.
- Terminal restore/resync P0 (2026-08-25): Computer Use comparó el mismo layout visible tras relanzar una sola Flutter y una sola GPUI a tamaño completo. La auditoría del contrato encontró que Flutter aplica el snapshot completo devuelto por `setOutputPaused(false)`, mientras GPUI descartaba esa respuesta; GPUI ahora procesa delta/full snapshot, reemplaza el emulador cuando corresponde, limpia modos de interacción y evita requests duplicados. Además, el stream vivo se encola mientras el snapshot incremental se reproduce, para que los bytes nuevos no se intercalen con el restore. Se añadieron la regresión `terminal_output_waits_until_restore_snapshot_is_replayed` y el gate `cargo test --bin alera-gpui --all-targets` con 103 tests principales + 13 de terminal; `cargo fmt`, Clippy con `-D warnings`, recompilación por `make gpui-debug` y `git diff --check` pasan. La comparación histórica con scrollback repetido quedó explicada por resizes provisionales y se cubre en la entrada de debounce; la fila continúa `parcial` por los escenarios de scrollback histórico y restore sostenido pendientes.
- Terminal resize/redraw P0 (2026-08-25, superseded by generation debounce): la lectura del host compartido confirmó que los redibujos repetidos venían de `sync_terminal_size` enviando tamaños provisionales durante cada render. GPUI pasó primero a bounds reales y debounce corto, y después se endureció con la generación-based 650 ms debounce registrada arriba; se añadió `redraw_sequence_keeps_prompt_on_one_row`. Después de `make gpui-debug`, se crearon sesiones nuevas en Flutter y GPUI y ambas quedaron limpias, con cwd, prompt y cursor equivalentes. El álbum enfocado enviado a Telegram queda en `/tmp/alera-parity/terminal-resize-fix/flutter-focus.jpg` y `/tmp/alera-parity/terminal-resize-fix/gpui-focus.jpg`. La fila sigue `parcial` por scrollback histórico, selección/copy, links, shortcuts, restore sostenido y estados de error/reconnect.
- Explorer context menu P0 (2026-08-25): con click secundario sobre el mismo `.prettierrc`, Flutter y GPUI muestran el mismo catálogo y orden de `New file`, `New folder`, clipboard, paths, `Duplicate`, `Reveal in Finder`, `Rename`, `Collapse folder`, `Refresh` y `Delete`, con cierre externo; la diferencia de `Paste` habilitado proviene del clipboard interno de la sesión. Se envió el álbum enfocado `/tmp/alera-parity/explorer-menu/flutter-focus.jpeg` y `/tmp/alera-parity/explorer-menu/gpui-focus.jpeg`. La fila sigue `parcial` por selección/scroll, errores y acciones de filesystem end to end.
- Explorer scroll P0 (2026-08-25): con `src/components` expandido, Flutter respondió al scroll dirigido al contenedor del árbol y desplazó el thumb hasta las filas inferiores. GPUI mantiene `overflow_y_scroll()` en código, pero Computer Use solo expone la ventana raíz y el scroll semántico no alcanzó el contenedor; el intento de arrastre sobre el borde también hit-teó una fila y abrió un archivo. Se envió un álbum de evidencia del gap en `/tmp/alera-parity/explorer-scroll-gap/flutter-focus.jpg` y `/tmp/alera-parity/explorer-scroll-gap/gpui-focus.jpg`.
- Explorer scroll P0 fix (2026-08-25): el árbol de GPUI ahora usa un `ScrollHandle` persistente, `track_scroll`, un `Scrollbar` vertical siempre visible y un hitbox reservado para que terminar un drag del thumb no active ni abra la fila que queda debajo. Tras rebuild, el drag nativo del thumb movió el árbol hasta `footer.astro`/archivos raíz sin overlay y sin crear una nueva acción de archivo; la evidencia enfocada `/tmp/alera-parity/explorer-scroll-fixed/gpui-focus.jpeg` y `/tmp/alera-parity/explorer-scroll-fixed/flutter-focus.jpeg` se envió como álbum a Telegram. Falta repetir con la misma posición inicial exacta y validar Page Up/Down, rueda sostenida, restauración de scroll y exposición AX, por lo que la fila sigue `parcial`.
- Workbench split/editor P0 (2026-08-25): al mover `Terminal 1` a un split derecho, Flutter conserva el preview Markdown del pane izquierdo; GPUI dejaba `This editor tab has no file.` porque solo cargaba el tab seleccionado y sustituía los panes inactivos por placeholders. GPUI ahora renderiza el documento cacheado en panes editor inactivos, bloquea su Input/acciones hasta seleccionar el pane y, al restaurar un terminal activo, carga el primer documento visible para evitar un pane vacío. Se comparó el mismo drag en ambas apps, se relanzó GPUI con una sola instancia y se envió el álbum enfocado `/tmp/alera-parity/workbench-inactive/focus/flutter.jpg` y `/tmp/alera-parity/workbench-inactive/focus/gpui.jpg`. La matriz sigue `parcial`: varios documentos editor simultáneos aún requieren caché por pane para paridad completa.
- Workbench split drop P0 (2026-08-25): se reprodujo el fallo intermitente reportado por el usuario: GPUI aplicaba el movimiento entre panes pero un `DragMove` tardío repintaba el overlay gris después de `pointer up`, mientras Flutter eliminaba el pane vacío sin residuo. `set_tab_drop_target`, `set_pane_drop_target` y el inicio del drag ahora ignoran eventos durante la mutación optimista y limpian cualquier preview tardío. Después del rebuild se creó el mismo split lateral en Flutter y GPUI y ambas superficies quedaron sin overlay residual; al mover de nuevo el único tab al pane izquierdo tampoco quedó residuo. Álbumes enfocados enviados en `/tmp/alera-parity/splits-create/flutter-focus.jpeg`, `/tmp/alera-parity/splits-create/gpui-focus.jpeg`, `/tmp/alera-parity/splits-move-fixed/flutter-focus.jpeg` y `/tmp/alera-parity/splits-move-fixed/gpui-focus.jpeg`. Falta cubrir en vivo las cuatro direcciones, reorder dentro del mismo split y cancelación fuera de ventana.
- Terminal scroll extremo P0 (2026-08-25): se generó el mismo bloque de 80 líneas en el terminal activo de Flutter y GPUI y se desplazó el workbench tres páginas hacia arriba mediante Computer Use. Ambas superficies conservaron el cwd, el cursor, el scrollback y el thumb vertical, sin saltos ni pane residual; el álbum enfocado `/tmp/alera-parity/terminal-scroll-work/flutter-focus.jpg` y `/tmp/alera-parity/terminal-scroll-work/gpui-focus.jpg` fue enviado a Telegram. La fila sigue `parcial` por selección/copy, enlaces, shortcuts, restore sostenido y error/reconnect con fixtures equivalentes.
- Workbench split vertical P0 (2026-08-25): desde un pane único se arrastró `Terminal 1` al borde inferior en ambas apps. Flutter y GPUI crearon el split vertical con el editor arriba y el terminal abajo, conservaron el scrollback y no dejaron overlay residual; el álbum enfocado `/tmp/alera-parity/splits-vertical/flutter-focus.jpg` y `/tmp/alera-parity/splits-vertical/gpui-focus.jpg` fue enviado a Telegram. La matriz de splits sigue `parcial` hasta repetir arriba/izquierda/derecha, reorder dentro del mismo split y cancelación fuera.
- Workbench reorder P0 (2026-08-25): se creó un fixture compartido con tres terminales en el pane inferior y se reordenó un tab dentro del mismo split. La primera lectura aislada parecía divergente, pero el host comparte el layout entre clientes y la segunda acción ya partía del orden que acababa de persistir la otra app; al repetir con estado compartido controlado, Flutter y GPUI convergieron al mismo orden, sin pane vacío ni preview residual. Álbum enfocado enviado en `/tmp/alera-parity/splits-reorder-shared/flutter-focus.jpg` y `/tmp/alera-parity/splits-reorder-shared/gpui-focus.jpg`. Falta una prueba aislada con snapshot inicial idéntico para separar cálculo de índice de la sincronización entre clientes.
- Editor fuente/layout P1 (2026-08-25): se normalizaron temporalmente los anchos persistidos del sidebar y Context Sidebar al baseline Flutter para comparar el mismo `readme.md` en fuente. Ambas superficies muestran la misma barra de acciones, estado dirty, numeración, wrapping y ritmo de línea; el álbum enfocado `/tmp/alera-parity/editor-source-aligned/flutter-focus.jpg` y `/tmp/alera-parity/editor-source-aligned/gpui-focus.jpg` fue enviado a Telegram. El dominio permanece `parcial` por selección/focus de teclado, scroll medido, conflictos y loading/error sostenidos.
- Editor multi-pane P1 (2026-08-25): la auditoría del workbench confirmó que GPUI solo tenía un `EditorDocument` global, por lo que dos editores visibles podían reutilizar el contenido activo. Se añadió caché por ruta para documentos y previews, actualización de claves al renombrar/mover, limpieza al borrar/cerrar y render read-only del documento correcto en panes inactivos; el click sobre el pane lo activa para continuar editando. La captura `/tmp/alera-parity/editor-multi-pane/gpui-two-documents.jpg` muestra `readme.md` arriba y `tsconfig.json` abajo sin mezclar contenido; Clippy, fmt y 106 tests del binario + 14 del terminal pasan. Persisten selección/scroll por pane, edición simultánea real, conflictos y loading/error sostenidos.
- Editor syntax theme parity (2026-08-26): el editor GPUI ya consume la preferencia `Editor Theme` compartida con Flutter y actualiza el `HighlightTheme` global para fondo, foreground, números de línea y tokens. Se mapearon `Alera`, `GitHub Dark`, `GitHub Dark Dimmed`, `VS2015`, `Atom One Dark`, `Night Owl`, `Nord`, `Monokai`, `Tokyo Night Dark` y `Dracula`; Computer Use confirmó `GitHub Dark` en fuente y un cambio reversible a `Dracula`, restaurando después la selección original. La captura local queda en `/tmp/alera-parity/editor-theme/gpui-top.png`.
- Editor theme regression test (2026-08-26): `editor_palette_catalog_matches_flutter_names` valida las diez entradas, sus fondos raíz y la deserialización de estilos para `gpui-component`; el test dirigido pasa sin warnings propios. La implementación vive en el módulo aislado `experiments/alera-gpui/src/editor_theme.rs`.
- Editor scroll delta isolation (2026-08-26): una llamada idéntica `pages=1` de Computer Use llevaba Flutter a la línea 45 y GPUI a la 55, pero el mismo gesto normalizado a `pages=0.82` en GPUI terminó en la línea 45 y con el mismo viewport que Flutter. La causa es el delta semántico/escala del backend de Computer Use, no el line-height ni el scroll interno de GPUI; no se altera el renderer. La rueda física nativa queda para el gate final. Evidencia en `/tmp/alera-parity/editor-current/wheel-equivalent`.
- Editor focus validation (2026-08-26): al hacer click en la misma posición del editor fuente, Flutter y GPUI conservan el active line/caret sin dibujar el borde blanco externo que existía en la regresión reportada. La captura enfocada quedó en `/tmp/alera-parity/editor-focus/flutter.png` y `gpui.png`; selección, scroll, conflictos y loading/error sostenidos siguen parciales.
- Settings Agents spacing P1 (2026-08-25): la comparación transversal mostró que el trigger de dropdown de GPUI estaba sobredimensionado respecto al `AleraDropdownField` de Flutter; se normalizó el control compartido a 34 px y se mantuvo el floor de las filas en 66 px para evitar drift acumulado. El álbum enfocado `/tmp/alera-parity/agents-pane/focus/flutter.jpg` y `/tmp/alera-parity/agents-pane/focus/gpui.jpg` fue enviado a Telegram. La diferencia vertical restante en esta captura proviene del estado transitorio de registro de CLI que Flutter muestra cargando y GPUI muestra como aviso persistido; falta repetir con el mismo fixture de estado y recorrer Hooks/Behavior.
- Settings Application geometry P1 (2026-08-25): el panel `Workspace Directory` de GPUI usa 16 px de padding y un campo de 40 px, aislado del alto regular de los demás inputs, para seguir el ritmo de `AleraTextField` y `OutlinedButton` de Flutter. El diálogo usa insets relativos del 4% para conservar el 92% de ancho/alto de Flutter, y el contenido conserva el inset vertical de 24 px que Flutter aplica a todos los panes. La comparación a tamaño completo confirma que `Workspace Directory`, `Safety` y `Runtime` arrancan con el mismo ritmo; el álbum `/tmp/alera-parity/settings-application-geometry/flutter-full.jpeg` y `/tmp/alera-parity/settings-application-geometry/gpui-full.jpeg` fue enviado a Telegram. La superficie sigue `parcial` hasta recorrer inicio/mitad/final del scrollbar con el mismo fixture y probar escritura, Browse, persistencia y error.
- Mobile Devices geometry and group anchors P1 (2026-08-25): se comparó `Mobile Gateway` y `Link A Device` en Flutter y GPUI a tamaño completo después de restaurar el inset vertical. Los grupos, controles, botones, chips, empty states y el ritmo de las filas quedan alineados; el álbum `/tmp/alera-parity/mobile-devices/flutter-full.jpeg` y `/tmp/alera-parity/mobile-devices/gpui-full.jpeg` fue enviado a Telegram. El salto a `Active Pairing Offers` funciona en ambos, pero queda pendiente medir la alineación exacta del ancla y probar pairing activo, QR y errores de red.
- Settings Keyboard scrollbar P1 (2026-08-25): el pane de GPUI tenía su contenido forzado a `flex_1`, y el último row `Close Split` quedaba fuera del rango efectivo aunque Flutter sí llegaba al final. Se ajustaron el contenido intrínseco, `min_h_0` del viewport y `flex_shrink_0` de los grupos/rows; la captura post-rebuild confirma que ambas apps llegan a `Close Split` y conservan el padding inferior. GPUI todavía comienza aproximadamente un row antes al usar el mismo salto semántico de Computer Use, por lo que queda medir el gesto/altura exacta antes de cerrar la fila de Settings.
- GPUI gate después del ajuste de Settings (2026-08-25): `cargo fmt --manifest-path experiments/alera-gpui/Cargo.toml --all -- --check`, `cargo test --manifest-path experiments/alera-gpui/Cargo.toml --bin alera-gpui --all-targets` (101 tests de aplicación + 12 de terminal), Clippy con `-D warnings` y `git diff --check` pasan. La sesión Computer Use quedó con una sola Flutter, una GPUI y un runtime; ambas apps fueron llevadas a tamaño completo con doble click de titlebar antes de la captura.
- Pull Request estado sugerido P1 (2026-08-25): el código Flutter conserva el composer de creación cuando existe un PR detectado pero no enlazado y solo muestra el candidato al entrar en `Link Existing Pull Request`; GPUI lo abría prematuramente como formulario de enlace. Se corrigió la condición de render y el retorno a creación, dejando el candidato disponible sin bloquear `Create`/`Draft`. La captura de la superficie común queda en `/tmp/alera-parity/pull-request/flutter-panel.jpeg` y `/tmp/alera-parity/pull-request/gpui-panel.jpeg`, enviadas como álbum enfocado a Telegram. Falta un fixture vivo con PR sugerida para cerrar la interacción completa.

- Merman/image preview P1 (2026-08-25): GPUI ya trata `fileRole=mermanPreview` como viewer de primer nivel, coincide con Flutter en el flujo Explorer -> editor fuente -> `Open Preview` -> `Open Editor`, aplica el tema oscuro a fondos, nodos, flechas y textos del SVG, y expone `Refresh Preview`/`Open Editor` con el mismo estado de busy. Los errores de imagen (`outside`, `invalid`, `not found` y fallback) se normalizan a `Image is outside the workspace`, `Image path is invalid`, `Image not found` y `Image cannot be opened`; el test `image_preview_errors_match_flutter_copy` y el test de mapeo SVG pasan. Se usó un fixture temporal real y se envió el álbum `/tmp/alera-parity/merman-preview/flutter-focus.jpeg` + `gpui-focus.jpeg`; el fixture fue eliminado. Sigue abierta la diferencia de sizing del diagrama, InteractiveViewer zoom/pan, enlaces, errores visibles y archivos grandes.

### Orden histórico de trabajo después del inventario

1. P0: cerrar terminal daily-driver, Context Sidebar y Explorer con selección, scroll, cursor, links, menús, errores y restore sostenido.
2. P0: cerrar la confiabilidad del drag/preview de splits y conservar sidebar/workspaces/agentes como referencia ya inventariada, reabriéndola solo ante una regresión o un estado faltante.
3. P1: completar Search/Editor/Explorer, Source Control, Git history y Pull Request con loading/error/conflictos/confirmaciones.
4. P1: recorrer los panes restantes de Settings y persistencia; perfiles, Quotas, Resource Manager y Runtime solo como parte de su matriz, no como bloque aislado.
5. P2: validar Mobile, Orchestration, Diagnostics, loaders, iconos, accesibilidad y teclado.
6. Gate: repetir Computer Use en todos los estados, conservar evidencia enfocada local por dominio y actualizar el estado solo con evidencia. No enviar avances ni capturas por Telegram.

## Historial de gaps supersedidos

Las entradas siguientes documentan hallazgos intermedios y no son pendientes vigentes. Todos quedaron cerrados o reemplazados por la matriz operativa final de 30 dominios validados; se conservan para trazabilidad.

- La selección visual del texto inicial de Change Title no se puede exponer con la API pública de `gpui-component`; la conducta funcional de reemplazo al escribir está validada, pero el resaltado visual exacto permanece como un gap menor.
- La paridad total de toda la matriz no está demostrada todavía, aunque los gaps críticos revisados hasta ahora están cerrados.
- La nueva proyección de Agent Activity está compilada y cubierta por Clippy/tests; falta validarla visualmente en fixtures con estados `waiting`, `blocked`, `done` y presencia expirada, porque el runtime actual no expone todos esos estados simultáneamente.
- La comparación visual del sidebar actual muestra la estructura y densidad base alineadas, pero el runtime de validación no tiene presencia viva simultánea para comprobar el indicador agregado y la expansión de Agent Activity; el comportamiento ya está cubierto por pruebas de dominio. La diferencia residual de unos pocos píxeles en el baseline de filas se mantiene bajo observación y no se cambia sin un fixture con agentes visible.
- La carga tolerante de `workspaceActivity.list` pasa Clippy y tests; el host de validación actual devuelve actividad persistida pero no presencia viva, por lo que los buckets `needsYou/done/working` siguen pendientes de una sesión de agente real.
- Computer Use: después de relanzar y hacer doble click en la barra superior, GPUI quedó a 1302x768 como Flutter; el panel lateral se volvió a capturar sin duplicados y se envió un álbum enfocado de la comparación con Agent Activity y fallback persistido.
- Validación final del relanzamiento: exactamente una GPUI, una Flutter y un runtime host; ambas capturas Computer Use quedaron en 1302x768, sin modal de error, y se envió el álbum final del panel lateral.
- Validación del ajuste de Resource Manager: `cargo fmt --check`, `git diff --check`, Clippy con `-D warnings` y `cargo test --all-targets` pasan; 76 tests principales y 11 de terminal, con el único aviso externo de future-incompatibility de `block v0.1.6`.
- Validación del fix de cleanup del drag: `cargo fmt` y `cargo test --all-targets` pasan; 76 tests principales y 11 de terminal.
- Resource Manager quedó prácticamente alineado en ritmo de filas y anclaje; queda una verificación de píxeles de borde y una pasada equivalente de Runtime/Quotas antes de cerrar la fila de la matriz.
- La app GPUI muestra algunos datos de fixtures/runtime distintos a Flutter cuando se cambia de workspace; la comparación debe hacerse con el mismo workspace activo y no confundir estado persistido de cada perfil.
- La sesión directa anterior de Computer Use dejó a Flutter mostrando loaders de editor/Explorer y se conserva solo como antecedente histórico; la sesión vigente, relanzada por `make app-debug` con Dart/Flutter 3.12.2, carga Search y `readme.md` de forma estable y es la única referencia válida para nuevos escenarios.
- La pasada antigua de Search quedó supersedida: el query asentado y `No results` coinciden ahora en Flutter y GPUI; los estados de backend deliberadamente lento siguen pendientes de una prueba específica.
- La pasada antigua de Editor quedó supersedida para la carga inicial: la fuente y el preview de `readme.md` coinciden funcionalmente, y el soft wrap/ritmo de línea se corrigió en GPUI; selección multilinea, edición, scroll, conflictos y loading/error sostenidos siguen abiertos.
- El scroll de Settings está funcional y comparable, incluido el salto de búsqueda al grupo coincidente y el fade del thumb; la API de accesibilidad de GPUI solo expone la ventana raíz, por lo que los desplazamientos GPUI se validan con screenshot y scroll dirigido a esa raíz, mientras Flutter expone el contenedor interno. La matriz completa de panes aún no está cerrada.
- Durante la primera prueba del salto de búsqueda apareció un abort reproducible de GPUI por llamar `Window::request_animation_frame` dentro del callback de frame; el crash report `Alera GPUI-2026-08-24-233823.ips` señaló `app_init.rs:497`. La llamada fue eliminada, el build completo se relanzó y la prueba posterior no volvió a abrir modal ni cerrar el proceso.

## Gate final 2026-08-28

- Inventario: 30 dominios incluidos con `paridad validada`, 0 parciales, 0 sin validar, 0 gaps y 7 exclusiones explícitas sin cambio.
- Computer Use: una Flutter release y una GPUI release en la pantalla nativa, ambas a tamaño completo por doble click, mismo workspace/tab/contexto y sin modal fatal.
- Rendimiento sincronizado: GPUI dentro del 10% de Flutter en CPU mediana, CPU p95 y RSS durante el mismo stream de 2,400 líneas; idle GPUI con menor RSS.
- Plataformas: release final compilado en macOS, Linux x86_64 y Windows.
- Calidad: `cargo fmt --check`, Clippy de todos los targets con `-D warnings`, `cargo test --all-targets` con 129 + 28 tests y `git diff --check` pasan. El aviso de future incompatibility pertenece al crate externo `block v0.1.6` y no es un warning de Alera.
- Estado operativo: una Flutter, una GPUI y un runtime; fixtures de streaming limpios, `recent-posts.astro Unstaged` restaurado y workspaces/proyectos temporales retirados.
