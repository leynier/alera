diff --git a/packages/serve-sim/Package.swift b/packages/serve-sim/Package.swift
index d0f49de..53e3c4a 100644
--- a/packages/serve-sim/Package.swift
+++ b/packages/serve-sim/Package.swift
@@ -10,7 +10,7 @@ let package = Package(
     name: "SimStreamHelper",
     platforms: [.macOS(.v14)],
     dependencies: [
-        .package(url: "https://github.com/httpswift/swifter.git", from: "1.5.0"),
+        .package(path: "../swifter"),
     ],
     targets: [
         .executableTarget(
diff --git a/packages/serve-sim/Sources/SimStreamHelper/HTTPServer.swift b/packages/serve-sim/Sources/SimStreamHelper/HTTPServer.swift
index 093a054..90d7151 100644
--- a/packages/serve-sim/Sources/SimStreamHelper/HTTPServer.swift
+++ b/packages/serve-sim/Sources/SimStreamHelper/HTTPServer.swift
@@ -157,3 +157,6 @@ final class HTTPServer {
+        // The helper exposes unauthenticated control and stream endpoints.
+        // Keep them reachable only by the Alera sidecar on this machine.
+        server.listenAddressIPv6 = "::1"
         try server.start(port, forceIPv4: false, priority: .userInteractive)
-        print("[server] Listening on http://0.0.0.0:\(port)")
+        print("[server] Listening on http://[::1]:\(port)")
     }
