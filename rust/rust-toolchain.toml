# Pins the Rust toolchain so native build hooks (linux/windows CMake, macOS
# Xcode) and CI produce reproducible sidecar binaries from a clean checkout.
[toolchain]
channel = "1.98.0"
components = ["rustfmt", "clippy"]
