# GPUI integral review

## Active goal and acceptance

This review executes the user-approved integral audit and remediation plan in task `019fb5f8-caf9-7da0-ae22-ce78731035f8`. The goal is active. macOS debug and release, Flutter comparison, current code traces, regression tests, 20 lifecycle cycles, split stress and a 30-minute release soak are required. No blocked or unobserved scenario is counted as passed.

The scenario ledger is `experiments/alera-gpui/docs/review-matrix.md`. `todo.md` is the resumption index. The earlier crash review remains historical evidence, not sign-off for this new audit.

## Baseline

- Start: 2026-08-30T04:04:28Z. macOS 26.6.2, build 25G83.
- Checkout: `feat/rust-desktop-migration`, HEAD `5302b1491d95`.
- Pre-existing changes preserved: `docs/gpui-crash-review.md`, `experiments/alera-gpui/src/app/project_config_requests.rs`, `experiments/alera-gpui/src/app/terminal_search_refresh.rs`, `todo.md`.
- GPUI: 0.2.2, Zed revision `5218009a2d338898083b4789f5c5fd37ddc73b70`; dependency versions are unchanged for this audit.
- Source inventory: 32 Flutter feature directories, 366 presentation files, 180 GPUI Rust files; matrix expanded from 198 to 300 scenario rows, including 29 keyboard actions, three scenarios for each of 31 input keys, and nine Automation actions.
- Initial processes: GPUI UI PID 22449; installed sidecar PID 14402; development sidecar PID 59843. No Flutter UI was present in the initial process inspection. Re-resolve PIDs before any lifecycle action.
- Builds/tests from previous turns are not fresh evidence for this audit. No broad checks have been rerun yet.
- Checkout change observed during remediation: HEAD advanced externally from `5302b149` to `60d892c5dab952e9082657c5eae6ed84deee18a6` (`refactor: reorganize application code`). It records the prior GPUI audit work through F-019 and does not change lib/ or rust/. The already-built Flutter reference is therefore source-equivalent for these tests. This audit did not create that commit, and it remains preserved. Later F-017/tracking edits are still uncommitted.
- Evidence root: `/tmp/alera-gpui-review-20260830.E1BZ6q`.

## Isolation and operating rules

- Use disposable projects, local Git remotes and a dedicated app/runtime profile. Resolve and verify every storage path before mutating data.
- The debug tool's `--app-support-dir` controls host paths, but the Flutter client database resolves its own application-support path through the platform plugin. Passing that flag alone is not evidence of client isolation. Verify the bundle/application identity and actual database files.
- GPUI local preferences are keyed by `ALERA_APP_ID`; shared Flutter UI settings resolve relative to `ALERA_RUNTIME_DIR`. Both paths must refer to the test profile before changing preferences.
- Do not alter personal projects, installed runtimes, credentials, global hook configuration or paired devices. Do not update main or change the GPUI framework.
- Computer Use is exclusively `node_repl` plus `@oai/sky`. Keep one comparison UI per framework on the integrated display, refresh state after actions, and retain screenshot/AX discrepancies as failed evidence rather than assuming success.
- External writes and destructive UI operations retain the Computer Use confirmation gates. Backend fixtures are labelled separately from native UI coverage.

## Findings and scope questions

| ID | Category | State | Evidence / next action |
| --- | --- | --- | --- |
| SCOPE-001 | AI Dictation scope | Resolved: excluded | The user explicitly excluded AI Dictation from GPUI during this audit. Do not add capture/transcription/provider UI. AI Assist/Reading Diff/Text Actions/Speech Messages remain included. |
| ENV-001 | Test isolation prerequisite | Verified | Both staged bundles use profile `dev.leynier.alera.review20260830.dev`; `lsof` confirms Flutter PID 40523 opens that profile's `alera.sqlite` and host PID 35328 opens its `terminal_host/runtime.sqlite`. GPUI PID 41252 uses the corresponding injected runtime path and scoped preferences. Runtime inventory is initially empty. |
| TOOL-001 | Native hover/IME evidence | Known limitation | Sky has no pure-pointer-move method; a click does not prove hover. Framework event tests and native evidence remain separate gates. |
| F-001 | Terminal search focus | Active-owner case revalidated in debug | Closing search now returns focus only when its owner terminal is still active. GPUI test covers active and replaced owners. Native Close Search followed immediately by printf produced review_focus_ok without another click. Evidence TE-011-gpui-after-close-focus. Escape, live owner-switch and release remain pending. |
| F-002 | Automation stale detail/action completion | Fixed in source; native/race integration revalidation pending | Per-view/list/detail epochs reject stale responses, close/reopen invalidates completions, mutation refresh preserves current selection, and each editor saves its own initial definition snapshot. Four targeted epoch/snapshot regressions pass. Deterministically delayed bridge integration and native AU-003/AU-007 remain open. |
| F-003 | Typography, sidebar geometry and Welcome | Weights/scale/leading/responsive scroll corrected in debug; precision variants remain | Native font selection now distinguishes all bundled weights; explicit Flutter sizes and inherited 1.43 leading replace rem-dependent text and golden-ratio leading. Workspace metadata stays inline and rows no longer collapse vertical margins. Matched 997x646 Welcome scroll reaches the final shortcut in both clients after fixing a newly observed intrinsic-height regression. Tracking, remaining small row deltas, other component variants and release remain open. See F003-* evidence and remediation trace. |
| F-004 | Terminal search input width | Corrected width revalidated in debug | The terminal search field now owns remaining row width and the counter does not shrink. Before/after native captures show the field expanding from nearly zero width to usable input; review-alpha is visible with 1 of 1 match. Evidence TE-011-gpui-before / after-empty / after-match. Narrow-pane and release variants remain pending. |
| F-005 | Add Project parity | Field workflows corrected/revalidated in debug; complete operation/geometry variants pending | GPUI now matches path-derived defaults, manual-name preservation, parent-before-URL, derived destination, inline/floating labels, clone glyph, outside dismissal and keyboard-invoked focus return. Evidence F005-*. Registration/clone progress and error paths, exact geometry and F-017 activation remain open. |
| F-006 | Text Actions empty-state UI | Empty state revalidated in debug; CRUD/release separate | Native F006-gpui-empty-after now matches the reference structure: full-height master/detail divider, labelled New Action, centered icon/title/description states and no unsupported Reset control. Rendering is separated from mutations. Header/button density also aligns after F-025. Complete CRUD/overrides remain open under F-024; release and broader precision cases are not approved. |
| F-007 | Agents skill controls | Confirmed debug gap | GPUI omits Copy from All Alera Skills and orders Orchestration before Agent Canvas, unlike Flutter. Shared switch/input/typography differences also appear here. Evidence SE-001-*-agents. |
| F-008 | Keyboard registry coverage | Fixed in source; native revalidation pending | Open Automations now routes Mod+Shift+A, Find in Terminal uses Global, and navigateBack/navigateForward match Flutter with legacy override migration. Four keyboard tests pass, including registry reconciliation excluding Browser. Evidence of original gap: SE-001-*-keyboard. |
| F-009 | Markdown table styling/layout | Dark paint revalidated; sizing/borders pending | Initializing the full dark theme, applying Alera colors, mirroring resolved paint tokens and syncing Base removes the white table. Native PV-001-gpui-after-dark versus PV-001-flutter-recheck confirms legibility. GPUI still fills the pane and differs in border/radius; Flutter uses intrinsic width. Those parts and release remain open. |
| F-010 | PR unsupported-provider state | Confirmed debug gap | With the same local bare remote, both report Provider Not Detected, but GPUI drops the Pull Request header and Refresh action while Flutter retains them. Evidence PR-006-*-local-remote. |
| F-011 | Agent Canvas empty state/filter | Confirmed debug gap | GPUI keeps an empty master/detail split and puts Select an Agent Canvas into a narrow top-aligned detail column; Flutter centers No Agent Canvases across the body and exposes an Active filter dropdown instead of Show History. Evidence OM-004-*-empty. |
| F-012 | Search result opens preview instead of source | Source navigation revalidated in debug | FileOpenIntent::SearchResult opens source Editor while retaining the ephemeral preview-slot flag. The rebuilt GPUI and Flutter each opened docs/guide.md line 3 and selected needle one through their actual search result. Evidence FS-005-gpui-after / FS-005-flutter-recheck-click. Routing regression covers md/mdx/svg/mmd/rs; other query variants and release remain pending. |
| F-013 | Automation catalog layout and empty state | Confirmed debug gap | GPUI shows search/state filters over a static top-aligned master/detail layout; Flutter's empty catalog has a resizable master, centered states, a master add action and different header icon/action placement. Evidence AU-001-*-empty. |
| F-014 | Missing quota provider logos and duplicate labels | Corrected artwork/labels revalidated in debug | Kimi/MiniMax/Z.ai use the existing full-color SVG image decoder, retaining original artwork. OpenCode labels prefix once. Paired regional crops ST-004-gpui-after-crop / ST-004-flutter-recheck-crop visibly confirm the assets and Go/Zen names. Overall popup typography/spacing and release remain pending; no resets were invoked. |
| F-015 | Native Quit shortcut | Corrected/revalidated in debug | Cmd+Q now invokes MenuQuitApp, matching Flutter's explicit native Quit shortcut. One GPUI dispatch test passes; native terminal-focused Cmd+Q closed PID 63856 without stopping the shared isolated host. Release remains pending. |
| F-016 | Startup workspace selection | Corrected/revalidated in debug; release pending | Removed automatic pinned/main/available selection; only an explicit still-existing selection is retained. Both GPUI and the newly restarted Flutter open populated-profile Welcome without inventing a workspace. Selecting Alpha restores persisted tabs. Unit selection regression passes. Evidence F016-flutter-populated-cold-start plus GPUI rebuilt Welcome captures. Release remains pending. |
| OBS-003 | Sidebar wheel calibration | Requires native/event audit | shell.rs multiplies macOS wheel deltas by 1.25 with a comment specifically about Sky. A tool-normalized synthetic gesture must not define shipping physical scroll behavior. Verify/remove the special calibration when closing the scroll-input gate. |
| F-017 | Add/clone activation | Local activation/defaults revalidated in debug; clone/filter/release pending | Registration/clone now retain project identity, expand it without changing positive filters, park editor banks and clear active workspace/tab. Native duplicate Add Beta expands Beta and returns Welcome in both clients; Welcome New Workspace defaults to Alpha while sidebar uses active Beta. GPUI's prior Alpha draft survives. Explicit project menu/plus routing is corrected in source too. One identity regression passes; clone, positive-filter variants and release remain pending. |
| F-018 | Unsaved editor data lost on workspace navigation | Core navigation/ownership corrected and revalidated in debug | Original A -> B -> A lost an unsaved draft while disk stayed unchanged. Workspace-owned entity banks now preserve drafts and history without sharing the first editor across workspaces. Native Alpha/Beta same-name files stay independent, undo/redo works, save updates Beta only, and remote B close preserves dirty A then reopens B without old history. FS-013 debug passed; release and in-flight writes remain pending under F-019. |
| F-019 | Editor save completion after navigation | Source corrected; 14 directed editor tests pass; native/release pending | Writes now use workspace/path/entity tickets, serialize each document and update parked baselines without dropping newer edits. Autosave and conflict confirmation are document-owned. Search reveal uses guarded UTF-8 selections rather than deferred keystrokes. Tests cover reordered A/B completion, newer edits, closed/reopened entities, duplicate replies, independent autosave epochs, stale confirmations and focus changes. These are model/component tests, not native delayed-runtime acceptance. |
| F-020 | Replace protection with inactive dirty editors | Corrected/revalidated for bounded debug cases; races/release pending | Inactive dirty protection matches Flutter and preserves disk. Native clean-cache reload initially failed, led to the clean buffer correction, and now passes on 6f375cc3: GPUI replaces and opens updated content, Flutter independently replaces/reloads, and GPUI restores fixture via another match action. Directed regression covers cached clean buffers and newer edits before InputEvent. Delayed cross-workspace/release gates remain open. |
| F-021 | Explorer Save All | Normal global save/no-op revalidated in debug; errors/release pending | Save All collects dirty values across workspace banks, checks entity identities and serializes document-owned writes. Both Flutter and GPUI save distinct Alpha/Beta conflict.txt drafts and report Saved 2 editor files. GPUI no-op feedback and Undo/Save All restoration also pass (FS-014). Source additionally omits documents with read errors; native error/conflict/delayed variants are tracked in FS-015. |
| F-022 | New Workspace presentation | Default/growth/caret/reset improved and debug revalidated; precision/variants pending | Flutter-measured textarea heights 96/180, thin outline, label, surface/header/segment/caption/checkbox sizing now align closely. Native ten-line paste reveals rows 3..10 and the caret; manual scrolling to rows 1..8 remains stable, then returns to the end. Cancel/reopen resets prompt. Parent dropdown escapes the form clip and closes externally. Remaining gates include short-window body scroll, complete picker/keyboard states and exact text/scrollbar geometry (component thumb includes padding unlike Flutter). Dictation microphone intentionally excluded. |
| F-023 | Search result row overflow | Corrected/revalidated in debug; variants/release pending | Full-width virtual rows, shrink-constrained preview, two-line clamp and fixed action width keep Replace match visible. A real GPUI ListState test at 280 px passes; native F023-fixed-visible-preview and F020-fixed-match-replaced show wrapped text and a working per-match action. Long filenames/deep trees/release remain independently pending. |
| F-024 | Text Actions draft/override correctness | Full normal-flow source batch in progress; native pending | Introduced a typed draft and shared editor for new/existing actions, Enabled and inherited agent/model/reasoning selectors, scoped menu callbacks and keyboard navigation, preserved per-model overrides, duplicate seeding, removed-action save guard, and explicit Delete confirmation with focus trap. Rows now use stable IDs, duplicate controls and typed reorder payloads guarded by the original ID order. Native CRUD/override/reorder/confirmation and delayed persistence coverage remain pending; no action definition has been saved from these new controls yet. |
| F-025 | Material desktop control density | Measured and source corrected; native consumers pending | Actual macOS widget probe passes: Filled/Outlined 26 px, Text/Elevated 32 px, default IconButton 40 px, AleraIconButton min30 ->22 px and settings close min34 ->26 px. Shared buttons now use the measured type-specific heights and disabled alpha roles; private workspace primary buttons delegate to them. Settings Close/Reset sizing is corrected. Generic icon_button callers retain explicit actual sizes until their nominal/actual contracts are audited. A GPUI drawn-bounds test passes; representative native revalidation follows. |
| F-026 | AI model catalog parity | Corrected source and bounded native cases; full discovery/release pending | Discovered-first deduplication now retains order and metadata. AGY/fx expose Agent Default; fx supports discovery and discovered defaults cannot override agent inheritance. Native Codex menu order and AGY empty catalog verified in 8f5659ac. Two catalog regressions pass. |
| F-027 | Canvas action/catalog ownership | Source corrected; native race pending | An agentCanvasChanged event can advance the catalog generation before an action reply, leaving busy stuck. Action epochs and workspace identity now own completion independently; events during catalog loading request a coalesced follow-up. Model tests pass; real bridge ordering remains a required gate. |
| F-028 | Canvas Close/Remove confirmation | Debug Close/Cancel verified; remaining states/release pending | GPUI previously sent both mutations immediately. Scoped confirmation now exposes a Dialog; Close/Cancel matched Flutter natively in SHA068fceaf and retained the live pinned Canvas. Remove, changed-target/state and final mutation gates remain open. |
| F-029 | Runtime Canvas capability advertisement | Corrected and verified in isolated runtime B | Added the omitted existing agentCanvasV1 control-file capability without protocol bump; five tests pass. Rebuilt B advertises support and CLI publication/native decision resolution succeed. Preserved older A host was not force-stopped. |
| F-030 | Canvas decision/action document contract | Source implemented; bounded native debug passes | Ten specialized cards and typed local dispatch replace generic fields/RPC-only actions. Native decision resolution, Pin, Open Search, Files, width/scroll and Close/Cancel work. Twelve Canvas tests pass. Remaining action/error/race, line-wrap/density and release gates are open. |
| F-031 | Automation editor and catalog feature gaps | Full editor source implemented; native validation in progress | Full calendar/target/metadata/policy form replaces seven inputs. SHA1e0e008f verifies multiline sizing, selector heights/accessibility and nested editor cancellation. Flutter-like option menu/triangle/keyboard/per-opening nonce is being built. Native creation/persistence, long-list/error/race, adaptive-toggle interaction and release gates remain open. |
| F-032 | Global shortcuts without focus | Corrected and verified in bounded debug paths | Initial shell focus preserves existing owners and restores focus around Automation dialogs. Two focused tests pass; native cold-Welcome Cmd+Shift+A and close/reopen work. Wider keyboard/focus and release matrix remains open. |
| F-033 | Master/detail divider drag | Corrected and revalidated in debug | The coalesced-single-move regression reproduced a stationary divider. Shared grip now applies final MouseUp coordinates; two tests pass. Native SHA294bac24 dragged Automations and Text Actions/Settings dividers out and back. Stress, interruption and release remain open. |
| F-034 | Automation edit changes schedule/target kind | Bounded edit verified; expanded form testing pending | Native B name-only oneTime/existingTab edit retained date/target/revision metadata and stayed draft with runs[]. The full form now replaces the preservation-only interim module; round-trip tests cover both schedules and all three targets. Managed-workspace native edits, races and release remain open. |
| F-035 | Runtime automation enum field contract | Corrected and verified in runtime B | serde variant fields now emit canonical camelCase and accept legacy snake_case, without schema/protocol change. Seventeen core automation tests pass. B runtime native GPUI edit/save and Flutter read succeed; complete native creation flow is still required. |
| F-036 | Automation detail presentation/features | Confirmed native/source gap | Flutter shows Overview/Runs/Audit tabs, prompt previews, effective policy and signature timeline. GPUI stacks short summary cards and omits part of that content/navigation. B-reference-automation-after-gpui-save versus B-automation-fixture-loaded documents the same record. This is independent of the corrected empty catalog. |
| F-037 | Automation Run Now/Pause missing choices | Confirmed source gap; not triggered natively | GPUI sends runNow immediately with fixed precheck/overlap/draftTest and Pause with continue-active. Flutter opens Run Now and Pause choice dialogs first. Add scoped choices and cancellation before these live action paths are tested; no automation was executed during the audit. |
| F-038 | Automation failed-save draft retention | Source corrected; native failure case pending | Closing the editor before the upsert reply lets a failed request return to a different catalog selection and lose access to the draft. Keep the form mounted/disabled during save, scope completion by editor epoch, close only on success, and show bounded errors above the footer so they are visible without scrolling. This intentionally preserves the failed draft instead of copying Flutter's close-before-request flow. Native duplicate-slug/no-write retry fixture is pending. |
| F-039 | Add Project mode-switch autofocus | Confirmed native/source gap | After selecting Clone From URL, direct typing is ignored in GPUI; Flutter focuses Git URL and derives review-origin from the same typed fixture path. select_add_project_mode changes the form/defaults but never transfers focus from the removed local-path field. Native clone testing continues via an explicit field click; add a mode-focus regression and fix both directions. |
| F-040 | Clone cleanup can delete a competing destination | Critical shared-runtime source finding; isolated regression in progress | Start validates that destination is absent, then an async clone can lose the race to another job. Failure/cancel/restart cleanup calls remove_owned_clone_destination, which checks only the parent path and deletes the current destination, not a proven job-owned directory. No live competing clone test is run. Replace cleanup with per-job staging and exclusive publish; preserve old/interrupted final destinations when ownership cannot be proven. |
| F-047 | Sidebar Rename input focus and dialog contract | Native debug behavior/AX revalidated; release pending | Native initial selection/typing, empty validation, error clearing and Enter submit pass. Root-backed regression reproduced native Tab escape; FocusTrapElement plus tab-enabled handles fix it. fc535944 Tab/Enter activates Cancel without changing sidebar;402f2275 restores Role::Dialog and revalidates Tab/ShiftTab/input/Escape. Four Root tests pass. Native Flutter Tab comparison was tool-rejected before execution, not retried; Flutter selection/title/field reference exists. No public protocol change. |
| F-048 | Source Control view preference and toolbar layout | Native/source defects confirmed | Tree/list callback only updates the bool, never persists; GPUI flat rows appear while Flutter stays tree in R2-gpui-source-flat-unsaved/R2-flutter-source-after-gpui-flat. Group tooltip is inverted relative to Flutter. Flutter toolbar gives title/icons equal flex widths and a reverse horizontal scroll area; GPUI places every icon inline and wraps title to two lines. AI icon is not an extra feature: Flutter clips it off the initial trailing viewport. |
| F-049 | Source Control external edits remain stale | Native failure confirmed | Three fixture changes appeared automatically in Flutter but GPUI kept No Changes through fresh activation/captures; only its Refresh button populated the three changes. R2-gpui-source-dirty-tree versus R2-gpui-source-after-refresh. Trace watcher lifecycle and coalescing before fixing; no stale state accepted as clean. |
| F-046 | View-preference initialization, fallback and stale loads | Six real-bridge tests pass; five failed before fixes | Uninitialized defaults replaced local choices, older gets reverted newer loads/user changes, runtime failure ignored local prefs and remote notification raced local save. Explicit desktopInitialized=false seeds local prefs; initialized/legacy replies merge; generation rejects stale loads; offline fallback uses local data; save awaits local completion before remote update. Test-only in-memory/gated store and unchanged real RuntimeBridge verify ordering without user files. Native ordinary grouping/pin/collapse mutations now synchronize in C; restart/offline/release and rapid overlapping save coalescing remain unverified. |
| F-045 | Source Control preference wire value | Rust/Flutter and migration tests pass | GPUI wrote list while Flutter accepts only tree/flat; both regressions failed before fix. Writer now emits flat and reader accepts legacy list. Existing local SQLite I/O moved behind workbench_prefs_store; atomic json_set repairs only the enum and rereads current data. Flutter Drift suite3 passes with shared fixture; two Rust storage tests prove idempotence, unknown/Unicode preservation and concurrent field/mode preservation. Native/release replay pending locked Mac; no user profile was modified by these tests. |
| F-044 | Sidebar grouping, pins and collapsed search | Five regressions pass; native core cases replayed | Flat Recent, global pins in None, pinned child with regular parent collapsed, search with All/Project collapsed, and Repeat Pinned off now match Flutter C in R1 captures. Five source regressions had failed before fixes. Stale-cycle recovery is test-only, not injected into live metadata. Activity variants, identical-name tie order, row geometry, restart and release are not accepted by this partial replay. |
| F-043 | Stale workspace-dialog requests | Controlled real-bridge regressions pass | Older branch list overwrote a newer result and late errors escaped closed dialogs; both failed before fix. Five request tests and twelve combined manual-workspace tests now pass, including busy/retry, profiles and dropped-owner replies. Build7742e1ed completed; native smoke blocked by locked Mac. No public API/protocol change. |
| F-042 | Manual creation completion/focus and empty source | Normal debug and Setup replay pass; variants ongoing | Native SHA93661431 fixes initial terminal/cwd, Create Another reset/Step2 and focus. SHA5f537d64 orders normal terminal before active Setup and retains it after autoclose. Busy/reentry/error/retry and dropped-owner paths pass against the controlled real bridge. Release, visual Step2 precision and remaining failure variants still open. |
| F-041 | Manual workspace picker parity | Native debug corrected; release/large lists pending | C-F041-* confirms path matching, stable ordering, contained long rows, undistorted exact Lucide indicators and Tab/Space segment activation. Filter failed before fix; filter and GPUI geometry regressions pass. Remaining large-list, release and typography precision checks are not closed by normal-state screenshots. |
| REF-004 | Flutter clone error duplicated | Reference behavior observed | Profile C invalid local origin produces two identical error messages in AX, each prefixed Bad state; earlier B existing-destination error also showed two toasts. GPUI shows one error. Preserve single-error GPUI behavior; no additional Flutter remediation in this audit. |
| REF-003 | Flutter automation Project filter overflow | Reference defect observed | Default-width master overflows when the Project dropdown measures its UUID option. B-reference-automation-after-gpui-save shows Flutter's debug overflow stripe. Do not reproduce that failure in GPUI; use matched wider masters for remaining comparisons and retain the narrow-state difference explicitly. |
| REF-002 | Flutter preview-slot content identity | Reference defect observed; isolated for comparison | The reused tab ID labelled conflict.txt initially displayed the earlier guide.md contents in Flutter, even after a second fresh capture. Closing that tab and opening conflict.txt with a new ID restored correct content. No Save was invoked on the stale view. Evidence F018-flutter-content-check / F018-flutter-fresh-file. |
| REF-001 | Flutter inline SVG preview | Reference limitation observed | The fixture's valid local SVG renders in GPUI Markdown but shows an image-error placeholder in Flutter. Add a shared PNG fixture for positive image parity; do not break GPUI SVG support to reproduce the reference failure. |
| OBS-001 | AI defaults and reference group catalog | Needs stable-state recheck | First AI Assist captures show Medium in GPUI and Low in Flutter with empty persisted thinking maps. Recheck after model discovery settles before calling this a value-sync defect. Flutter renders Speech Messages but lacks its navigation anchor; GPUI includes that anchor. |
| OBS-002 | Mobile comparison readiness | Resolved | Mobile Devices was reopened in both clients with loaded forms and matching disabled gateway/default port/expiry data. MB-001-gpui-ready and MB-001-flutter-reopened replace the initial loading frame for comparison. Actions and error variants remain pending. |
| TOOL-002 | Application resolver cache | Resolved for this session | Changing bundle ID at the same GPUI path left Sky resolving the old identity. Name lookup launched the regular Flutter bundle, which was closed before proceeding. A launch was rejected until closure was verified; `ps` and `sky.list_apps` then confirmed zero UIs. Unique staged bundle paths now resolve both isolated clients. No data actions were performed in the regular Flutter UI. |

F-003 source trace: gpui-component Root sets `window.set_rem_size(cx.theme().font_size)`, while Alera sets that font size to 13 px. Views use `text_2xl`, `text_sm` and other rem-based styles against Flutter's explicit type scale. Validate the shared rem/type-scale fix before compensating with per-view offsets. Both empty Welcome and populated workspace rows exhibit the difference in matched captures.

Fixture manifest: `/tmp/alera-gpui-review-20260830.E1BZ6q/fixtures.json`. Three projects and six workspaces are present; Alpha has root/child/grandchild plus a sibling. GPUI created terminal `gpui-tab-41252-1788064460296` by clicking the Alpha workspace; Flutter attached to that same terminal without creating another session. Folder project `review-notes` was added through GPUI UI and synchronized to Flutter. These subcases do not close the untested variants of WS-002/TE-001/RT-007.

Settings sweep: all eleven included panes opened in both isolated clients without a panic. GPUI set Workspace Directory to `/tmp/alera-gpui-review-20260830.E1BZ6q/workspaces`; a read-only check of `settings.general.workspaceDirectory` in the isolated runtime confirmed that exact persisted path. The first native Cmd+, attempt in Flutter did not open Settings; its visible footer button did. This remains a keyboard/tool-context recheck, not a conclusive shortcut defect. Equal 1000x610 crops at x=250,y=125 were generated from equal 1302x768 captures for control comparison. Sky supplied JPEG bytes despite the historical `.png` evidence suffix; no pixel-perfect metric is claimed.

Context-sidebar sweep: same dirty Alpha fixture produces 2 unstaged and 2 untracked files in both Source Control panels; the local bare remote prevents external Git writes. PR unsupported-provider and empty Canvas states are captured, yielding F-010/F-011. File search returns one match in one file, but result navigation yields F-012. Add a normal PNG alongside the SVG for later positive preview comparison; current Markdown captures precede that fixture refresh.

Operational sweep: Automations and Execution Plans opened from each native app menu; no definitions/runs were created yet. Resource Manager and Runtime were opened by click and Resource Manager closed on the second click in GPUI. Runtime correctly shows host build `bb81b44`, bundled build `5302b14`, one session and Update Runtime in both clients. The older isolated host is retained until its dedicated lifecycle test. Quota overview captured with all default providers; F-014 confirmed. Resource popover horizontal anchoring/density and missing static AX metric text remain observations to verify after the shared scale correction, not reasons to reopen an unbounded Runtime-only investigation.

Workbench sweep: Terminal 2 was created from GPUI's New Tab menu. Tab context-menu labels/order match between clients. A right-edge drag created a split independently in each client, and dragging the sole right tab into the left tab strip ultimately restored a single pane. Early AX snapshots lagged the mutation; the reintegration needs a repeat where the other client stays read-only until completion before being marked validated. No persistent drag preview was observed in this cycle. Mid-drag preview frames, other directions, cancellation and 20-cycle stress remain pending.

Diagnostics/Mobile base states are now captured in both clients. No diagnostics were exported and no crash-reporting, gateway, pairing, reset or external write was enabled. The first cross-domain base-state sweep is complete; it is not completion of the 300 scenario gates.

## Remediation checkpoint

- Shared floating label follow-up: source geometry test corrected notch21px to outline20px, but native before/after detail crops still showed a crossed label. Pinned GPUI Style::paint paints borders after children. The floating label/notch are now siblings after the outline, preserving their absolute coordinates; input_label2 and Root Rename4 pass again. Native76ad176d and matching R1-gpui-label-paint-order-fixed-field-detail/R1-flutter-workspace-rename-before-field-detail confirm a clean outline gap. F003 letter spacing remains a distinct open difference. Crops were generated inside node_repl using pure pngjs/jpeg-js (2x nearest-neighbor); sharp could not load due module signing, no signing policy was changed.
- F047 final functional native evidence: R1-gpui-rename-final-ax contains Role::Dialog; R1-gpui-rename-final-tab/returned-input/escaped show keyboard staying inside, ShiftTab returning to text and Escape preserving Recent Fixture. R1-gpui-rename-root-cancel-focused/cancelled show visible Cancel focus and Return dismissing without sidebar activation. Both clients left Welcome, no drafts. All-section inactive collapse delta and rejected Flutter Tab reference remain explicitly recorded.
- F047 Root regression: updated fixture mounts actual gpui-component Root; Tab fails before fix and demonstrates a gap hidden by the earlier test harness. FocusTrapElement prevents escape, but tracked handles default tab_stop=false so only input cycles until handles explicitly enable it. Both fixes now pass all four Root tests. Shared input notch regression failed at21px versus outline20px, then passes after subtracting the1px border inset; both input_label tests pass. Native replay of Root focus and notch remains pending new build. Other custom dialogs need the same Root-level keyboard audit; do not infer their acceptance from Rename.
- F047 directed batch: initial typing, keyboard cycle/escape, Enter validation/request identity, and inside/outside dismissal pass4 tests. Test-only event traces were removed. Native GPUI30622 was closed from clean Welcome; canonical make gpui-debug started for profile C. Flutter/runtime C remain live. Visible baseline restored by UI (Project, Name/Name, Repeat Pinned on, no pins, expanded parent/projects, empty queries). Inactive allSectionCollapsed stays true: the attempted temporary switch to None for cleanup was tool-rejected as inconsistent with Project baseline, and was not retried through another route. This retained fixture-only delta is not silently reported restored.
- R1 native resumed: display unlocked, same source HEAD3e315a8 preserved. Flutter C28302/runtime C28328 port49874 restored2 projects/9workspaces; personal runtime917 untouched. GPUI build e3a8e64a3a046ca86d7d4269dcb5deb90a035c12046f5beabde242b617f76d51 launched PID30622, same1302x768 captures after titlebar double-click. Fixture project labels changed through Flutter UI to Review Alpha(e64ab0fe) and Review Beta(1e0eec3b) without moving paths. Group None and Recent set in GPUI synchronized to Flutter. Renaming Setup Order(b13a0038) to Recent Fixture moved that linked workspace ahead of main roots in both clients, proving F044 flat-Recent behavior. Rename GPUI focus probe did not accept initial x until the field was clicked; separate regression pending. Normal None Name has a tie-order difference between the identical main labels, and the View Options/row geometry still needs measured comparison; neither is silently accepted.
- Historical blocked gate (resolved): the Mac was locked across three goal turns; the user resumed after unlocking. Computer Use is now operational. No visual/release acceptance was inferred during that interval. The complete audit, remaining matrix, release replay,20 cycles and30-minute session remain unfinished.
- R1 sidebar search: exact query Review Child with All collapsed yields only the pinned copy and closed All header in both clients; Repeat Pinned off removes the empty All header. Under Project, the matching regular copy appears on expansion and disappears when Review Alpha is collapsed, while the pinned copy remains. Evidence R1-*-search-all-collapsed, R1-*-search-repeat-off and R1-*-search-project-collapsed-active. Flutter and GPUI searches cleared afterward. Current grouping Project/Recent, Repeat Pinned on; pins and root collapse still require restoration.
- F046 final directed verification: six integrated preference-request tests pass; Flutter runtime-repository plus Drift suites pass6 tests; Rust storage migration tests pass2. All use isolated/in-memory fixtures, not profile C. No public runtime protocol or desktop conflict policy was changed.
- F045 cross-client persistence check: the old JSON fixture made Flutter Drift restore tree/defaults and made GPUI reject flat. The corrected fixture loads through Flutter without losing grouping, widths, expanded workspace ids, selected projects or Source Control roots. Existing desktop last-writer policy intentionally ignores expectedRevision, so no CAS/protocol change was made. Follow-up inspection remains for initial desktopInitialized seeding and ordering/coalescing of rapid preference writes; these observations are not yet accepted defects or completed scenarios.
- WS025 order replay passed on SHA5f537d643dda148056fbfe42700f2dd5b41e2298e703c1874df2d0f8df09b0a2: C-WS025-trace-1 captures Terminal1 followed by active Setup, matching Flutter. Setup then auto-closes and Terminal1 remains. The first Setup Order replay ended before intermediate capture, so only the bounded trace on workspace2dc229a9-1e68-44e1-ac95-879862c7c7e8 proves visual order. Seven directed tests pass. Live delayed-create busy/error and release remain separate gates. The temporary override is restored through the existing projectConfig.remove API after exact fixture/project/path checks, never by SQLite writes.
- WS025 native Setup check: temporary UI override on projecte64ab0fe contains only printf markers and sleep8. Flutter Setup Flutter17da3744-b545-4fd6-9fcb-ed4d5ee406dd opened Terminal1 then active Setup; Setup auto-closed and Terminal1 remained. GPUI Setup GPUI45b5c314-a0be-4ac4-be1f-b4fa59f1304f opened active Setup before Terminal1, then auto-closed correctly. Order is a confirmed F042 remainder. Source now schedules the initial normal terminal before Setup while leaving prompt-agent activation semantics unchanged; directed scheduler regression added. Override originally absent and must be restored after replay. Evidence C-WS025-*.
- F042 native debug success on SHA93661431a215f0b803e8172d3926e82f3f262c7f5ac3fb5fa2f5a16f5504524f: direct typing after entering Step2 populated the branch, Create Another created Review Completion0df857bc-0eba-4852-b324-01ed1c36fdda and Terminal1 gpui-tab-21034-1788128689322, left Step2 mounted with empty fields/No Parent and checkbox on, then accepted feature/next-draft without another focus click. Escape discarded that draft; neither branch nor workspace next-draft exists. Native pwd showed the isolated review-completion directory, not home. Evidence C-F042-*; latest extra guards against opening/changing a busy form still need a deliberate busy scenario, Setup overlap is unit-tested only, and release remains pending.
- F042 source completion fix now reuses select_workspace instead of only assigning an id, preserving the initial-terminal request and workspace-owned cleanup. Create Another remains on ManualSettings, resets fields/parent/reuse mode and focuses the new branch; fresh dialog entry and mode/project/back mutations are guarded while creation is busy. The pending initial-terminal intent waits while Setup creates its tab and is consumed once a normal terminal exists. Six directed tests pass for identity, draft/focus, pending-terminal scheduling, path filtering, empty-source routing and row geometry. Native replay of this batch is next, not yet accepted.
- F041 native debug replay on SHA7c6d98458bdffdfbf7e92452f8431ca6df1fa0ce21b27b41fdd326bc58cf44c4: path query now retains the correct project, same stable ordering, no text overflow or squeezed radio, and matched 1302x768 screenshots show the selection dialog close to Flutter geometry. Tab navigation from search through the project/segments followed by Space changes Existing Branch. C-F041-* evidence saved. Shared radio now uses the exact bundled Lucide glyph; no API/dependency change. Existing Branch also confirms WS023 missing manual-input fallback, tracked separately. Native release, high-cardinality scrolling and remaining typography precision are still open.
- F041 filter regression failed with an empty result before correction. The updated filter includes case-insensitive trimmed name/path and Flutter's name/id tie breakers; its test and the GPUI long-copy geometry regression pass. Source now preserves an 18px indicator slot, uses the exact Lucide circleDot/circleCheck glyphs, truncates both lines, bounds project/branch lists and supports Enter/Space row/segment activation. Manual selection frame uses Flutter's width/padding/header and selected surface. Native build/replay is in progress; tests do not close visual acceptance.
- Current profile C supersedes the process/path assumptions in older checkpoints below. Previous temporary fixture/evidence root vanished and no previous Alera processes remained. New apps/evidence: `.dart_tool/gpui-review-20260830.4ve14G`; data/fixtures: `/Users/leynier/Library/Application Support/dev.leynier.alera.review20260830c.dev`. One GPUI PID27737 and Flutter PID33740, both titlebar-double-clicked to the same native-display size. GPUI Start launched isolated runtimePID28948 at port50700 from its bundled sidecarSHA6f44ec08; lsof confirms Flutter connects to that port, no second host. No personal runtime was started.
- C native clone revalidation: Flutter and GPUI cloned the local bare fixture into clone-flutter and clone-gpui, both clean HEAD0d7dd1286292d4a55029d79e8d5d051031564ba2. Read-only inventory has exactly two projects, ids1e0eec3b-4176-40a4-acf2-f73209acc07d and e64ab0fe-d831-4f57-9c17-18ebf09896b2. Invalid-origin attempts left failed-flutter/failed-gpui absent, no staging siblings and no registrations; both clients recovered. Native Cancel before submit discarded drafts; GPUI direct typing after switching either mode reaches its visible field. Evidence C-*-clone-*, C-*-failure-* and C-*-cancel* in the C evidence directory. Release GUI and sustained loading comparisons are not closed by these debug results.
- Safe live B restart completed through GPUI: the sole fixture zsh3840 had no child processes and exited normally via typed exit, automatically removing its terminal tab. host status reported zero sessions/agents; non-forced Stop removed PID99393 while A35328 and personal hosts14402/59843 stayed alive. After packaging the final sidecar SHA6f44ec085142a514e8bdf987e042b628328b8f63de3c993f88c02e1417c51bb3, Start launched PID40147 from GPUI B Resources/alera/alera and recovered all three projects with zero sessions/agents. Current control file is host.json, not runtime-host.json. Flutter is still closed to avoid restarting an older host. The GUI's bundled build label60d892c differs from host3e315a8, so its update badge is a packaging-metadata gate; Update Runtime was not invoked.
- Related F040 lifecycle regression reproduced soft host.shutdown accepting an active clone. Source now counts clone jobs for Stop/Restart and retains a cancelling job in the activity map until worker cleanup finishes; duplicate cancel remains idempotent. Actor tests are running. The release b0f4 bundle already has ownership-safe staging but not this latest busy-guard addition, so a final sidecar rebuild is required before live acceptance of the complete batch.
- F040 release sidecar built via make cli-help into the isolated runtime-safe-clone bundle, SHA b0f4a3b9f124a760c0583fb2b9acead4f395af7a5d975d239a487cc797b5bda9. The eleven clone tests pass. F039 now transfers focus on both mode switches; its directed GPUI test passes. Native B still uses the old host; its GUI bundle resolves Resources/alera/alera without a global CLI override. Safe live validation must update that resource and restart only the isolated host after its fixture sessions end, without Force Stop or touching unrelated hosts.
- F040 eleven directed clone tests pass on macOS: competing exclusive publications, refusal to replace empty destinations, marker/symlink protection, legacy recovery, the original sentinel regression, command construction, and real Git worker success/failure/cancel-before-spawn. The live B host99393 still runs the earlier binary; no claim of deployed/live acceptance. Linux/Windows implementations were source-checked, not executed. Next: build sidecar and perform a safe isolated lifecycle/reclone validation without stopping unrelated runtimes.
- Initial staging tests exposed a canonical-path mismatch between /var and /private/var: both competing publishes were refused before reaching the OS move. Publish now canonicalizes the requested parent and checks it against the staging owner before constructing the final path. The original competing-destination sentinel regression already passes with the new implementation. Added real-worker tests for clone/register success, competing-destination failure and cancellation before spawn; all are isolated and currently running, not live-host acceptance.
- F040 isolated regression failed before remediation: the competing job's sentinel was deleted by destination cleanup. New source clones into per-job staging with an ownership marker, publishes without replacement, and only removes owned staging on failure/cancel/recovery. Published/legacy destinations are retained. Exclusive publish uses the OS primitives documented by [Apple](https://developer.apple.com/documentation/foundation/urlresourcevalues/volumesupportsexclusiverenaming), [Linux man-pages](https://man7.org/linux/man-pages/man2/renameat2.2.html) and [Microsoft](https://learn.microsoft.com/en-us/windows/win32/api/winbase/nf-winbase-movefileexw). Windows adds only the FileSystem feature to its already-pinned windows crate. Source also hydrates the clone command from the login-shell environment and disables terminal prompts. Directed clone tests are in progress; the live runtime B remains the old binary and no live competing clone is attempted.
- WS003 native success: Flutter created Review Clone Flutter (f82428d5-cfa0-4b94-965b-062331db426a) and GPUI created Review Clone GPUI (7057242e-e791-4cd7-a535-a5daf609b782), using the same local review-origin.git into separate previously absent directories under the fixture root. Both clones resolve HEAD1915de8d1d7a96d82551c3ff1a9e0401a7ed0bb6 and registered once, returned to Welcome and showed Project cloned. No external remote/credentials were used. The transient progress state was not captured; mode autofocus F039, failure/cancel/release remain pending.
- Native2078232e verifies the final modal fix: Overview stays within the dialog, dragging the prompt produces visible gray selection matching Flutter, outside-click cancels only the editor while keeping the catalog, and Project menu selection keeps its editor open. The temporary Project=None draft was discarded. This closes the bounded width/highlight/nested-layer regression, not clipboard/native long-selection, typography precision, all menu variants or release. F037 remains tool-blocked and was not retried.
- Nested-modal regression initially failed with the lower layer receiving the outside click (10 vs1). The final surface now measures panel bounds and dismisses only a bubbled backdrop hit; both real-surface selection and top-layer-only dismissal tests pass. This supersedes the interim on_mouse_down_out implementation, whose capture order was unsuitable for stacked modals. Native revalidation/build is in progress; no final selection approval yet.
- The real modal-selection regression failed with empty text before replacing the inner mouse-down stop with an outside handler; it now passes. Outside dismissal consumes only its event, preventing an editor click outside from also closing the underlying catalog; a nested-modal regression is included. Interior events can now reach the framework selection layer. UI selection uses its own gray40% token and terminal selection remains unchanged. Native repeat still requires rebuilding; do not count the previous no-highlight drag as passed.
- Native1dfbf95e fixes Overview width, but a prompt drag still did not select inside the custom modal. A new regression using the real modal surface reproduced an empty selection: the inner stop_propagation blocked gpui-base's bubble-phase selection start. Source now dismisses on outside mouse-down instead, leaving interior events available; selector menus explicitly consume their own mouse-down to preserve parent dialogs. The framework UI selection token is also separated from terminal selection: Flutter MaterialApp uses primary e0e0e0 at40% opacity. Native selection/error/dropdown repeat is required after this batch; terminal palette/overrides are unchanged.
- F036 integrated-width regression reproduced the native overflow before the fix: detail toolbar width1288 at x273 inside a1140-wide frame. Source now constrains catalog/frame/columns/detail minimum widths. Plain selection now subscribes to change refresh and paints projected ranges with the selection token, with a wrapped-range regression; native highlight and width repeat are pending. F037 scope validation was also changed to the visible detail record rather than a potentially stale catalog revision, without retrying the tool-rejected native action.
- F038 native debug30a963be duplicate-slug regression passed: entering review-one-time in Review Form Parity produced the visible error while the editor retained all fields. CLI confirmed both persisted records unchanged after rejection. Correcting the slug and retrying succeeded, closing the editor; the fixture is now revision2/draft/approvedRevision=null/runs[], with the same data. Evidence F038-duplicate-slug-rejected and F038-retry-succeeded. Deterministic delayed-save/close races and release remain pending.
- Native build30a963be: Runs and Audit tabs work, including Right-arrow navigation; Audit displays the full actor. Overview overflows the catalog with populated content despite the isolated info-panel test, so integrated width constraints require a new regression. Computer Use rejected the GPUI Run Now click as a potential immediate execution; the action was not retried or routed through another UI path. Native F037 remains tool-blocked even though source routes through a choice dialog. No Run/approval was submitted. Continue source/headless and unrelated native cases.
- F036 source now adds Overview/Runs/Audit with keyboard focus, full metadata/prompt preview/effective policy/timeline, stable run controls guarded by selected automation and target identity, readable structured audit actors, and scoped detail loading/error/Retry. Plain selectable prompt text uses gpui-base selection primitives already present transitively at the identical pinned revision; this is a direct dependency exposure, not an upgrade. Seventeen Automation tests passed before the new plain-selection/long-panel tests were added. Neither F036 nor F037 is in the currently running cfd92ac6 binary; native validation remains pending.
- F037 reference Run Now chooser was opened and cancelled natively; its final Run was never pressed. Source now routes GPUI Run Now/Pause to owned choice dialogs, mutually exclusive draft-test/exact-revision options and captured revision/selection validation before dispatch; only final submission calls the existing request boundary. Native GPUI build is still cfd92ac6 without this fix. Directed tests are in progress; chooser visual geometry, overlap-menu fidelity, focus/cancel and no-run native postconditions remain pending.
- F031 cross-client read and reopening verified: Flutter reloaded the catalog and showed the exact newly created fixture values (F031-flutter-new-record-loaded). GPUI reopened its editor with the same Unicode description,2031 date, terminal IDs, Setup parallel, queue4 and retries2; Cancel left the record unchanged (F031-new-record-reopened/cancelled). Both clients are back in the catalog, no unsaved Automation drafts. This bounded positive path does not close other targets/calendars, negative paths, live refresh, stress or release gates.
- F031 native creation now succeeds entirely through UI in SHA cfd92ac6: Review Form Parity (08c5e46f-8fd7-48f0-a3cc-a4be7f1f6cd2), three-line Unicode description, explicit Alpha project, oneTime2031 UTC, existingTab Terminal1, synthetic conversation ID, setup parallel, queue4 and retries2. CLI show confirms revision1/draft/approvedRevision=null/runs[]. This is a real native creation pass, unlike the earlier CLI fixture setup. Flutter read/reopen and other schedule/target/error/race/release variants remain pending. No approval/run/notification toggle was invoked.
- F031 native SHA cfd92ac6: Material triangle and full-width selector are present; Up/Enter changes only the local Project draft, Enter reopens from restored trigger focus, Escape closes only options, and Cancel discards the probe. Thirteen Automation tests pass including per-menu nonce rejection. Debug build with CARGO_INCREMENTAL=0 completed in1m08 after the previous low-CPU build was stopped; no cache/runtime/data was removed and the attempted sample produced no report. New UI creation/save still pending; remaining menu padding/hover, long-list, toggle animation/drag and release deltas are not accepted yet.
- F031 first native full-form pass (d6d35c9) exposed collapsed multiline heights, 34-versus-48 selector density, unnamed AX selector buttons and content entering the header while scrolling. Source now fixes multiline bounds (75/117 px asserted), selector sizing/labels and an outer scroll clip; native repeat is pending. Escape cancels only the editor and retains the catalog. Project display discrepancy traced to existing runtime contracts: list infers Alpha from target while show returns stored projectId=null; Flutter edits the list record. GPUI now does the same with a captured-ID guard. No fixture save occurred in this pass. Cupertino Notify On Success rendering now follows Flutter macOS defaults, but animation/drag and native geometry are not yet accepted.
- F031 full editor source now replaces the seven-field interim form: a single field catalog seeds input entities, editor epochs scope selectors/profile replies, and all Flutter schedule/target/policy fields are editable. Four directed tests pass for every target/schedule round trip, metadata/policies, validation/clamps and drawing all inputs with independent Unicode drafts. The catalog remains behind the editor modal and Escape/outside cancel only the top editor. Native visual/persistence/keyboard checks are pending; Notify On Success still needs comparison with Flutter's macOS adaptive switch. The transient preservation-only module was replaced, not a user file. No automation was approved or run.
- Debug SHA068fceaf revalidates the Canvas integrated-width fix: all header actions, cards and wrapped question stay inside the 460 logical-pixel panel; native scroll reaches Actions without horizontal clipping. Matching Flutter top/bottom captures still expose line-wrap/density differences, so full visual parity stays open. Close Canvas now opens a semantic Dialog and Cancel retains the live pinned Canvas in both clients (F028-gpui-close-confirmation/cancelled and F028-flutter-close-confirmation/cancelled). No close/removal mutation was submitted. Twelve directed Canvas tests passed for this build; release, history, delayed actions and remaining typed controls are pending.
- Computer Use activation handling: an AX toolbar click while GPUI was inactive only activated the window and left Canvas selected. The exposed window Raise action activated it, then the same fresh Explorer control worked. This is recorded as an interaction/tool precondition, not a new context-sidebar defect. Raise the target window before native scenarios instead of interpreting an inactive first click as a failed callback.
- Debug SHA294bac24 natively fixes F033 in both Automations and Settings/Text Actions: each divider moves to the release point and was dragged back to its original position. F030 Open Search now switches to Search, and FileReferenceList opens src/reveal-review.txt as a persisted editor tab with the expected Unicode content. No file edits occurred. Canvas layout still overflowed horizontally in the integrated scroll container; source now constrains frame/columns/header/document width and adds a full populated-scroll/header geometry regression (passed), pending another native revalidation.
- F030 native before-fix: Open Search in the populated GPUI Canvas leaves the Canvas panel active (B-canvas-open-search-before-fix); the previous code only sent the validation RPC and did not dispatch a local UI/controller action. New source separates passive cards, document controls, detail toolbar and typed UI actions: all ten renderers, runtime-owned decision IDs, real navigation/file/clipboard callbacks after acceptance, controlled/destructive confirmations, and scope/revision/root guards. Header action IDs are scoped by canvas; no new arbitrary command or URL executor was added. This source batch is not yet built or natively accepted.
- F034/F035 native save in B succeeded: GPUI changed only the name to Review One Time Edited and showed Automation saved. Runtime record eb9df59e-8e5e-4c67-8cdb-e84435f93513 is revision2/draft/approvedRevision=null; schedule remains oneTime 2030-01-01T00:00:00Z/UTC and target remains existingTab with the same workspace/tab/conversation IDs. automationRuns is empty. This closes the bounded oneTime/existingTab edit path, not new-form coverage, managed-workspace variants, delayed races or release.
- The coalesced-single-move GPUI regression reproduced F033 at width240 instead of300 before the fix; both resize tests now pass after the shared grip applies final release coordinates. Native revalidation still requires the next rebuild. All review.resize traces have been removed from source.
- Profile B fixtures are live through supported CLI/UI boundaries: project e4419ec8-8543-4a01-bf4d-6034596fa8f3, root workspace 0fcf91b0-e48a-4397-a304-b7115871a2ca, terminal gpui-tab-1990-1788100855467 created by clicking the GPUI workspace and attached by Flutter without another session. Canvas eec831b0-cf9d-4c9c-b8c8-42490db6b1bc contains the ten-component fixture. Native GPUI resolved decision 3b7a6cfe-5cfe-4420-9c81-665d8495acfa to Editor, then Pin succeeded; runtime confirms resolved/pinned=true. This is synthetic Canvas metadata, not a running Codex agent. Generic-card visual/local-action gaps remain open.
- Automation fixture eb9df59e-8e5e-4c67-8cdb-e84435f93513 was prepared via CLI in B for the edit regression: draft, revision1, approvedRevision=null, oneTime 2030-01-01 UTC, existingTab. No Approve/Run Now was invoked. Its creation is fixture setup, not a native UI-creation pass. A native edit/save must still validate F034/F035.
- F033 cause narrowed with a failing GPUI event regression: one coalesced MouseMove arms the drag in bubble phase but has no earlier DragMove capture callback; ignoring the final MouseUp position leaves width240 instead of300. The grip now applies release coordinates before finishing. Temporary review.resize instrumentation was removed without a native instrumented build; no claim of collected native trace logs. The ordinary context-sidebar resize did work and both clients share width460 for the populated Canvas comparison.
- F032 native: SHA4cd92302 opens Automations directly from cold Welcome via Cmd+Shift+A. This supersedes the prior Welcome key failure, not the complete keyboard gate. F033 native drag STILL did not resize after the first grip change; do not count it as fixed. Source now relies solely on on_drag_move ownership instead of a redundant raw pressed-button flag, with a no-button-flag continuation regression. Native recheck remains required.
- Focus-trap accessibility trace: the pinned gpui-component FocusTrapContainer forwards layout/paint but not Element::a11y_role/write_a11y_info, so wrapping a role-bearing Dialog drops its semantic node. Source now keeps the modal semantic wrapper outside the focus trap in Automations, Text Action Delete and Canvas confirmation. Keyboard focus trapping remains; native AX revalidation is pending. No dependency was updated.
- Debug SHA 0e0bc9d6686ce51b0dd1bb039b82d65f4aa8d2c57a7f7ecd22cc463a9ea66c04 now revalidates F010 Provider not detected with header/Refresh/centered copy, plus F011 full-width empty Canvas and disabled Active menu. Native Refresh settles back to unavailable; disabled Active does not open options. Matching full screenshots and 650x220 panel crops retained. PR007 debug validated; populated Canvas/history, auth/error variants, all release gates remain open.
- F029 regression failed before adding agentCanvasV1 to control_file.rs; all 5 control-file tests pass after the additive fix, with no protocol bump. make cli-build is building the real release sidecar and native helpers. Live host remains old35328 until the isolated updater scenario; do not bypass negotiation by patching runtime-host.json. Ten-component Canvas fixture is prepared but not yet published.
- Native Quit safety recheck: first request was rejected because Save/Discard appear in AX even when disabled. File/visible content matched and native Save All explicitly reported No unsaved editor files; only then did the normal Cmd+Q succeed. No forced termination of the original comparison UI was used. Disabled AX semantics stay part of the accessibility audit.
- F026/F024 native follow-up on SHA 8f5659ace553e8c93af49c1b3da8f2b8d6bf703a53245f0b768a6f8a1e8e65ca: popup now sits 4 logical px below its trigger, with Flutter-like insets and row density; deselected switch uses the smaller thumb. Cold GPUI retained Copy/Alpha/Beta order and exact fields. Model menu follows discovery order; Antigravity exposes Global () and Agent Default without invented static models. All temporary override changes were discarded by selecting stored Beta, then closing Settings. Remaining typography/row deltas, fx live discovery and all release gates stay open.
- F010/F011 correction batch in source: missing PR unavailable-state header/Refresh restored with 44 px header, shared centered empty presentation and guarded auth retry. Canvas catalog extracted into pure grouping/selection plus its own renderer: full-width empty body, disabled Active/History without history, pinned/waiting/live/history groups and retained selection. Two Canvas catalog/layout tests passed before the subsequent action-epoch fix; native batch rebuild still pending.
- F027 confirmed source race: agentCanvasChanged starts refresh and advances agent_canvas_generation before an action's reply. The action then returns before clearing busy. Separate action epochs/workspace identity now own completion; catalog events during a pending load are coalesced for a follow-up refresh instead of discarded. Focused tests are running; delayed real-bridge/native verification remains pending. Close/Remove still lack Flutter's confirmation and must be corrected before native destructive scenarios.
- F-024 native Beta Copy duplicates and seeds all fields; drag third->first preserves its identity/draft; both Delete confirmation dialogs match closely and Cancel retains all records. Native comparison exposed popup anchoring one trigger-height too low: absolute measurement canvas lacked top/left, so static positioning followed the trigger. Source now anchors to inset0; GPUI drawn-bounds regression passes (7 text_action_ tests). Menu insets/row density and deselected switch thumb also now match the Flutter source contract, pending rebuild/native check. F026 source reconciles discovered-first models, Agent Default for AGY/fx, fx discovery and duplicate model options; directed tests in progress.
- Coexistence observation narrowed: after Flutter restart, the first GPUI duplicate event appeared live, but the subsequent reorder did not. Flutter runtimeSettingsChanges emits repeated void/null values; its Riverpod notification equality may suppress later events. Do not conflate the durable GPUI save with this reference refresh defect. No Flutter settings writes were performed from that stale catalog.
- F-024 GPUI native creation now persists Beta with enabled=false, Codex, gpt-5.4 and medium reasoning. Read-only runtimeMetadata inspection confirms exact values and IDs (Alpha 21857102-4fb3-4c12-a84d-7a285a6c2e74; Beta 3651c367-c3db-4ace-8a96-89f1df04afad). Flutter did not refresh the live list after the write, but cold restart loaded Beta and its exact form values (F024-flutter-beta-restored). Persistence is demonstrated; live coexistence refresh remains a separate failed observation requiring event/provider tracing. Do not perform a stale Flutter settings save that could overwrite Beta. No agent ran.
- OBS-001 discovery recheck: Flutter AI Assist completed discovery and now displays Medium for GPT-5.5, matching GPUI. F024-flutter-ai-recheck records the converged state. The previous Low/Medium difference was stale per-client discovery metadata, not evidence to hardcode another default. Historical bb81b44 also has Low static fallbacks, so the old runtime alone does not explain the earlier observation. F-026 catalog ordering/inherit policy remains a separate confirmed source mismatch.
- F-024 initial native on SHA `06341d771b85745c43f17505eba357a1fe593a85b51a75565730cc72c30b4b54`: Flutter created Review Action Alpha with prompt Return the selected text unchanged., Claude Code / Opus / High. GPUI loaded and auto-selected it with matching values. New GPUI action exposes all controls and shows Action name is required on blank Save. Default reasoning showed Global (Medium) versus the earlier Flutter Global (Low), so discovery cache state is being normalized through the Flutter AI Assist pane before classifying OBS-001. No agent execution has occurred and the only saved action is the isolated fixture Alpha.
- F-006 native empty-state revalidation on SHA `05d08c087e3d3634425d4236e7f080f1ccd9236c1f4547c31a6df23ace4fb8c7`: full-height master/detail, matching New Action and centered states now visible against F006-flutter-empty-recheck. The previous collapsed list/short divider and extra Reset are gone. This rebuild also includes the F-022 fixed scroll gap, pending its native repeat, and F-024 partial map/validation safeguards, pending native mutation coverage. Full Text Actions form parity is not complete.
- F-022 short-window native comparison: both windows resized through Sky to identical 996x644 images; both scroll the prompt-form body to Create And Start Agent while retaining the header. GPUI placed the 20 px segment/form gap inside the scroller, so it disappeared at the bottom unlike Flutter. Extracted workspace_prompt_layout::prompt_form with the gap outside the scroll viewport; the regression confirms its origin stays y=90 and height stays bounded while scrolling. This final gap correction is source-only until the next batch rebuild. Both apps restored with titlebar double-click to full native size. Remaining exact thumb/text precision and picker/keyboard variants stay open.
- F-022 native follow-up on SHA `550d13d2c88ceb01646ec0201c8a2b7f0bf3650d095bd1d42d7f9ab5791f2e2a`: default/growth styling is substantially closer; F022-gpui-final-ten-lines exposes the final caret after paste, manual-scroll-top/stable/bottom shows no snap-back, matching the reference flow. Test textarea_ passes both exact heights and post-frame scroll/manual-scroll behavior; component_theme_ passes. No fatal modal was observed. Track remaining precision/short-window variants rather than marking the whole dialog approved. No files or workspaces were created from this prompt fixture.
- F-022 second native build SHA `76b96941f765b149d1a866761c5b860d98cdfcc1ad29ed9b364003fa59839828` matches measured textarea height and fixes Cancel/reopen reset. Parent dropdown escapes the form's scroll clip and closes on an outside click within the modal. A ten-line paste still hid the final caret; the new failing test traced this to the dependency moving only one row on selection change. The adapter now requests a guarded post-paint scroll correction without modifying selection/focus/IME, and tests pass for final-line visibility and preserving subsequent manual scroll. Source-only follow-up also uses Flutter labelSmall for dropdown captions, fixes checkbox label padding, segment colors and muted placeholder tone. Rebuild/native revalidation still required for this follow-up; no overall F-022 sign-off yet.
- F-022 first native adjusted build SHA `a48e8685021f6b54fe619374e141ce8ed03110db6d38d346062d959f6820a2e6` fixes the major surface/header/segment/label mismatch, but the textarea still double-counted component padding and remained too tall. A Flutter macOS widget probe with the actual Inter font measures 96 px at four lines and 180 px at eight, 21 px line height (Android's density initially gave different values; the final macOS probe passes). Source now compensates internal component padding/layout border, uses those exact heights in a GPUI test, and removes the stale prompt on a new dialog open. Native cancel/reopen reproduced retained line tenX in GPUI versus Flutter's empty form. Captures F022-*-default, ten-lines and reopened-*. Padding/reopen changes still need a rebuild; check cursor visibility and scrollbar travel with ten lines as well.
- F-021 normal native pass on SHA-256 `090c4af06cb446b47b923fdef5b68cb4a18ac73539fbe54648ee35c6d67066ad`: Save All from Beta writes Alpha/Beta distinct drafts and reports two files, a second click reports no unsaved files, and Undo in each bank followed by Save All restores both disk baselines. Flutter independently produced the same two-file save. Flutter's old Alpha probe was deliberately included in its fixture test, then the app was cleanly closed and both fixture files restored before reopening. No old unsaved probe remains. Current UI PIDs GPUI 3022, Flutter 1140, no build/test running. The latest failed-read eligibility guard is source-only until the next rebuild. Matrix expanded to 305 scenarios with explicit normal/error Save All rows.
- F-021 reference native confirms global scope: from Beta's Explorer, Save All writes Beta's new fixture marker and Alpha's old parked probe, then reports `Saved 2 editor files`. Both disk payloads verified. F021-flutter-save-all-two-workspaces is the reference evidence. Undo did not remove the old parked Alpha probe in the preceding attempts; no assumption about reference undo parity was made. The two deliberate Save All writes now require baseline restoration before GPUI comparison.
- Latest verified debug SHA-256 `6f375cc38b22a611edd51e72506cc8fb6baa0077c9b191fe89733368682f7555`; GPUI 97206, Flutter 40523, no build running. F020-fixed-clean-cache-reloaded shows target three matching disk; F020-flutter-clean-replaced-reloaded shows Flutter changing it to target four; F020-fixed-fixture-restored shows GPUI restoring target one in editor and disk. Raw fixture third line retains its tab. conflict.txt GPUI buffer/disk match; Flutter alone retains its old unsaved probe. A Computer Use quit denial was resolved by fresh buffer/disk verification before native quit, not forced termination. A transient make-launched duplicate was stopped separately; comparison again has one app per framework.
- F-020 native clean-cache follow-up FAILED on b98900e8: replacement wrote target three on disk, but opening cached reveal-review.txt still showed target one even in a second fresh capture. Tab switching had stored clean text in editor_buffer_text; invalidation removed documents but not that stale buffer, so reload restored it. Added regression for cached clean buffers, removed them on invalidation, and tab navigation now retains only truly dirty text based on the live value. Fixture disk restored explicitly from target three to target one after preserving failing evidence. Rebuild/revalidation required; do not mark clean reload passed.
- F-017/F-020 current debug SHA-256 `b98900e82b851740589e3738cb1f9ad21d5357bb24369bfa574916f41236c201`, GPUI PID 92758 and Flutter 40523, no builds running. Native duplicate Add Beta expands Beta/Welcome without new rows; Welcome creates against Alpha while sidebar selects Beta in both frameworks. GPUI's Alpha dirty draft survives this activation (F017-gpui-draft-after-add). Replace in file refuses the inactive dirty conflict.txt and leaves disk unchanged, matching Flutter (F020-*-inactive-dirty-*-protection). The per-match GPUI button was offscreen, not a successful action; F-023 records it. No full scenario/release sign-off is implied.
- F-019 follow-up native: after modifying the selected match, clicking another editor tab then the search result retains the cached draft and both Undo groups. Save writes the intended fixture; switching Alpha/Beta and returning preserves history and allows Undo/Save without false conflict. The fixture is clean in GPUI; its modified line now uses spaces (native write semantics) and the unchanged third line retains a tab. Flutter's old dirty conflict.txt probe remains unsaved. Evidence F019-gpui-reveal-cached-draft/restored and native-save/save-return/native-save-restored. UI PIDs GPUI 86245, Flutter 40523; no compilation running. Next F-017, then F-020/F-021. These bounded successes do not close the full F-019 race/release gate.
- F-019 debug rebuilt through make and staged with SHA-256 `6e564468a2186b2341bed84212259ac285bd1d703112e7b7afa825012863f975`. Native Unicode reveal uses the new src/reveal-review.txt fixture with a tab before emoji/accent. Both clients select and copy exactly `😀é` (clipboard UTF-8 hex f09f9880c3a9), without trailing space. Flutter AX reports an extra selected space, but actual clipboard bytes disprove it; this is an AX discrepancy, not a reference defect. Evidence F019-*-unicode-search/reveal. Full delayed runtime race/native autosave and release remain pending.
- F-018 native after-fix evidence: independent gpui_alpha_draft / gpui_beta_draft survive repeated A/B switches; undo/redo retains history; Beta save modifies only Beta; undo/save restores its disk baseline. Closing the inactive Beta file tab from Flutter leaves alpha_remote_close_draft intact in GPUI. Reopening Beta reads a clean baseline and Cmd+Z does not resurrect the retired history. Alpha was restored through undo too. Both on-disk fixtures are back to their baselines. Flutter retains its own unsaved Alpha probe for the reference, so do not save it accidentally during follow-up.
- Current staged GPUI SHA-256: `0d230f8aa59468a204496cbb8b4f2b19068659036664b04bac3a8ba01ca8669f`. One GPUI PID 72068 and one Flutter PID 40523 remain; GPUI restored through titlebar double-click to the native full-size window. No build/test is running. Next priority is F-019, then complete F-005/F-017 and remaining matrix domains. The full goal is not achieved.
- F-018 base correction in source: an EditorWorkspaces bank parks complete editing entities/subscriptions and per-path state under the workspace ID; first files no longer share one global EditorState. Input events validate owner and entity identity. Snapshot reconciliation uses all_tabs and prunes only closed-tab state, including parked workspaces. Workbench/editor mounting is gated while a different workspace snapshot is pending. One targeted GPUI entity/draft-isolation test passes; native revalidation is pending on the running rebuild. Save completion while navigating still needs a document-owned request lane: the existing global editor generation can ignore a successful write's updated conflict baseline.
- F-018 Flutter comparison now confirms preservation: a unique flutter_unsaved_alpha marker remains after Alpha -> Beta -> Alpha with unchanged disk content. The marker was inserted into the baseline line because synthetic Cmd+A did not select the entire CodeForge buffer; the preservation evidence does not depend on replacing the entire file. F018-flutter-dirty-before / F018-flutter-return.
- F-015 native Cmd+Q from the active terminal closed audit GPUI PID 63856; the isolated host remained alive. No get_app_state call was used to verify exit (it would relaunch); process inspection confirmed termination. The next launch is the editor-owner rebuild, not a retry of a still-running app.
- Current F-005/F-015/F-016 source batch: owned AddProjectDraft tracks automatic versus manual values, parent-before-URL, mode changes and picker epochs; shared callbacks cover Enter/focus/blur and restore the invoking focus on dismissal. Inputs retain the maintained editing core and now render inline/float labels with correct normal text size. Clone progress and completion/error toasts follow Flutter's launcher flow. Native Cmd+Q uses the existing MenuQuitApp action, and startup selection no longer chooses a workspace by fallback.
- Directed tests passed: project_ (8), input_label_ (1), native_quit_ (1), workspace_selection_ (1). Build/re-stage and native revalidation are pending for this batch. The original compile error was a missing AppContext import in the new test, fixed before the passing runs.
- Flutter reference evidence: F005-flutter-autoname-keyboard confirms path-derived review-alpha; F005-flutter-manual-name and clone-manual preserve Custom Review; parent-empty-url opens the native chooser before any URL; parent-derived-destination shows /private/tmp/.../fixtures/clone-name after entering a URL. None was submitted. Sky set_value changed AX but not the visible Flutter control; subsequent actual click/type/shortcut interactions provided valid evidence instead.
- F-003 current source batch: `app_fonts.rs` registers static instances generated from the exact Flutter variable files, with preserved default optical size, outlines and license records. Native `check_font_faces --variable-baseline` proved all weights resolved to one face per family; the corrected native example passes 9 Inter and 8 JetBrains Mono distinct faces without a window/runtime. A macOS-only weight-200 adapter corrects pinned font-kit's -0.4 CoreText mapping (~237 CSS), which otherwise chooses the 100 outline. Other user font families remain untouched.
- F-003 type/layout batch: component rem base is 16 px; app root body is explicitly 13 px. Own relative sm/xs/lg/2xl styles were mechanically mapped to Flutter roles 13/12/16/22 px across 48 UI files (Codex Chat excluded). Welcome uses its measured container width, Flutter's inner 760 px breakpoint, constrained/scrollable vertical centering and content-sized cards. These current source changes are not yet native-app revalidated. F-005 copy, 600 px dialog width and shared 10 px button radius were also aligned, but label/default behavior remains open.
- F-003 first native rebuild: `F003-gpui-welcome` / `F003-flutter-welcome` show actual semibold/bold faces and aligned column widths; remaining vertical drift led to an effective Flutter theme probe with Inter loaded. `.dart_tool/gpui_theme_probe_test.dart` passed after putting font I/O under tester.runAsync: Material inherits height 1.43, yielding body 13 -> 19 px, caption 12 -> 17 px, headline 22 -> 31 px. A native-font-metrics-only 1.21 leading was rejected before launch. Source now uses 1.43, explicit Markdown 1.45 and PR-comment 1.4, 31 px workspace tile floor plus its existing outer spacing, and inline workspace metadata icons. Rebuild/revalidation follows.
- Font regeneration was repeated and all 17 TTF hashes were byte-identical. Root and previous GPUI variable font source hashes also match, so this is not a font-version substitution. `typography_` and `welcome_` targeted tests passed one case each. Remaining type detail: Flutter inherits +0.25 letter spacing for body text and uses -0.3 for the welcome headline; pinned GPUI has no ordinary style field for tracking. Keep the width delta open rather than reducing the font size to disguise it.
- Fixture preparation for Welcome: Flutter created a terminal by selecting the non-Git review-notes workspace, then its tab was closed from the tab strip. The idle dot cleared and Welcome appeared; no files or agent work were deleted. Both clients remain in the audit profile. The source/native/runtime gates for the full terminal lifecycle remain separate.
- F-003 second native build aligns the welcome header and card boundaries closely after effective leading correction; captures F003-gpui-leading and F003-flutter-welcome retain the remaining tracking delta. Native border drags then produced matched 997x646 screenshots, confirmed by image dimensions and 300 px sidebars. Both stack columns; Flutter scrolls to the final shortcuts, but GPUI did not move or show the scrollbar. F003-gpui-short-bottom versus F003-flutter-short-bottom is a failed gate, not a pass. A targeted GPUI scroll regression and source fix are in progress; full-height screenshots alone missed it.
- Sidebar row trace also found block-flow margin collapse: Flutter uses vertical Padding for each row, whereas GPUI put rows with vertical margins in a block container. Source now uses a flex column and non-shrinking row/header/agent containers; native cumulative spacing still needs revalidation.
- F-003 final narrow revalidation passed the previously failing scroll path: intrinsic content is non-shrinking and the scroll owner has an explicit stable ID. Two targeted `welcome_` tests pass, including a GPUI wheel/drawn-bounds regression. Native `F003-gpui-scrollfix-bottom-matched` and `F003-flutter-short-bottom` are both 997x646 with 300 px sidebars and visibly expose Split Right plus the scrollbar. The initial UI failure is superseded only for this path; remaining matrix variants are not approved automatically.
- Current staged GPUI executable SHA-256 is `87dfbb4928a6615fb88ec7c3ebc73075c681ba030567afb2f38300c4ce0fae6b`; current UI PIDs are GPUI 55824 and Flutter 40523. Both were restored through titlebar double-click to 1302x768 and neutral-click captures saved as F003-*-final-neutral. No compile/test process remains active. Existing runtimes were not stopped, and no fatal modal was observed.
- First source batch: F-001 focus restoration and F-012 source-editor search navigation, with internal-only changes and no protocol/schema changes.
- Targeted command: `cargo test --manifest-path experiments/alera-gpui/Cargo.toml --bin alera-gpui --features gpui-tests --locked search_ --quiet` passed 13 tests, 0 failed. The full suite and native revalidation are not yet rerun for this batch.
- Second source batch: F-002 request ownership and editor snapshot, F-008 registry/overrides, F-004 search width, F-014 provider artwork/labels and F-009 dark paint-token synchronization. Automations and keyboard targeted suites passed four tests each; icon/label/theme and native revalidation follow.
- Additional targeted checks passed: `component_theme_` (1), `quota_` (4), `colored_provider_` (1), with `--features gpui-tests --locked`. No broad suite or release sign-off is implied.
- Native before-capture `TE-011-gpui-before` confirms F-004 on the isolated baseline: Cmd+F opens a nearly zero-width field. Quit via the native app menu then stopped only audit GPUI PID 41252; Flutter stays open. Cmd+Q did not quit from this context; F-015 records the missing binding. Rebuild uses `make gpui-debug` with the audit app ID and runtime directory.
- Flutter search recheck: snapshots `TE-011-flutter-reference` and `TE-011-flutter-reference-focused` did not show a search overlay after synthetic Cmd+F; AX retained a quota overlay that was absent from the screenshot. These captures are not positive terminal-search reference evidence. Resolve actual activation/AX freshness before retrying that comparison.
- Dependency authority: active gpui-component Git revision is `6d07863fe7077f85abfa0ec2fcb05f3e17c573b2`; the old vendor tree is not a Cargo dependency. Read the resolved checkout when tracing APIs. The pinned macOS text system chooses registered faces but does not instantiate variable weight axes, so F-003 also needs actual face-weight verification, not only rem adjustment.
- Rebuild completed through `make gpui-debug` using the audit app ID/runtime directory; source compiled with no new warnings (existing dependency future-incompatibility notice for block 0.1.6). The generated launch PID 1997 was stopped and its signed bundle copied to the unique staged path before relaunching through Sky. Current GPUI PID 3965, Flutter PID 40523, one each; existing hosts 14402/35328/59843 remained running. Re-resolve PIDs next time.
- GPUI staged executable SHA-256: `1a1f1617c3ac765221b8705387b5598e93d1ce11408f170e3b394545fbb88747`. Native titlebar double-click toggled smaller then back to the matched 1302x768 capture. Final clients show the fixture readme Markdown with no fatal modal observed.
- Native batch: F-001/F-004 active terminal search, F-012 source/selection, F-008 Open Automations from terminal focus, new-editor draft preservation/cancel, F-014 artwork/labels and F-009 dark paint were exercised. No automation was saved/executed, no reset was consumed and no external writes occurred. Remaining variants are tracked individually, not counted as passed.

## Progress

- Complete: goal creation, baseline inventory/crosswalk, catalog expanded to 301 scenarios, isolated runtime/bootstrap, bundle identity and actual storage verification.
- Complete: first cross-domain base-state code/UI sweep and paired evidence; fixtures now include three projects, six workspaces, two terminal sessions and a source-editor preview tab.
- In progress: prioritized source fixes and native debug revalidation; detailed variants remain open in the matrix.
- Pending: F-003 font scale/weights, shared widgets, intrinsic Markdown tables, remaining findings including F-015, full per-scenario states/actions, delayed bridge races, release replay, broad quality gates, 20-cycle stress, performance/30-minute soak and cleanup.

## Results

SH-001 retains visual precision gaps (F-003); release remains pending. FS-013's bounded normal-navigation debug case is validated with native evidence. Other scenarios remain independently tracked; the review is not complete.
