# Shared Checkout Precheck Validation

This continues the evidence and pending work in [the implementation tracker](shared-checkout-implementation.md). It does not change the approved scope or its unfinished status.

## Restart Migration

The native linked precheck enrollment test was extended to reopen its owner database. It failed because migrate_workspace_checkouts imported projects.repoPath into a local project checkout every time that association was absent. For a newly enrolled legacy bare origin this silently created a principal after restart. The failing evidence is /tmp/alera-shared-checkouts.Gk4ThK/linked-precheck-reopen-before.log.

The legacy project backfill now writes checkout.projectBackfillV1 after migration, atomically with nonempty backfill changes. Later owner projects without a main association remain unassociated on reopen. Existing workspace binding backfill still preserves legacy task records. A dedicated regression models an old database without the marker, imports its empty project exactly once and verifies a subsequently enrolled owner project remains without a principal.

The linked owner reopen test, isolated legacy migration test, full 405-test core suite and final migration test passed. The new migration test was added after the full suite compiled and ran separately against the final source. All-target Clippy, the unrestricted size ratchet and git diff --check passed. Evidence is under /tmp/alera-shared-checkouts.Gk4ThK in linked-precheck-reopen-after.log, checkout-backfill-core-tests.log, checkout-backfill-migration-tests.log, checkout-backfill-final-tests.log and checkout-backfill-clippy.log. No generated or exposed FRB API changed, and no validation, fixture or reviewer process remains active.

## Pending Integration

- Enable Home linked dispatch after negotiating owner support and validating physical coordination. Home now constructs exact linked metadata, including hosts with no main association, but its pre-launch refusal remains. The owner RPC has since been enabled as recorded below.
- Actual SSH acceptance must cover later main-checkout registration after initial bare-origin enrollment; native and store regressions now cover this transition.
- Verify physical retirement and relocation coordination using actual isolated SSH commands before removing the linked execution gate. Rebuild the stale release binary before SSH acceptance.
- Complete same-boot owner recovery, native platform acceptance, the remaining original requirements, final validation, fixed review loop and user-authorized delivery/release steps.

## Later Principal Registration

Added register_owner_project_checkout to atomically persist the first owner principal and update the project path while preserving its name and linked bindings. It rejects a different existing principal, a different project type and a path already owned as a linked checkout; rejected changes roll back together. Native task enrollment and project-folder precheck enrollment use this operation. Linked precheck retries continue to use their retained origin after the principal changes.

The core regression passed after correcting its expected task to use persisted millisecond timestamps. The native enrollment regression now registers a real main checkout after bare-origin enrollment and verifies that retrying the linked precheck does not revert the principal or its legacy binding. All 29 CLI precheck tests, six remote owner registration tests, all-target Clippy, unrestricted size ratchet and git diff --check passed. Logs are owner-principal-registration-tests-2.log, owner-principal-precheck-tests-2.log, owner-principal-workspace-tests.log and owner-principal-clippy-2.log under /tmp/alera-shared-checkouts.Gk4ThK.

The initial precheck suite exposed a timing-dependent fixture: its location scenario changed the checkout while the native precheck could still hold its closure fence. The scenario now waits for the completion message before changing location, then passes that message to the actor, verifying revalidation before task allocation without racing a correctly protected live process. The initial failure is retained in owner-principal-precheck-tests.log. No generated or exposed FRB API changed and no test, fixture or reviewer job remains active. Home linked metadata, actual SSH coordination and final review/delivery gates remain pending.

## Home Envelope

Home now builds the owner envelope from the retained precheck scope. It loads the exact source workspace and binding, rejects changed instance, host, path, project, status or repository origin, and sends the task record only for linked execution. An existing owner principal supplies project metadata; absent that association, the retained legacy repository origin supplies it without writing a checkout registration. Project-folder requests still omit workspace metadata.

The isolated envelope regression covers absent and present owner principals, preserved source identity, replacement instance, changed origin, changed host and the project-folder payload. It passed along with all-target Clippy, unrestricted size ratchet and git diff --check (home-precheck-envelope-tests.log and home-precheck-envelope-clippy.log). No command was launched by this test. No generated API changed and no validation job remains active. The gates still need to be replaced by capability-aware linked dispatch and actual SSH acceptance. Also verify legacy bindings with missing repositoryPath using native ownership evidence, never by silently guessing a principal.

## Linked Owner Capability

The native CLI now requires linkedOwnerAutomationPrecheckV1 for an envelope carrying exclusive linked scope; project-folder operations retain ownerAutomationPrecheckV1. It uses the existing connect-with-required-capability path, so an incompatible live host is preserved instead of replaced. The new capability is deliberately not advertised until the linked RPC implementation is enabled with physical coordination acceptance.

The actual socket regression runs the linked envelope against a host advertising only ownerAutomationPrecheckV1. It confirms the new requirement is reported, the connection performs only hello, the control file is unchanged and no runtime database is created. Seven owner CLI tests, all-target Clippy, unrestricted size ratchet and git diff --check passed (linked-precheck-capability-tests-2.log and linked-precheck-capability-clippy.log). No generated API changed and no validation process remains active. Home still needs pre-launch capability discovery and removal of its linked-path refusal; the runtime must advertise the capability only when its linked gate is removed. Actual SSH linked/legacy execution and the original acceptance/review/delivery gates remain unfinished.

## Physical Precheck Aliases

Linked-worktree retirement now checks pending owner reservations and unresolved native prechecks by host and checkout path as well as source task ID. This prevents an alternate project registration from bypassing the physical-storage fence. The lookup uses the bound checkout path when available. Shared task retirement retains its task-only check because it does not remove the shared directory.

The owner-scope regression includes another project registration of the same linked path and the identical path on another host. The former is blocked while pending and claimed and released after verified no-spawn closure; the latter remains independent. Targeted validation is in linked-precheck-physical-alias-tests-2.log, with Clippy in linked-precheck-physical-alias-clippy.log. This is store-level evidence; actual SSH retirement/relocation and path-alias platform acceptance remain pending before linked RPC activation.

## Native Linked RPC Activation

The owner runtime now advertises linkedOwnerAutomationPrecheckV1 and accepts linked precheck requests through the existing exact reservation, native ownership/declaration validation and independently supervised process flow. Its runtime mutation barrier blocks new Start requests during mutations; retirement preparation checks pending and claimed prechecks before changing files or stopping task sessions. Status and cancellation remain available.

A real Git worktree regression starts a native command, attempts managed-workspace retirement preparation while that command is reserved, verifies the precheck error and preserved directory, releases the command and observes closureVerified with the exact workspace instance. An idempotent Start returns the stored result with one command execution; the principal task, linked task and principal branch remain intact. The isolated RPC test and four control-file tests passed (linked-owner-rpc-tests.log and linked-owner-control-tests.log). All-target Clippy passed (linked-owner-rpc-clippy.log). All 31 CLI precheck tests passed against the final source (linked-owner-precheck-suite.log), as did the unrestricted size ratchet and git diff --check. No generated API changed and no test, fixture or reviewer job remains active. The updated regression releases its fixture command before assertions about rejection to avoid leaving a test process behind after assertion failure.

Home still refuses linked launch until pre-launch capability discovery is connected. Actual SSH linked/legacy execution, interrupted transport and physical relocation acceptance remain pending, as do final platform validation, review and delivery. The earlier notes about the owner RPC gate and unadvertised capability describe superseded intermediate states.

## Home Linked Dispatch

The owner CLI now has a probe action that validates the checkout and checks the required capability against an existing runtime without starting one, opening its database or registering a command. With no runtime, the current binary reports its supported capability. The reply echoes the exact request and states whether a runtime is running. Its no-runtime regression passed without creating the requested state directory.

Home uses that probe before recording a linked precheck process intent. It derives the exact workspace and repository scope, requires a valid linked capability response and reuses the same operation ID for the transactional launch intent. If the reserved scope or location differs from the probed scope, Home records SpawnFailed before sending any command. The unconditional Home linked-path refusal has been removed. Missing legacy repository origins still require native verification instead of being guessed. Project-folder prechecks retain the previous path and do not require the new probe action.

All 32 CLI precheck tests and all-target Clippy passed (home-linked-dispatch-tests.log and home-linked-dispatch-clippy.log); the unrestricted size ratchet and git diff --check passed. A final no-command location mismatch guard was added after the test build started and was checked by Clippy; end-to-end SSH acceptance must cover the completed source. The release CLI rebuild passed in 65 seconds (home-linked-dispatch-build.log) and staged the binary through make cli-build. No build, test, fixture or reviewer process remains active. Next add linked and legacy SSH fixture cases, including scope preservation, cancellation, retirement rejection, absent principal and lost responses; run them with the rebuilt binary. Earlier notes about the Home refusal are superseded by this section. Final acceptance, review and delivery remain pending.

## SSH Linked Acceptance

The previous OpenSSH acceptance suite passed with the rebuilt binary, SHA-256 24a16e43899165f101580b472ad7dc4eebf9462d43ae51f24792f0758d0296df. Lost Start response, timeout and Home restart preserved a single execution and verified native closure; restart cancellation took 64.73 seconds under the existing scheduler tick. The suite also passed its registration, clone, terminal, retirement and stable-identity relocation cases and closed its fixture runtimes, sshd and processes. Evidence is ssh-linked-precheck-baseline-1.log and ssh-linked-precheck-baseline-1/result.json.

Added a real linked Git precheck target to the SSH fixture. It creates a principal and linked task through the CLI, uses FreshTab precheck scope, checks exact owner instance/repository evidence, attempts retirement while the precheck is active and expects the existing task to remain after cancellation. The first expanded attempt failed before command execution because a fixture loop reused the removed initial task variable as its target; the variable was renamed. Teardown completed in ssh-linked-precheck-acceptance-1/result.json. The corrected attempt passed with completed=true and ssh_precheck_linked=true in ssh-linked-precheck-acceptance-2.log and ssh-linked-precheck-acceptance-2/result.json. The same binary SHA was used. It verified the retained linked instance/repository, one command execution, cancellation closure, a rejected retirement attempt and one retained task on Home and the owner. All existing cases also passed; Home restart cancellation took 64.79 seconds. Runtime stop, sshd teardown and fixture process closure were verified. No fixture or validation process remains active.

The linked acceptance case currently covers a registered principal and cancellation. Legacy bare/no-principal SSH enrollment, linked lost-response/restart variants, exact retirement rejection reasons and the remaining failure/recovery/platform matrix still require their own evidence. Python fixture compilation, unrestricted size ratchet and git diff --check passed. These results do not complete the original acceptance, review or delivery gates.

## Linked Transport Recovery

Expanded the SSH precheck fixture with linked-lost-response and linked-home-restart cases. Start retry counting now compares against the log length captured for that scenario, so earlier operations cannot satisfy the retry assertion. Retirement rejection must mention automation or precheck ownership instead of accepting any CLI failure. The new scenarios retain exact workspace and repository evidence and reuse the existing root/child closure checks.

The expanded suite passed in ssh-linked-precheck-recovery-1.log and ssh-linked-precheck-recovery-1/result.json with completed=true. All six precheck scenarios passed, including linked lost-response and linked Home restart without another Start. Recovery cancellation took 64.72 seconds for shared and 64.79 seconds for linked targets. Exact scope, one execution, expected retained tasks and process closure were checked. The existing SSH acceptance cases also passed; owner stop, sshd teardown and fixture process closure were verified. Python compilation, unrestricted size ratchet and git diff --check passed. The binary SHA remains 24a16e43899165f101580b472ad7dc4eebf9462d43ae51f24792f0758d0296df. No fixture or validation process remains active. Legacy bare/no-principal targets, missing-origin migration, owner same-boot recovery, platform acceptance and final review/delivery remain pending.

## Native Legacy Origin Discovery

Added linked_worktree_repository_origin in the internal Rust Git boundary. It opens the actual linked repository, verifies the requested path is its root, follows Git's common directory to the owner and verifies worktree membership. It returns the owner's canonical working directory or bare repository path. It does not infer ownership from managed-folder naming. The regression covers normal and bare owners, subdirectory/main-root rejection, symbolic-link aliases on Unix and unchanged branches.

The hidden project inspect-linked-checkout CLI returns version, canonical path, repositoryPath and branch without starting a runtime. Its real invocation on the retained SSH fixture returned the expected principal, linked path and branch (legacy-linked-inspection-cli.json and legacy-linked-inspection-cli.log). The Git regression passed (legacy-linked-origin-tests-2.log), as did all-target Clippy (legacy-linked-inspection-clippy.log), unrestricted size ratchet and git diff --check. An initial edit script stopped before writing because it used the wrong test-module name; its empty filtered test run is not validation. The detector was then added in a domain-specific module and the actual test ran successfully.

Next connect the readonly owner inspection to Home for missing legacy origins, persist the verified origin only if task/instance/host/path/binding still match, and exercise bare/no-principal SSH cases. The release binary used by previous SSH acceptance predates this detector and needs rebuilding before testing that new command remotely. No generated or exposed FRB API changed and no build, fixture or reviewer process remains active. Original acceptance, final validation, review and delivery remain pending.

## Missing-Origin Persistence And Home Integration

Added conditional persistence of verified linked repository origins. The store checks the task instance, project, host, path, active state and exact checkout binding and fills only an absent or matching origin. A different existing origin is never replaced. The regression covers stale instances/hosts/bindings/paths, idempotent evidence, conflicting origins and restart with unchanged task identity and no SSH principal.

Home now inspects missing linked origins through the owner's readonly inspect-linked-checkout command before constructing its precheck scope. It bounds the response and timeout, checks protocol version, exact checkout path, absolute owner-platform origin and nonempty branch, then persists against the captured identity. Known origins do not repeat inspection. The adapter regression covers POSIX and Windows payloads, incompatible versions, changed paths, relative origins and preservation of task/principal state. These Windows assertions are construction/validation coverage, not execution on Windows.

The conditional store test and final rerun passed (legacy-origin-cas-tests.log and legacy-origin-cas-tests-2.log), as did the adapter test (legacy-origin-resolution-tests.log), 32 CLI precheck tests (legacy-origin-precheck-suite.log), all-target Clippy (legacy-origin-resolution-clippy-2.log), size ratchet and git diff --check. The final active-snapshot check was added after the CLI test build began and verified by the store rerun. No generated or exposed FRB API changed and no test or fixture job remains active. Rebuild the release binary and add actual legacy bare/no-principal SSH cases, including missing-origin discovery, before claiming that acceptance. Canonical-alias handling in legacy remote bindings also still needs platform-level evidence. Final validation, review and delivery remain pending.

## Legacy Bare SSH Fixture

Added a legacy-linked scenario to the real SSH fixture. It creates a bare clone and a linked worktree using isolated fixture Git state, registers a stable task through workspace.upsert with no repository origin and confirms Home has no SSH principal. The normal precheck flow must discover and persist the native bare origin, retain the exact task on Home and owner, reject retirement while active, cancel with verified process closure and leave both databases without an SSH/owner principal.

The release rebuild passed in 66 seconds (legacy-origin-ssh-build.log). Python fixture compilation, unrestricted size ratchet and git diff --check passed. The expanded acceptance run passed in ssh-legacy-precheck-acceptance-1.log and ssh-legacy-precheck-acceptance-1/result.json with completed=true and ssh_precheck_legacy_linked=true. Binary SHA-256 is 41670fe0ee41cb40ba6555eb373132332aa6795445dc2367e55413c8dee95dc0. It verified native discovery and persistence of the bare origin, unchanged workspace instance, one command execution, cancellation closure, protected retirement and no principal registration on either side. All seven precheck scenarios and the existing SSH acceptance cases passed. Home restart cancellation took 64.82 seconds for shared and 64.74 seconds for linked tasks. Runtime stop, sshd teardown and fixture process closure were verified; no fixture or validation process remains active.

Remaining work includes same-boot owner recovery, native Windows/macOS and canonical-alias acceptance, the rest of the original migration/concurrency/client/performance matrix, final root/mobile/Rust validation, the fixed review loop and the later user-authorized delivery/release sequence. This Linux loopback result does not prove the platform-specific cases or finish the whole goal.

## Owner Runtime Reconciliation

Owner RPC queries now reconcile claimed prechecks that no longer have a live supervisor in the actor. Persisted ClosureVerified or SpawnFailed evidence can finalize an interrupted operation as cancelled when cancellation was requested, otherwise as failed with an unavailable command result. The runtime never turns closure evidence into a passed/rejected exit result and never repeats the command. Open records can use the existing verified Linux previous-boot closure rule; same-boot uncertainty retains the claim and reports lost native ownership instead of inferring closure from a missing PID. Active supervisor entries are excluded from reconciliation.

Two regressions cover same-boot launch uncertainty with and without cancellation, later verified no-spawn completion and idempotent Start, plus an actual native command whose persisted outcome is removed in the isolated fixture to model interruption after closure but before result commit. Its native closure evidence remains unchanged and recovery reports failed. All 34 CLI precheck tests passed (owner-precheck-reconcile-suite.log), as did all-target Clippy (owner-precheck-reconcile-clippy.log), the size ratchet and git diff --check. No generated or exposed FRB API changed and no test or reviewer job remains active.

This provides conservative recovery of persisted evidence; it does not regain a lost native process handle after a same-boot crash. A real owner-runtime interruption fixture must verify the retained unknown state and protected resources, as well as the recoverable closed-result boundary. Native platform and remaining original acceptance/review/delivery gates are still pending. The release binary used by the last SSH suite predates this RPC reconciliation and needs rebuilding for that new acceptance.

## Owner Crash Acceptance

The rebuilt release CLI (SHA-256 b4c83dd84221dd948954d17cea64420db1cd643da80ab1b294d1d10750ecfc9d) passed the Linux owner-runtime crash fixture in owner-restart-acceptance-2/result.json. The open scenario kills the captured owner runtime, restarts that isolated profile and verifies one command execution, retained unknown closure, cancellation without fabricated closure, idempotent Start and protected project removal. Even after fixture cleanup removes the captured command processes, missing PIDs do not release the ownership fence. The closed scenario preserves native closure evidence and removes only its isolated persisted outcome to model the closure/result commit gap; recovery reports an unavailable result without rerunning the command. Both scenarios verified teardown of their captured processes. The first attempt stopped before the induced crash because the fixture expected terminal-host instead of the actual runtime-host command; that assertion was corrected before the successful run.

Owner crash acceptance is completed for this Linux fixture. Native platform acceptance and the remaining original integration, final review and authorized delivery/release steps remain in progress.

## Full Linux Gates and Ninth Review

Root flutter analyze passed, and root flutter test passed 3,313 tests with two explicit skips. Mobile analyze passed and mobile tests passed 460 tests with four explicit skips. The first mobile test invocation used an older default Dart because the PATH assignment applied only to the preceding analysis command; mobile-final-tests-2.log is the corrected successful run. make rust-test passed with Rust 1.98.0, including workspace formatting, Clippy, 1,172 CLI tests (four ignored), 409 core tests, 206 native tests and all integration/doc-test binaries. No PTY isolation exception was needed. Logs are root-final-analyze.log, root-final-tests.log, mobile-final-tests-2.log and rust-final-validation.log under the stable temporary evidence directory.

The ninth fixed review used Codex GPT-6 Astra Low, --uncommitted and the same review.log. Before/after snapshots of all tracked/untracked files and the Git index matched exactly. Reviewer session 01a09ac5-0437-7640-9e3f-7fdd6581d7b2 was archived. Both P2 findings are valid. First, clean editor snapshots survived relocation even when Leave Changes produced different destination bytes; transferDocuments now clears clean snapshots and save tokens before remapping the path, while preserving unsaved buffers for the existing transfer contract. Both editor transfer tests and root analysis passed (review9-editor-tests.log and review9-editor-analyze.log). Second, desktop branch loading and validation still use the local project path after selecting an SSH host. This remains pending and also requires auditing the mobile project.branches.list route, which currently ignores the selected host. No clean review is claimed.

## Native Platform Validation in Progress

macOS core validation passed 407 tests and exposed two relocation fixture failures caused by /var versus /private/var. The manual return journal now uses the registered canonical project checkout, matching production preparation; path assertions compare canonical locations. All nine relocation execution tests passed after repair (shared-checkout-relocation-final-2.log on the Mac mirror). The first native CLI build lacked test/fixtures/terminal_snapshot_geometry.json in the mirror; fixture synchronization repaired that build prerequisite. Native precheck tests then passed 27 cases and exposed six owner-request fixture failures using uncanonicalized paths. Their requests now canonicalize the physical fixture path, matching native owner inspection, and the rerun is in progress. These edits change test construction, not production acceptance of unchecked paths.

Windows core validation remains in progress in the isolated shared-checkouts-Gk4ThK-windows-jobs mirror. The initial PowerShell launcher treated Cargo stderr progress as an exception; the corrected launcher uses Start-Process with separate streams and an explicitly checked native exit code. Its suite has reported Git failures; the host has core.autocrlf=true, but this configuration is only a diagnostic lead until the completed failures are inspected. Do not change the personal global Git configuration or count this platform as passed. Remaining work includes native/SSH platform coverage, the second review finding, final affected checks and another fixed review before any user-authorized branch/PR/release operations.

## Host Branch Catalog and Desktop Repair

The second ninth-review finding is now repaired for desktop. project.branches.list resolves the registered project checkout by host and executes outside the server actor. A new hidden native inspect-checkout-branches command returns branch choices and local branch names without starting a runtime. Home bounds the SSH response, requires the exact registered path and supported schema, and rechecks the binding before replying. Desktop requires the echoed project and host, rejects legacy/mismatched replies, reloads on host changes and drops stale branch loads. Both manual creation and From Prompt use the owner catalog for branch collision checks. Manual reuse filtering scopes task branches to the selected host.

The owner-catalog regression passed, including missing registration, remote-only branches, exact checkout matching and unsupported replies. All 1,173 CLI tests passed with four ignored (review9-branch-catalog-cli-suite.log); final all-target Clippy passed after extracting the branch request helper and file-request tests to maintain the size limit. Thirty desktop form/client tests passed (review9-host-forms-tests-2.log), including a new host-switch scenario whose remote-only source is submitted and whose local-only branch is valid as a new SSH branch. The first launcher test run timed out because its harness supplied only the old local branch controller; the harness now implements the new controller contract. Final root analysis passed (review9-final-analyze.log). Root generation ran once for each completed batch, followed by EOF normalization and formatting; no FRB API changed. The unrestricted size ratchet and git diff --check passed.

Mobile currently queries branches on its paired runtime and its creation form has no separate SSH checkout host selector. The optional checkoutHostId exists on shared creation transport but is not wired through that form. This is an original-client-scope gap, not a completed mobile SSH acceptance result. Add explicit checkout host selection and retain the paired runtime identity separately, propagate the selected checkout host through manual/From Prompt requests and branch lookup, and test older-runtime refusal for a requested SSH host. Also extend platform/actual SSH acceptance for the new branch catalog before final delivery.

## Platform Fixture Repairs

All 33 macOS precheck tests passed after canonicalizing native owner fixture requests (shared-checkout-precheck-final-3.log). The original Windows core run completed with 393 passed and seven failures in 865.75 seconds. Five failures compared LF fixture content with normal core.autocrlf=true checkout output; fixture repositories now explicitly disable autocrlf locally, without changing personal Git configuration. One fixture built an invalid Windows file URL and now uses url::Url::from_directory_path (url is a test dependency so core tests work without runtime features). The final path comparison is covered by the canonical relocation fixture repair. All 49 Windows Git tests passed after repairs (windows-git-fixture-recheck-transport.log); all 50 corresponding Linux Git tests passed (review9-platform-git-linux-tests.log). Windows relocation revalidation is still running. No claim is made that full current platform/SSH acceptance or the next whole-checkout review is complete.

## Tenth Review and Unix Closure

The tenth fixed review completed with two valid findings. Tracked/untracked files and the Git index matched the before/after snapshots; session 01a09ada-0f97-7d32-a666-81fc98553c18 was archived. P1 identified that Unix workspace AI Assist commands already waited for their native session guard but persisted only RootExited. Execution now retains whether that guard verified closure before the root was reaped and records ClosureVerified after root exit. Cancellation does not rescan a session whose closure was already verified, avoiding a second sweep after reaping. Failed verification still returns before recording closure. The success regression verifies that the retirement fence is released after reopening the store; cancellation verifies the same fence while retaining process evidence. All 33 AI Assist tests passed on Linux and macOS, including orphan-child cleanup with a neighboring process preserved. Evidence: review10-ai-closure-final-tests.log, review10-ai-closure-clippy.log and the Mac mirror review10-ai-closure.log.

P2 identified that document invalidation covered only locally initiated transfers. Clean snapshot invalidation now lives at EditorDocument.attachFile when its checkout path changes, so normal widget synchronization after another client's relocation also clears old bytes and save tokens. Unsaved buffers retain their existing protection and transfer behavior. All three transfer/document regressions and root analysis passed (review10-editor-tests.log and review10-editor-analyze.log). The size ratchet and git diff --check passed. No generated or exposed FRB API changed in this repair batch. The latest reviewer output still contains those findings; a later full review is required before describing the review loop as complete.

Windows relocation revalidation initially passed eight tests and exposed one additional fixture assertion using a hardcoded forward slash. Its expected payload now uses Path.join. All nine cases then passed, including failure/restart boundaries (windows-relocation-fixture-recheck-transport-2.log). Together with the 49-test Windows Git rerun this resolves all seven failures from the original 400-test Windows core run, without altering personal Git configuration. These targeted reruns do not constitute current native SSH end-to-end coverage.

## Current Execution Queue

| Outcome | Status | Evidence or next action |
| --- | --- | --- |
| Owner precheck crash/restart acceptance on Linux | Completed | owner-restart-acceptance-2/result.json |
| Ninth-review editor and desktop SSH branch repairs | Completed | Transfer/client/form tests, CLI suite and Clippy passed |
| Tenth-review Unix AI Assist closure and external editor relocation repairs | Completed | 33 Linux and 33 macOS tests, document tests and analysis passed |
| Windows core fixture failures | Completed | 49 Git and 9 relocation tests passed after scoped fixture repairs |
| Mobile SSH checkout selection | In progress | Creation, host routing and attachment ownership tested; review eleven exposed missing actual SSH Quick Open indexing |
| Current native SSH and branch-catalog acceptance | Pending | Build current binaries in isolated mirrors and verify macOS/Windows owner flows; prior Linux SSH binary predates branch changes |
| Remaining original acceptance and final root/mobile/Rust gates | Pending | Reconcile original matrix; rerun affected complete gates after the final implementation batch |
| Final fixed review loop | Pending | Same read-only reviewer/model/effort/target/log; classify every finding and archive sessions |
| Authorized branch, PR and Cut Release publication | Pending | Start only after implementation, required validation and review are resolved |


## Mobile Checkout Host Routing

Mobile now lists registered project checkouts with SSH aliases and keeps checkoutHostId separate from the paired runtime hostId. Manual and From Prompt creation, owner branch queries, saved retries and checkout Quick Open propagate that owner. Branch selection discards stale project/host loads, and remote branch replies must echo the requested project and host instead of accepting legacy local results. The runtime checkout catalog adds an optional hostName without changing the terminal protocol. Forms require a registered location before submission.

Mobile generation completed once for the provider batch, followed by EOF normalization and formatting. An accidental root generation also completed and its output was normalized. The initial mobile suite ran against an invalid Object-to-map conversion and is not a passing gate; parsing now uses asJsonMap. The Create Another fixture needed to unfocus the prompt and await its automatic scroll before scrolling and tapping the newly lower control. The full corrected mobile suite passed 463 tests with four skipped (mobile-checkout-host-full-final.log); mobile analysis passed (mobile-checkout-host-analyze-final.log). A subsequently added saved-SSH-checkout attachment regression passed with the two host creation/pipeline tests (mobile-checkout-attachment-host.log). The transport test rejects unscoped legacy SSH branch replies and verifies the explicit owner request (mobile-checkout-transport-tests.log). The unrestricted size ratchet and git diff --check passed.

Remaining attachment ownership issue: uploaded files/images are stored on the paired runtime and inserted as absolute paths. The SSH creation flow must not submit those paths as though they exist on the checkout owner. Resolve this explicitly, including changing the selected host after uploading, without introducing implicit cross-machine synchronization. Checkout file search already targets the selected SSH checkout and returns relative paths. The latest review remains iteration ten with its repaired findings; neither a final clean review nor current native SSH acceptance is claimed.


## Mobile Attachment Ownership and Parent Ordering

The attachment ownership gap above is now guarded. Upload results are recorded in a generated provider, captured in the background request and preserved by withCreated and retry form hydration. A remote-owner prompt containing one of those local paths fails before any workspace, branch or terminal operation. Removing the path permits creation; historical metadata alone does not block a prompt. SSH offers checkout file search rather than the paired-runtime upload sources, so this change does not introduce automatic file synchronization or pretend that a paired-runtime absolute path exists remotely. Paths manually typed by the user retain their usual prompt semantics.

The mobile Parent comparator also still preferred isMain records, unlike the already updated desktop comparator. That final ordering privilege is removed; the project grouping and deterministic name/ID tie-breakers remain. All ten affected selection, Parent and creation tests passed (mobile-parent-order.log). After the attachment provider batch, mobile build_runner generation, EOF normalization and formatting completed. Mobile analysis passed and the complete suite passed 465 tests with four skipped (mobile-attachment-owner-analyze.log and mobile-attachment-owner-tests.log). Focused attachment assertions additionally cover saved SSH host file search, omission of local upload sources, no side effects on a rejected local path and preservation of retry metadata after a path is removed. The mobile checkout catalog Rust change passed all-target Clippy (mobile-checkout-catalog-clippy.log). This does not close the remaining native SSH/platform acceptance, full requirement audit or final reviewer gate.


## Current Linux SSH and Native Catalog Acceptance

Rebuilt the current Linux release CLI successfully and ran the isolated OpenSSH fixture with --home-checkouts --owner-prechecks --owner-relocation. current-ssh-catalog-acceptance/result.json reports completed=true for binary SHA-256 1ba57e88553b8b0471951d6fb7e5f435e100a6ca737e84a5607cfd7dcda8bc4c. Both owner and Home relocation round trips/retries, registration/cloning, owner-repository worktree creation, linked retirement, initial transport failure, terminal close/restart, and all seven precheck scenarios passed. Recovery after Home restart took 64.75 seconds for shared scope and 64.72 seconds for linked scope, consistent with the persisted recovery ticker. The fixture verified neighboring processes and shared files were preserved, and all fixture processes and the isolated SSH server were closed.

The fixture now creates distinct local-only and owner-only branches and checks project.branches.list returns the exact owner/path/project without leaking either branch into the other catalog. It also verifies checkout.list returns the registered SSH alias. Both assertions passed in current-ssh-catalog-acceptance.log. This supersedes the earlier stale Linux SSH binary limitation.

The current macOS CLI built in the validated external-volume mirror. A native inspect-checkout-branches command executed over SSH against an isolated macOS repository returned the canonical path and both fixture branches without changing the active branch; mac-native-catalog.log records the result and the fixture /Volumes/ExternalStorage/remote-builds/alera/native-catalog-rso9txe5. This is native catalog evidence, not complete macOS Home-to-owner relocation acceptance. Windows current CLI compilation is still running through tool session 29914, using the isolated existing mirror and C:\c\alera-gk4thk-jobs target directory; do not restart it from this status alone. Its log currently contains two unused-import warnings and no terminal result. The Windows catalog fixture is prepared as current mirror windows-native-catalog.ps1 and must run only after this build succeeds. Remaining native platform flows, original matrix audit, final full gates and the fixed review loop remain open.


## Native SSH Relocation and Windows Command Transport

The Home-to-macOS SSH fixture passed on the current native owner binary: remote registration, shared neighbor tasks, owner branch catalog, leave-changes Hand Off, Hand On, stable ID/instanceId, exact operation retries, local-checkout isolation and neighboring task preservation. mac-home-relocation.log and its result under mac-home-relocation-3e938cfdab3c441cb1a8e73dc6f69fd3 record completion; all fixture runtimes were stopped. Windows current compilation also succeeded in 5m48s, with eight existing/platform warnings. Its native catalog fixture passed, including canonical Windows extended paths (windows-native-catalog.log).

The full Windows SSH fixture first failed because its staged sidecar lacked Windows current.txt. Correcting that fixture exposed a product failure: Hand Off embeds a large command in PowerShell -EncodedCommand and exceeded the Windows OpenSSH command-line limit. Windows remote commands now use a short encoded launcher that reads a base64 UTF-8 script from stdin before execution. The local SSH process retains kill-on-drop, inherited login-shell configuration and concurrent output drainage. The new unit case, all 70 SSH-filtered CLI tests and all-target Linux Clippy passed (ssh-windows-stdin-test.log, ssh-windows-stdin-ssh-tests.log, ssh-windows-stdin-clippy.log). A new release CLI was built for actual validation.

The fixed transport reached native relocation, but the long Windows fixture path failed at PreparingDestination. The operation remains journaled and task identity/location retained. A separate shorter fixture path then passed the full Windows Home-to-owner round trip, catalog, retry, neighbor and local-isolation assertions (windows-home-relocation-short-path.log, result under windows-home-relocation-2278d81b066346eeaeb1c715e6b2dd0e). The remote fixture is C:/Users/leyni/remote-builds/alera/nr-05c5abbf and all its runtimes were stopped. This proves normal-path Windows relocation; it does not resolve the long-path failure. Recovery errors previously hid the nested Git cause; git_step now includes it in the reported retained-phase message. All nine core relocation tests passed (relocation-error-context-tests.log); the now-unused Context import exposed by that run was removed. Next build that diagnostic on Windows and inspect/retry the retained long-path operation rather than guessing the cause or treating the shorter-path pass as universal acceptance. No final whole-checkout review has run after these changes.


## Long-Path Diagnostic and Migration Metadata

The Windows diagnostic CLI build completed and was copied only into the stopped, isolated owner installation for the retained operation. Replaying the same Home request with relocation ID f22d539c-d692-4777-b9df-03c4b797c4be preserved its identity and returned the nested Git cause: path too long while opening repo/.git/worktrees/alera-relocation-f22d539c-d692-4777-b9df-03c4b797c4be/. Evidence is windows-retry-longpath.log and the original fixture retry-longpath-result.json. All fixture runtimes were stopped after the retry. The displayed absolute admin path is approximately 195 ASCII characters, so the generic label alone is insufficient to attribute this to the normal 260-character Win32 limit. The bundled libgit2 open_worktree_dir calls git_path_validate_length(NULL, dir); investigate the actual native validation before changing repository configuration, dependency code or naming. Normal-path Windows acceptance remains passed, and this retained-path case remains unresolved.

Added checkout_migration_metadata_tests.rs to compare both shared task records, stable instances, editor/terminal tab records, ordered layouts, pins, sections, tags and parent/child relations across deleting legacy-missing checkout bindings and reopening the runtime store. Both tasks then reference one checkout without merging their task identities. The first test fixture referred to an unregistered agent profile and correctly failed the existing foreign-reference guard; that irrelevant reference was removed. The corrected test passed (checkout-migration-metadata-tests-2.log), and core all-target Clippy passed (checkout-migration-metadata-clippy.log). The prior relocation error-context change also passed core all-target Clippy (relocation-error-context-clippy.log). No validation or fixture subprocess remains running from this turn.


## Compact Worktree Registration on Windows

The direct native probe accepted the visible 195-character administrative path but Repository::open rejected the linked checkout. Bundled libgit2 1.9.7 repository.c::validate_repo_path explains the difference: opening a repository reserves objects/pack/pack-.pack.lock plus the maximum object-ID hex width in addition to the displayed path. That static reservation is also applied to linked administrative directories and is not bypassed by core.longpaths. No personal Git or Windows system configuration was changed.

New relocation registrations use alera- plus the full UUID in compact hex form. Existing alera-relocation-UUID registrations remain discoverable and are reused; two registrations claiming one ID are rejected. Windows validates the administrative path plus libgit2's suffix before creating a relocation journal and repeats the check before branch/worktree creation. Paths still outside that limit fail with an explanatory message before new Git effects. The previous overlong legacy fixture remains retained; it was not silently renamed, pruned or claimed recovered.

The current core implementation created and reopened a compact worktree in the same long Windows fixture repository and repeated that request successfully (windows-compact-path-probe.log). All three native Windows regression tests passed: compact identity/retry, legacy registration reuse, and insufficient metadata-path space with no branch or folder created (windows-relocation-admin-tests.log). All 35 Linux relocation-filtered tests passed, the two cross-platform registration tests passed after correcting a StringArray::get Result expectation, and core all-target Clippy passed (compact-relocation-admin-tests.log, compact-relocation-admin-identity-tests-2.log, compact-relocation-admin-clippy.log). The unrestricted size ratchet and diff whitespace check passed. Next rebuild the Windows sidecar and run the full long-path Home-to-owner fixture; then reconcile remaining gates and classify the next fixed review. The temporary low-level diagnostic example exists only in the owned Windows mirror, not in the repository checkout.


## Eleventh Review and Client Repairs

Review eleven completed successfully with six valid P2 findings; before/after snapshots matched and the Git index was unchanged. Session 01a09b1d-e8dd-77b0-bc0d-fd318c8170bf was archived after completion (an earlier archive attempt was rejected while its writer was active). The fixed reviewer/model/effort/target/log were retained. This output is not clean and no finding was classified as a false positive.

Five findings now have targeted repairs: legacy runtimes receive local creation options without an unsupported checkout.list call and skip the newly introduced project dependency lookup while keeping their existing removal confirmation; project location summaries filter kind=project; uploaded attachment metadata is watched by the whole creation screen so Manual/From Prompt toggles cannot dispose it; stack candidates retain all tasks until the live branch correction, then deduplicate. Six mobile tests and four stack tests passed, including the newly reported counterexamples (review11-mobile-fixes-tests.log and review11-stack-tests.log). Mobile analysis passed (review11-mobile-fixes-analyze.log). No generated model/provider declaration or exposed FRB API changed in these repairs. Root analysis passed and the full root suite completed with 3318 passed and two skipped (review11-root-analyze.log and review11-root-tests.log); targeted stack tests revalidated the changed candidate path separately.

The sixth finding remains actionable: checkout.quickOpen.start rejects SSH owners, despite the form exposing remote workspace files. Previous fake-client routing tests prove transport selection only, not a functioning remote index. Implement actual owner-side indexing/search consistent with the approved selected-checkout requirement and test it through SSH; do not count hiding that source or a fake-only test as completion of remote search. This corrects the earlier mobile checkout-selection completion claim.

After the review, full long-path Home-to-Windows SSH acceptance passed with compact registration names (windows-compact-home-relocation.log; fixture windows-home-relocation-93345fa173bb476fa226756314d956a4). The owner and Home runtimes were stopped. Mac Terminal Pulse validation returned 51 passed/18 failed with default /var temp aliases; using canonical external-volume TMPDIR returned 65 passed/four failed, all four attempting non-UTF8 filenames rejected by APFS. Fix fixture canonical paths and separate filesystem-specific filename tests while preserving Linux and valid macOS coverage. Windows Terminal Pulse compilation initially missed test/fixtures/terminal_snapshot_geometry.json; the mirror now includes it and the repeated native test run is in progress. The repeated run writes windows-native-pulse-tests-transport-2.log. Also removed the newly introduced deprecated Oid::zero use in Windows path validation and the Windows-unused CommandBuilder import from the PTY spawn module. The existing fx_herdr warning remains outside this repair.

## SSH Quick Open and Portable Watcher Fixtures

The sixth review-eleven finding is repaired: the owner CLI exposes a bounded file catalog using the existing ignore/protected-path scanner without symlinks, and Home imports only relative path data into its existing Quick Open registry. Search, expiration, session limits and explicit close reuse that registry. Registration is rechecked after loading; unsupported responses and mismatched owner paths are rejected. No remote checkout path is opened on Home. Three focused CLI tests passed, including invalid/oversized response rejection and close behavior. The existing missing-host test now asserts the specific missing-target error rather than the former unconditional SSH rejection.

Actual isolated Linux SSH acceptance passed in quick-open-ssh-acceptance/result.json, with the new remote-file/ignored-file/symlink assertions in quick-open-ssh-acceptance.log. Home and owner process cleanup completed and the fixture SSH server stopped. This verifies remote transport and owner indexing, beyond fake-client routing. Other selected Home checkout, branch catalog, clone, linked worktree and terminal lifecycle cases also passed in that run. This does not yet establish current native macOS/Windows Quick Open transport acceptance.

Windows Terminal Pulse returned 62 passed and two failed; both failures were invalid Git-config escapes in test-created Windows paths. The fixtures now quote and escape those paths. Mac fixtures now use canonical temporary roots where event paths are compared directly; arbitrary non-UTF8 cases remain on supported Unix filesystems, while APFS keeps literal-backslash and distinct Unicode path coverage. Linux Terminal Pulse passed all 69 cases after these fixture repairs (pulse-portable-fixtures-tests.log). Native macOS and Windows reruns are in progress; no native pass is claimed yet.

## Twelfth Review and Project Retirement Guard

Review twelve completed with one valid P1: bulk project removal could discard SSH task ownership while terminating only the local bridge, leaving the persistent owner terminal running. Before/after snapshots matched, the index was unchanged, and reviewer session 01a09b34-2e23-79a1-99bb-4448b06c5000 was archived. Project removal and bulk workspace removal now refuse while any SSH workspace remains, identifying the workspace/host and directing the caller to individual verified retirement first. This guard preserves disconnected owner records as well. The actor regression passed and checks project/task identity, tabs and neighboring sessions remain present (review12-project-removal-tests.log). No false positive was reported.

Root analysis and all 3319 root tests passed with two skips. The first full mobile run had 464 passed, four skipped and three dependency-test failures because its fake WebSocket never negotiated sharedCheckoutWorkspacesV1 after the compatibility repair. The fixture now answers mobile.hello with that capability and the normal post-authentication status request. A first targeted repair exposed that extra status request; the complete mobile suite is being repeated after correcting the fixture.

Native macOS Terminal Pulse passed 67 applicable cases with the default temporary directory (portable-native-pulse-tests.log on the mirror). Current Mac SSH Quick Open plus stable-identity relocation, retries, neighbor/file preservation and runtime shutdown passed (mac-quick-open-relocation.log, fixture mac-home-relocation-cd9857da68bb4b289cf4c35f702f8fde). The first Windows fixture repair fixed both Git-config escape failures but std::fs::canonicalize introduced verbatim path prefixes in test roots, causing 14 comparison failures; the fixture now uses dunce::canonicalize to retain canonical macOS paths without Windows verbatim spelling. Windows native rerun and current Quick Open owner build are pending. This is not a claim of a Windows pass.

After the fixture repair, mobile analysis and all 467 mobile tests passed with four skips (review12-mobile-repair-analyze.log and review12-mobile-repair-all-tests.log). All 69 Linux Terminal Pulse tests passed again after the portable canonical-path correction, and all-target CLI Clippy passed (pulse-portable-fixtures-tests-2.log and review12-fixes-clippy.log). The broader make rust-test run is still in progress. No annotated generated declaration or exposed FRB API changed in this repair batch.

## Thirteenth Review and Legacy Mobile Creation

Windows native Terminal Pulse passed all 64 applicable cases after the portable path correction (windows-portable-native-pulse-transport-2.log). The current Windows owner CLI built, and real SSH Quick Open plus stable-identity relocation/retry, neighbor preservation and runtime shutdown passed (windows-quick-open-relocation.log; fixture windows-home-relocation-e8167641f2074fee947e15b16bcbeada). The complete make rust-test run passed: 1176 CLI tests with four ignored, 412 core tests, 206 native tests and the integration suites (review12-rust-tests.log); the subsequent project-removal guard was checked by its focused regression and Clippy.

Review thirteen completed with two valid P2 findings: the mobile workspace dependency RPC lacked the legacy capability fallback already added for projects, and creation exposed Project Folder on unsupported runtimes. All tracked/untracked/index snapshots matched; reviewer session 01a09b39-dc9f-76f3-8fb6-8f4074d9eb37 was archived. No false positives were identified. Workspace dependency inspection now returns an empty list without the unsupported RPC on old runtimes. A negotiated shared-checkout support flag reaches the list data, creation form and retry forms; old hosts disable Project Folder, default Git projects to New Worktree, and explain the required update. Folder-only projects stay disabled until supported. Shared prompt requests fail before identity generation when support is missing. Checkout-based attachment search is also capability gated.

The mobile generated provider batch completed, followed by EOF normalization and formatting. An accidental root generation was also completed and normalized; no exposed FRB API changed. Initial analysis caught a test calling the wrong controller interface and an unbraced condition; both were corrected before testing. Mobile analysis and all 17 targeted creation/compatibility/dependency tests passed (review13-mobile-analyze-2.log and review13-mobile-targeted-tests.log). Full mobile validation and the next fixed review remain pending. No branch, commit, PR or release action has started.

## Current Acceptance Reconciliation

The implementation stages and original acceptance rows now reference the completed evidence below rather than the historical pending entries. Integration remains in progress until the last fixed review and affected checks pass; the authorized PR and release sequence remains pending. This records automated tests and isolated native/SSH fixtures, not manual UX coverage or a CPU benchmark.

| Area | Evidence |
| --- | --- |
| Persistence and identity | checkout_store_tests covers restart, empty projects, canonical aliases, host separation and exclusive linked ownership; checkout_migration_metadata_tests preserves task IDs/instances, tabs, layouts, relationships, pins, sections and tags |
| Concurrency and editor state | workspace_file_service_test scopes overlays to task tabs; editor_buffer_guards_test preserves sibling edits and blocks in-flight saves; editor transfer tests invalidate clean relocated snapshots; checkout_buffer_guards_tests requires all editor clients and rejects new/disconnected clients |
| Removal | Managed/shared cleanup regressions preserve neighboring sessions and records after failed closure; automation cleanup/dependency tests protect runs and history; project removal refuses SSH ownership until individual verified retirement; actual SSH fixtures verify owner receipts and native child closure |
| Relocation | workspace_relocation_execution_tests covers both directions, leave/move choices, replacement branches, restart and each persisted boundary; remote commit tests preserve identity and owner receipts; Linux/macOS/Windows SSH roundtrips and repeated requests passed |
| SSH | Isolated OpenSSH cases cover register, clone/refuse-existing, owner branch catalogs, linked origin preservation, owner crash/lost-response recovery and terminal closure; native Quick Open passed on Linux/macOS/Windows |
| Automations | Project-checkout tests allocate distinct tasks from an empty project and preserve uncommitted files/history; shared cleanup tests protect files, sibling tabs, takeover and run identity; owner-precheck/SSH cases verify authorization, cancellation, timeout, restart and no local fallback |
| Clients | Root 3319 tests and mobile 469 tests passed, with two/four existing skips respectively; compatibility tests cover old/new capabilities, guarded mobile operations, disabled unsupported shared creation, CLI explicit confirmations and stable retry identity; stack tests deduplicate only after live branch correction |
| Resources | Desktop source-control lifecycle compares one and thirteen consumers with one watcher/status load and release after the last consumer; native Terminal Pulse shared-watcher tests passed within Linux 69, macOS 67 and Windows 64 applicable tests |

After the final mobile compatibility changes, mobile analysis and all 469 tests passed with four skips (review13-mobile-final-analyze.log and review13-mobile-all-tests.log). Hash comparison against the review13 snapshot confirmed no root lib files changed during the accidental root regeneration, so the successful root validation remains applicable. No review, validation or publication failure is being counted as success.

## Fourteenth Review Repairs

Review fourteen completed with two valid P2 findings, with matching tracked/untracked/index snapshots. Reviewer session 01a09b48-7f68-7173-bc31-8dbe9fdb937b was archived. The background desktop prompt pipeline now receives branch checks bound to the selected host: SSH uses the owner's catalog without reading the local repository, and task-branch collisions exclude other hosts. The new regression verifies a local-only branch and unavailable local path cannot reject a remote name, while remote branch collisions remain detected. Four branch/pipeline tests and root analysis passed (review14-root-targeted-tests.log and review14-root-analyze.log).

For mobile runtimes without shared-checkout support, Workspace File attachments again use the compatible existing-workspace Quick Open operation: a selected same-project local parent takes precedence, otherwise the legacy project's local main task supplies the index. Modern runtimes continue indexing the selected project checkout. This supersedes the earlier decision to hide that legacy source; its operation remains supported and does not require the new checkout RPC. Fourteen attachment/legacy tests and mobile analysis passed (review14-mobile-targeted-tests.log and review14-mobile-analyze.log). Root provider generation, EOF normalization, formatting, size ratchet and whitespace checks passed; no mobile generated declaration or exposed Rust API changed in this batch.

The repeated make rust-test completed successfully with 1177 CLI tests/four ignored, 412 core tests, 206 native tests and integration suites (review14-rust-tests.log). Twelve task-created Python bytecode cache files were removed after the reviewer snapshot verification; no source or test fixture was removed. Full affected Dart suites and the next fixed review remain pending before PR delivery.


## Final Implementation Review and Delivery

Review fifteen completed without actionable findings using Codex GPT-6 Astra Low, --uncommitted and the same stable review.log. Tracked/untracked contents and the Git index matched before and after the read-only subprocess. Session 01a09b50-753b-74c3-8dba-70634445b06a was archived. Root analysis and all 3320 tests passed with two skips; mobile analysis and all 470 tests passed with four skips (review14-root-analyze.log, review14-root-all-tests.log, review14-mobile-analyze.log and review14-mobile-all-tests.log). The last full Rust workspace gate passed, and no Rust implementation changed afterward. The size ratchet and whitespace checks passed. There are no unresolved review findings or known failing validation gates.

Implementation and acceptance are completed. Delivery is in progress: create and commit the feature branch, fetch/prune and integrate remote main, resolve any conflicts, validate and merge its PR, synchronize local main and remove the feature branch, then run Cut Release and verify the version PR and published release. No implementation claim implies publication has already completed.
